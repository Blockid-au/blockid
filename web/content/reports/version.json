{
  "version": "v2.0.0-beta.6",
  "updated_at": "2026-07-17T13:10:00Z",
  "git_sha": "8816cef",
  "release_id": "phase8-public-surfaces-rpc-csp",
  "task_ids": [
    "Public /roadmap page — 8-stage platform journey with in-progress chip on Cap Table",
    "Public /changelog page — reads web/CHANGELOG.md via fs + inline minimal renderer",
    "Public /status page + /api/status aggregator — healthz + deploy-log + cron-health tail summary",
    "Migration 0081 lifecycle_rpc — pick_lifecycle_due with FOR UPDATE SKIP LOCKED (closes H1/M6 code from audit)",
    "advance_lifecycle RPC — atomic history-append UPDATE (M6 race closed)",
    "web/src/proxy.ts — Next 16 nonce-based CSP (drops unsafe-inline + unsafe-eval on script-src, closes H4 sec from audit)",
    "GoogleAnalytics + layout.tsx — accept + apply nonce prop threaded via x-nonce header",
    "next.config.ts — static CSP removed (moved to proxy), other security headers preserved",
    "4 agents ran in parallel on strictly disjoint file domains; all typecheck clean; migration applied to prod"
  ],
  "notes": "v2.0.0-beta.6 — Public status surfaces (roadmap/changelog/status) + deferred audit fixes (H1/M6 lifecycle RPC with SKIP LOCKED + H4 CSP nonce). 3 deferred findings from Phase 6 audit are now closed. Deploy pipeline also hardened with 20 @react-pdf transitive peer deps to prevent standalone tracer under-count issue. All 4 parallel agents ran on non-overlapping file domains and shipped with clean typecheck. Migration 0081 applied to prod DB and verified callable."
}
