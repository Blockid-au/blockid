// ADK-style agent layer (free, zero-dependency) — see ./agent.ts for rationale.
export {
  LlmAgent,
  SequentialAgent,
  newSession,
  type ModelCaller,
  type AgentSession,
  type LlmAgentConfig,
  type AgentRunResult,
} from "./agent";
