/**
 * (accelerator) persona layout — Master Upgrade Plan §16.5.
 *
 * Thin passthrough. Accelerator surfaces (cohort, applications,
 * accelerator-criteria) move in a follow-up. Persona/entitlement
 * guard will land alongside the physical move.
 */

import type { ReactNode } from "react";

export default function AcceleratorLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
