import { SVIEntrance } from "@/components/svi/svi-entrance";

export const metadata = {
  title: "BlockID — Startup Value Index",
  description:
    "Measure, prove and grow your startup value from day one. Get your free Startup Value Index now.",
};

export default function HomePage() {
  return <SVIEntrance />;
}
