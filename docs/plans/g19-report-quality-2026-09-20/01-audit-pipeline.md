# G19 audit 1 — engine, ReportV2 contract, valuation (2026-09-20, read-only)

Paths under `web/src/`. Back-link: [`../g19-report-quality-2026-09-20.md`](../g19-report-quality-2026-09-20.md).

## Contracts
- `lib/report-v2/schema.ts:61-110` `DimensionChapter { dim, title, titleVi, weight, ownerAgent, supportingAgents, score, band, benchmark{p25,p50,p75,percentile,stage}, verdict(≤80w), primaryVisual, secondaryVisuals, evidence: EvidenceRow[], criteria: CriterionCard[], strengths, gaps, nextAction{title,window,expectedLift,evidenceToAdd?}, phaseLens, frameworks, modules[{id,output}], audit: AuditStamp, runIds, renderAs, degraded?, degradeReason?, proposedScore?, scoreNote? }` — **no field for signal → points reasons**; `modules[].output` untyped.
- `schema.ts:129-248` other blocks: `ValuationChapter{ methods[6]{method,lowAud,midAud,highAud,weight,rationale,applicable}, consensus{low,mid,high,confidence}, ask?, sectorMultiples, comparables{n,rows}, unitEconomics?, scenarios, narrative, audit }`, `cover{ svi{total,band,cohortPercentile,cohortN,deltaVsLast}, dims[], threeQuestions, verification? }`, `executive{ thesis, strengths, gaps, verdict, confidence, phaseNow }`, `phaseGates`, `moneyOnTable`, `actionPlan{steps}`, `appendix`, `quality{ score, groundedShare, consistencyIssues[], degradedSections[] }`.
- `lib/svi-analysis.ts:237-329` `SVISubScore{ label,key,value,adjustment,rationale,evidence[],gaps[] }`, `RiskPenalty{label,points,reason}`, `SVIEvidenceGap{priority P0-2,label,action,impact,evidenceType}`, `SVIAnalysis{ totalSVI, baselineSVI 100, netAdjustment, confidenceMultiplier 0.2–1.0, subs, riskPenalties, evidenceGaps, nextActions, signals, stage 0-7, stageBonus, percentileRank?, meta.verification? }`. Formula `:1512-1515` `total = 100 + Σ round((dimScore−50)×weight×confidence) + stageBonus − Σ penalties + metricsBonus + sectorAdj + ciBoost`; `sectorAdj` (`:1370-1390`) folded into `netAdjustment`, never returned.

## 1. Missing "why" per score
- Signals are pushed as prose (`ftvEvidence.push("Serial founder with exits")` `:1001`, points only in code); 15/110 push lines carry numbers.
- `/workspace/score` renders subs evidence/gaps + penalties (`components/dashboard/svi-explainer-card.tsx:115-170`, `components/svi/svi-results-panel.tsx:1830-1948`); the TBR does not: `adapter.ts:937-948` keeps only `subs[].gaps → insights`; `buildChapter` sets `evidence: []` (`adapter.ts:498`); `agent-dispatcher.ts:1286-1316 chapterInput()` passes booleans only. `riskPenalties` only reach legacy `chart-generator.ts:135`. `proposedScore/scoreNote` (`agent-dispatcher.ts:1173-1186`, `consistency-gates.ts:125-143`) and `quality.consistencyIssues` never rendered (chapter.tsx, tbr-pdf.tsx, tbr-docx.ts).
- Fix: `SVISubScore.breakdown[{signal,points,source}]` + return `sectorAdj`; `DimensionChapter.scoreBreakdown{base,signals,confidence,adjustment}` + `cover.sviLedger`; copy in `agent-dispatcher.ts:1157 buildDimensionChapter` and `adapter.ts:470 buildChapter` (extend `AssembledReportContext:904`); pass into `chapterInput()`; render table in `chapter.tsx` + twins.

## 2. Valuation
- `lib/agents/cfo-valuation.ts:538-642`: revenue_multiple (w 0.35; 0.1 at ARR 0), berkus (`:562-565`), dcf_proxy = ARR×(multiLow+1) or **berkus×1.5** (`:566`), comparables = … or **berkus×1.1** (`:585`), risk_factor_summation = … or **berkus×1.2** ×(1+ESIC 20 %+RDTI) (`:574-579`), scorecard w 0. Consensus blend `:599-605`; `confidence = min(85, 35 + 25·(mrr>0) + 15·(growth≥10) + 10)` (`:606`, unconditional +10 → floor 45 %). VC method (`:115`) unused; no DCF.
- Inputs never shown: `gather.ts:788-803` (MRR from Stripe/Xero <90 d else founder-stated; growth only with prior MRR else default **10 %/mo** `cfo-valuation.ts:539`; `esicQualifies:false` hard-coded `:796`; RDTI = rdSpend×0.435 `:786`); `unitEconomics` in schema, not rendered (`valuation.tsx`, `tbr-pdf.tsx:470-530`, `tbr-docx.ts:330-354`). Ask: `valuation-chapter.ts:159-181`; raise invented `blendedMid×0.2` (`cfo-valuation.ts:615`). Fallback path `adapter.ts:534-596` = 6 "Not computed" rows, confidence 0.35. Consistency gate `consistency-gates.ts:145-164` (stage band ×0.5..2 → ×0.8) recorded, never rendered. No backtest hook (`content/reports/svi-backtest-latest.json` read only by `lib/backtest/latest.ts`).
- Fix: `ValuationChapter.inputs{mrrAud,arrAud,revenueSource,monthlyGrowthRatePct,growthAssumed,esicQualifies,rdtiRefundAud,berkusPillars,stage,sector}` + `derivation`; pre-revenue `applicable:false` for the ×k methods; honest confidence; growth undefined + flag; backtest quartile row.

## 3. Unassessed vs scored
- Baselines with no data: FTV/MPC/PTD 50 (`svi-analysis.ts:996,1036,1066`), TRE 30 (`:1101`), CGH/IRI/LCO 40 (`:1138,1175,1195`); `band:"pending"` (`adapter.ts:646`, `agent-dispatcher.ts:1188`) only on non-finite score → dead. Chapter says "No evidence rows" (`chapter.tsx:118`) under a confident number. Confidence multiplier not shown.
- Four lift models: `evidenceGaps` (`:1595-1628`, dashboard only), `nextActions` (`:1632-1697`), `lib/dashboard/evidence-gaps.ts:72-118` = `EVIDENCE_CATALOG.estimatedSviImpact` (`lib/svi-completeness.ts`), TBR `expectedLift = weight×(70−score)/100` (`adapter.ts:481`, `agent-dispatcher.ts:1210`; LLM 0-40 `:912`).
- Fix: `assessed` per sub → pending band; single lift = catalogue; P0/P1 gaps into `actionPlan`; confidence + evidence level on cover.

## 4. Executive synthesis + stage
- `/dashboard` (`app/(app)/(founder)/dashboard/page.tsx:176-193`): where-you-stand, next best action, money, evidence to add, reports — no strengths/weaknesses/value. TBR executive strengths/gaps = `"<Dim> 82/100 (strong)."` (`adapter.ts:751-752`), confidence hard-coded 0.5 (`:754`).
- Stage decided 3 ways: `detectStage` 0–7 (`svi-analysis.ts:403`) on cover; `inferPhase` (`adapter.ts:193-207`) in TBR; `navPhaseFromSvi` (`lib/nav/founder-phase-shared.ts:45-53`) on dashboard (`dashboard/page.tsx:111-114`). Cover `cohortPercentile` always null (`adapter.ts:729`).
- Fix: `components/dashboard/landing/executive-synthesis.tsx` from `report_v2`; `inferPhase` everywhere; fill percentile; strengths/gaps from cards.

## 5. Layout / consistency
- `moneyOnTable` never mapped from `gather.ts:705-713` (`orchestrator.ts:1053-1062`) → always empty (`adapter.ts:846`). `scoreNote/proposedScore/degradeReason/consistencyIssues` unrendered. Audit copy `grounded:false` = "not yet audited" (`shared.tsx:73`, `tbr-pdf.tsx:342`, `tbr-docx.ts:273`) while pipeline `grounded` = "cites an id" (`agent-dispatcher.ts:1257`); `revised` always false. Free-tier web vs PDF trimming (`chapter.tsx:72-90` vs `free-tier.ts:41-72`); DOCX has no locked-preview twin. `unitEconomics`/`comparables.rows` unrendered; cover shows SVI stage 0–7 beside 12-phase id (`cover.tsx:38`).

## 6. Showcase
- `/showcase/blockid` = metadata mirror of `content/reports/*.md`, no TBR. `/tbr/demo` = invented fixture. `scripts/run-self-analysis.mjs` → `content/reports/blockid-self-analysis-2026-06-18.md` (stale, admin-only viewer). BlockID projects in DB have 87–180 snapshots, zero `report_v2`.
- Fix: extend `run-self-analysis.mjs` → `runReportPipelineForProject(tier:"standard")` + persist; `app/showcase/blockid/report/page.tsx` renders `<TbrReportV2>` unlocked.

## 7. Quality controls
- Call cap only (`TIER_CALL_MAX free 16 / standard 30 / premium 40 / investor_memo 48`, `orchestrator.ts:118`), no A$ stop; budget degrade → deterministic cards (`agent-dispatcher.ts:1351-1358`); `quality.groundedShare` (`orchestrator.ts:993`); consistency gates (`consistency-gates.ts:119-216`); prompt-eval nightly on `test-fixtures/prompt-eval/{AIR-*,TBR-<dim>-v2.0.0}.json`. **No per-run quality artefact** under `content/reports/`; KPI groundedShare ≥ 0.85 has no tile.
