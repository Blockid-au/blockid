# BlockID.au — SOURCE OF TRUTH

> **Version:** 2026-09-20 (rev.321) · **Owner:** CEO (Do Van Long) · **Consumer:** founders, human team, autonomous loop agents.
> **State 2026-09-19:** goals G1–G17 closed or founder-blocked; live v3.17.0 (`c6a00850b`); positioning = Startup Value Index by BlockID, evaluator-first; pricing v4 (Free / Starter A$29 / Growth A$69 · Scout A$79 / Firm A$149 / Program A$349 · Fund A$999 / Intake link A$249 / Index API A$299 · Cohort 25 A$5K / Cohort 100 A$15K per year · Trusted Business Report A$3); G18 truth sweep, G19 report quality (peer, batches 1–2) and G20 ready-for-sale shipped 2026-09-20.
> **Rule:** Consult this file BEFORE any specialised plan doc. Every specialised plan carries a top-of-file back-link to this one.
> **Entity:** marketing surfaces PPL Food PTY LTD · Sydney NSW (founder decision 2026-09-10; no ABN/ACN in copy) · billing / legal / invoices / JSON-LD Auschain PTY LTD ACN 659 615 111 / ABN 79 659 615 111 (deliberate split — keep both).

---

## 1. Active Goals

### G1 — Reseller module v1
- **Source:** [`docs/plans/reseller-module-goal.md`](./reseller-module-goal.md) · plan [`docs/plans/reseller-module-plan.md`](./reseller-module-plan.md) · delta [`docs/plans/plan-delta-2026-07-23.md`](./plan-delta-2026-07-23.md)
- **Tick:** 317 · **Track A focus:** `P2_redemption_attribution` (P0/P1/P2 landed, P3/P4/P5/P6/P7/P8 shipped except P8.5, P10 pin-hardening in-flight). **Track B focus:** `done` (B1..B10 shipped).
- **Status:** in-progress; C-Level blocking reviewers all approved; advisory notes closed at tick 68.
- **Next action:** P10 wire-shape pin cross-surface pairs continue (auto-loop). Track A HUMAN-BLOCKED on P1.5 (InfoVision seed) only — **P8.5 resolved 2026-09-11** (annual add-on price `price_1UEdGcJ7OAnXQ9sVGs6PUrqy` minted A$590 GST-inc; monthly already live; both in `.env` + `.env.runtime`).
- **Blocker:** H.20 InfoVision ABN + contact email for `0106_infovision_seed.sql`; reseller-agreement counsel (founder).

### G2 — Real-world workflow parity
- **Source:** [`docs/plans/real-world-workflow-parity-audit-2026-07-23.md`](./real-world-workflow-parity-audit-2026-07-23.md)
- **Status:** audit remediation 9/10 shipped; #10 remains founder-blocked (needs real founder quotes).
- **Top-10 remediation (ranked):**
  1. Publish canonical 8-stage vocabulary (`architecture-designer`, S) — **shipped** (`7f499264`).
  2. Wire 12↔8 bucket map `web/src/lib/journey-map.ts` (`typescript-pro`, M) — **shipped** (`c4b70877` + `a1bf4542`).
  3. Extend data-room to 60+ items with AU compliance (`au-compliance`, M) — **shipped** (`b0ae8d8b`).
  4. Add Tax + AU-Compliance sections to template library (`au-compliance`, M) — **shipped** (`b0ae8d8b`).
  5. Overlay canonical-stage badges on 4 showcase cases (`react-expert`, S) — **shipped** (`b9ee0b7f`; dashboard parity `29c00fe1`).
  6. Add Airwallex + Culture Amp showcases (`deep-research` → `react-expert`, L) — **shipped** S19-B 2026-09-11 (`/showcase/airwallex`, `/showcase/culture-amp`; public-record facts only, every A$/US$ figure carries a `data-source` link + illustrative-SVI disclaimer; data `web/src/lib/showcase/public-record/cases.ts`).
  7. Reseller `customer_stage` tracking (`db-migrate` + `fullstack-guardian`, M) — **shipped** S19-B 2026-09-11 (migration 0333 `reseller_customers` — ladder `lead → onboarded → scored → data_room → fundraising → invested` + `churned`, `stage_source auto|manual`; nightly cron `reseller-stage-sync` never moves backwards; owner/admin override `POST /api/reseller/customers/[id]/stage` audit-logged; weekly digest "n customers moved stage this week"). **Migration 0333 pending manual apply.**
  8. Add Step 6 "Create first startup" to onboarding wizard (`nextjs-developer`, S) — **shipped** (`41ab0cfc`; Stripe-return route fix `b176ffea`).
  9. Rename SCN externally + cite framework overlays (`code-documenter`, S) — **shipped** (`9e5d71e8`).
  10. "Real founder was here" callouts across 12 guide chapters (`deep-research` + `typescript-pro`, L) — founder review.
- **Next action:** none pending — 0333 is in the ledger and `reseller-stage-sync?dry=1` answers ok (4 resellers, 0 customers) as of 2026-09-13; #10 stays parked until real founder quotes exist.
- **Blocker:** item 10 needs real founder quotes (founder-supplied); no invented testimonials.

### G3 — SVI Exchange (SVI EXC)
- **Source:** [`.claude/goals/svi-exchange-orchestration.md`](../../.claude/goals/svi-exchange-orchestration.md) · queue `web/content/reports/svi-exchange-tasks.json`
- **T_SVI_EXC_0001** Watchlist table + API (v0.2) — **DONE** (`shipped_v2.18`).
- **T_SVI_EXC_0012** Founder-side secondary offer intake form (v0.5) — **DONE** (tick shipped 2026-07-23, commit `bfd7a5bf`).
- **T_SVI_EXC_0013** Investor EOI book (v0.6) — **DONE** (`shipped_v0.4.0_ujveHxHJR157`).
- **T_SVI_EXC_0014** Institutional API tier — pricing + auth (v0.9) — **DONE** (`marked_deployed`).
- **Status (2026-09-11):** **✅ CLOSED** — queue `svi-exchange-tasks.json` is 15/15 done; nothing pending for the orchestrator.
- **Blocker:** none.

### G4 — Feature-upgrade roadmap v2
- **Source:** [`.claude/goals/feature-upgrade-roadmap-v2.md`](../../.claude/goals/feature-upgrade-roadmap-v2.md)
- **Status:** Q3 2026 in-flight (partially shipped; 21 sections). **v3.9.23 sync — items 1 and 2 shipped as Contact Sales row** (Enterprise custom + Accelerator A$500+ + VC A$349+ landed via B8 `58f34d45d`).
- **Top-5 open Q3 items:** (SHAs re-mapped after the 2026-09-10 history rewrite)
  1. Enterprise tier + team features — **shipped** (Contact Sales row Enterprise custom + `founder_enterprise` SKU + project members via `d7557c3ec`).
  2. Accelerator partnership pricing — **shipped** (Contact Sales row Accelerator from A$500/mo + `accelerator_*` cohort SKUs via `d7557c3ec`).
  3. PDF branding customisation for paid plans — **shipped** (feature-gate + settings form `b9311f0eb`; renderer wire `adbc48326`).
  4. Dashboard personalisation (pin/reorder widgets) — **shipped** (server-synced layout, S6-A 2026-09-11).
  5. ProductHunt launch campaign — **partial** (launch kit `docs/marketing/traction-kit-2026-09/producthunt-launch-kit.md`; launch dated 13 Oct 2026, founder-gated).
- **Reconciliation 2026-09-11:** all 61 unchecked roadmap-v2 items checked against code + git log → **25 shipped (ticked), 28 partial, 8 open**; per-item evidence inline and a ranked 8-item no-human-input backlog under `## Reconciliation 2026-09-11` in the roadmap-v2 file.
- **Next action:** none engineering — reconciliation 2026-09-18: the ranked 8-item backlog is fully shipped in code (#1 `project_members` readers in `lib/projects.ts` S-IA/G13, #2 S20-A audit wrapper + 0335, #3 S20-B + G14-S38 destinations, #4/#5 S17-B, #6 NDA gate `lib/dataroom/nda.ts` on `/s/dr/[token]`, #7 `lib/dataroom/engagement-client.ts` section views, #8 per-recipient watermark in `api/data-room/share/[token]/pdf`). ProductHunt (13 Oct 2026) waits on the founder. **G4 engineering closed.**
- **Blocker:** none.

### G5 — Pricing upgrade v2 — **✅ CLOSED (v3.9.23, 2026-09-07)**
- **Source:** [`docs/pricing-upgrade-plan-2026-07-16.md`](../pricing-upgrade-plan-2026-07-16.md)
- **Status:** **CLOSED** — Universal 3-rung ladder shipped v3.9.23 workstream B (B1..B8). Founding-50 marketing surface purged (Stripe SKU grandfathered only). GST unified (exclusive). Credit packs monotonic. Persona pages deep-link to `#tier-growth`/`#tier-pro`. `share_management` gate resolved for Growth. Trial-days copy reconciled (7-day public / 14-day pilot).
- **Ship commits (v3.9.23):**
  1. B1+B2 pricing consolidated to Free / Growth / Pro public ladder + `public:false` flag on hidden SKUs — `5f45f0b63`.
  2. B3-lite legacy `PRICING_TIERS` array retired — `d68a37eb9`.
  3. B3-tail Founding-50 legacy marketing surface purged; Stripe SKU kept for grandfathered renewals only — `1ec7f2657`.
  4. B4 GST-exclusive unified — `1820f99f8` + `20a7541fb` (FAQ JSON-LD + body stragglers).
  5. B5 persona pages deep-link `#tier-growth`/`#tier-pro` — `da9f36000` + `e6c99cbc3`.
  6. B6 share_management gate resolved — `1188c130e`.
  7. B7 trial-days copy reconciled — `2ddc7381d`.
  8. B8 3-card ladder + ContactSalesRow (Accelerator A$500+ / VC A$349+ / Enterprise custom) + credit-pack monotonic fix — `58f34d45d`.
- **Residual:** PRC-EQ Equity-for-solution workflow remains `human_blocked` on `legal_review_passed=true` — carries forward under G4 as its own tracked item.
- **Blocker:** none — goal closed.

### G6 — Unicorn masterplan
- **Source:** [`.claude/goals/unicorn-masterplan.md`](../../.claude/goals/unicorn-masterplan.md)
- **Pillars (1-sentence status each):**
  - **Revenue trajectory** — 2026 H2 A$60K ARR target; SVI credits + subscriptions live, reseller channel booked (2027 A$150K → 2030 A$8M).
  - **Cap-table & Fundraise** — Pre-Seed A$500K target; deck v1.1 shipped with Channel-Economics slide; data-room GTM one-pager shipped.
  - **Product roadmap (8 phases)** — Phases 1-3 shipped; Phase 4 (Equity/Cap Table) in-flight via reseller add-on (P8) + Share Management drawer.
  - **C-Level self-upgrade** — 11 C-Level agents on cron; CTO/CFO/CISO/CLO advisory notes closed tick 68.
  - **Blockchain (CBO)** — Private EVM (Anvil chainId 420) + Otterscan live at `:5173`; ping.pub reserved for Cosmos testnet.
  - **Governance & Compliance (CLO/CISO)** — APP 5 notice + reseller agreement template shipped; SOC2 lite backlog open.
  - **Team/ESOP (CHRO)** — Div 83A qualifying-tests checklist landed tick 2026-07-23; ESOP scheme placeholder in ch08.

### G7 — UX information architecture (startup flow)
- **Source:** [`docs/plans/ux-ia-startup-flow-goal.md`](./ux-ia-startup-flow-goal.md) · user note [`docs/user/menu-walkthrough.md`](../user/menu-walkthrough.md)
- **Status:** **P0–P9 all shipped — G7 closed 2026-09-11.** P8 founder review resolved by delegation (founder 2026-09-11: adopt each recorded `recommendation:` as the decision); S19-A recorded `decision:` / `decided_by:` / `status: shipped` on Q1–Q4 in the goal doc and closed the two code gaps (Q3 mobile collapse, Q4 skip anchor). The v3.9.23 Free Tools dropdown grouping (A7, commit `9c247175d`) had already resolved the top-nav grouping strategy in-flight.
- **Ship commits:**
  1. `43f172f3` — goal doc with audit + IA proposal + phased plan (313 lines).
  2. `27ab1553` — global DEMO menu (NavV2 + legacy site/navbar + WorkspaceLayout topbar + both footers) linking `/showcase/atlassian?step=1`.
  3. `79672c4a` (auto-tick) — `JourneyStepLadder` 12-phase visual + dashboard mount.
  4. `a5c295e0` — E2E `web/tests/e2e/nav/menu-structure.spec.ts` pinning nav contracts per role.
  5. Round 5.13 — P5 progressive-disclosure polish (`Later phases (N)` collapse + lock glyph), P6 role-menu-overlay helper (`web/src/lib/nav/role-menu-overlay.ts`), P7 a11y contracts (`aria-haspopup="menu"`, workspace `aria-label`, disclosure buttons) + `docs/design/menu-a11y-audit.md`.
  6. P9 close-out: SOURCE-OF-TRUTH + `docs/user/menu-walkthrough.md` refreshed to reflect P5–P7 shipped; goal doc `phased_tracks.P9` flipped to `shipped`.
  7. **S19-A (2026-09-11) — Q1–Q4 decisions adopted:** Q1 ≤ 5 phase-clusters (already true after G8-P3/S7-A; pinned in `workspace-layout.test.tsx`) · Q2 Demo = top-nav link everywhere, no floating CTA (already shipped; pinned in `nav-v2.test.ts`) · Q3 `journey-step-ladder.tsx` keeps all 12 on desktop, collapses to current + next 2 below 640 px with a "Show all 12 phases" toggle (`aria-expanded`, motion-reduce safe) · Q4 completed phases = check-mark links to `PHASE_ROUTES`, "Skip to current phase" anchor (`#phase-current`) once index ≥ 3, current `<li>` gets `id="phase-current"` + `aria-current="step"` once (single responsive list). Colocated `journey-step-ladder.test.tsx` (12 tests).
- **Next action:** none — decisions adopted, lane closed. Successor work lives in G8 (sidebar) and G11 (public nav).
- **Blocker:** none. No new deps, no CI touch, tsc clean.
- **Successor:** G8 continues this lane — G7 built the IA, G8 makes it phase-aware and closes the chrome gaps G7 left untouched.

### G8 — Progressive unlock & chrome parity
- **Source:** [`docs/plans/unlock-next-level-2026-07-31.md`](./unlock-next-level-2026-07-31.md) · continues G7 · founder request 2026-07-31
- **Decisions (founder, 2026-07-31):** unlock spine = **Growth phases 12** (`vision`…`funding`, the taxonomy `startup_phase_progress.phase_id` already stores) · lock policy = **hybrid** (progress-locked ⇒ hidden, tier-locked ⇒ dimmed + upgrade chip).
- **Status (reconciled 2026-09-10):** **P0–P7 shipped** (goal doc §5: P2 migration 0303 `963a36332`, P3 hybrid nav `1f91c916a`, P4 next-unlock card `610aca3ab`, P5/P6 shell matrix + chrome backfill `56dd31a6b`, P7 CI guard `dccdbe8`). **P8 docs in flight (S6-B)** — generated phase×tier matrix + `/docs/unlocks`.
- **Audit findings (ranked):**
  1. **✅ FIXED (P0)** — two different 12-phase taxonomies were silently conflated. `startup_phase_progress.phase_id` stores string ids; `PHASE_CRITERION_SUBSET` + `COMPLIANCE_PHASE_GATES` key off numeric `PhaseKey 1-12` with **different phase-N semantics** (string #6 = `legal_equity`, numeric #6 = Revenue/Business Model). `web/src/lib/nudge/next-steps.ts:426` bridges via `indexOfGrowthPhase(id)+1` — mis-scored every founder. Closed by `web/src/lib/growth/phase-taxonomy.ts`: string ids are canonical, the numeric bridge is explicit and test-pinned, and `readiness_by_phase` + `current_phase.slug` now key on growth ids.
  2. **✅ FIXED (P1)** — no advance-gate existed for the 12-phase model, only `completion_pct`. `web/src/lib/growth/phase-gate.ts` now mirrors Unicorn's `computeStageProgress()`: `PHASE_EXIT_RULES` (required criteria + SVI dimension floors per §2c), four typed blocker codes, partial-credit `completionPct`, opt-in deliverable gating.
  3. **~70 pages render with no shell** — `/reseller/*` 13/13, `/showcase/*` 15, `/admin/*` 24/36, `/compliance/*` 7/7, `/workspace/*` orphans 8, marketing strays 7. Root layout renders no chrome (`web/src/app/layout.tsx:96-172`); shells are opt-in per file.
  4. **No footer in `WorkspaceLayout` or `AdminLayout`** — ~107 pages have no bottom bar.
  5. **Unlock machinery built but imported by nothing** — `web/src/lib/nav/filter-nav-for-user.ts:94` and `web/src/lib/nav/hide-when-locked.ts:47` already implement decision D2 exactly; the live renderer `workspace-layout.tsx:141-168` ignores `persona`, `journeyGroup`, `hideWhenLocked` and dims `minPhase` groups instead of hiding them.
  6. `web/src/app/reseller/layout.tsx:11` carries a "P4 hardening will replace this with the reused WorkspaceLayout" TODO that was never executed.
- **Evaluation criteria (§2c of goal doc):** 12 phase exit gates, each requiring named criteria at ≥ `good` (from the 13 in `web/src/lib/evaluation-criteria.ts:66`, weights = 100) **plus** an SVI dimension floor (from the 8 in `web/src/lib/svi-analysis.ts:1195-1275`). Compliance gates re-mapped by intent: `rd→product_dev`, `gst→go_to_market`, `esic→investor_review`, `s708→investor_review`.
- **Next action:** none — P8 docs shipped (`/docs/unlocks` live, 200 on 2026-09-18). **G8 closed 2026-09-18.** Historical: P8 docs (S6-B) then close G8. Historical note: P2 persist unlock state (migration 0300 on `startup_phase_progress`) then P3 nav wiring. ⚠ The autonomous loop is concurrently landing route-group moves (`(marketing)`/`(app)`/`(persona)` — commits `76f2febe`, `e34f3fed`, `9ae661ff`) that overlap P5/P6; reconcile the shell matrix against that work before starting chrome backfill.
- **Blocker:** none for P0/P5. Q1–Q3 in the goal doc are founder-review only and non-blocking.

### G9 — Value-First Hero (user outcome copy enforcement)
- **Source:** [`docs/plans/value-first-hero-goal.md`](./value-first-hero-goal.md)
- **Status:** open — reference/enforcement goal; no autonomous loop (VALUE_FIRST_HERO_LOOP=off).
- **Principle:** Every hero section must lead with what the user GETS and what hard problem it SOLVES — not with product identity or marketing copy. Marketing/pricing CTAs are lower priority.
- **Success criteria (summary):**
  1. All `/tools/*` hero H1s lead with a concrete user outcome + the hard problem solved — never a product name or category label.
  2. Landing page hero sub-headline quantifies benefit with numbers (time, AUD, %, count).
  3. No tool page H1 starts with "tool" or "calculator".
  4. Tool page CTA buttons use outcome language ("Get my valuation") not action language ("Submit", "Calculate").
  5. Dashboard first-view shows a live founder-relevant metric above the fold — not a welcome banner.
- **Phased tracks:** P0 audit → P1 tool hero copy → P2 CTA language → P3 landing numbers strip → P4 dashboard metric card.
- **File boundaries:** `web/src/app/tools/*/page.tsx` (copy only) · `web/src/components/landing/hero-v3.tsx` (numbers strip) · `web/src/app/dashboard/page.tsx` (additive metric card). Do NOT touch `web/src/lib/**`, `nav-v2.tsx` (G7), `/pricing/**` (G5), migrations, or CI.
- **Blocker:** Q1 (which live metric for dashboard card) is founder-review-blocking for P4. P0–P3 have no blockers.

### G10 — v3.9.23 Unify sprint (message truth + pricing + feature drift)
- **Source:** approved plan `h-y-review-t-on-b-foamy-pixel` (Agent C audit → workstreams A/B/C/D).
- **Status:** **✅ SHIPPED (2026-09-07)** — full messaging↔code synchronisation sprint. Release `JctZ0PfqkXowL6mTrcEs3`, git `8ed44c24a`, `web/package.json` `3.9.23`.
- **Workstreams (all closed):**
  - **A — message truth on marketing surface (8 items):** A1 score-first hero H1 + quantified subheadline (`a2b5c8971`); A2 quick-tag chips route to real targets (`4b5288699`, chips render as `<a href>` `8ed44c24a`); A3 "50+ AI agents" → "11 C-Level agents" (`f8001541a`); A4+A5 "13 criteria" → "8 SVI dimensions" verbatim PRD labels (`7080aecc8`); A6 CTA subtext scrub — no implicit user-count (`5f853225a`); A7 Nav Free Tools dropdown (17 tools grouped) + demo unify + hide Compare (`9c247175d`); A8 Revenue Tracker card → Trust Report share links (`dccb78487`).
  - **B — pricing simplification (8 items):** B1+B2 3-rung public ladder + `public:false` on hidden SKUs (`5f45f0b63`); B3-lite legacy `PRICING_TIERS` retired (`d68a37eb9`); B3-tail Founding-50 marketing purged (`1ec7f2657`); B4 GST-exclusive unified (`1820f99f8` + `20a7541fb`); B5 persona deep-links (`da9f36000` + `e6c99cbc3`); B6 share_management Growth gate (`1188c130e`); B7 trial-days (`2ddc7381d`); B8 3-card + ContactSalesRow + credit-pack monotonic (`58f34d45d`).
  - **C1 — investor pack CLevelChapter render + ToC 9 chapters** (`2dd695b98` + `f50a42be1` + colocated test).
  - **D — under-promised feature surface:** D1 `/features` page (cohort percentile, per-investor tracked share links, ATO tax invoice, dividend engine, 17 free tools, 12-chapter guide, evidence completeness, LP anonymisation) (`aead3ed1e`); D2 sitemap + nav (`0850f2b25`); D3 `/tbr/demo` hero tertiary link (`947057278`).
  - **Followups:** homepage title + og:image:alt (`abeca292e`); Playwright post-deploy smoke (`b6d83a852`).
- **Agent C drift-audit resolutions (unify list §7 P0/P1):**
  1. ✅ Founding-50 name/price drift — purged from marketing surface (B3-tail).
  2. ✅ CLevelChapter render — shipped C1.
  3. ⚠️ Agent count reconciliation — marketing surface aligned to 11 (A3, `/features`, PRD §6). Follow-up: **trend UI `Role` union in `web/src/components/dashboard/AgentTrends.tsx` still limited to 5 roles**; carried forward as G10-followup, low priority.
  4. ✅ Blockchain stack — current implementation clarified as private EVM (Anvil chainId 420) + Otterscan across README, ARCHITECTURE.md, PRD §17, blueprint §7; Cosmos SDK retagged long-term roadmap.
  5. ✅ Investor pack ToC bumped to 9 (Evidence Completeness added).
  6. ✅ Founding-50 route deleted; Stripe SKU grandfathered.
  7. ✅ Hero H1 aligned to product name and quantified promise.
  8. ✅ Under-promised capabilities surfaced at `/features`.
- **Blocker:** none. Sprint closed.
- **Docs synced (this tick):** README.md (new), ARCHITECTURE.md, GOALS.md, ROADMAP.md, KNOWLEDGE_BASE_INDEX.md, blockid_prd.md, blockid_master_project_blueprint_v1.md, blockid_gtm_sales_first_v1.md, blockid.au.md (deprecated banner), this SOURCE-OF-TRUTH.md, `/version` page + `web/CHANGELOG.md` + `web/package.json` bump to 3.9.23.

### G11 — Money Finder: simple public menu + "Do you need money?" (AU grants, programs, Founder Radar)
- **Source:** [`docs/plans/money-finder-2026-09-10.md`](./money-finder-2026-09-10.md) · seed data `web/content/data/grants-au.seed.json` (56 rows) + `web/content/data/programs-au.seed.json` (8 capitals + national)
- **Status (2026-09-10 19:50 UTC):** **S0–S5 LIVE — G11 complete** (final release `fea2aa62b`, Gate 12 12/12; S4 release `b119b8627`, nav E2E 5/5 on production) — S0 T0237 · S2 T0239 / T0238 / T0241 / T0250 · S3 T0240 (grant-advisor), T0242 (`/funding` intake → free preview → A$3 `FUNDING_REPORT_3AUD` / 3 credits / plan; migrations 0315–0316), T0243 (`refresh-funding-sources` cron `0 4 * * 0`) · S4 T0244 (`/funding/report/[id]` SVG Gantt + PDF + data-room save, `/workspace/funding`), T0245 (`funding_matches` 0318, `money-radar-sweep` `0 5 * * 0`, bell + ICS), T0246 (radar drips T-30/14/3 + `status_changed` 0320, digest money block, program events, `svi_trend_alert`), T0247 (Founder Radar in Starter, Package 90-day grant 0319, A$3→subscribe upsell), T0248 (`MoneyRadarTile` 5 states on `/dashboard`, copy pack EN/VI, `/vi/funding`), T0249 (pricing FAQ + consultant anchor, 9 insight cross-links, `funding_directory_viewed`, Stripe sync drift). Stripe `STRIPE_PRICE_FUNDING_REPORT` minted; 7 dead program URLs repointed after `verify-funding-urls`. · S5 T0251 (Growth extras: `au_grants.application_prompts` 21 seeded + `grant_application_drafts` 0323, `POST /api/funding/draft` Growth free / Starter 2 credits with confirm, investor reverse-match tab + intro mailto, `analysis-refresh-quarterly` cron `0 6 1 1,4,7,10`). **Open follow-ups (not blocking):** `investor_discoverable` opt-in switch shipped on `/workspace/investor/preferences` (deploys 2026-09-11 12:05 UTC); GA4 `hero_variant` custom dimension still to be created in the GA4 console; ESLint baseline cleared 2026-09-10 21:40 UTC (271 → 0). Ledger = `implementing-plan.md`.
- **Founder decisions (2026-09-10):** public nav = 5 items (Get my score · Get funding ▾ · Free tools ▾ · Pricing · Demo ▾) + CTA "Do you need money?" · gate = A$3 one-off (guest SKU, same pattern as One-Click Report) + 3 credits + plan-included · programs = all 8 capitals in v1 · "Founder Radar" alerts/re-match/digest bundled into **Starter A$29** (no new tier, no add-on).
- **Parts:** A public-menu simplification (legacy `site/navbar.tsx` mirrors, not retired — 52 importers, shell swap stays in G8-P6) · B Money Finder (`au_grants`/`au_programs`/`funding_reports`/`project_grant_profiles`, `lib/agents/grant-advisor.ts`, `/funding*`, A$3 SKU) · C Founder Radar (deadline drips T-30/14/3, monthly re-match, weekly digest money block, ICS, capital map, application drafts) · D weekly refresh cron + dashboard `MoneyRadarTile` + messaging pack + **D-5 hero one-liners** (founder view F1 "See your startup the way an investor will — your score, what it's worth, and where the money is, in 60 seconds." · investor view I1 "One score across 8 investor dimensions, backed by evidence — screen an Australian startup in minutes, not weeks." · tagline "A credit score for startups."; winners recorded back in G9).
- **Amendments:** G7 public-nav file boundary and G9 "do not touch `nav-v2.tsx`" are superseded by G11-P1 for the **public** nav only; G7 "never hide a feature" continues to govern the logged-in sidebar (G8).
- **Positioning constraint:** business.gov.au says "don't pay for government grant information" — lists + official links stay free; A$3/plan buys eligibility analysis, ranking, A$ estimate, timeline, drafts. CC BY 3.0 AU attribution on reused Commonwealth text.
- **Execution model (2026-09-10 review):** the autonomous code channel is effectively off — goal-loop driver removed `fd7bb0b03` (2026-08-13), `agent-auto-improve` ships 0 (all agents FROZEN), only `self-upgrade-agent.sh` implements (1 task/night, 1–3 files, stale priority list). G11 therefore ships via **founder-driven Claude Code sessions in 6 waves** (worktrees, off-peak deploys), `project-state.json` as ledger, T-id in every commit subject so `stageUpdateArtifacts` closes tasks + bumps version. Night loop takes only S-size follow-ups (T0249).
- **Next action:** see **Unified sprint plan** below (S0 → S5). G11 lanes: S0 T0237 → S2 T0239 ∥ T0238 → T0250 → T0241 → S3 T0240 → T0242 → T0243 → S4 T0244 → T0245 → T0246 → T0247 → T0248 → T0249 → S5 T0251.
- **Blocker:** none for Wave 0–1. Human-blocked before Wave 3/5: mint `STRIPE_PRICE_FUNDING_REPORT` (T0242); Q1 Starter label (T0247); Q2 free-tier in-app alerts (T0245); GA4 `hero_variant` dimension (T0250); `ABR_GUID` env (T0244) — see §5.

### G11 + G12 — Unified sprint plan (pre-implementation review 2026-09-10, approved)
- **Source:** goal docs §9 (`money-finder-2026-09-10.md`, `evaluator-traction-2026-09-10.md`) · ledger `project-state.json` tasks carry `sprint` (S0–S5) + `priority` (P0–P3).
- **Order:** **S0 Hygiene** T0237 (re-id collisions · `nextTaskId` max+1 · `merged` status · `stagePlan` content · migration **0309** DB `plans` = csv · entity → **PPL Food PTY LTD** · header drift) → **S1 Sell to evaluators** T0268 → T0269 → T0275 → T0274-1 → **S2 Founder funnel + data** T0239 ∥ T0238 → T0250 → T0241 → **S3 Reports that make money** T0240 → T0242 ∥ T0270 → T0271 → T0243 → **S4 Retention** T0244 → T0245 → T0246 → T0247 ∥ T0273 → T0248 → T0249 (night loop) → T0274-2 → **S5 Scale** T0272 → T0251. ~14 working days, ≤ 2 parallel worktrees, deploys off-peak, T-id in every commit subject.
- **Review findings that changed scope:** nav consolidated `1c359f000` (T0238 shrinks: edit `MENU` 7→5 + CTA); DB `plans` rows for 7 B2B SKUs still 0074 seed (**blocking**, → 0309 in S0); csv flag names ≠ page gates (→ T0268 uses gate vocabulary); A$5.50 SKU re-priced in place (Q-C); `/solutions/advisor` 301 → `/for/advisor` selling Growth A$69 (→ T0274); two privacy policies, provider list only Anthropic (→ T0275); snapshot test `tier-visibility` must be regenerated with new flags; `deploy-live.sh` = 12 gates; `menu-structure.spec.ts` not run by deploy.
- **Go checklist (2026-09-10 11:45 UTC):** ✅ `npm test` on HEAD `7cb4f03fb` — 1,110 files / 28,717 tests passed (85 s) · ✅ `/api/health` ok v3.10.0 (db, stripe, audit_chain, ga4), guardian healthy every 2 min, load 0.4–1.5 — the 87.1 % 24-h figure is the earlier incident window, not a current fault · ⏳ Stripe evaluator prices (S1 ships without checkout until minted) · ✅ entity = PPL Food PTY LTD, no ABN in copy · **ready for founder "go" → S0 T0237 starts same day.**

### S6 — Close-out & traction kit (opened 2026-09-10 22:00 UTC, founder: "tự động tiếp tục phase tiếp theo cho tới khi xong")
- **Scope:** finish what the register still lists as open without a human blocker — **S6-A** G4 #4 dashboard personalisation → server-synced layout (`app_users.dashboard_layout`, migration 0326, `GET/PUT /api/dashboard/layout`, localStorage cache) · **S6-B** G8 P8 docs (generated phase×tier visibility matrix + unlock criteria → `docs/user/menu-walkthrough.md` + `/docs/unlocks`) and G9 P4 close-out (dashboard live metric = SVI position + Money Radar tile) · **S6-C** G11/G12 traction kit T1 (angel groups) + T2 (accelerator pilots) + G4 #5 ProductHunt launch kit + week-1 LinkedIn posts under `docs/marketing/traction-kit-2026-09/`, pilot CTA on `/solutions/accelerator`.
- **Rules:** no fabricated social proof (placeholders only), entity PPL Food PTY LTD, doctoral sentence verbatim, ESLint stays at 0 errors, deploys off-peak (UTC 12–20) only.
- **Sequence:** scheduled deploy of the review fixes 2026-09-11 12:05 UTC → live verify + smoke → merge S6-A/B/C → full suite → second off-peak deploy → post-ship review → fix → SOT sync. Still human-blocked elsewhere: G1 (InfoVision ABN, `STRIPE_PRICE_ADDON_SHARE_MGMT_*`), G2 #6/#7/#10 wording, GA4 `hero_variant` dimension.

### S7 — Consistency & conversion (opened 2026-09-10 23:15 UTC, autonomous continuation)
- **Found during S6:** (a) the workspace sidebar hides phase-gated groups on ~133/135 pages because only `/dashboard` and `/workspace/funding` pass `currentPhase` (menu changes shape page-to-page); (b) no public Money Finder sample report (conversion + ProductHunt screenshots); (c) evaluator **trial gets the full Scout quota (10 reports)** instead of plan §3b's 1 included report — AI-cost exposure on a cancellable card.
- **Scope:** **S7-A** one founder nav-phase resolver (`lib/nav/founder-phase.ts`, SVI band ∨ 12-phase bucket, provided by the `(founder)` route-group layout) · **S7-B** `/funding/report/demo` (deterministic from the seed, sample banner, "Build mine for A$3") + Gate-12 hydrated smoke for `/funding` intake→preview, demo report, `/docs/unlocks`, `/compare/chatgpt`, pilot CTA · **S7-C** trial allowance 1 then credits, batch limited to the same 1, trial banner + dialog copy, reminder line.
- **Status (2026-09-11 00:10 UTC):** S7-A/B/C merged on master `3bf836a36` (1,235 files / 30,025 tests green, ESLint 0) — ships with the 12:05 UTC deploy.

### S8 — Quality gates on the new surface (opened 2026-09-11 00:10 UTC)
- **Scope:** code-level audits with fixes, one report each under `docs/plans/reviews/` — **S8-A** SEO (metadata, JSON-LD, breadcrumbs, sitemap coverage for 56 grant + 199 program detail pages, keyword map) · **S8-B** accessibility + 400 px mobile (tabs/dialogs/switches/SVG Gantt/tables/widget controls) · **S8-C** API security (input caps, SSRF guard on `fetch-source`, constant-time cron auth, cache headers, CSRF posture) · **S8-D** (after deploy) live performance pass on `/funding*` with `perf-audit`.
- **Status (2026-09-11 02:10 UTC):** S8-A/B/C merged (reports under `docs/plans/reviews/`: SEO — doubled title suffix + lost OG images on 16 page types + invalid JSON-LD fixed, 53+199 detail pages in sitemap; a11y — 26/31 fixed incl. a real light-theme contrast bug on the Next-unlock card, modal focus traps, keyboard reorder; security — SSRF guard, safe redirects, constant-time cron auth, CSRF `Sec-Fetch-Site` check, body caps, rate limits). Full suite 1,248 files / 30,152 green, ESLint 0. Ships with the 12:05 UTC deploy. **S8-D** perf pass after deploy. **S8-E** done (2026-09-11 03:00 UTC): all 80 cron routes on constant-time `isCronAuthorised`, fail-closed when `CRON_SECRET` unset, `local-dev` literal removed from `agent-research`, static regression guard; pre-deploy behaviour review `reviews/s7-s8-predeploy-review-2026-09-11.md` — no P0/P1, six P2 fixed (`getCurrentUser` memoised per request, Gate-12 warm-up list, etc.). **LIVE 2026-09-11 ~06:30 UTC** (founder waived the off-peak wait): first attempt failed **Gate 7** — S7-A's client context reached `server-only` supabase through a *dynamic* `import()`; fixed by splitting `lib/nav/founder-phase-shared.ts` + a repo-wide client→server-only import-graph guard (`lib/client-safe-graph.test.ts`, walks all "use client" files incl. dynamic imports); second attempt **12/12 gates** (lint gate clean for the first time), 18/18 hydrated smoke, 80/80 cron routes → 401 on a wrong secret, nav e2e 5/5. Post-ship: S8-D perf pass + review of the unreviewed range in progress. Remaining follow-up: `rejectCrossSite` in `proxy.ts` (S9-A ready in a worktree).

### S9–S11 — post-launch hardening & conversion (2026-09-11, founder: deploy each phase immediately; review → test → fix after every deploy)
- **S9 LIVE** (12/12): **S9-A** cross-site cookie-authenticated `/api/**` mutations rejected once at the edge proxy (`crossSiteApiGate`, allow-list = the SVI-exchange origin on `/api/watchlist|eoi`); per-route guards removed, static guard prevents re-adding · **S9-B** 252 program/grant detail pages enriched from seed fields only (at a glance · who for · what you get · how to apply · timing · related · FAQ + `FAQPage` JSON-LD) — median 145 → 612 words, 0 pages under 300.
- **Post-ship fix deploy** (12/12): constant-time secret handling outside `cron/` (`api/internal/ai-complete`, `api/status`, `history/ingest`, GitHub webhook length guard), API-wide `CRON_SECRET` scan, trial count bounded by `trial_end − 7 d` when `trial_start` is missing, broader dynamic-import shapes in the client-safe graph guard; **S8-D** perf: hourly tagged cache + per-request memo on the funding catalogue with `revalidateTag` from admin patch / refresh cron / seed (`POST /api/cron/revalidate-funding`) — preview API 32 → 13 ms local. Report `reviews/perf-audit-2026-09-11.md` (finding 1: root layout `headers()` nonce keeps every route dynamic — accepted for now).
- **S10 LIVE** (12/12): **S10-A** grouped, collapsible `/funding/programs` (1.28 MB → 393 KB live, all 199 detail links kept, `<details>` tails, capped ItemList) and `/funding/grants` (479 → 199 KB); `upcoming` rows with a close date now classified `future` · **S10-B** ProductHunt gallery: 6 × 1270×760 PNG + 240 thumb + `MANIFEST.md` under `web/public/producthunt/`.
- **S11 LIVE** (12/12): **S11-A** Founder Radar activation nudge — sweep found 4 subscribers / 0 targets (no profile); in-app `radar_setup_nudge` + `radar_setup` email once, `radar_setup_2` at 14 d, cap 2, `?from=radar_setup` opens the intake first; migration 0327 applied; live dry-run: 4 candidates → 4 in-app + 4 email queued for the Sunday sweep.
- **S12 LIVE** (12/12): **S12-A** `lib/seo/site-meta.test.ts` sweeps 163 public pages (title ≤ 65, one brand, description 70–165, unique, canonical) — 76 pages fixed incl. `/pricing`, tools, showcase, VI solutions; visible FAQs + `FAQPage` on `/funding/grants|programs` from product facts only.
- **S13 LIVE** (12/12): **S13-A** evaluator activation checklist (add startup → run included report → set thesis + discoverable → second startup) under the trial banner, GA4 `evaluator_checklist_*`, trial-end reminder deep link `?from=trial_reminder` opens the report dialog.
- **S14 LIVE** (12/12): **S14-A** privacy policy **v2.2** (`content/legal/privacy-v2.mdx`, `DISCLAIMER_VERSIONS.privacy = v2.2-2026-09-11`, registry migration 0328 applied): Money Finder intake + optional demographic flags, guest A$3 purchases, Radar emails + opt-outs, evaluator-entered third-party startup data + founder claim/removal route, investor discoverability, 12 retention rows; approved data sentence verbatim, no training statement.
- **S15 LIVE** (12/12): **S15-A** `privacy-retention` cron (Mon 03:15 UTC) — 4 rules mirrored from the v2.2 §4 table (guest reports anonymised at 12 mo, radar matches 90 d, drips 90 d, claim tokens 90 d) with a parity test that parses the policy; active subscribers protected; audit `retention-history.jsonl`; live dry-run OK.
- **S16 LIVE** (12/12): **S16-A** program application drafts — `au_programs.application_prompts` seeded for 15 programs + generic 6-question fallback, `POST /api/funding/draft { program_id }` on the same gate/cost rails, kind-aware editor, draft links on report cards + detail pages, migration 0329 applied. Roadmap-v2 reconciled against code (25 shipped / 28 partial / 8 open; ranked backlog in the goal doc).
- **S17 LIVE** (12/12): **S17-A** shared projects made real — member-aware `listProjects`/`getActiveProject` (slug or UUID), `assertProjectAccess`/`getProjectScope` on 13 project-scoped routes, role-aware switcher/cards, invite accept → shared project, IDOR closed in `evidence/analyze`, migration 0331 · **S17-B** AUD valuation band (Recharts, right axis, table twin) on `/dashboard/history` + connected-MRR revenue-multiple cross-check (`SECTOR_MULTIPLES` via `vcBenchmark`), `valuation_method` persisted (0330). **Post-ship review** (`reviews/s17a-member-access-review-2026-09-11.md`): 2 P1 (owner-email fallback reachable from ~20 unconverted routes → viewer could mutate owner data; member could analyze the owner's other-project evidence) + 5 P2 — **fixed and live** (12/12): owner-email fallback only via `getProjectScope`, member IDOR closed in `evidence/analyze`, project-scoped data-room reads, invite bound to the invited email, `project_role_for` self-only (0332). Exposure was 0 (no memberships).
- **S18-A LIVE** (12/12, 2026-09-11 23:20 UTC): every project-scoped API route now resolves role + owner data key via `getProjectScope` / `projectScopeOrDeny` / `projectScopeOrRedirect` (OAuth callbacks) / `assertProjectScope` (explicit `?project_id`) — **0** `getProjectIdFromRequest` uses left under `src/app/api`. Role map: viewer = reads (`svi/*`, `score`, `valuation`, `valuation/vc`, `metrics`, `blockchain/token`, `cap-table` GET, `esop` GET/score/pool GET, `founder/*` lists, `founder/conferences`, `founder/revenue-90d`, `investor-pack/generate|preview`, `data-room/readiness`); editor = writes (`svi` POST/rescore/docx, `rnd`, `evaluation/*`, `evidence/*`, `valuation/clevel`, `cap-table` POST/DELETE, `esop/grants` POST + `[id]`, `esop/pool` POST, `esop/div83a-check`, `founder/*` create/patch/delete + `ai-fill`, `founder/gtm` PUT, `founder/crm-push`, `journal/reflect`, `term-sheet`, `investor-pack/one-click`, `data-room/auto-fill|generate`, `dataroom/reseed-templates`); admin = third-party linking (`integrations/*/callback`, `oauth/*/callback`, `auth/*/callback`, `auth/stripe/connect`); owner-only = `blockchain/create-token` mint (`ownerOnlyDenied`). `valuation/clevel` `body.email` IDOR removed. `data-room/readiness` evidence count keyed on `svi_accounts.id` (0008 FK) resolved on (owner email, project); shareholders keyed on `ownerUserId` + `project_id` in readiness/generate; cap-table POST now stamps `project_id` so the project-filtered GET sees new rows. `founder-crud` (all `/founder/*` tables) keyed on the project owner. Static guard `lib/projects.scope-guard.test.ts` (rules A/B/C, owner-only allow-list with reasons). Shared test kit: `test/project-scope-mock.ts` (`projectsMock` / `scopeAdapter`), `test/member-access-suite.ts`, `test/fake-supabase.ts`. Verified: `tsc` clean, eslint 0 errors, 100 test files / 1964 tests green over the touched dirs. Credits always charged to the CALLER (`creditChargeNote`).
- **S18-A review fixes LIVE** (12/12, migration 0334 applied): P1-1 id-keyed mutations bounded by `project_id` (`cap-table` issue/update/delete pre-checks, `esop-grants` `getGrant`/`updateGrantStatus`/`updateDiv83AStatus` take `projectId`, `data-room/auto-fill` resolves the project's room via `data_rooms(user_id, project_id)` and requires `document.data_room_id = room.id`) — editor on A can no longer touch the owner's project-B rows; P1-2 OAuth callbacks (`oauth/{ga4,github,linkedin,stripe,xero}/callback`) bound to the SESSION via `lib/project-members/oauth-session.ts` (`getCurrentUser()` + `state.email === user.email` case-insensitive → else `?error=<provider>_email_mismatch` / `_unauthenticated`, fallback `scope?.dataEmail ?? user.email`), dead legacy `/api/auth/analytics[/callback]` pair deleted (`.env.example` now lists `/api/oauth/ga4/callback` — **drop the old URI from the GCP client**); P2-1 email-only reads bounded by the project's `svi_accounts` row (`metrics` GET, `investor-pack/one-click` exit block, `journal/reflect` snapshots/evidence/actions, `svi/phase-progress` auto-detect via `findLatestAnalysisWithFallback`); P2-2 scope guard rule B also catches `getActiveProjectIdOrNull(`, new rule C2 flags `.eq(user_id|account_id, user.id)` on scope-aware routes (allow-list: `term-sheet`, `svi/docx`, `svi/full-report` — caller-owned paid rows), `competitive-positioning/{matrix,positioning}` converted to `projectScopeOrDenyFor` (explicit ids → `assertProjectScope`, viewer read / editor write, owner-keyed data); P2-4 `esop_pool` one per (account, project): migration **`0334_esop_pool_per_project.sql`** (unique `(account_id, project_id) NULLS NOT DISTINCT`, legacy partial unique) + `setup_esop` `onConflict: "account_id,project_id"`; `fundraise`/`exit-model`/`dividends` read the latest pool deterministically; P2-5 `valuation/clevel` GET = viewer read-only compute (no snapshot), POST unchanged. **P2-3 (founder pages on `getProjectIdFromRequest`) → S18-B.** Verified: `tsc` clean, eslint 0 errors, 69 files / 1586 tests green over the touched dirs.
- **S19 LIVE** (12/12, 2026-09-12 00:20 UTC): **S19-A** G7 Q1–Q4 adopted (delegated by the founder) — mobile-collapsed 12-phase ladder with Show-all, skip-to-current anchor, Demo pin; G7 closed · **S19-B** G2 #7 `reseller_customers.stage` pipeline (auto from real signals, manual override with audit, nightly `reseller-stage-sync`, digest line; migration 0333 applied, crontab installed) + #6 Airwallex and Culture Amp showcases from public sources only (18 `data-source` links live). Also **G1 P8.5 resolved** (equity add-on annual price minted). Human-blocked now: G2 #10 (real founder quotes), G1 P1.5 (InfoVision ABN/contact), reseller counsel, GA4 `hero_variant`.
- **S18-B LIVE** (12/12, 2026-09-12 01:00 UTC): 22 `(founder)` server pages member-aware (`page-scope.ts`, `founderFeatureScope`), no split `svi_accounts` for members, viewer read-only props on 6 client components, pages scope guard. Review `reviews/s18b-founder-pages-review-2026-09-12.md`: no owner regression; 1 P1 (shared report sections) + 8 P2 → fix in flight.
- **S18-B review fixes LIVE** (12/12, 2026-09-12 01:40 UTC): shared `report_sections` never re-charged or re-owned by a member (`report-section` returns existing rows free, `ignoreDuplicates`), tech-analysis route/page on the owner's project, Money Radar tile = owner's project data + caller's credits (`creditNote`), read-only gaps closed (data-room, tech panel), dead investor-pack CTA replaced, onboarding redirect guarded for members, `dashboard/page` render test.
- **S18-B in flight:** `(founder)` server pages member-aware (no split `svi_accounts` for members, viewer read-only props, pages scope guard).
- **S20-A LIVE** (12/12, 2026-09-12 04:25 UTC; migration 0335 applied; **live check found every audit insert failing with 42883** — `digest()` not on `service_role`'s search_path — fixed by migration 0337 pinning `search_path = public, extensions` on both hash functions; rows now flow, chain verify run): audit log covers every mutating API route — `apiRoute()` wrapper (`web/src/lib/audit/api-route.ts`) + AsyncLocalStorage context annotated by `getCurrentUser`/`createSessionRow`/`authenticateRequest`/`getProjectScope`/`assertProjectScope`, codemod `web/scripts/codemods/wrap-api-routes.mjs` (idempotent, `--write --catalogue`) wrapped **298 route files / 336 handlers** (was 14 routes via `logUserAction`); 94 files allow-listed with reasons in `web/src/lib/audit/allowlist.json` (83 `api/cron/**`, Stripe + GitHub webhooks, internal ai-complete, 7 telemetry POSTs, health/status). Rows land in the existing hash-chained `audit_events` (0076): actor id/kind/role, project, `<family>.<verb>` action (manifest overrides win), entity id, method, route pattern, status (2xx/4xx/5xx + thrown), salted IP hash, UA family, id-only params; body never read. Migration `0335_audit_coverage.sql` serialises the chain trigger (advisory lock), adds viewer indexes and `audit_events_verify_chain(from,limit)` RPC; nightly `audit-chain-verify` cron (03:40 UTC, `?dry=1`) writes `content/reports/audit-chain-verify.json` → `/api/status` + `/api/health` `audit_chain: ok|broken|unknown`. `/workspace/audit-log` filters by action family / actor (owner+admin see the project log, editor/viewer own rows only), owner-only formula-guarded CSV at `/api/audit-log/export`; `?source=legacy` keeps the old per-user table. Static guard `coverage.test.ts` fails on any new unwrapped, un-allow-listed mutating route. Verified: `tsc` clean, eslint 0 errors, 330 files / 7123 tests green.
- **S20-A review fixes LIVE** (12/12, 2026-09-12 05:30 UTC): audit finalize non-blocking (SSE first byte), redirect/notFound status mapping, client IP = `cf-connecting-ip` → last XFF hop (also `lib/iphash.ts`), `audit_events` append-only grants + `(project_id,id desc)`/`(user_id,id desc)` indexes (0338), id-like `auditNote`, checkpoint hash cross-check; `AUDIT_IP_SALT` set. Review `reviews/s20a-audit-wrapper-review-2026-09-12.md`.
- **S20-B LIVE** (12/12, 2026-09-12 06:10 UTC): signed outbound webhooks — `webhook_endpoints`/`webhook_deliveries` (0336), events `svi.rescored`, `evidence.uploaded`, `funding.report_ready`, `evaluation.report_ready`, HMAC `X-BlockID-Signature`, SSRF guard, retry ladder 1m/10m/1h/6h → dead, auto-disable at 20 failures, `webhook-dispatch` cron every 5 min, `/api/webhooks*`, Webhooks section on `/workspace/integrations`, `/docs#webhooks`; `WEBHOOK_SECRET_KEY` set; live dry-run OK. Review `reviews/s20b-webhooks-review-2026-09-12.md`: P1 (revoked member kept channel) + 6 P2 → **fixed and live** (12/12): project endpoints always admin-gated, dispatcher deactivates `creator_not_member`, cascade on revoke, atomic failure RPCs (0340), DNS-pinned delivery via undici Agent (also funding fetcher), sealing fail-closed, secret columns revoked from anon/authenticated, docs verifier = reference impl, PATCH rate limit.
- **S21-A LIVE** (12/12, 2026-09-12 08:10 UTC): investor data room — NDA click-wrap gate (server-enforced before any document), engagement heatmap (link × section views/dwell, table twin), per-investor PDF watermark (`@react-pdf` fixed layer), `engage` GET IDOR closed (was unauthenticated), gating on `investor_links.premium` (Starter+), migration 0339 applied. Review `reviews/s21a-data-room-review-2026-09-12.md`: 2 P1 (NDA email overwrote founder-set investor identity → showcase-review spoof; anonymous token routes unlimited) + 6 P2 → **fixed and live** (12/12): identity never written from the acceptance body, `showcase-reviews` refuses null identity, `data-room-token`/`data-room-pdf` rate buckets, section allow-list, tracker restarts after acceptance, traceable watermark (`link <id8>`), NDA gate enforced regardless of plan lapse, ledger tenancy key; **privacy v2.3** (retention rows for NDA acceptances + engagement, 0342 applied).
- **S22-A LIVE** (12/12, 2026-09-12 10:40 UTC): investor-DD valuation certificate PDF — 3-page A4 (range + radar, method/`svi+arr_multiple` note, evidence summary, doctoral sentence, APES 225 / general-advice disclaimer, Auschain signature block, optional watermark), `VC-XXXXX-XXXXX` + content hash, public `/verify/valuation/[no]`, revoke, 5 credits / included Growth+, data-room save; migration 0341 applied.
- **S23 LIVE** (12/12, 2026-09-12 11:20 UTC): **S23-A** OAuth connector tokens sealed at rest — the 4 live rows (github ×3, linkedin ×1) were **raw plaintext** in the legacy `oauth_connections` table; resealed with `OAUTH_TOKEN_ENCRYPTION_KEY` via `scripts/reseal-oauth-tokens.mjs --write` (idempotent re-run = 0), `OAUTH_TOKEN_MIGRATION` closed; runbook `web/docs/ops/oauth-token-sealing.md`; `/api/status.oauth_tokens_sealed` · **S23-B** GA4: `scripts/ga4-register-dimensions.mjs` + `/admin/growth` panel (blocked at GCP until the founder enables `analyticsadmin.googleapis.com` in project 990415480608 and grants the SA Editor on property 538350801 — the panel prints both steps), weekly `ga4-event-audit` cron + `/api/status.ga4_events`; **real bug found: no custom GA4 event had ever arrived** (events fired before gtag loaded were dropped) — fixed with a replay queue; hero param is `arm`.
- **Accepted / deferred:** nonce-driven dynamic rendering (needs CSP redesign or CDN cache rules — founder/ops call); `/api/status` public payload by design.

### S24 — Release readiness (2026-09-12, founder: "review toàn bộ, full test, QA/QC, fix, deploy, ready to sell")
- **Method:** four parallel QA lanes on production (crawl/a11y/console · real founder+member+evaluator journeys · commercial/compliance/ops · API contract/security) → findings ranked → fix agents → one release-fix deploy. Reports under `docs/plans/reviews/release-qa*-2026-09-12.md`.
- **P0s found and fixed (all LIVE 12/12, 2026-09-12 13:40 UTC):** project creation broken since 2026-08-15 (unapplied migration; 15 migrations partial/missing → schema parity repair 0344 + `schema_migrations` ledger 0345 + apply/status scripts) · Startup Package A$149 checkout pointed at a recurring Advisor price (new one-off price minted) · credit packs 50/100 charging A$15/A$25 vs displayed A$35/A$60 (Stripe re-priced, 0 drift, cadence-drift audit added) · three contradictory refund policies (terms **v2.1** §3A, `/terms`→`/legal/terms`, FAQ aligned, registry 0346) · `/developers` selling API on "Growth $499/mo" (→ Program A$349) · **DB backups dead since 2026-07-20** (daily pg_dump + weekly restore test live; restore verified 16/16 projects; Drive off-site needs the founder's OAuth) · two enforced CSP headers blocking GA/GTM site-wide (single nonce CSP; with the gtag queue fix this is why no custom GA4 event ever arrived).
- **P1s fixed:** SVI report IDOR (`/api/svi/report/[projectId]`), Next.js 16.2.5 critical advisory → **16.3.5** + deps (audit 19 → 2), invite links with `0.0.0.0:4001` origin, fabricated A$535K valuation on empty accounts, Spam Act identity/unsubscribe on every marketing email, GST on credit checkout + billing copy, PDF advice disclaimers ×5, lead form rate limit/honeypot/alert, Stripe endpoint rate limits, change-plan cancel-after-pay, generic auth responses (no enumeration), token-based password reset (0343), JSON-body 500s → 400, dead owner checks, anon `/api/status` trimmed, Cloudflare email-obfuscation hydration mismatch, auth rate-limit keys, evaluator trial copy (`trial=1`, "1 included report").
- **Ops:** `.env.bak-*` moved to `~/.blockid-vault` (0600); GitLab remote with PAT removed; `docs/runbooks/{db-restore,secret-rotation-log}.md`, `docs/ops/db-migrations.md`.
- **S24-B LIVE** (12/12, 2026-09-12 17:10 UTC): account erasure — 124 FKs to `app_users` mapped (76 delete / 34 anonymise / 14 detach), atomic `erase_account` RPC (0348; `audit_events` FK cascade dropped in 0347 because the append-only ledger blocked every deletion), self-service `POST /api/account/delete` with re-auth + 7-day grace + cancel link + daily `account-erasure` cron, admin erase, Stripe cancel/detach, tombstone `deleted+<hash>@erased.blockid.au`; the two QA accounts were erased with it. Release QA-1 batch also live (sitemap 404s → 301, canonical loop, pa11y 0 on login/esic/cap-table, contrast tokens, external links).
- **Still needs the founder (not blocking sales):** Drive off-site backup OAuth (`scripts/db-backup-offsite-auth.mjs`), off-box uptime/backup monitor account, revoke Telegram bot token + GitLab PAT, rotate `STRIPE_WEBHOOK_SECRET` + Supabase service key, GA4 Admin API enable + SA Editor, Cloudflare Scrape Shield → Email Obfuscation off, G2 #10 real founder quotes, InfoVision ABN.

### S25 — post-release product depth (opened 2026-09-12 17:30 UTC; founder: "không chặn gì cả, tiếp tục như plan")
- **S25-A** connected revenue: weekly `connector-resync` cron + `connector_snapshots` (0349), magnitude-based revenue→SVI contribution with freshness decay (replaces the flat +15), connector-fed P&L with source labels, Xero un-flagged.
- **S25-B** AU dividend distribution statements per shareholder + dividend register PDFs (franking, TFN withholding), void, credits / equity-add-on gating, data-room save (0350).
- **S25-A + S25-B LIVE** (12/12, 2026-09-13 01:16 UTC; suite 34,016 tests, tsc/eslint 0 errors). Migrations 0349 (`connector_snapshots` + resync lease columns), 0350 (`dividend_statements`), 0351 (`erase_account()` re-created for the 125th `app_users` FK — erasure map 77 delete / 34 anonymise / 14 detach); crontab installed (`connector-resync` Mon 05:00 UTC, ≤20 connections/tick, 240 s budget, `?dry=1`). `/api/revenue` now keys manual rows on `scope.dataEmail` (viewer GET, editor POST — scope-guard rule C). Contribution table `lib/svi/connected-revenue-score.ts`: MRR tier 0/5/10/15/20/25 + growth vs ~90 d (+5…−5) + Stripe 90-d churn (0…−6), × freshness decay (≤90 d ×1, ≤180 d ×0.5, else ×0); replaces the flat +15 only on the `stripe`/`xero_revenue` **tre** rows. Xero scopes gain `offline_access` (older links have no refresh token → one throttled `connector_reconnect` notification; live vault holds none today).
- **Live verify found a 3-month-old P0** (fix-1 LIVE 12/12, 01:38 UTC): the Stripe / Xero / GA4 OAuth callbacks and `/api/evidence/disconnect` had written `account_id` + `raw_profile` with `ON CONFLICT (account_id, provider)` to the legacy `oauth_connections` vault since T0016 (2026-06-13) — columns that never existed (0027 shape is `user_email` / `metadata`), so every such link failed silently and the resync dry run answered `candidates_unavailable`. Migration **0352** adds the columns; callbacks fill `user_email` (owner data key), conflict on the real `(user_email, provider)` index and surface the upsert error (`?error=<p>_save_failed`). Dry run now `ok:true`.
- **Post-ship review** ([`reviews/post-ship-s25-2026-09-13.md`](./reviews/post-ship-s25-2026-09-13.md), GO): merged — P1 provider 401/429/5xx were snapshotted as "MRR 0" and rescored accounts *down* (typed `ConnectorHttpError`; 401/403 → `needs_reconnect`, else `failed`, nothing written) · P1 the `stripe`/**mpc** customer-count row lost its +15 (carve-out keyed on type AND dimension) · P2 v2 rows in `status="error"` were never retried. **Fix-2 LIVE** (12/12, 02:24 UTC): statement charge is once-per-record (`credits_charged` marker → partial failure returns `partial:true`, re-press free), Stripe `mrrAud ≤ 0` falls through to Xero income/3, Xero evidence on SVI keys (`xero_revenue`→tre, `xero_pl`→iri, 0353) and bank-statement evidence → iri (0354, was `financial_health` — never scored), register-PDF pdf-parse test, inline `VoidReasonForm`. Suite 34,036 tests.
- Cycle unchanged: merge → full test + build → deploy → live verify → post-ship review → fix.

### S26 — fundraising + equity paperwork depth (opened 2026-09-13 02:30 UTC; roadmap-v2 partials that need no founder account)
- **S26-A** fundraise tracker: `fundraise_commitments` (soft/committed/signed/funded/withdrawn, instrument), round progress bar + summary API, auto data-room attach when a round goes `active`, investor-view follow-up (`investor_viewed` founder notification, opt-in investor follow-up drip 2 business days after a view, `investor-followups` cron 21:00 UTC).
- **S26-B** share price from SVI ∪ ARR × sector multiple ÷ fully diluted shares (source labels), board resolution PDFs (share issue s 254X reminder, dividend s 254T, ESOP adoption; s 248A circular form), multi-term-sheet compare with founder-friendliness score, acqui-hire exit scenario.
- **S26-A + S26-B LIVE** (12/12, 2026-09-13 04:25 UTC; suite 34,253 tests, tsc/eslint 0 errors). Migrations 0355 (`fundraise_commitments` + `data_room_access_tokens.auto_follow_up`), 0356 (`erase_account()` re-created for the 126th FK `fundraise_commitments.created_by` — detach), 0360 (`board_resolutions`, frozen payload + hash, unique per project/kind/record so re-downloads are free). Crontab installed: `investor-followups` daily 21:00 UTC (live dry-run ok). Routes: `/api/fundraise/[roundId]{,/activate,/summary,/commitments[/id]}`, `/api/share-price`, `/api/board-resolutions/[kind]/[recordId]{,/pdf,/data-room}`, `/api/cap-table/issues`, `/api/term-sheet/compare`, `PATCH /api/investor-data-room`. Deviations recorded: follow-up toggle lives on `data_room_access_tokens` (the table with engagement/NDA), not `investor_links`; activation charges 3 credits (`data_room_generate`) only when no room exists, round still activates without credits (`dataRoom.attached="none"`); share price blend 40 % SVI / 60 % ARR × sector multiple; acqui-hire per-engineer value A$500K–1.5M is an editable assumption; directors on resolutions = cap-table holders whose role contains "director" (no directors store yet). Credits: `board_resolution: 1`, `term_sheet_compare: 2` (both included Growth+ / `esop.manage` for resolutions).
- **Post-ship review** ([`reviews/post-ship-s26-2026-09-13.md`](./reviews/post-ship-s26-2026-09-13.md), GO) → **fix LIVE** (12/12, 05:07 UTC): activation refunds the 3 credits when the compile persists nothing; follow-up cron pages past already-sent links (was re-reading the same 50 forever); `first_view` alert survives the server render's `first_accessed` stamp (15-min grace); term-sheet compare built before the spend (422, nothing charged); "Open round" copy shows the cost only when a room must be compiled; `POST/GET /api/fundraise` keyed on the project owner via `projectScopeOrDeny` (editor / viewer) so a co-founder's round no longer 404s. Suite 34,268 tests. Open P2/P3 handed to S27-A (resolution supersede, close-round admin, AEST business days).

### S27 — remaining no-founder-input partials (opened 2026-09-13 04:50 UTC)
- **S27-A** ESS start-up concession annex on the valuation certificate (s 83A-33 checklist from stored data, ATO *Methods for Valuing Unlisted Shares* Approval 2015 named, explicit not-a-safe-harbour sentence), board-resolution regenerate/supersede (0361), close round = admin + `fundraise.round.closed`, AEST business days.
- **S27-B** chain → cap-table read-back + weekly `chain-reconcile` (0365, `chain_drift` notification), sandbox secondary order book on `/workspace/secondary-offer` (0366, Chapter 6D/7 sandbox banner, `secondary_market.view` gate).
- **S27-C** sector multiples: approved-override resolver over the static table (0369), quarterly `sector-multiples-refresh` cron proposing rows only with verbatim fetched excerpts from a fixed source allow-list, admin approve/reject UI, `docs/ops/sector-multiples.md`. Nothing goes live without an admin approval.
- **S27-A merged** (2026-09-13 06:20 UTC, 0361 applied; deploys with B/C): ESS annex (`?annex=ess`, frozen into the certificate hash, 0 credits), regenerate/supersede with version banner, close round admin + `fundraise.round.closed`, AEST business days. Repo-wide Div 83A citations aligned to the statute (s 83A-33(2) unlisted · (3) < 10 y · (4) turnover · (5) discount/exercise price · (6) resident employer; s 83A-45(4)–(5) holding · (6) 10 % cap) — the older checker/qualifying-tests/Chapter 8 used a home-grown (1)(a)–(c)/(6)(a)–(d) scheme. **Tax-agent review requested (not blocking):** exact NTA safe-harbour conditions under the *Methods for Valuing Unlisted Shares* Approval 2015 as worded in the annex (< 7 y incorporation, no > A$10m raise in prior 12 months, director/CFO sign-off) and whether "recent arm's-length sale" sits inside the instrument; turnover row shows "met" from the founder-entered figure with an aggregation caveat (flip to "not confirmed" in `buildEssChecklist` if preferred).
- **S27-A + B + C LIVE** (12/12, 2026-09-13 07:16 UTC; suite 34,463 tests). Migrations 0361, 0365–0371 applied (0371 re-creates `erase_account()` over the merged **128-entry** map — S27-B `secondary_sim_orders.user_id` delete + S27-C `sector_multiples_overrides.approved_by` detach, which the parallel branches had each emitted alone in 0368/0370). Crontab: `chain-reconcile` Sun 06:00, `sector-multiples-refresh` 1 Jan/Apr/Jul/Oct 03:00 (dry-runs ok). First deploy attempt aborted at gate 6 (old build stayed live): the cron-auth guard's correct-secret probe cold-started `sector-multiples-refresh` past vitest's 5 s under load → probe budget 20 s. `src/lib/chain/` is gitignored by the root `chain/` rule → module lives at `src/lib/onchain/`. `secondary_market.view` flag added to Growth+ (0367, unlock matrix re-rendered). Sources for the multiples cron: SaaS Capital Index, Aventis Advisors, Damodaran psdata, BVP Cloud Index, Meritech (the last two are interactive pages — expected to log `empty_text`; drop after the first live quarter if they never yield).
- **Post-ship review** ([`reviews/post-ship-s27-2026-09-13.md`](./reviews/post-ship-s27-2026-09-13.md), GO, no P0/P1) → **fix LIVE** (12/12, 07:47 UTC): ROFR-released sells now match (book could stay crossed), chain push refuses 409 when `sync_enabled=false`, `sandbox:true` on sim 400s, and the push API/panel now say **on-chain execution is not enabled** — `blockchain-sync.ts#executeOnChainTx` is a placeholder (no server signing key; `wallet.ts` is the MetaMask path), so corrections sit on the queue and balances do not change. **Founder decision:** wire a server signer for the private Anvil chain (mint/burn = destructive → push becomes admin+) or keep push queue-only.
- Open P3s (not scheduled): ESS turnover row "met" from a founder-entered figure; `?annex=ess` on a certificate issued without the annex renders an empty-facts annex; sandbox `place` has no rate limit / same-holder concurrent oversell documented; refresh `?dry=1` still fetches + calls the model.
- Roadmap-v2 re-ticked 2026-09-13 (16 items S18–S26; S27 ticks below). Remaining partials need founder accounts (QuickBooks, SSO, Slides, Discord, Cosmos, ProductHunt) or the signer decision above.

### S28 — dividends/tax, investor CRM, expenses (opened 2026-09-13 07:50 UTC; last roadmap-v2 items that need no founder account)
- **S28-A** shareholder FY tax statements (`TS-<FY>-<n>`, franking/TFN totals per FY, 0372) + DRIP elections/allocations feeding cap-table share issues (0373–0374).
- **S28-B** investor CRM `/workspace/investors` (contacts, pipeline stages, touchpoints auto-linked from data-room views + commitments, CSV import/export, weekly-digest block; 0375–0376).
- **S28-C** expense categorisation `/workspace/expenses` (bank CSV → rules → AI with confirm-before-charge, learned per-project rules, monthly P&L by ATO-aligned category, feeds revenue sources + burn/runway; 0377–0378).
- Schema rule this phase: new tables FK to `projects` only (no `app_users` FK) so parallel branches stop colliding on the erasure map.
- **S28-A + B + C LIVE** (12/12, 2026-09-13 09:49 UTC; suite 34,743 tests). Migrations 0372–0378 applied (all FK to `projects` — erasure map unchanged at 128). Credits: `shareholder_tax_statements: 2` per FY run, `expense_categorise: 1` per started 100 AI rows (rules-matched rows free), both included Growth+/`esop.manage`; DRIP elections free. Nav: `Investor CRM` (fundraise) + `Expenses` (revenue) leaves, goldens + unlock matrix re-rendered. Design notes: DRIP shares land on the register at statement issue (`share_transactions` has no approval state — founder call if a pending step is wanted); tax-statement AIIR wording hedged; s 708(13) DRIP disclosure exemption not cited; `/api/pnl` now keyed on `scope.dataEmail` (incidental rule-C fix).
- **Post-ship review** ([`reviews/post-ship-s28-2026-09-13.md`](./reviews/post-ship-s28-2026-09-13.md), GO) → **fix LIVE** (12/12, 10:34 UTC; gate 3 verified with the 8 GB heap): **P1** CRM data-room hook wrote a touchpoint on every engage beacon (timeline flood) → one per (contact, link, trigger) per 24 h; P2 DRIP statement re-frozen when the cap-table write fails; P2 bank-CSV MRR now revenue-category only (grants stayed on income); P3 strict ISO cursor. Open P3s in the report (regenerate-retry charging, no pro-rata AI refund, burn precedence mismatch between `burnRateWithBankFallback` and `sources.ts`, CRM import rate limit, `shares_held` non-atomic increment).
- **Toolchain:** `tsc --noEmit` now OOMs at node's default heap on this tree — deploy gate 3 runs it once with 8 GB and **fails on a crash exit** (a crash printed zero `error TS` lines and would have passed silently); `npm run typecheck` + `qa-release-gate.sh` carry the same heap.
- Left after S28: ASX/NASDAQ listing checker + clean-room guide (content-heavy), comparable DB 1000+ (needs data), white-label custom domain (infra), and founder-account items.

### S29 — exit-phase content + hardening sweep (opened 2026-09-13 10:10 UTC)
- **S29-A** listing readiness checker (ASX Chapter 1 admission conditions + Nasdaq Capital Market initial standards, rows computed from cap table / share price / bank data / a founder-ticked `listing_profile`, `confirm_current_rule` where a threshold is uncertain, PDF export 1 credit) + clean-room preparation checklist backed by data-room controls (0379–0380).
- **S29-A + S29-B LIVE** (12/12, 2026-09-13 12:18 UTC; suite 34,855 tests). Migrations 0379–0382 applied. `/workspace/listing-readiness` (ASX 11 rows / Nasdaq 11 rows, `confirm_current_rule` for escrow + rule currency + Nasdaq 2025 amendments; US$ rows `not_confirmed` until an AUD→USD rate is entered; PDF 1 credit once per project / Growth+), `/workspace/clean-room` (7 stages / 16 tasks, 6 computed from the data room). Live fetch-only dry run of the multiples cron: all 5 sources return text (521–14,000 chars). Content notes for the lawyer in the review report: LR 1.1 condition numbering, CGC 4th edition currency, Nasdaq "50 % of round-lot holders" read as 150, LR 1.2.5 2-month recency.
- **Post-ship review** ([`reviews/post-ship-s29-2026-09-13.md`](./reviews/post-ship-s29-2026-09-13.md), GO, no P0/P1) → **fix LIVE** (12/12, 12:51 UTC): readiness age rows measured to today (`asOf`) not the rules' as-at constant; Nasdaq insider line strictly > 10 %; young company with an audited FY → `not_confirmed`; cap-table `issue_shares` validates count (400) before the RPC; 0383 pins `increment_shares_held` search_path + |delta| ≤ 1e12. Open P3: PDF concurrent confirm can double-charge 1 credit; clean-room legacy-room fallback under a project scope; `loadBankProfit` 5,000-line cap.
- **S29-B** the S26–S28 open P3s: unified burn-rate precedence, atomic `increment_shares_held` RPC (0381), tax-statement regenerate-retry charge marker, pro-rata refund for failed AI batches, CRM import + sandbox place rate limits, ESS annex 409 on certificates issued without it, fetch-only dry run for the multiples cron, CRM owner must be a member, needs-AI partial index (0382), formula-stripped bank narrations.
- **Backlog state after S29:** every roadmap-v2 item that needs no founder account/decision is shipped. What remains: QuickBooks (Intuit OAuth app), SSO (IdP tenant), Google Slides export (Google API project), Discord/Slack community, ProductHunt launch (13 Oct), production Cosmos chain, chain signer key for push (mint/burn), white-label custom domain (Cloudflare automation), comparable DB 1000+ (real data), plus the founder-only ops follow-ups listed under S24. Next autonomous work = review-driven hardening, live QA sweeps, and traction content — not new roadmap features.
- **Live QA sweep S25–S29** (2026-09-13 12:55–14:00 UTC; reports [`reviews/live-qa-s25-s29-lane1-2026-09-13.md`](./reviews/live-qa-s25-s29-lane1-2026-09-13.md) 53 rows · 35 pass / 9 pass-with-note / 6 fail / 3 blocked, [`lane2`](./reviews/live-qa-s25-s29-lane2-2026-09-13.md) 275 checks · 266 pass; all QA accounts erased). Found and **fixed — LIVE 12/12, 14:04 UTC**:
  - **P0** `POST /api/fundraise` had failed 23503 since migration 0043 — `fundraise_rounds.account_id` FK pointed at `svi_accounts` while every writer stores `app_users.id`; 0 rounds ever created in production (so S26-A rounds/commitments/activation were unreachable). **0384** repoints the FK (129th `app_users` FK → erasure map).
  - **P1** erasure left the cap-table / dividend register behind (`shareholders` with third-party name+email, `share_classes`, `share_transactions`, `dividend_records`): `account_id`-keyed, no FK, and the column holds a **mix** of `app_users` ids and legacy `svi_accounts` ids (5 vs 22 rows live). Non-FK extras gain `by: "account"` (matches either key; text cast because `dividend_records.account_id` is text) — **0385/0386**; proven end-to-end with a seeded throwaway user (both key styles removed, round cascaded, tombstone). Dividends route now owner-keyed and stamps `project_id`.
  - **P1** fabricated valuation still live on fresh accounts: the release-QA guard accepted `current_svi > 0`, which the column DEFAULT (100) satisfies → `/dashboard/valuation` hero A$535K, `/api/share-price` A$1.0875 "SVI only", DRIP previews and listing readiness computed from it. Shared `hasRealSviScore()` (analysis row / snapshot / index base date) now gates the VC dashboard and the share price.
  - **P1** "Connect Xero" dead-ended in a JSON 503 — `XERO_CLIENT_ID` is not set on production (founder: create the Xero OAuth app); the panel now says "not available yet" when unconfigured.
  - **P2/P3 batch — LIVE 12/12, 14:54 UTC** (suite 34,900): mobile header → account menu below `sm` (`header-account-menu.tsx`), categoriser rules (consulting/advisory/INV+services, bare PTY LTD → review, AI ≥ 0.7 for salaries/bank_fees/drawings/transfer, bank_fees > A$500 needs a keyword), Cloudflare-injected GTM/Zaraz scripts documented in `docs/ops/analytics.md` §4 (founder: tag gateway OFF, Web Analytics auto-setup OFF, Email Obfuscation OFF), share-link 409 inline guidance, fundraise cap-table dead-end → `/workspace/cap-table` or `/pricing?feature=cap_table.write`, admin 401 body standardised, board-resolution foreign GET 404, sandbox 429 carries `sandbox:true`, included previews say "Included in your plan — no credits charged." (`lib/credits-preview.ts`), tax-statements `listedCost`, multiples extraction: JSON-only prompt + balanced-block parse + relevance window.
  - **S30-A LIVE-QA SUITE** (merged 16:10 UTC; `web/tests/live-qa/`, 14 specs / 97 tests, `npm run qa:live`, self-provisioned `qa-live-<stamp>@blockid.au` erased + verified in teardown, `docs/ops/live-qa.md`, `content/reports/live-qa-latest.json` + history; cron line proposed, not installed: `0 7 * * 0 … LIVE_QA_ALLOW_DB=1 LIVE_QA_ELEVATE=1 bash scripts/qa-live.sh`). **Run 1** (15:56): 91/97 → found **P1** `fundraise_rounds.closed_at` missing on prod (table from 0043 predates 0030's column; the parity waiver hid it) → **0387** + waiver removed, expenses-import creditNote → **LIVE 16:36**. **Run 2** (16:40, after that deploy): 93/97 — the 2 fails were suite issues (click before hydration; `$` vs `A$` in formatAud) → hardened; also surfaced two stale-price copies: workspace upsell banner "Growth A$99/mo for 100 credits" and BlockID's own pitch-deck PDF ("Founding 100 A$5 / Growth A$99") → both now priced from `lib/plans-v2` with pins → **LIVE 17:16** (gate 4 reported 184 lint errors from Playwright's generated report viewer — ESLint now ignores `playwright-report*/` + `test-results/`; source is 0 errors). **Run 3** (17:20, on that build): **95 pass / 0 fail / 1 skipped (round activation needs spend) / 1 expected (F16 label association)** — clean baseline. Weekly cron installed (Sun 07:00 UTC; remove the `qa-live.sh` line from `web/scripts/crontab.production` to stop it).
  - **S30-B** (merged 19:05 UTC): live-qa extended to funding, reports, evaluator, data room (NDA/watermark/engagement as an anonymous investor), webhooks (SSRF rejects), account deletion request/cancel, and a member lane (editor/viewer) — 157 tests; production run 143 pass / 1 fail / 11 skipped (spend-gated) / 2 expected. The 1 fail is a **P1: `/workspace/funding` crashed for every Starter+ founder** — the server page called `isFundingTab()` from the `"use client"` workspace module (Next.js client reference → "Attempted to call … from the server"; digest 1907948635); the free branch never reached it, so no unit test saw it. Fixed (`funding-tabs.ts`) + new static guard `src/lib/server-calls-client-export.test.ts` (proven against the old page) — **LIVE 12/12, 20:22 UTC**; full live-qa run on that build: **145 pass / 0 fail / 10 skipped (spend-gated) / 2 expected**. **P2s fixed — LIVE 12/12, 21:19 UTC:** data-room trust settings/heatmap/link list survive a reload (`GET /api/data-room/generate` seeds the room id, member-aware, no spend), revoked members are re-invited in place (`reinvited: true`) and roles change via `PATCH /api/projects/[id]/members/[memberId]` (admin+; admin→non-admin runs the S20-B webhook-endpoint cascade; audit `project.member.reinvited` / `role_changed`), DELETE now 404s a foreign member before any write. Live-qa on that build: **146 pass / 0 fail / 10 spend-gated skips / 1 expected (F16)** — day-end baseline. P3s: `ReportPaywallGate` mounted nowhere, data-room generate has no preview path, no account export endpoint. Ops: Cloudflare Email Obfuscation still on (`email-decode.min.js` CSP refusals on `/pricing`, `/workspace/settings`).
  - **Day summary 2026-09-13:** 21 production deploys (20 green, 1 gate-6 abort with no swap), migrations 0349 → 0387, unit suite 34,016 → 34,942 tests, S25–S30 shipped and reviewed, live-qa suite (157 tests) green on the final build and scheduled weekly. Every roadmap-v2 item that needs no founder account is live. Next autonomous work: keep the deploy → live-qa loop, react to the weekly run + cron health, and pick up founder-unblocked items as keys/decisions arrive.

### S31 — trial-wave readiness (opened 2026-09-13 22:45 UTC; founder: review all, deploy, full QA/QC, defer 3rd-party partner connections, newest AI models with the largest quota)
- **AI backbone audit (live probes, 22:40 UTC) — BLOCKING for a large trial wave, needs the founder:** `ANTHROPIC_API_KEY` in `.env`/`.env.runtime` is **invalid (401)**; the Anthropic proxy (`taphoaapi.info.vn`) answers **401** (dead); OpenRouter is a paid account with **~US$1.55 credit left** (30 total / 28.45 used); Groq `gpt-oss-120b` works at 1,000 RPM but only **8,000 tokens/min** (free tier — one report exceeds it); Cerebras / SambaNova / DeepInfra keys are empty at runtime; the only working Claude path is the **Claude Max CLI OAuth token** (personal subscription: 429s under load, not a product-serving licence). `ai-model-health`: 9/44 models healthy. The direct Anthropic tier is currently *disabled in code* as "paid".
  - **Founder actions (money/keys):** (1) create a new Anthropic API key at console.anthropic.com with credit (Sonnet 5 = US$2/US$10 per 1M tokens; ~1,000 trial users × 3 reports ≈ US$130–200 incl. Opus 5 synthesis) and set `ANTHROPIC_API_KEY` in `web/.env.runtime` + `web/.env`; (2) optionally top up OpenRouter (cheap DeepSeek/Qwen overflow) and/or Groq developer tier; (3) revoke the dead proxy key. Nothing here needs code from the founder — S31-A makes a valid key instant capacity.
- **S31-A merged** (2026-09-14 00:05 UTC; deploy in flight; `docs/ops/ai-providers.md`): live probe on the box → `anthropic invalid_key`, `claude-proxy invalid_key`, `openrouter low_credit US$1.55`, `groq valid rpm≈1000 tpm≈8k`, `claude-oauth valid (personal)`; `/api/status` (trusted) now carries `ai_providers` + `quality_tier_ready` + `ai_queue_depth`. Cost model: ≈ US$0.51 / A$0.79 per founder for 3 reports → ≈ A$790 for a 1,000-founder wave. Founder steps: console.anthropic.com key with ≥ US$40 prepaid (tier 2 = 1,000 RPM; ≥ US$200 for a 1,000-founder day) → `ANTHROPIC_API_KEY` in `web/.env.runtime` + `.env` → `npx tsx --env-file=.env.runtime scripts/ai/probe-providers.ts --force` → `quality_tier_ready: true`; raise `AI_DAILY_SPEND_CAP_AUD` for the wave. Originally scoped as: Anthropic tier via the official SDK (upgrade to 0.125.x), task-class routing Haiku 4.5 / Sonnet 5 / Opus 5 with adaptive thinking + effort, prompt caching, streaming for long outputs, rate-limit headers into the dispatcher, provider validity probes → `ai_providers` on `/api/status` (invalid keys never retried on the hot path), `AI_DAILY_SPEND_CAP_AUD` + `OPENROUTER_MIN_CREDIT_USD` guardrails with founder notification, bounded queue + per-user fairness + 503/Retry-After instead of 500s, dead-model auto-prune, `docs/ops/ai-providers.md`.
- **S31-A/C LIVE** (12/12, 2026-09-14 00:53 UTC): `/api/status` trusted payload shows `ai_providers` (`usable: 2`, `quality_tier_ready: false`) and the AI queue (`max_concurrent 120`). Gate 6 now logs vitest to `/tmp/blockid-deploy-vitest.log` and prints failing test names (one flaky run under load had hidden them).
- **S31-B merged** (01:05 UTC; deploy in flight; [`reviews/feature-completeness-2026-09-13.md`](./reviews/feature-completeness-2026-09-13.md)). **Revenue-path P1s found and fixed:** CreditGate / `/svi` advertised A$5/A$9/A$15 packs while Stripe charged A$9/A$20/A$35 and still sold the closed Founding-100 deal; `/workspace/billing` (the only in-app Stripe path) offered the retired ladder (no Starter, "Growth A$99") — now plans-v2 per segment with `?plan=` checkout; `/pricing` "Start trial" and `/signup` dead-ended signed-in founders; dashboard read only `svi_analyses` while `/analyze` writes `analyses` → "Run your first SVI score" forever (read-side bridge); onboarding never marked complete and **`/api/onboarding/complete` overwrote the auth `role` column** (one live row had `role='founder'` — repaired by hand to `user`); advisor notes form 400'd on every save; checkout success page defaulted to `founding50`. P2s: `/pricing?feature=&from=` notice ("To open Cap Table you need … included from Growth (A$69/mo)"), shared `NotAvailableYet` (reason + alternatives + "Notify me" → leads) replaces every "Coming Soon — Q3 2026 / Upgrade to Enterprise" stub (SSO, white-label, QuickBooks, applications, digest, listing form), gated CTAs carry `?feature=`. Deferred (report §3): ~80 raw `err.message`/`alert()` sites, duplicate/orphan routes (IA decision), `/api/svi` free-floor policy, email templates with A$15/A$49.
- **S31-D merged** (01:05 UTC; deploy in flight, **flag OFF**; [`docs/ops/public-page-caching.md`](../ops/public-page-caching.md)): `CSP_PUBLIC_HASH_MODE=1` (build-time + runtime, i.e. a deploy) makes the public allow-list static/ISR with a hash CSP computed from the exact served document (`.next/server/app/<route>.html`), `public, s-maxage=300–600` for anonymous visitors, `private` when `blockid_session` is present; app inline scripts (theme, consent, GA/GTM) hashed in both modes. Measured locally: **128 req/s** on `/pricing` (p50 144 ms) vs the audit's ~50, before any Cloudflare caching; also fixes 8 `force-static` pages that never hydrated in production (no nonce on their scripts). `/pricing` keeps the S31-B feature notice client-side (`useSearchParams` in Suspense) so it stays static. **ENABLED — LIVE 12/12, 2026-09-14 02:01 UTC** (`CSP_PUBLIC_HASH_MODE=1` in `web/.env` + `.env.runtime`, env backed up to `~/.blockid-vault/.env.bak-20260914-hashmode`): origin headers verified (`/`, `/pricing`, `/funding/grants[?state=NSW]` → `x-blockid-csp: hash`, `public, s-maxage=300/600`, `x-nextjs-cache: HIT`; `/dashboard` and any page with `blockid_session` → `private`); live-qa on the build **146 / 0** (one transient 5-fail run re-ran clean); the S31-D e2e spec fails on production only because Cloudflare's **Google Tag Gateway** injects two un-hashed inline scripts for browser UAs (`GTM-TRHH4MH2`, 14 inline scripts vs 12 at origin) — the same lane-1 F6 finding, blocked under the nonce policy too. `cf-cache-status` is still `DYNAMIC` until the founder adds the Cloudflare cache rule (hosts blockid.au/www, not `/api/*` `/_next/*`, cookie without `blockid_session`/`blockid_locale`/`sb-`, respect origin cache-control). Rollback = unset flag + redeploy.
- Originally scoped S31-B: feature-completeness review + polish (stubs → honest "not available yet", stale prices → `plans-v2`, gated pages → upgrade card, dead controls, empty states, validation), report `reviews/feature-completeness-2026-09-13.md`.
- **S31-C merged** (23:20 UTC; [`reviews/capacity-audit-2026-09-13.md`](./reviews/capacity-audit-2026-09-13.md)). Numbers: 8 vCPU / 50 GB, **one** `next-server` process (~1 core saturates at ≈50 renders/s; p95 ≈ 550 ms at 20 concurrent through Cloudflare, 0 errors); nothing is cacheable at CF/nginx/Next because the root layout reads `headers()` for the CSP nonce; DB 90 MB / 49 of 100 connections / PostgREST pool 10; `audit_events` chain trigger ≈ 0.25 ms/row (not a bottleneck); signed-in rate limits were keyed by **IP** (one office shared the 20/min SVI bucket). Estimate: ~500–800 concurrent anonymous browsers, ~150–300 active workspace users; AI reports bounded by provider RPM and proxy timeouts, not CPU. **Done:** per-user limiter identity (sha256 of the session cookie), shared-IP ceilings (register 60 / login 120 per 15 min per IP, per-email+IP stays 5), `svi_analyses` hot-path indexes (**0388** applied), `startup-index` 300 s cache; on the box: two orphan `next-server` processes (ports 4011/4321, deleted cwds, ~415 MB) killed; **nginx** `blockid.au` block now has an upstream with `keepalive 64`, **310 s + `proxy_buffering off`** on the AI/stream API routes (default 60 s produced 504s on 60–180 s report routes) and 120 s elsewhere (backup `/etc/nginx/sites-available/blockid-live.bak-20260913`; copy tracked at `docs/ops/nginx/blockid-live.conf`). **Proposals (not done):** nonce→hash CSP in the root layout to unlock ISR/CF caching (biggest lever), second app process (needs `REDIS_URL` for the limiter), `PGRST_DB_POOL=20` + Postgres memory sizing, cron re-timing to 15:00–17:00 UTC + `nice`, Cloudflare real-IP + `limit_req`, offsite backup auth (founder).
- **QA lead production audit** ([`reviews/qa-lead-audit-2026-09-14.md`](./reviews/qa-lead-audit-2026-09-14.md), 03:10 UTC): crawl **452/452** URLs 200 (0 redirects/5xx/soft-404, TTFB p50 119 ms; CSP hash 435 / nonce 17); 0 first-party console errors on 15 key pages (30 CSP blocks = Cloudflare tag gateway only); auth/edge 33/34; money paths 22/22 (credit packs code == Stripe live == billing == pricing); axe 4/5 clean; 390 px clean; ops healthy (disk 19 %, RAM 30 %). **Verdict GO for the wave after the AI key.** P1 = AI providers (founder). P2: React #418 hydration mismatch from SVG `<title>` array children on `/` + showcase/tools pages; Cloudflare "Always Use HTTPS" off (founder); `/analyze` has no nav/footer; offsite backup failing (founder); `bq-export` cron `tsx: not found`. P3 ×11 (placeholders "— verify" in 9 program records, `/vi` lang under S31-D, api unknown → HTML 404, `audit_chain: unknown` state-file path per release, reset-password limit, `/api/auth/me` ×3, 90-day session, GA4 events unseen, …). Code-fixable items → fix lane in flight.
- **2026-09-15 founder report:** Google login broken; `/analyze` result shows neither the full entered data nor the AI analysis of it; first (C3) analysis must give a complete AI-agent analysis + valuation and email a ≥10-page report.
  - **Shipped/merged:** S31 security-review partial (per-IP ceiling behind the session-cookie rate key, AI per-user slot first, log redaction) + QA-audit F2 (SVG `<title>` hydration mismatch) / F4 (`/analyze` in MarketingShell) — LIVE 05:09; S31-E error-message sweep (`lib/ui/user-error.ts`, ≈115 sites, 0 `alert()`, static guard) — LIVE 05:41; **S32-A Google sign-in — LIVE 12/12, 06:15 UTC** (`/api/auth/google/start` → 302 to Google with PKCE + sealed state cookie; callback 400 on bad state, Google `?error=` → `/auth/login?google_error=`; following the authorize URL, **Google itself answers `redirect_uri_mismatch`** → the redirect URI is not registered yet; the GIS popup path needs no redirect URI, so the founder's original failure is most likely the consent screen): verified on prod that GIS loads, the button renders and the popup reaches "Sign in to continue to blockid.au", but **no `POST /api/auth/google` since 9 Sep** → failure is inside Google's popup after the account pick. Most likely cause: **OAuth consent screen in "Testing"** (only listed test users can sign in; everyone else sees "Access blocked"). Added a server-side OAuth redirect flow (`/api/auth/google/start` → `/callback`, PKCE + sealed state, shared verify/complete path with the POST route), "Continue with Google (redirect)" button, Google error codes rendered on `/auth/login?google_error=`, `[auth:google]` server log lines, `docs/ops/google-sign-in.md`. **Founder checklist:** Google Cloud → OAuth consent screen → *Publish app* (In production); Credentials → web client → add redirect URI `https://blockid.au/api/auth/google/callback` (origins `https://blockid.au`, `https://www.blockid.au`); then sign in once via each button.
  - **S32-B first analysis — LIVE 07:13 UTC** (0390 applied, `first-analysis-report` cron every 5 min). Live run 1 (agri-robotics pre-seed, guest): echo 9 rows, SVI, 7 C-level sections (220–512 words, Groq), **18-page PDF (148 KB) emailed** to the QA address in ≈90 s. Defect found: valuation **A$29.7M–55.1M** for A$36k revenue on a A$6M cap — extractor missed "Revenue: A$36,000 in the last 6 months", stage detector promoted to 4 on the keyword, baselines/Berkus ≈2× AU medians → **S32-D — LIVE 09:48 UTC**: CTV 2024/25 baselines (pre-seed 3/5/8M, seed 6/10/15M, Series A 15/30/45M), Berkus A$500k pillars, ARR < A$250k clamp, revenue/ask/cap extraction EN+VI, stage ≥ 4 needs ARR ≥ A$250k or 12 months, founder-cap cross-check → the same input on production now reads stage 3 "Early Traction", SVI 78, **A$2.8M–6.6M** (Revenue 12× 50 % + Scorecard 35 % + Berkus 15 %), echo "Revenue A$36,000 / 6 months → MRR A$6,000 (ARR A$72,000)", ask A$1.2M, "Your stated cap A$6.0M · indicative A$2.8M–A$6.6M → consistent".
  - **S32-C routing — LIVE 09:13 UTC** (founder: subscription token is a fallback, not a product tier): report/synthesis → Anthropic API (valid key only) → **DeepInfra** (DeepSeek-V4-Flash ≈ US$0.003/report → V3.2 → Qwen3-235B) → **Gemini** (3 Flash / 2.5; Pro for synthesis) → Claude CLI fallback (20 RPM, only when the paid tiers have no headroom) → Groq → free report-grade models; classify cheap-first; `AI_REPORT_PROVIDER_ORDER` override; exact per-model cost; "Prepared with <model via provider>" on the PDF + `ai_last_report_provider`. DeepInfra + Gemini keys set (founder-supplied, valid). Live run 2 (allied-health SaaS): sections written by **DeepSeek-V4-Flash via deepinfra** ✔, but the grounding validator rejected 4/7 sections for benchmark figures (CHRO "$70k–85k" salaries) and 3/7 was discarded as `failed` → **S32-E — LIVE 12:25 UTC** (0391 applied; first attempt at 11:54 aborted at the build gate with SIGTERM from a concurrent peer-session deploy — deploys now serialized from one session, stand-down sent to the four peer sessions, two acked): validator only rejects figures asserted as the company's own facts (benchmarks kept + tagged), `done_partial` at ≥4/7 with "(part 1)" email + per-section cron backfill, provenance on every run, normalised `report.agents`. **Live run 5** (allied-health SaaS): **7/7 sections in 4 min 20 s**, 432–742 words each, 30 benchmark figures tagged / 0 rejected, all DeepSeek-V4-Flash via DeepInfra, valuation A$3.2M–7.5M vs stated cap A$5M → consistent, **PDF emailed**. Live-qa 149/2 → the 2 were load timeouts; re-run surfaced: `/api/valuation/vc` 404 on a fresh dashboard (now the `empty` 200 — committed, next deploy) and a duplicated footer prefetch of `/legal/privacy` holding one connection ~30 s on `/analyze` (P3; page interactive at 0.5 s; check now waits for `load`).
  - **S32-F + QA/QC sweep — LIVE 17:11 UTC** (founder: "check lại lần nữa, review và full test QA, QC, test lỗi 404, lỗi click không ra trang web ==> fix all"). Google Cloud console changes verified: the authorize URL for `https://blockid.au/api/auth/google/callback` no longer answers `redirect_uri_mismatch`; the founder still has to sign in once via each button (server sees no `POST /api/auth/google` until then). Crawl of **900 pages → 0 × 404 / 5xx / soft-404**; redirects `/score`, `/terms`, `/privacy`, `/founding-50`, `/company`, `/for/advisor` correct; unknown `/api/*` → JSON 404; browser click tests desktop + iPhone 13 (mobile menu, hero CTA, pricing → signup, footer links, `/analyze` submit → results in 9 s) all pass. Fixes: nginx canonical 301 for `X-Forwarded-Proto: http` and `www.` (tracked copy `docs/ops/nginx/blockid-live.conf`); `/api/valuation/vc` `empty` 200 instead of 404; `/analyze` skips the 401 `report-estimate` call when signed out; **S32-F interactive AI ordering** — Money Finder `POST /api/funding/report` timed out after S32-C put DeepSeek-V4-Flash first (60–90 s for a 1,600-token narrative): `callAI({ interactive: true })` orders providers by throughput (`INTERACTIVE_PROVIDER_ORDER`: Groq → Cerebras → Gemini → DeepInfra → …), 30 s default budget, `pickFirstUsable` (not tier ranking); applied to the funding narrative, cfo-advisor, svi full-report / report-section / dimension-analyze. Live-qa `20-funding` "FK violation" in the server log was a red herring: the 20 s Playwright `actionTimeout` aborted the client while the server kept generating and inserted after teardown had erased the QA project — helpers now take a per-call `timeoutMs` (150 s on AI routes); re-run **9/9, whole funding lane 61 s**. Repo cleaned (0 uncommitted files, agent worktrees/branches pruned, releases 3.9 G → 1.3 G, 39 GB RAM free). **Deploy v3.13.2 12/12 at 17:49 UTC** (`/version` + manifest had still read v3.12.0); full live-qa on it **151 passed · 0 failed · 10 skipped · 1 xfail**. **Follow-up deploy 18:17 UTC 12/12** (`e6352fa6d`): privacy-banner links `prefetch={false}` (closes the `/legal/privacy` P3) and fundraise wizard labels wired `htmlFor`/`id` (lane-1 F16 — the suite's last expected-failure now passes; cap-table + fundraise lanes 12/12). Remaining noise: AI-gateway ECONNREFUSED log lines (no user impact).
  - Originally scoped S32-B: input echo with sources, full SVI + indicative valuation + C-level commentary streamed into the result page (background job via the AI queue, `analyses.full_report_*`, migration 0390), ≥10-page PDF (free) / unlimited (paid) emailed once with resend + 5-min retry cron.
- Then: merge → full suite → deploy → live-qa → fix → deploy.

  - **Founder follow-ups from this sweep:** Xero OAuth app (`XERO_CLIENT_ID/SECRET`), the three Cloudflare switches, decide whether the mobile credit pill should stay visible outside the avatar menu.

### G12 — Evaluator Traction: pricing + positioning for investors, accelerators, incubators, consulting & service firms
- **Source:** [`docs/plans/evaluator-traction-2026-09-10.md`](./evaluator-traction-2026-09-10.md) · companion of G11 (Evaluator Progress Radar consumes G11 signals)
- **Status (2026-09-10 19:50 UTC):** **S1–S5 LIVE — G12 complete** — S1 T0268 / T0269 / T0275 / T0274-1 · S3 T0270 (`evaluations` object + 6 missing tables, migration 0314), T0271 (in-workspace Trust BizReport A$3 / plan quota, re-score A$1, 0317) · S4 T0273 (Evaluator Progress Radar: `evaluator_progress_sends` 0321, cron `30 23 * * 0`, progress column + panel on `/workspace/evaluations`), T0274-2 (`/compare`, `/compare/chatgpt`, `/compare/valuers`, evaluator GA4 funnel). Stripe prices for Scout/Firm/Program synced into `plans.stripe_price_id`; `STRIPE_PRICE_TRUST_REPORT_5AUD` re-minted at A$3 — evaluator checkout is provisioned. · S5 T0272 (Program batch scoring: `evaluation_batches` 0322, `POST /api/evaluations/batch` gated `lp_export`/`accelerator.cohort` with all-or-nothing quota, `evaluation-batch-runner` cron `*/20 12-20`, cohort table + CSV, `/api/reports/quarterly?batch|cohort` sponsor/LP report with doctoral sentence + evaluator disclaimer, `/solutions/accelerator` copy flipped). Rubric weights are display-only re-aggregation of the 8 dimension scores (SVI unchanged so cohorts stay comparable). **Next:** traction T1–T4 per goal doc §6 (angel groups → accelerator pilots on batch scoring → advisory firms via reseller → comparison page + case studies).
- **Founder decisions (2026-09-10):** evaluator trial = 7 days **card required** (same Stripe mechanism as founders) · public rungs **Scout A$79 · Firm A$149 · Program A$349** re-using `investor_angel / investor_advisor / investor_vc_small` (accelerator A$500+/1,500/3,500 + VC Enterprise stay Contact Sales; consulting firms also via reseller 0–40 %) · **A$3 = full Trust BizReport** for founders and evaluators, A$5.50 `TRUST_REPORT_5AUD` retired, re-score A$1 · founder ladder unchanged (Free → A$3 → Starter A$29 / Growth A$69).
- **Finding:** the B2B ladder already exists (`plans.csv` rows 7–13, entitlements, investor/accelerator/advisor workspaces, reseller module, partner API) but is `public:false`, has no Stripe prices, cannot self-serve trial, and every CTA lands on a contact form that drops `?plan=`. Missing: "startups I'm evaluating" object, migrations for `investor_portfolio` / `watchlist_digest` / `advisor_client_roster`, `/solutions/advisor`, batch scoring.
- **Positioning (6 proofs):** one rubric 8 dim × 13 criteria × 12 phases · 11 C-Level agents + auditor · the startup's own accumulating evidence (weekly snapshots) · AU-native (AUD methods, ESIC/R&DTI/s708, 500+ comps, grants) · A$3 vs A$2,985+ valuer / Equidam A$635 / Kruncher US$499 floor · documented method + founder's doctoral (DBA) research — **wording pending founder sign-off, never "PhD"**. "Why not ChatGPT" paragraph + cited evidence in goal doc §4b / Appendix C.
- **Traction (90 days):** T1 angel groups + First Believers alumni → T2 three accelerator pilots (batch scoring on a live intake, from our own programs seed) → T3 advisory firms via reseller → T4 solutions pages, comparison page, 3 case studies. KPIs: trial→Scout ≥ 15 %, ≥ 4 reports/evaluator/mo, pilot→paid ≥ 50 %.
- **Next action:** see **Unified sprint plan** above. G12 lanes: S0 T0237 (0309 DB sync) → S1 T0268 → T0269 → T0275 → T0274-1 → S3 T0270 → T0271 → S4 T0273 → T0274-2 → S5 T0272.
- **Blocker:** only `STRIPE_PRICE_INVESTOR_ANGEL|ADVISOR|VC_SMALL` (+annual) still to mint (needed by T0268 checkout). Founder **approved 2026-09-10** the doctoral-research sentence and the data-handling sentence (§5).
- **Pricing v4** = [`pricing-upgrade-plan-2026-07-16.md` § v4 (2026-09-16)](../pricing-upgrade-plan-2026-07-16.md): Fund A$999, Intake link A$249, Index API A$299, Cohort 25/100 public — live prices unchanged; tracked under G14.

---

### G13 — Investor Clarity: Investor Dossier + startup taxonomy · simplified menus & post-login (founder + investor) · Trusted Business Report v2 (8-dimension agents + visuals)
- **Source:** [`docs/plans/investor-clarity-2026-09-15.md`](./investor-clarity-2026-09-15.md) · specs in [`investor-clarity-2026-09-15/`](./investor-clarity-2026-09-15/) (01–03 codebase audits, 10 BA Investor Dossier + taxonomy, 11 PM/UX IA + post-login, 12 Product/AI TBR v2) · companion of G12 (evaluator surfaces) and G7/G8 (nav + phase scale — amended by this goal).
- **Opened:** 2026-09-15 (founder brief: investors must see the Trusted Business Report + evaluator assessments clearly; investors pick categories/industry/startup type in their profile; founder menus cluttered/duplicated → group + simplify, benefit-first post-login for both groups; report must follow the 8-dimension structure with the most visual output and upgraded agent know-how per dimension; sell without confusion).
- **Status:** **W1 LIVE 2026-09-16 03:58 UTC** (three parallel worktree agents → merged → full vitest 35,434 green → 12/12 gates): **S-IA1** nav v4 (`nav-groups.ts` v4 Home·Prove·Money·Company / evaluator Home·Deal flow·Reports, `persona.ts` 10 personas, `legacy-redirects.ts` 10 live + 83 deferred rows with a test that fails when a deferred destination appears, dead nav systems deleted, `/workspace/evaluation` → `/workspace/score/criteria`, `/workspace/investor/preferences` → `/workspace/investor/mandate`, `nav_click` event; founder phase-0 sidebar 10 links, investor 8 / advisor 6 / accelerator 7) · **S-R1** `ReportV2` contract + zod + adapter from stored snapshots, `dimension-owners.ts` (D8 table; legacy `DIM_META`/`DIMENSION_INFO` derived byte-identically), `report-visuals/` 17 deterministic SVG renderers, `components/tbr/v2` chapters on `/workspace/business-report`, `/tbr/[token]` ×4 locales, `/tbr/demo` (8 dimension primaries, 26 `svg[role=img]`; free fixture 9.0 pages), 0395 `svi_snapshots.report_v2` + `assembled_reports.report_json` (applied), F1 "Trusted Business Report" rename (55 files), F5 live comparables count (33) · **S-T1** `startup-taxonomy.ts` (22+2 industries, 10 models, 8 stages, crosswalks over 20 legacy vocabularies), 0394 `startup_taxonomy` (applied; FK to `projects` only), `suggestTaxonomy()` silent fill on project create/update, evaluation create, `/api/svi`, report pipeline; backfill written (15 projects, 5 honestly unclassified). Ops: a peer session's deploy captured a mid-merge tree (manifest SHA is stamped at the end — misleading); forced rebuild shipped the rest. **W1 post-ship review** (read-only agent): P1 fixed same day — every stored snapshot rendered unscored with an SVI-0 valuation because `dimension_scores` is a bare number per dim on 3,289/3,302 rows (fallback expected `{score}`), `svi_total` now forwarded, valuation chapter shows "pending" when nothing is scored; plus taxonomy store/backfill no longer downgrade on a no-opinion re-run, protected tags never labelled auto, web visuals always re-render from data (stored SVG untrusted), `account_type` on `/api/entitlement/me` so reseller/affiliate/journalist personas resolve, sidebar never plan-locks while the entitlement is loading (paying founders saw a padlocked menu on first paint; live-qa caught it). Deferred P3: RLS column restriction on `startup_taxonomy` UPDATE (not exploitable: no browser Supabase client), second `report_v2` probe could join the main select. **W2 — S-IA2 LIVE 05:08 UTC** (`hubs.ts` 17-hub tab catalogue, `HubTabs` a11y tablist with keyboard nav, 17 hub layouts, ~60 page directories `git mv`'d into tabs — score/evidence/plan/reports/investors/valuation/raise/accelerators/finance/equity/esop/team/strategy/documents/exit/settings/projects — All reports list, Investors Matches + Access, Documents Compliance, Settings Enterprise composed from existing components; **97 legacy redirects live** (5 deferred: evaluator Reports hub, `/dashboard/onboarding` S-IA4, `/investors` S-IA5); orphan guard in `nav-groups.test.ts` (a page with no inbound link fails); the 10 W1 orphans now have hub homes; live-qa 148/4 → 3 stale `toHaveURL` regexes fixed → 15/15 on the re-run) · **S-R2 merged, deploying** (`buildAgentPrompt` v2 with role card / phase lens / skill addon / knowledge files + `agent_knowledge_base` / module outputs and per-block token caps (total 4,560), phase-aware selector, W4 dimension chapters by the 8 owner agents with Zod + one repair + degraded cards, `generateChartsV2` number-provenance pass, orchestrator `onEvent` + per-tier call budget (standard 30 / free 16, never a failed report), auditor caps, 8 TBR prompt-eval fixtures, 0396 applied; `REPORT_PIPELINE_W4=off` rollback; live streaming routes untouched until S-R3) · **S-R2 LIVE** (10:35 UTC, after one flaky-test gate: Google callback tampered-cookie flipped the last base64url char — deflaked) · **S-D1 LIVE 11:15 UTC** (0392 `evaluation_assessments` applied + `watchlist.project_id`; `lib/evaluations/{assessments,dossier}.ts` with server-side consent masking; `GET /api/evaluations/[id]/dossier`; `/workspace/evaluations/[evaluationId]` header + radar/weighted table (F3 weights visible)/13 criteria, blocks 2–6 placeholders; alias `/workspace/investor/startup/[projectId]`; deep-links; `dossier_view`). **W2 post-ship review** (read-only agent + S-R2 sub-review): **P0 W4 double-charged the per-report call budget** (8 chapters = 16 units → later chapters degraded, CEO summary placeholder, still charged) → fixed (only injected callers are metered; 1 unit reserved for the summary); P1 fully degraded reports were persisted `complete` and charged → `report.fullyDegraded` + `assertReportUsable` (paywall retries, evaluator route refunds; D9 kept — orchestrator never throws on budget); P1 evaluator path stored the adapter projection instead of the W4 document; P1 evaluator's private label leaked to the claimed founder's dossier; P2 dossier showed "Not scored yet" for every startup (`svi_snapshots.project_id` NULL on 3,302 rows — 0397 backfill from `svi_accounts`, 633 linked); Δ30d false zero; auditor `[unevidenced]` marker; API/page gate mismatch; assessor read scoping; hub tab chrome shown to investors; `esop/manage` page tier gate; `/workspace/guide` bare 404; weekly rows same href. All fixed and deployed 2026-09-16 ~12:00 UTC. Deferred to W3: pipeline wall-clock deadline + parallel auditor + real cost telemetry (S-R3), 0392/`startup_taxonomy` RLS hardening (S-T2 0393). **W3** — **S-IA3 LIVE 12:57 UTC** (deployed by the peer session together with its annual-billing fix `ebba4641d` after I held my deploy for its uncommitted tree; `/dashboard` 177-line server page with 5 blocks — Where you stand · Next best action · Money on the table · Evidence to add · Your reports — single recommender on the 12-phase scale with `impact`, ladder/plan widgets → `/workspace/plan`, WidgetGrid → `/workspace/score`, 9 next-step components deleted, `landing_viewed`/`landing_block_click`, `NAV_IA_V4=off` rollback) · **S-T2 + S-R3 merged, deploying** (`54ff5376b`, full suite 35,951 green; **0393** creates `investor_organisations`/members/`investor_mandates`/`mandate_fit_scores` (the deferred portal-core file was never applied), `FIT_WEIGHTS_V2` both directions, 7-section mandate form at `/workspace/investor/mandate`, deal-flow v2 on `project_id` + filters + saved views, nightly `mandate-fit-refresh` (crontab 16:35 UTC installed), W2 RLS hardening for `evaluation_assessments` + `startup_taxonomy`, erasure map 132 FKs; **S-R3**: `runReportPipeline` is the one generator behind `/api/svi/dimensions/stream` + `dimension-analyze` (same SSE vocabulary + richer payloads, `criterion_addendum` retired, `REPORT_GENERATOR=legacy_stream` rollback), GATHER real (tech audit, GitHub via owner connection, connector signals from last sync, cap table, grants, VC valuation; 20 s timeouts, 24 h `tech_audits` cache), 5-method valuation chapter in the body (scorecard reference only pre-revenue, ask cross-check, AU stage bands), per-tier deadline 90/120/240 s with deterministic degrade, real cost telemetry into `ai-spend-daily.json`, §C.9 gates, parallel auditor; **0398** applied). Founder-visible change: streaming analysis now runs ~15 (free) / ~24 (standard) calls with chapters landing after W1 — slower but complete. **W3 LIVE 13:25 UTC**, live-qa **155/0**. **W3 post-ship review** (read-only + 3 lane sub-reviews) → fixes **live 14:08 UTC**: P1 revoked consent left a founder in every investor's deal-flow forever (stale `mandate_fit_scores` swept after a clean pass); P1 a deadline that degraded every chapter was still charged (`isFullyDegraded` now true on deadline / zero-call runs); P2 snapshot loader cap crowded heavy projects into "SVI not scored"; SSE heartbeat + no charge after client disconnect; viewers no longer persist the owner's snapshot; late wave writes dropped after the deadline; GATHER research own 60 s budget; TRE revenue gate narrowed (burn/cost/salary/target untouched, citation swallowed, founder-stated revenue counts); cron wall-clock budget; race-safe Scout limit; **0399** (founder view SELECT-only, taxonomy evaluator policy USING guards + column revoke). **W4 LIVE 16:29 UTC** (`52b6cbdd5`, full suite 36,357 green; shipped alongside the peer session's G14 pricing v4 — Fund A$999 / Intake link A$249 / Index API A$299, 0400 plan rows, v3.14.0 — after two of its gate-12 rollbacks on the accelerator pilot CTA expectation): **S-IA4** evaluator landing (investor/advisor/accelerator, 4 blocks: evaluating · deal flow / mandate slot · quota · mandate) + persona redirect from `/dashboard` (`PERSONA_LANDING=off`), single 3-step `/onboarding` × founder/evaluator flows (`ONBOARDING_V4=off` legacy), auth `next` resolved through `PERSONAS`, `/dashboard/onboarding` deleted, `onboarding_step`/`onboarding_completed` · **S-D2** evaluator assessment form (AI-vs-me 8 dims, 13-criteria stances, risks, questions, valuation view, decision/conviction/thesis fit, versioned drafts/submits, prefill from mandate fit), share allow-list dialog with the literal "The founder will see exactly these items" preview + total revoke + history timeline, founder read-only preview, `PUT /api/evaluations/[id]/assessment`, `POST/DELETE …/share`; founder taxonomy confirmation card + `/workspace/settings/project` + `PATCH /api/projects/[id]/taxonomy` (E1.4); legacy sector writers through the crosswalk (E1.5) · **S-R4** PDF/DOCX/email parity from `ReportV2` — react-pdf twins via a shared SVG AST (no duplicated geometry), `sharp` PNG for DOCX/email with SVG fallback, `tbr-pdf` with a real ≤10-page free-tier gate (level 2 → 10 pages), `/api/svi/report/pdf` (react-pdf default, `?v=1` Chromium for one release), DOCX + email rewritten (cover, three questions, weakest chapter, CTA), dossier valuation + progress blocks + "Δ since last view", **0401** `evaluation_reports.report_v2` (applied). Chapter order pinned identical on web/PDF/DOCX/email/dossier. **W4 post-ship review → fixes live 17:04 UTC**: **P0** every full-report run failed `no_account` since S-R3 (selected a non-existent `svi_accounts.user_id`; owner id now from `projects.user_id` — unexercised by users, deterministic); P1 member `/dashboard` 500 (`LANDING_BLOCKS` re-exported from a `"use client"` module = client-reference proxy); P1 Fund / Intake link trial CTAs with no Stripe price → cards render Contact sales until the founder mints `STRIPE_PRICE_INVESTOR_FUND` / `STRIPE_PRICE_ACCEL_INTAKE` (server-computed purchasable ids; signup never submits an unavailable plan); persona redirect moved into the `(founder)` layout (`dashboard/loading.tsx` turned the page's `redirect()` into a CSP-blocked inline script); post-login uses `needsOnboarding` (103 existing founders would have been bounced into the wizard); `/auth/login?next=` open redirect closed; draft autosave can no longer overwrite a just-submitted assessment; founder-confirmed industry drops the stale auto `sub_industry`; free-tier PDF/DOCX gate reads the snapshot tier. **Deferred to W5:** founders self-selecting an evaluator persona in the wizard (product-rule bypass, no money leak); `/api/svi/report/pdf` render cache + concurrency cap; traction cron not installed + writes under the release dir (peer's G14-S33); `subscription_created` GA4 plan label; Cohort trial CTA copy 7 vs 14 days; Vietnamese glyphs in react-pdf; email-path PDF render on the request path. **Ops note:** the peer session (blockid-au-17) commits into and deploys from the same checkout and runs `qa:live` concurrently — two of its deploys rolled back at gate 12 on its own pilot-CTA expectation, and a concurrent `qa:live` clobbered `test-results/live-qa/` mid-run (ENOENT + a false 402/200). Deploys stay serialized by the flock; live-qa collisions are avoidable only by one session running it. **W5 — S-IA5 LIVE 18:54 UTC** (`7f586b5c5`; one header — NavV2 light/dark, 53 importers switched, `site/navbar.tsx` deleted; one `Footer` with a Company column; shared user menu (`lib/nav/user-menu.ts`) with the persona landing as Dashboard; `/investors` → `/about/invest` (F2) with canonical + sitemap; persona lock — founders who own a project or completed onboarding can no longer self-select an evaluator persona (400 `persona_locked`); 14-day trial CTA copy; `subscription_created` plan label). Live-qa **159/0**. **S-D3 LIVE 20:04 UTC** (0403 `ic_reports` + org invites + `investor_portfolio.project_id`; seats & consensus — Scout 1 / Firm 3 / Program 5, invite magic links, median-per-dimension consensus with tie → split; IC memo (Scout one-pager, Firm/Program 2–4-page memo, weights only Program+ per F3) via `ic_reports`; cohort decision columns + bulk set + CSV + LP pipeline counts; dossier evidence block by consent tier + actions (watchlist/portfolio/intro/access) + audit trail; founder "Request intro" → `investor_contacts`; live-qa dossier lane seeding a third evaluator account; docs close-out `docs/plans/investor-dossier-taxonomy-2026-09-15.md`, ROADMAP §4, roadmap-v2, GOALS) · **S-R5 LIVE 20:39 UTC** (0402: `au_comparable_raises` seeded with the 32 verified rows + verified view, weekly allow-listed ingest (Startup Daily, Cut Through Venture, ASX) → pending + `/admin/comparables` review, `founder_signals` LinkedIn upload/URL parser + Evidence › Founder tab, `ga4_signal_snapshots` AARRR funnel + channel mix, cap table → CGH in `computeSVI`, Xero margins, chapter-level cache, nightly eval grounded-share gate + canary demotion, admin KPI tile; PDF render cache + 2-slot semaphore, report-email queue cron, Noto Sans VI font, dossier reads `evaluation_reports.report_v2`; crontab `comparables-ingest` Sun 17:40 UTC + `report-email-sweep` */5 installed). Full live-qa on the final build **163/1** (the 1 = key casing in the new dossier lane, fixed → 6/6). **W5 post-ship review → fixes live 21:20 UTC**: **P1** `pdf-parse`/`pdfjs-dist` were bundled → pdfjs' fake worker path broke in the standalone release (every LinkedIn PDF 422, guest deck PDFs byte-scanned) → `serverExternalPackages`; P2 nightly eval demoted canaries on a transport-only outage; invited seats never lapsed with the owner's plan; legacy `?v=1` Chromium PDF outside the render gate; email sweep head-of-line blocking + double-send window; persona-locked wizard save trapped founders in the wizard; P3 empty register earned CGH credit, Xero "Other Income" clobbered revenue, VI hyphenation leaked process-wide, evaluator user menu founder-shaped, homepage lacked a `<footer>` landmark. **Deferred backlog → all shipped 2026-09-16 23:26 – 2026-09-17 00:22 UTC in four code→commit→deploy batches** (`07100d3b2`, `1e85fc45a`+`2ea93ff11`, `d3d5ea1df`; 0404 GA4 daily key applied): IC memo behind the render gate + stored exporter + org-owner plan + Noto Sans for VI; seat-limit re-count after insert; invite mismatch no longer echoes the address; founder-signals refuses oversized bodies before buffering; ingest — indexOf strip, final-URL allow-list, 90 s shared deadline; GA4 snapshots one-per-day (upsert); dossier single `evaluation_reports` read; persona lock resolves legacy account types; `resolveActingOrg` prefers the owned firm + seats can leave (`DELETE {leave_org_id}`); `SVIEntrance chrome={false}` under the workspace shell; `/admin/comparables` paged; chapter cache key includes the model; live-qa register-bucket comments corrected. Remaining known-and-accepted: invite token in `?invite=` (magic-link convention), `investor_organisation_members_seat_select` self-referential policy (service-role reads only). Final live-qa on `967c03174`: 164/3 — the 3 are the peer session's G14-S35/S39 lanes merged after the build was captured (routes ship with its next deploy).

**G13 STATUS: CLOSED 2026-09-16** — all 15 sprints live in one day (W1 03:58 → W5 21:20 UTC), 12 migrations 0390–0403 applied, five post-ship reviews (3 P0 + 13 P1 found and fixed same day), live-qa 159–163 passing with 3 new lanes (evaluator landing, taxonomy, dossier). KPIs (§) now measurable via `landing_viewed`/`landing_block_click`/`nav_click`/`dossier_view`/`investor_decision_saved`/`assessment_shared`/`taxonomy_confirmed`/`onboarding_step`. Founder-only follow-ups: mint `STRIPE_PRICE_INVESTOR_FUND(_ANNUAL)`, `STRIPE_PRICE_ACCEL_INTAKE(_ANNUAL)`, `STRIPE_PRICE_INDEX_API` (Fund / Intake link / Index API cards say Contact sales until then); confirm F3 (weights visible to evaluators — shipped by default); review `/admin/comparables` pending rows weekly; set `MODEL_AGENT_CEO/CFO` if the Sonnet-class chapters are wanted at A$3.
- **Decisions taken (D1–D10 in goal doc §3):** Dossier keyed on `evaluations.id`, never public · new `evaluation_assessments`; reuse `investor_mandates` / `investor_organisations` / `mandate_fit_scores` / `ic_reports` with ALTERs; do not reuse `investor_decisions`/`investor_scores` · `startup_taxonomy` 1:1 table (22 industries + professional_services + unclassified, 10 business models, 8 canonical stages, customer type, geo, 11 tags) · canonical phase scale = 12 `GrowthPhaseId`s · one nav config + `persona.ts`, dead nav systems deleted, Account → avatar menu · every old route redirects · deterministic SVG visuals from `report-visuals/` · dimension owners TRE→CRO, MPC→CMO, FTV→CHRO, PTD→CTO, CGH→CFO, IRI→CLO, LCO→CLO, SVM→CEO (CDO evidence officer; CISO/COO always-on cards) · A$3 report ≤ 30 calls / COGS ≤ A$0.60 / ≤ 120 s · renames `/workspace/evaluation` → `/workspace/score/criteria`, `/workspace/investor/preferences` → `/workspace/investor/mandate`, founder "Portfolio" → "Compare"; prices unchanged.
- **Finding (audit 2026-09-15):** investors land on the founder dashboard (persona landing never wired); Preferences page edits only firm/thesis; deal-flow join keys on email vs `account_id` (filters no-op); evaluators can only store label + notes; ≥ 6 sector + ≥ 5 stage vocabularies; 16 `investor_portal_core` tables with zero code refs · sidebar 7 groups / 98 leaves, 3 nav architectures (1 mounted), 8 report surfaces, 5 "what-next" widgets on 2 phase scales, 2 onboarding wizards, 2 headers/2 footers, ~25 orphan pages · customer-facing report uses a generic analyst persona (C-level agents run only in the enhanced pipeline), `visuals: []` hardcoded so every chapter is text-only, knowledge/skill-map never injected, no owner for CGH/SVM, GATHER stubs, "500+ comparables" vs 33 in code.
- **KPIs:** time-to-first-action after login median < 20 s · founder phase-0 sidebar ≤ 10 links · `tbr_view → tbr_share_created` ≥ 25 % · free `score_done → trust_report_purchased` ≥ 12 % (72 h) · reports/evaluator/mo ≥ 6 Scout / 15 Firm / 40 Program · `dossier_view → investor_decision_saved` ≥ 40 % (14 d) · grounded share median ≥ 0.85 · report-clarity survey ≥ 8.5/10 · "can't find" feedback −50 % in 30 d.
- **Next action:** none — G13 closed. Backlog items listed under W5 review "Deferred"; founder-only items in §5. Next goal = the roadmap after G13/G14 (see ROADMAP §4) or founder direction.
- **Blocker:** none hard; F2 (`/investors` → `/about/invest`) needs CMO sign-off before S-IA5; F3 (weights visible to evaluators) is an svi-scoring guardrail exception the founder should confirm before S-D1.

### G14 — Investor Feedback: credibility loop — deck v3 (evaluator-first) · pricing v4 · feedback letter · intake link · verification integrity · founder execution · evaluator API v1 · backtest v0 · open AU signals
- **Source:** [`docs/plans/g14-investor-feedback-2026-09-16.md`](./g14-investor-feedback-2026-09-16.md) · companion [`g14-investor-feedback-2026-09-16/`](./g14-investor-feedback-2026-09-16/) (`00-pitch-feedback.md` 19 verbatim judge comments + traceability, `01-gtm-evaluators-90d.md`) · research instrument [`docs/research/evaluator-interviews-2026-09.md`](../research/evaluator-interviews-2026-09.md) · pricing ladder v4 in [`pricing-upgrade-plan-2026-07-16.md` § v4](../pricing-upgrade-plan-2026-07-16.md) · deck source `web/content/pitch/pitch-deck-v3.md` (supersedes deck v1 / v2 goal doc / video-assets slides) · extends G13 (S-D2, S-R4, S-R5) and G12 (evaluator ladder).
- **Opened:** 2026-09-16 — 19 investor/judge comments after the 3-minute pitch, clustered C1–C10; founder decisions: **evaluator-first · ask A$500K pre-seed · lead brand "Startup Value Index" (blockid.au = platform, entity/domain unchanged) · keep the A$3 wedge, raise the B2B tiers.**
- **Status:** **Wave A LIVE 2026-09-16** (release `CsYpAwoF4jVls5a5BS1go`, 12/12 gates, 119 smoke): deck v3 + 3-minute cut at `/pitch/svi-pitch-deck-v3-preview.html` + `.pptx`, site wording §2.4, pricing v4 code (Fund/Intake/Index API cards render; Stripe mints + 0400 still human-gated — pilot/accelerator CTAs stay on Program until `STRIPE_PRICE_ACCEL_INTAKE` exists, first deploy rolled back on exactly that smoke), S33 traction snapshot (`/api/status.traction: ok`), GTM docs. **Baseline snapshot 2026-09-16 (QA/seed excluded = 59):** 66 real users · 182 SVI analyses · 0 TBR purchased · 0 MRR · 5 evaluator accounts, none paying. **G14 ALL 8 SPRINTS LIVE 2026-09-17** — batch 1 (S34/S35/S36/S39, release CsYp…→ 12/12) and batch 2 (S37/S38/S40, release `WJlPd__nj54CYhxcRmXoQ`, 12/12 gates, 37,330 unit + 120 smoke, `qa:live` 112/0). Migrations 0405–0410 applied. Backtest v0: N=49, ρ round 0.76 [0.56–0.88], ρ valuation 0.94 [0.81–0.97]. Lesson: the peer build carried master's SHA but an older tree → `DEPLOY_FORCE=1` rebuild was required; always verify the live bundle. **Post-ship reviews 2026-09-17** (elevated `qa:live`, which the 112/0 run skipped): `7900bcb5c` — S35 intake rows were all "No dossier" because `createEvaluation` is quota-gated (free = 1 startup) while `canManageIntake` accepts any evaluator persona → inbox shows "Plan limit — upgrade", live-qa elevates its evaluator seat to `investor_angel`; audit hmac UPDATE (0338 append-only) warns once per process. `036ba5c2e` 16:05 UTC — `callAI` wall-clock budget: interactive calls default to max(60 s, timeoutMs) across providers AND model ladders (`AIBudgetExhaustedError`), funding narrative + auditor share a 55 s deadline; cause was DeepInfra's 4-model ladder × 30 s Worker timeouts = 120 s → Cloudflare 524 on `POST /api/funding/report`. Elevated live-qa **177/0** on `036ba5c2e`. External-signals weekly cron installed (Sat 03:00 UTC). **Stripe done 2026-09-17 (founder authorised .env keys):** Fund / Intake link / Index API products + monthly & annual prices minted, Cohort 25/100 annual prices minted (10 `STRIPE_PRICE_*` in `.env` + `.env.runtime`), `sync-stripe-pricing` audit = all match, `customer.subscription.created` enabled on `we_1TYooWJ7OAnXQ9sVpIpaTzqP` (`/api/health/stripe` missing_events []), pilot + accelerator CTAs flipped to `accelerator_intake` (release `GlDPN6-urrBAE_k1jl_aa`, 12/12). External signals live: R&DTI 13,128 rows + ABR bulk 12,827 entities (all 20 split files, allow-set = every known ABN; XML deleted after ingest, weekly cron re-fetches) — `/methodology` data sources + `signalsForAbn` now real; GrantConnect needs a manual CSV export. **Batch-2 post-ship review (2026-09-17/18, release `obvS5pKOpBy8evFlCsfPx`):** P0 — `/api/svi` keyed the founder profile by a caller-supplied email, exposing exits/raises/roles in the response → now keyed only by session-proven identity; P1 — `execution_source: linkedin_parser` could be forged to lift the 70 cap → HMAC attestation minted by `/import-linkedin` (24 h); P1 — unreadable PDF 500 → 422; P2 — register-cohort scan memoised 5 min. Open P2s: Slack webhook URL echoed to project admins (mask later), pre-0409 fallback moot, `api.access` uses key owner's plan (design). **Close-out merged:** deck slide 4 real dossier screenshot + 6-slide appendix `SVI-Pitch-Appendix-2026-09.pptx`; CHANGELOG G14 entries; ROADMAP/GOALS/roadmap-v2 ticks; `docs/ops/stripe-env-audit.md` v4 vars; weekly Δ email "why it moved" (F-6); GA4 `assessment_submitted`; data-sources ops notes. Seed fixture `qa-dossier-shot@blockid.au` (+1 evaluation on Zenya) exists in prod for the screenshot. **Founder-only:** F-1…F-9 confirmations; LOI/pilot names for deck S9; GrantConnect CSV.
- **Decisions taken (D1–D6 in goal doc §3):** **D1** migrations 0400 pricing rows v4 · 0401 feedback letters · 0402 intakes · 0403 evidence review · 0404 founder profiles · 0405 api v1 scopes + webhook kinds · 0406 external signals (highest applied today = 0399) · **D2** intake route public `/apply/[slug]`, `/submit` stays the public-index submission · **D3** feedback letter only at k ≥ 3 assessors from ≥ 2 orgs, ratings/risk buckets/questions only, never evaluator id/decision/conviction/notes · **D4** confidence capped by origin (founder ≤ `document_uploaded`, connector → `connected_source|transaction_data`, reviewer/admin → `third_party_verified`) replacing the keyword ladder in `extractSignals` L584–599 and the upload route L110 · **D5** one new feature flag `intake.manage` · **D6** 1 credit = A$1 list (packs to A$0.60), dead `v3-skus.ts` ladder deleted, pilot comps = admin credit grants never Stripe coupons.
- **Finding (audit 2026-09-16):** three contradictory decks (retired prices, three asks, "Chain Live"), no feedback file in the repo; `extractSignals` self-awards `third_party_verified` on "audit"/"asic"/"board signed" prose and the evidence upload route honours a founder-supplied `confidenceLevel`; FTV = regex although `founder_profiles` (0064) exists; `(investor)` route group is an empty passthrough; no feedback letter, no intake link; integrations stop at generic webhooks/API keys; comparables 72 rows, no backtest; Stripe 0 subscriptions / 5 one-off charges, GA4 audit 8 conversion events `missing`, no traction snapshot.
- **KPIs (30 days after S40):** ≥ 10 paying evaluators · ≥ 3 intake links with ≥ 1 submission · ≥ 30 % founders open the feedback letter · ≥ 25 % scored projects with evidence ≥ `connected_source` (server-derived) · ≥ 40 % paid TBRs with Verified ABN · ≥ 5 evaluator API keys + ≥ 2 outbound destinations · backtest published (ρ, N, CI) · ≥ 3 open sources ingested ≥ 2,000 rows · `/api/status.traction: ok` daily. GTM D30/D60/D90: interviews 10/12/15 · LOIs 2/6/8 · pilots 2/4/5 (cap 5) · pilot→paid ≥ 50 % · evaluator signups 120/300/500 · intake links 1/3/6 · evaluator MRR A$1.5K/5K/10K.
- **Next action:** G14 engineering closed; post-ship review of batch 2 (S37 FTV bonus decision, S38 Zapier app, S40 first ABR ingest off-peak) + 90-day evaluator GTM (docs/plans/g14-investor-feedback-2026-09-16/01-gtm-evaluators-90d.md) — pilots, interviews, LOIs are founder-led.
- **Blocker:** 6 Stripe price mints (`STRIPE_PRICE_INVESTOR_FUND(_ANNUAL)`, `STRIPE_PRICE_ACCEL_INTAKE(_ANNUAL)`, `STRIPE_PRICE_INDEX_API(_ANNUAL)`) + apply migration 0400 (founder) · real LOI / pilot names for deck S9 (placeholders until written consent, F-9) · founder decisions F-1…F-9 (defaults ship if silent — §5).

---

### G15 — Reliability: ship safety · observability · data safety · AI resilience
- **Source:** [`docs/plans/reliability-2026-09-18.md`](./reliability-2026-09-18.md) (evidence E1–E8, lanes R1–R3, acceptance).
- **Opened:** 2026-09-18 — founder direction "tập trung reliability" after G14 closed. Evidence: 25 failed deploys / 14 d incl. lock races and a mid-merge build; a 120 s AI ladder → Cloudflare 524; dead `AI_GATEWAY_URL`/`BILLING_URL` pointers (commented out 2026-09-18); **off-site DB backup failing nightly** (founder must run `scripts/db-backup-offsite-auth.mjs`), no restore ever exercised; `/tmp` production log, no error digest / latency / status sections; test flakes under load; stray agent processes.
- **Status:** **G15 LIVE 2026-09-18** — `3ce353da8` (three lanes merged, 37,525 unit green, 12/12; **gate 11 verified the live bundle SHA for the first time**) then review deploy `7e86fb700` 06:22 UTC (12/12, elevated live-qa **177/0**). Shipped: R1 manifest truth at build start + dirty-tree refusal + gate-11 live-SHA check, `--wait` lock etiquette (deploy + live-qa), vitest `pdf` project 20 s + flake report, live-qa 429 `Retry-After` + 52x classification + stale-run guard, rollback `--dry-run`, `docs/ops/deploy.md` · R2 `/data/logs` append-mode log + rotation, `error-digest` + `latency-sample` crons (installed), `/api/status` v2 (`git_sha`, `errors_1h`, `ai`, `queues`, `backups_detail`, `slo.latency_p95_ms`, `crons_failed_24h`) + `/status` sections, `docs/ops/slo.md`, nginx `blockid_timing` log format applied on the server · R3 weekly restore drill (first real run ok: 12/12 tables ≥ 99.8 %, audit chain head ok, 15.6 s; cron Sun 03:45 installed, legacy restore-test retired), off-site failure once-a-day alert + founder command, `getProviderHealthSnapshot()`, report-pipeline `fully_degraded` counter, `server-cleanup --procs` stray-session sweep (cron), cron-runner `--timeout` ceiling 600 s. **Post-ship review (read-only agent) → fixed same day:** P0 log fold-in unlinked the inode the live server was writing to (deferred while held open; lost lines recovered by hand); P1 status readers read the release-dir COPY of `content/reports` (now the live checkout); P1 restore-drill allow-list treated a missing relation as benign; rotation ran on `--dry-run`; dirty regex ignored MDX content / blocked on root docs; off-site alert window 24 h → 20 h; null-line guard; runtime outputs gitignored. **Findings outside the spec:** (1) **Telegram bot token is 401** — revoked per the rotation log, replacement never installed → every cron/backup/uptime alert had been silently dropped; e-mail fallback (ADMIN_EMAIL, ≤ 30/h) now wired into all four alert paths (§5 founder row). (2) `crontab.production` and the live crontab had drifted both ways — the peer's `feedback-letters` / `traction-snapshot` / `backtest` crons were never installed, my G13 lines were not in the file; reconciled and installed from the file. (3) `.env.runtime` had two unquoted `<…>` values that aborted `source` at line 8 in `qa-live.sh` (quoted). (4) 2026-09-17 08:02–08:08 UTC the watchdog restarted production 4× (guardian `http_local 000`) — cause not captured; follow-up below. **Follow-ups shipped 2026-09-18 (`74a5e109a` → review-2 `bac472af4`, 12/12, elevated live-qa 178/0):** `slo.uptime_pct_24h` from the 2-min guardian (99.6 %); watchdog records why it restarted (jq row → `watchdog-restarts.jsonl` + alert) — and the first rows exposed the real cause of the 09-17 and 09-18 restart bursts: **the watchdog itself kill -9'd a healthy server whenever a build pushed load to 10–18 and the 5 s probe timed out** → it now never kills a live pid during a deploy, needs two consecutive strikes otherwise, 10 s budget (the `bac472af4` deploy was the first with zero watchdog restarts); `build_sha` in the manifest (gate 11 compares the BUILT sha, `--skip-build` honest); `/data/logs/blockid-*.log` rotated; `/api/status` extras read the live checkout (route had passed the release-dir cwd); p95 latency now live (marketing 98 ms · workspace 65 ms · api_other 229 ms · api_ai 349 ms). Open: one unexplained death of the production pid at 2026-09-18 07:59 during gate 6–8 of a deploy (no OOM, no cleanup kill) — the next occurrence will carry a full `watchdog-restarts.jsonl` row.
- **Next action:** none engineering — Sun 03:45 first scheduled restore drill; watch `watchdog-restarts.jsonl` for the 07:59-type death. **G15 closed 2026-09-18.**
- **Blocker:** founder-only — (a) new Telegram bot token (§5), (b) Drive quota auth for off-site backups (`node --env-file=web/.env scripts/db-backup-offsite-auth.mjs`), (c) valid `ANTHROPIC_API_KEY` (`claude-apikey` shows `blocked` in `/api/status.ai`).

### G16 — First dollar: funnel truth · paywall → A$3 · evaluator pilot enablement
- **Source:** [`docs/plans/first-dollar-2026-09-19.md`](./first-dollar-2026-09-19.md).
- **Opened:** 2026-09-19 — founder "làm như bạn đề xuất". Evidence: 115 real sign-ups / 14 d (≈ 15/day) yet 0 `report_orders`, 0 trials, 0 MRR; `sign_up`/`checkout` events never emitted, `svi_score_computed` 1/14 d, `/admin/funnel` reads a table that does not exist; gate hits 97/14 d (cap_table.write 42, investor.dealflow 35).
- **Status:** **G16 LIVE 2026-09-19** — lanes merged (`388f73a82` C, `1f0773d96` B, `383e9740b` A), shipped by the peer session's deploy `8cbb6bdb8` 14:26 UTC (12/12), review fixes `58406efa2` live 15:19 UTC (12/12), elevated live-qa **184/0**; 0411 applied; crons installed (`funnel-report` 02:50 daily + Mon 03:05, `pilot-expiry` 04:20); first funnel report written; a throw-away pilot was started → Program plan → ended → plan reverted on production, audits `pilot.started`/`pilot.ended`, accounts erased. **Key finding from lane B:** the A$3 checkout was mounted by NO page and every free founder's web TBR was lifted to `standard` — the product had no reachable first purchase; now: locked-chapter preview + one unlock rail → quote-then-pay, `GET /api/reports/access`, gate cards with plan + price + one CTA, `feature_gate_hit.surface` populated, +48 h "unlock for A$3" nudge. Lane A: `sign_up`/`svi_analyze`/`svi_score_computed`/`report_view`/`checkout`/`trust_report_purchased` now emitted with `qa` flags (14-day dry-run before: all zero), `/admin/funnel` on real data, `traction.funnel_7d_v2`. Lane C: one-click Program comp (30 d, cap 5, never a Stripe payer), expiry cron, `/pilot` + apply form, `/admin/pilots`. **Review (read-only agent) → fixed same day:** P1 `content/pilots.json` dirtied the tree and blocked deploys (ignored by the gate); P1 deck-analyser snapshots stored as `standard` bypassed the paywall; P1 ingest limiter keyed on client session id; P1 D1 + nudge in the same hour; P1 stale-chunk error boundaries could dead-end (now cover the app segments too); P2s pilot-on-payer 409, evaluator gate CTA segment, gate-hit `qa`, params cap, gitignore. Deferred P2 (tracked here): per-address apply-form limiter + subject scrub, ledger mutex/fsync, nudge partial unique index, `LIVE_TODAY_LIMIT` paging, funnel offset paging on `ts`, anonymous first-analysis `svi_analyze` actor, static-archive check picks new chunks, `global-error` reload in render, failed access-fetch retry state. **Growth does NOT carry `report.premium`** in `plans.feature_flags` (founder ladder sells the report by credits) — live-qa now pins the contract, not the assumption.
- **Next action:** watch `/admin/funnel` after the first real sign-ups post-deploy (the reducer needs the new events); the 30-day KPI is the first paid TBR. **G16 closed 2026-09-19.**
- **Blocker:** none engineering. Founder decisions F-1…F-3 default-ship.

### G17 — Unicorn homepage & site template: evaluator-first hero · one design system · no pricing on home · every link works
- **Source:** [`docs/plans/unicorn-homepage-2026-09-19.md`](./unicorn-homepage-2026-09-19.md) (ui-ux-pro-max research, decisions D1–D7, phases, acceptance).
- **Opened:** 2026-09-19 — founder direction: modern unicorn-startup homepage, very clear evaluator-ladder hero, short/simple, no price tables on the home, one consistent template site-wide, keep the colour-ring search box, complex material → intro menu (`/product`, `/samples`); deploy live, review, fix links/content automatically.
- **Status:** **G17 LIVE + CLOSED 2026-09-19** — Phase 1 `fc155500c` (tokens, template primitives, evaluator-first hero E1, 5-entry nav, footer, home 448 → 187 lines / 6 sections, `/product` + `/samples`); Phase 2 `55c71d258` (P2-A: every `(marketing)` page + `/vi` mirrors on the template, pricing ladder untouched; P2-B: `scripts/link-check.mjs` — production 9 → 0 broken internal links, gate-12 homepage smoke, live-qa `31-marketing` lane, content sweep, cron 03:40 UTC); review fixes `97a2ad910` (link-check private-host guard (SSRF), gate-8 internal link check wired into the deploy — first run 510 pages / 1,036 links / 0 broken —, `/vi` login redirect, duplicate ids, ISR-safe home stats, VI hero wording "tám chiều đánh giá" + meta, `/solutions` in sitemap, 44 px mobile nav, 0412 ACS URL); `5a0c3dbb1` partner logos on the dark footer + screenshots `docs/design/screenshots/` (home/product/samples/solutions/pricing/pilot × 1440/375, no horizontal scroll). Final: 12/12, elevated live-qa **191/0**, production link check **525 pages · 1,638 links · 0 broken · 0 chains**. Deferred P2/P3 (tracked): apply-form per-address limiter; insights "2,700+ AU peers" + old article numbers; `/vi/solutions/advisor` description 166; "Pty Ltd" casing; `tier-ladder.tsx` orphan.
- **Next action:** none — founder visual review of the live home; the design contract `docs/design/unicorn-template.md` governs every new marketing page. **G17 closed 2026-09-19.**
- **Blocker:** none. Founder decisions F-1…F-3 default-ship.

### G18 — Truth sweep: pricing ↔ Stripe ↔ DB · version + docs · one message site-wide
- **Source:** founder direction 2026-09-19 ("update lại toàn bộ information, version, docs… review pricing cho đúng, check Stripe và các link, thông điệp đồng nhất trên toàn bộ site"); Stripe read-only audit [`docs/ops/stripe-price-audit-2026-09-19.txt`](../ops/stripe-price-audit-2026-09-19.txt) (39 `STRIPE_PRICE_*`, all livemode; amounts match pricing v4; `FOUNDER_SCALE` inactive as intended; 18 prices still `tax_behavior=unspecified`; legacy prices Founder 99 / Growth 99·499·950 / Founding 100 / Pilot 5000 / SVI Analysis 1·25 / Accelerator 20000 still active).
- **Status:** **G18 LIVE + CLOSED 2026-09-20** — v3.16.0 (`18c1c151e` 12/12) + review fixes (`32ea5b05e` 12/12, elevated live-qa 195/2 → the 2 were spec pins on the new GST spelling / CSS uppercase, fixed `33f69288b`). Shipped: **A** pricing truth — code ↔ Stripe ↔ DB parity table `docs/ops/pricing-truth.md`; the SVI Quick Report paywall had booked **A$25** since 2026-08-01 (→ A$3), reseller wholesale booked legacy Growth A$99 (→ A$69), checkout's A$499 escalation deleted, change-plan resolves v2 ids, every A$ figure from constants (31 files), GST-inclusive rule + policy line, trial_days parity pinned (plans-v2 === csv === migrations), `stripe-price-catalogue.json` + `stripe-map` test + weekly `stripe-price-audit.mjs` (cron Mon 03:20), 0413 cohort_enterprise custom (applied), legacy prices → archive list · **B** v3.16.0 (package.json = version.json = project-state M030; CHANGELOG incl. the missing G13 entry), README/web README rewritten, `docs/README.md` index, 14 stale docs archived, `/docs` + `/roadmap` facts (roadmap phases had rendered everything "planned" on v3.x), API docs +v1/analyze +v1/id, `api.access` = Fund/Program/Index API · **C** `docs/design/messaging.md` map + forbidden-phrase guard test, 57 i18n edits, "13 criteria" → "8 SVI dimensions" in public copy, agent counts → "a C-suite of AI agents", OG/JSON-LD/manifest/e-mail/PDF wording, 20 dead landing files deleted · **D** self-serve cancel on `/workspace/billing` (trialing → cancel now, no charge; active → period end + Resume; idempotent; exit survey; reseller-managed excluded), **Billing Portal configuration created in Stripe** (the account had none — "Manage billing" was broken for real customers), trial truth (`trialDays`/`firstChargeOn` from Stripe; banner thresholds were inverted), webhook `plan_id` parity for v2 prices, evaluator Billing menu row, live-qa `32-billing`. **Review (read-only) → fixed:** Remotion video scenes lost `src/content/how-it-works-copy.ts` (restored), `/docs` agent count + guard tree, cancel refuses a save-offer on a trial (409), checkout/change-plan answer `interval_unavailable` instead of silently billing monthly, change-plan mirrors `contact_sales` for custom tiers, admin stripe-sync/`--fix` never re-mint retired SKUs, archived doc pointers; gate-8 link check caught `/workspace/settings/connectors` (never existed) in the peer's new valuation CTA → `/workspace/evidence/connectors`. **Founder-only in Stripe:** set `tax_behavior=inclusive` on the 14 active unspecified prices (Scout/Firm/Program ± annual, Cohort monthly, credit packs, One-click); archive the 9 legacy prices (Founder 99, Growth 99/499/950, Founding 100, Pilot 5000, Accelerator 20000, SVI Analysis 1/25); decide founder annual prices (mint Starter A$290 / Growth A$690 or drop `annual_aud`).
- **Next action:** none — the weekly `stripe-price-audit` cron reports drift; **G18 closed 2026-09-20.**
- **Blocker:** founder-only Stripe actions above.

### G20 — Ready for sale: feature audit · hide the unfinished · every page error-free · purchase path
- **Source:** [`docs/plans/g20-ready-for-sale-2026-09-20.md`](./g20-ready-for-sale-2026-09-20.md) — founder 2026-09-20: "review lại toàn bộ feature, ẩn đi những chức năng thừa hoặc chưa hoàn thành, rà soát toàn bộ các trang trong site để không còn lỗi và fix, commit, deploy live để product ready sale".
- **Baseline:** 367 page routes; "not available yet / coming soon" still on `workspace/settings/enterprise` (SSO, white-label), `workspace/score/listing`, `showcase`, `workspace/weekly-digest`, `admin/rnd`, `admin/tokens`; signed-in pages never swept; gate-8 link check + production link-check cover public pages only.
- **Status:** **G20 LIVE + CLOSED 2026-09-20 — v3.17.0** (`0709a0507` 12/12 → OAuth fix live `c6a00850b`; elevated live-qa **210/1** → the 1 fixed). **F1** `docs/ops/feature-inventory.md` (founder 92 working / 5 partial / 9 hidden · evaluator 21/1/5 · accelerator 13/1/2 · reseller-mentor 15/2/3 · public 34/3/4 · admin 44/1/1); `HIDDEN_FEATURES` registry (19: SSO, white-label, public listing submission, investor access-link builder, weekly/investor digests, portfolio, roster invite, cohort add, LP quarterly, innovator console, mentor tools, unprovisioned Stripe Connect/Xero/QuickBooks, `/es` `/ja` forks, admin tokens) rendering a "Not offered yet — talk to us" card, noindex, filtered from hubs/nav/menus/tours/sitemap; plan bullets ↔ flags ↔ pages reconciled (0414 applied: `accelerator.cohort` on Program/Fund/VC-ent; enterprise bullets no longer promise SSO/white-label); dead `/score` + `/svi` removed (redirects kept); a guard test forbids any live href to a hidden route. **F2** `scripts/page-sweep.mjs` + live-qa `33-page-sweep` (every page × founder/evaluator/accelerator; weekly cron Sun 04:30) — first run found 35 defects, all fixed: streamed `redirect()`s behind `workspace/loading.tsx` → real 307s from the founder layout (`founder-layout-redirects.ts`; CSP inline-script errors gone), `/workspace/projects/compare` React #441 (`webpackIgnore` import), the analyze page rendered the whole marketing landing inside the workspace, the Program cohort page queried columns that never existed (every subscriber saw an empty cohort), missing h1/main on dashboard/onboarding/score/strategy/ESOP/reports, `/dataset` prefetch 400s, 375 px overflow on /version /changelog /legal /developers/api + five /tools, forecast page calling a non-existent API. **F3** live-qa `34-purchase-path` (nothing paid), `purchase-surfaces` mapping test (42), workspace copy on the messaging map (11 Beta chips, 16 emoji icons, price literals → constants incl. `lib/startup-package/price.ts`), onboarding trial days from the plan, member invite accept → `/dashboard`. Production link check 536 pages · 0 broken internal. **Review → fixed same day:** six live CTAs still pointed at hidden routes (advisor roster invite, investor access "New link", reports LP quarterly, mentor quick-actions, accelerator tour step, an insights article), onboarding "0 days free", enterprise SSO bullets, messaging guard re-includes the G19 trees. Unprovisioned Xero/Stripe OAuth routes now redirect browsers to the connectors tab instead of a JSON 503 page. **Follow-ups (tracked):** `/dashboard` first-time `needsOnboarding` redirect still streams behind `dashboard/loading.tsx`; forecast wizard POSTs without `projectId`; `/api/svi/report/[projectId]` no-snapshot → 200 empty (peer informed); `svi` rate bucket 20/min hits fast-tabbing accelerators (`phase-progress`); `/showcase/**` 16 pages without `<main>`, `/tbr/demo` valuation table overflow at 375 px, `/sample-business-report` anonymous peers 401 — G19 surfaces.
- **Next action:** none — the weekly page sweep + link check + Stripe price audit report drift; founder to un-hide features from `feature-inventory.md` as they are built. **G20 closed 2026-09-20.**
- **Blocker:** none engineering.

## 2. Requirements Register

| ID | Source | Category | Status | Owner (skill) | Ship commit |
|---|---|---|---|---|---|
| D1-CTO-01 | plan-delta-2026-07-23 | data-model | shipped | database-optimizer | migration 0091 |
| D1-CTO-02 | plan-delta-2026-07-23 | data-model | shipped | database-optimizer | migration 0091 |
| D1-CTO-04 | plan-delta-2026-07-23 | ledger | shipped | database-optimizer | 0091 events table |
| D1-CTO-05 | plan-delta-2026-07-23 | seed | human_blocked | db-migrate | — (H.20 ABN/GST) |
| D2-CFO-01 | plan-delta-2026-07-23 | tax/AU | shipped | au-compliance | 0092 gst_registered+abn |
| D2-CFO-03 | plan-delta-2026-07-23 | refund-GST | shipped | typescript-pro | 3-part reversal |
| D2-CFO-04 | plan-delta-2026-07-23 | economics | shipped | typescript-pro | `web/src/lib/reseller/cogs.ts` |
| D2-CFO-06 | plan-delta-2026-07-23 | GST-invariant | shipped | au-compliance | G.2 rewrite |
| D2-CFO-07 | plan-delta-2026-07-23 | KPI | shipped | typescript-pro | C.6 CSV schema |
| D2-CFO-08 | plan-delta-2026-07-23 | sandbox-cap | shipped | secure-code-guardian | tick 317 pins |
| D3-CISO-01 | plan-delta-2026-07-23 | RLS | shipped | secure-code-guardian | `reseller/supabase.ts` |
| D3-CISO-02 | plan-delta-2026-07-23 | feature-gates | shipped | secure-code-guardian | manifest tick 44 |
| D3-CISO-03 | plan-delta-2026-07-23 | k-anonymity | shipped | secure-code-guardian | progression queries |
| D3-CISO-04 | plan-delta-2026-07-23 | redaction | shipped | secure-code-guardian | reseller view |
| D3-CISO-05 | plan-delta-2026-07-23 | sandbox-cap | shipped | secure-code-guardian | tick 317 |
| D3-CISO-06 | plan-delta-2026-07-23 | Stripe-portal | shipped | secure-code-guardian | `fd4a1eb3` |
| D3-CISO-07 | plan-delta-2026-07-23 | hash-metadata | shipped | secure-code-guardian | `reseller/hash.ts` |
| D3-CISO-08 | plan-delta-2026-07-23 | CI-lints | shipped | test-master | R-01..R-09 |
| D4-CLO-01 | plan-delta-2026-07-23 | APP 5 notice | shipped | au-compliance | EN+VI |
| D4-CLO-02 | plan-delta-2026-07-23 | legal-agreement | human_blocked | clo | requires counsel |
| RES-U.4 | reseller-module-plan §U | wholesale | shipped | fullstack-guardian | P4 console |
| RES-U.7 | reseller-module-plan §U | progression | shipped | react-expert | drawer overview |
| RES-U.8 | reseller-module-plan §U | guide-ch1-12 | shipped | typescript-pro | ticks 48/49/50 |
| RES-U.9 | reseller-module-plan §U | auto-DataRoom | shipped | fullstack-guardian | phase snapshot |
| AUD-R1 | real-world-audit §6 | taxonomy | shipped | architecture-designer | `7f499264` |
| AUD-R2 | real-world-audit §6 | 12↔8 map | shipped | typescript-pro | `c4b70877` + `a1bf4542` |
| AUD-R3 | real-world-audit §6 | data-room AU | shipped | au-compliance | `b0ae8d8b` |
| AUD-R4 | real-world-audit §6 | Tax+AU template | shipped | au-compliance | `b0ae8d8b` |
| AUD-R5 | real-world-audit §6 | showcase badges | shipped | react-expert | `b9ee0b7f` + `29c00fe1` |
| AUD-R7 | real-world-audit §6 | reseller-stage | founder_blocked | fullstack-guardian | — |
| AUD-R8 | real-world-audit §6 | onboarding step 6 | shipped | nextjs-developer | `41ab0cfc` + `b176ffea` |
| AUD-R9 | real-world-audit §6 | SCN rename | shipped | code-documenter | `9e5d71e8` |
| Q3-PDF-BRAND | feature-upgrade-roadmap-v2 | PDF branding | shipped | cpo + ui-ux-pro-max | `26678366` + `880df71c` |
| PRC-INV | pricing-upgrade-plan | per-seat SKU | open | cro | — |
| PRC-ACC | pricing-upgrade-plan | accelerator SKU | shipped | cro | placeholder Stripe IDs, iter-7 |
| PRC-EQ | pricing-upgrade-plan | equity-for-solution | blocked | clo | legal_review_passed=false |
| EXC-0001 | svi-exchange-tasks | watchlist | shipped | fullstack-guardian | v2.18 |
| EXC-0012 | svi-exchange-tasks | founder-side offer | shipped | nextjs-developer | `bfd7a5bf` |
| EXC-0013 | svi-exchange-tasks | investor EOI book | shipped | fullstack-guardian | v0.4.0 |
| EXC-0014 | svi-exchange-tasks | institutional API tier | shipped | typescript-pro | marked_deployed |
| SHOWCASE-ATL | atlassian-standard-mapping-goal | showcase-walkthrough | shipped | react-expert + nextjs-developer + test-master | fixture `aa2f8808`; shell test `5fed65f8`; guide `42bfad17`; summary + landing CTA `4bcd4c09` |
| G8-P0 | unlock-next-level-2026-07-31 | taxonomy | shipped | typescript-pro | `4f7a922f` + `6c8819f6` |
| G8-P1 | unlock-next-level-2026-07-31 | gate-engine | shipped | typescript-pro | `885acbd7` |
| G8-P2 | unlock-next-level-2026-07-31 | data-model | open | db-migrate | — (migration 0300) |
| G8-P3 | unlock-next-level-2026-07-31 | nav-hybrid | open | react-expert | — |
| G8-P4 | unlock-next-level-2026-07-31 | next-unlock UI | open | cpo | — |
| G8-P5 | unlock-next-level-2026-07-31 | shell-matrix | open | nextjs-developer | — |
| G8-P6 | unlock-next-level-2026-07-31 | chrome-backfill | open | nextjs-developer | — (~70 pages) |
| G8-P7 | unlock-next-level-2026-07-31 | CI-guard | open | test-master | — |
| G8-P8 | unlock-next-level-2026-07-31 | docs | open | code-documenter | — |
| DR-SBOM-01 | dataroom license-risk review | licence-classifier | shipped | typescript-pro | `563a3124` |
| G11-P0 | money-finder-2026-09-10 | goal-doc + seed data | shipped | senior-pm | (this commit) |
| G11-P1 (T0238) | money-finder-2026-09-10 | public-nav 5 items + CTA | open | react-expert + nextjs-developer | — |
| G11-P2 (T0239) | money-finder-2026-09-10 | migration 0308 + seed script + /admin/funding | open | db-migrate + fullstack-guardian | — |
| G11-P3 (T0240) | money-finder-2026-09-10 | grant-advisor agent + tests | open | cfo-advisor + clo-advisor + typescript-pro | — |
| G11-P4 (T0242) | money-finder-2026-09-10 | /funding landing + preview + A$3 SKU + report API + grant_finder flag | open | fullstack-guardian + stripe-saas-billing | — |
| G11-P5 (T0244) | money-finder-2026-09-10 | paid report page + PDF + dataroom save + workspace leaf | open | react-expert + nextjs-developer | — |
| G11-P6 (T0241) | money-finder-2026-09-10 | public directories /funding/grants, /funding/programs/[city] | open | seo-content-au + nextjs-developer | — |
| G11-P7 (T0243) | money-finder-2026-09-10 | refresh-funding-sources cron + fetch helper + research topics | open | cto + rnd | — |
| G11-P8 (T0249, night loop) | money-finder-2026-09-10 | pricing row, insight links, GA4 events, stripe sync drift | open | cmo + code-documenter | — |
| G11-P9 (T0245) | money-finder-2026-09-10 | funding_matches + money-radar-sweep + notification kinds + email category + ICS | open | fullstack-guardian + db-migrate | — |
| G11-P10 (T0246) | money-finder-2026-09-10 | radar drips T-30/14/3 + digest money block + events + svi_trend_alert writer | open | cto + cmo | — |
| G11-P11 (T0247) | money-finder-2026-09-10 | Founder Radar packaging in Starter + upsell card | open | cro + stripe-saas-billing | — |
| G11-P12 (T0251) | money-finder-2026-09-10 | Growth extras: investor reverse-match, per-grant drafts, quarterly refresh (v2) | open | investor-relations + cfo-advisor | — |
| G11-P13 (T0248) | money-finder-2026-09-10 | MoneyRadarTile + /workspace/funding tabs + messaging copy EN/VI | open | react-expert + cmo + conversion-optimizer | — |
| G11-P14 (T0250) | money-finder-2026-09-10 | hero one-liners (founder/investor/general, EN+VI) + 5-second test + A/B | open | cmo + cro + conversion-optimizer | — |
| G11-W0 (T0237) | money-finder-2026-09-10 §8 | loop hygiene: nextTaskId max+1, stagePlan content, TaskStatus merged, self-upgrade priority list | open | cto | — |
| G12-P1 (T0268) | evaluator-traction-2026-09-10 | Scout/Firm/Program public + limits + A$5.50 retired | open | cro + stripe-saas-billing | — (needs Stripe prices) |
| G12-P2 (T0269) | evaluator-traction-2026-09-10 | evaluator signup, card-required 7-day trial, account_type | open | cto | — |
| G12-P3 (T0270) | evaluator-traction-2026-09-10 | evaluations object + missing migrations | open | cto + db-migrate | — |
| G12-P4 (T0271) | evaluator-traction-2026-09-10 | in-workspace A$3 Trust BizReport + re-score A$1 | open | cfo + fullstack-guardian | — |
| G12-P5 (T0272) | evaluator-traction-2026-09-10 | batch scoring + sponsor/LP report | open | cpo | — |
| G12-P6 (T0273) | evaluator-traction-2026-09-10 | Evaluator Progress Radar digest (G11 signals) | open | cmo | — (after T0245) |
| G12-P7 (T0274) | evaluator-traction-2026-09-10 | /solutions/advisor + rewrites + ChatGPT comparison + messaging + GA4 | open | cmo + seo-content-au | — |
| G12-P8 (T0275) | evaluator-traction-2026-09-10 | compliance wording, disclaimers, doctoral-research sign-off | open | clo + au-compliance | — (founder wording) |
| G13-W1-IA1 | investor-clarity-2026-09-15 (PM §E S-IA1) | nav v4 + persona.ts + ~95 redirects + delete dead nav systems | shipped | cpo + nextjs-developer | `02d653c79` (live 2026-09-16) |
| G13-W1-R1 | investor-clarity-2026-09-15 (Product §F S-R1) | ReportV2 contract + dimension-owners + report-visuals + web TBR chapters | shipped | cto + dataviz | `02d653c79` (live 2026-09-16) |
| G13-W1-T1 | investor-clarity-2026-09-15 (BA E1.1–E1.3) | canonical startup taxonomy + crosswalk + `startup_taxonomy` + backfill + pipeline suggest | shipped | cdo + db-migrate | `02d653c79` (live 2026-09-16) |
| G13-W2-IA2 | investor-clarity-2026-09-15 (PM §E S-IA2) | 17 hub pages with tabs; Reports / Investors / Documents hubs | shipped | cpo + react-expert | `e58ede9d2` (live 2026-09-16 05:08) |
| G13-W2-R2 | investor-clarity-2026-09-15 (Product §B, C.2, C.6, C.10) | agent ownership matrix + knowledge injection + phase-aware selection + W4 chapters + 24 eval fixtures | shipped | cto + prompt-engineer | `2dd06a7f9` (live 2026-09-16 10:35) |
| G13-W2-D1 | investor-clarity-2026-09-15 (BA E4.1, E3.1–E3.2) | `evaluation_assessments` + Investor Dossier route + block 1 (radar, weighted table, 13 criteria) | shipped | fullstack-guardian + db-migrate | `dc6f0600e` (live 2026-09-16 11:15) |
| G13-W3-IA3 | investor-clarity-2026-09-15 (PM §B, S-IA3) | founder landing 5 blocks + single recommender; retire 9 next-step widgets | shipped | cpo + cro | `1966cc471` (live 2026-09-16 12:57) |
| G13-W3-R3 | investor-clarity-2026-09-15 (Product §C.1, C.3, C.5) | pipeline unification + GATHER un-stub + 5-method valuation in body | shipped | cto + cfo | `63035e3b5` (live 2026-09-16 13:25) |
| G13-W3-T2 | investor-clarity-2026-09-15 (BA E2.1–E2.5) | investor mandate form + FIT_WEIGHTS_V2 + `mandate-fit-refresh` cron (fixes deal-flow join) + filters/saved views | shipped | investor-relations + db-migrate | `083dfd763` (live 2026-09-16 13:25) |
| G13-W4-IA4 | investor-clarity-2026-09-15 (PM §C, B.3) | persona-aware investor/advisor/accelerator landing + single onboarding wizard | shipped | cpo + customer-success | `d03f0dd05` (live 2026-09-16 16:29) |
| G13-W4-R4 | investor-clarity-2026-09-15 (Product §F S-R4) | PDF/DOCX/email parity from ReportV2 + Dossier consumes ReportV2 | shipped | cto + code-documenter | `ed1682e77` (live 2026-09-16 16:29) |
| G13-W4-D2 | investor-clarity-2026-09-15 (BA E4.2–E4.4, E1.4–E1.5) | assessment form + share allow-list + history; founder taxonomy confirmation; legacy writers via crosswalk | shipped | fullstack-guardian + au-compliance | `46185f67f` (live 2026-09-16 16:29) |
| G13-W5-IA5 | investor-clarity-2026-09-15 (PM §E S-IA5) | one header/footer + shared user menu + `/investors` → `/about/invest` | shipped | cmo + seo-content-au | `234cac02f` (live 2026-09-16 18:54) |
| G13-W5-R5 | investor-clarity-2026-09-15 (Product §C.7, S-R5) | comparables table + ingest, LinkedIn/GA4/cap-table connectors, KPI tile, eval harness | shipped | cfo + cdo | `e15bd080b` (live 2026-09-16 20:39) |
| G13-W5-D3 | investor-clarity-2026-09-15 (BA E4.5–E4.7, E3.3–E3.7, E2.6, E5) | seats/consensus, IC memo PDF, cohort decisions + LP counts, watchlist project_id, portfolio write, intro CRM, qa:live lane, docs close-out | shipped | investor-relations + qa-lead | `b2ba43fea` (live 2026-09-16 20:04) |
| G14-S33 | g14-investor-feedback-2026-09-16 (§4 S33, Wave A) | traction snapshot + `/api/status.traction` + admin tile + investor-update script + 6 server-side money events (GA4 truth; `assessment_submitted` waits for S-D2) | live 2026-09-16 | coo + cfo-advisor | — |
| G14-S34 | g14-investor-feedback-2026-09-16 (§4 S34, Wave C) | founder feedback letter "What investors said" (k ≥ 3, ≥ 2 orgs) + cron + email + landing block + opt-out | live 2026-09-17 | customer-success + cpo | — (0401; after G13 S-D2) |
| G14-S35 | g14-investor-feedback-2026-09-16 (§4 S35, Wave B) | program intake link `/apply/[slug]` + scored inbox + `intake.manage` flag + `intake.submission_received` webhook | live 2026-09-17 | cpo + fullstack-guardian + ciso | — (0402) |
| G14-S36 | g14-investor-feedback-2026-09-16 (§4 S36, Wave B) | confidence cap by origin (replaces keyword ladder + upload trust hole) + L0–L5 multiplier + reviewer queue + public `/methodology` + Verified ABN badge | live 2026-09-17 | ciso + svi-scoring | — (0403) |
| G14-S37 | g14-investor-feedback-2026-09-16 (§4 S37, Wave C) | founder execution profile (0064 ALTER) + rubric → FTV override + profile form + LinkedIn PDF import + FTV chapter card | live 2026-09-17 | chro-advisor + svi-scoring | — (0408) |
| G14-S38 | g14-investor-feedback-2026-09-16 (§4 S38, Wave C) | evaluator API v1 (`/api/v1/evaluations*`, scopes) + OpenAPI + Slack / Affinity / Airtable webhook destinations | live 2026-09-17 | dev-relations + api-designer | — (0405; after S-D2 + S-R4) |
| G14-S39 | g14-investor-feedback-2026-09-16 (§4 S39, Wave D) | backtest v0 (72 comparables, Spearman ρ within stage, bootstrap CI) → `/methodology/calibration` + `/api/status.svi_backtest` | live 2026-09-17 | cdo + cfo-advisor | — (after G13 S-R5) |
| G14-S40 | g14-investor-feedback-2026-09-16 (§4 S40, Wave D) | open AU external signals (ABR bulk, business.gov.au grants, R&DTI transparency) → evidence rows + real cohort percentile + admin + cron | live 2026-09-17 | cdo + database-optimizer | — (0410; cron installed) |


### G19 — Report Quality: score ledger · honest valuation · unassessed ≠ scored · one synthesis · paid view = ReportV2 · BlockID showcase
- **Source:** [`docs/plans/g19-report-quality-2026-09-20.md`](./g19-report-quality-2026-09-20.md) + audits [`01-audit-pipeline.md`](./g19-report-quality-2026-09-20/01-audit-pipeline.md), [`02-audit-ux.md`](./g19-report-quality-2026-09-20/02-audit-ux.md).
- **Opened:** 2026-09-20 — founder brief: reasons behind every score, standard valuation, ask for data where nothing can be assessed, synthesis on the dashboard, harmonious layout, apply to blockid.au.
- **Status:** **batch 1 LIVE 2026-09-20** (S41 score ledger + S42 valuation truth shipped in release `32ea5b05e`; `qa:live` 132/0; ledger visible on `/tbr/demo`); **batch 2 LIVE 2026-09-20 09:00** (S43 evidence & CTAs + S45 paid view/i18n/survey, release `Axp15hEhaiTKaArhuuH1c`, 12/12; `qa:live` 138/4 — the 4 = S44 cover test pre-deploy + 3 in G20-F2's new sweep/purchase/Xero lanes: pre-existing `/workspace/projects/compare` React #441 boundary, h1/CSP/overflow defects on old workspace pages, Stripe card iframe count 2, Xero label — handed to G20, not G19); **S44 LIVE 09:50** (release of `aff82c90a`, `qa:live` 140/2 — both remaining fails are G20 lanes); S46 (BlockID showcase + quality telemetry) built, awaiting the batch-3 merge/deploy; batch 2 (S43 evidence & CTAs ∥ S45 paid view + i18n + clarity survey); batch 3 (S44 synthesis + one phase rule + layout ∥ S46 BlockID showcase + quality telemetry). One deploy per batch, serialized behind any peer session. **S46 built 2026-09-20 (worktree, awaiting the batch-3 merge/deploy):** quality telemetry — every `run-for-project` pipeline run appends one row to `web/content/reports/tbr-quality.jsonl` (`ts, projectId hash, snapshotId, tier, calls, costUsd, groundedShare, degradedSections, consistencyIssues, pendingDims, words, pages, durationMs, sviVersion, pipelineVersion`; a fully-degraded run logs too), `/api/status.tbr_quality` (24 h runs / grounded median / cost median / degraded share → `ok | watch | missing`, watch = grounded < 0.85 or > 20 % degraded) + the admin "Pipeline (24 h)" KPI cell; prompt-eval fixtures `TBR-valuation-inputs-v2.1.0` (pre-revenue must say pre-revenue/Berkus, invented ARR hard-fails; Stripe case names the source) + a CGH case on the S41 ledger fixture; `scripts/run-self-analysis.mjs --report` (tsx-loaded `runTrustReportForProject`, standard tier, seeds the 13 criteria + honest pre-revenue raw input from the public facts when empty; weekly cron Mon 04:00 UTC) on the canonical project `2bf55234…` "Blockid.au 1"; public showcase **`/showcase/blockid/report`** (stored `report_v2` only, 1 h cache, "not published yet" empty state, no survey) + "Read our own report" card on `/showcase/blockid`; live-qa sweep page + `21-reports` assertion (≥ 8 visuals when published). Founder inputs seeded 2026-09-20: 13 `evaluation_criteria` rows + a fresh `svi_analyses` row (SVI 135, stage 3) — ABR verification skipped (`ABR_GUID` unset → cover keeps "ABN not verified"), GitHub connector skipped (no token). **The real run exposed five pipeline defects (all fixed in S46, all affecting every paid standard report):** (1) the structured W1–W3 system prompt carried the legacy markdown "## Output Format" while the user turn asked for JSON → DeepSeek answered in markdown, the repair pass overran the 1,275-token standard budget and every criterion cost 3 calls, unvalidated, confidence halved (now: JSON contract in the OUTPUT_SCHEMA slot + hard 400–700-word cap + ≥ 2,600 output tokens → 1 call, schema-validated); (2) pipeline calls sat behind ai-client's per-user fairness cap (2 in flight / 45 s queue) → calls 3–6 of every wave failed "AI capacity busy" (now: no `userId` on pipeline calls; the per-report agentId semaphore + call cap guard); (3) W4 owner calls had 900 output tokens → every full chapter cut mid-JSON → 8 deterministic cards (now 3,200 / 700); (4) `DimensionChapterPayload.phase_lens.floor` rejected the null the owners echo (now nullable); (5) **`assembled_reports.analysis_id` was `uuid` while `svi_analyses.id` is a text slug → every assembled_reports insert had failed (22P02) since slug ids — the table held 0 rows, `report_json` never written** → migration **0415** (applied + ledger). Remaining: at the interactive standard cap (30 calls / 120 s) a run still needs ~24 calls and ~6 min at DeepSeek-V4-Flash latency (W1–W3 ≈ 3.5 min, W4 ≈ 1.5 min) — the offline weekly run widens to 48 calls / 8 min via `REPORT_CALL_MAX_STANDARD` / `REPORT_DEADLINE_MS_STANDARD`; the interactive path needs either a faster model class or a longer wall clock (P1, owner CTO); groundedShare on our own run is 0.36 (KPI ≥ 0.85) — the auditor revises 8 sections but the owners cite few `[ev:]` ids (P1, S43 owner). BlockID report persisted: snapshot `ba680de6-f1db-4027-b37c-1003c47d97cb` (report `rpt-mu9nxds7-vk0xxd`, 48 calls, US$0.033, 7/8 chapters live, valuation pre-revenue A$3.0–6.6M consensus at 0.35 confidence, 7,335 words). **S46 LIVE 11:04** (release of `c6a00850b`): BlockID's own report at `/showcase/blockid/report` (snapshot `ba680de6`, 48 calls, US$0.032, pending dims 0, valuation A$3.0–6.6M @ 35 % pre-revenue), `tbr-quality.jsonl` + `/api/status.tbr_quality` (watch until prompt owners lift groundedShare 0.41 → 0.85); the real run exposed and fixed 4 production pipeline bugs (structured prompt markdown-vs-JSON, per-user fairness cap on fan-out, W4 900-token cut, `assembled_reports.analysis_id` uuid → 0 rows persisted since slug ids; 0415 applied). Follow-up `d9b25c6fc`: paid orders run with 48 calls / 7 min in the background drain (the interactive 30/120 s budget had degraded every paid chapter). **G19 ENGINEERING CLOSED 2026-09-20** — final `qa:live` 143/1 (page-sweep = G20). Open P1s for prompt owners: groundedShare (few `[ev:]` citations; CMO criteria overrun tokens; W4 verdict > 80 words fails the chapter instead of trimming); `ai_runs` FK on `prompt_version_id`.
- **Findings (verified):** TBR shows no signal→points reasons (only `/workspace/score` does); pre-revenue valuation = Berkus × {1,1.5,1.1,1.2} pseudo-methods, growth default 10 %/mo hidden, confidence floor 45 %, raise invented; dimensions never "unassessed" (baselines 50/40/30 shown as scores); four "+N" lift models; pipeline never reads the Evidence Hub (`svi_dimension_evidence`); Money on the Table always A$0; three phase rules; **paid A$3 lands on a legacy markdown page, not ReportV2**; VI = ASCII-stripped headings over English internals; no clarity survey.
- **Decisions:** D1 ledger explains, formula unchanged (pending band for unassessed) · D2 one lift model = `EVIDENCE_CATALOG` · D3 pre-revenue = Berkus 0.5 + scorecard 0.3 + stage baseline 0.2 · D4 paid product = ReportV2 page · D5 `inferPhase` everywhere · D6 clarity survey 0–10, KPI ≥ 8.5.
- **KPIs:** clarity median ≥ 8.5 (N ≥ 30) · report_view → share ≥ 25 % · score_done → A$3 ≥ 12 % · groundedShare ≥ 0.85 (S46: `tbr-quality.jsonl` median, `/api/status.tbr_quality`) · pending-dimension share ≤ 20 % (S46: `pendingDims` per row) · showcase ≤ 1,300 words (`/showcase/blockid/report`).
- **Next action:** prompt-owner sprint on groundedShare (S47 candidate); watch `tbr_quality` + clarity-survey median for 30 days; cosmetic: TRE sparkline label overlap on the showcase.

---

## 3. Shipped Log (last 24h)

**Total commits in window:** 294 (154 substantive `feat/fix/docs`; 140 `chore(loop)` autonomous ticks). Sample below capped at 40.

### Reseller module (P10 pin-hardening — 74 substantive commits)
- Admin/reseller list + detail wire-shape pins (ticks 219–290): `commission_share_pct`, `gst_registered`, `allowed_tiers`, `monthly_credit_budget`, `monthly_sandbox_credits`, `created_at`, `updated_at`, `decision_at`, `decision_reason`, `decision_by`, `requested_by`, `linked_credit_transaction_id`, `reseller_id`, `status`, `request_type`, `payload`, `resellers(code,display_name)` embed, `progression[0]`, `masked_email`, `promo_code`, `reseller.code`.
- Loop-status observability schema pins (ticks 235–258): `human_review_minutes_7d`, `last_log`, `phase_dispatched`, `delegated_dispatch`, `auto_deploy_{triggered,skipped,finished,failed}`, `auto_commit_{started,finished,failed}`, `phase_failed`, `human_blocked_snapshot`, `cron_removal`, `goal_completed`, `frontier_computed`, `tick_start_end`, `idle`, `error`.
- Wave-3 credit-grant chain: rows 152 → 156c four-chain HTTP + DB companion (ticks 200–213).
- Wave-5 audit-log strict-equality tightening (ticks 214–217).
- Fix: split `node:crypto` out of `attribution.ts` (client bundle) — `7eca0c95`.
- Fix: remove `/admin/resellers/[slug]` route (conflicts with `[code]`) — `d19c777e`.
- Advisory: Customer-Success VI translation for Grant modal + Customer drawer — `6cf9400f`.
- P12.9 Playwright E2E for user management — `3b676853`.

### Security / compliance
- CISO D3-CISO-06 — block Stripe customer portal for wholesale-provisioned founders (`fd4a1eb3`).

### ESOP / HR
- CHRO Div 83A qualifying-tests checklist in API + knowledge base (`6213d649`).

### SVI Exchange
- T_SVI_EXC_0012 v0.5 founder-side secondary offer intake page (`bfd7a5bf`).

### Content / SEO
- Auto SEO article publish via cron (`db66d18b`).

### Docs / audit
- Real-world workflow parity audit (`442d5fba`).
- CRO Share-Mgmt remove-path finding marked resolved at tick 56 (`95d71d9c`).

### Audit remediation (iteration 5-9)
- AUD-R1 canonical 8-stage vocabulary published (`7f499264`).
- AUD-R2 12-phase ↔ 8-stage bucket map (`c4b70877`); startup-growth-phases bridged (`a1bf4542`).
- AUD-R3 + AUD-R4 data-room expanded to 60+ items with Tax + AU-Compliance sections (`b0ae8d8b`).
- AUD-R5 canonical 8-stage badges on Atlassian/Canva/Xero/SafetyCulture showcase timelines (`b9ee0b7f`); parity on founder dashboard + SCN payloads (`29c00fe1`).
- AUD-R8 Step 6 "Create first startup" onboarding wizard (`41ab0cfc`); Stripe-hosted checkout return route fix (`b176ffea`).
- AUD-R9 SCN renamed externally to "Startup Compass"; Sean Ellis/T2D3/Porter/JTBD/BVP framework overlays cited (`9e5d71e8`).

### Product / branding
- PDF branding feature-gate + settings form for Growth+/Scale/Enterprise (`26678366`); brand_settings threaded into SVI report renderer (`880df71c`).
- Showcase OG + Twitter card images for /showcase/blockid (`05d30fe2`).

### QA / infra
- `@axe-core/playwright` installed; a11y CI lens unlocked (`f9c04c7c`).

### Orchestrator
- Platform auto-upgrade 2026-07-23 (`6b81bf40`).
- Reports-snapshot before parallel-agent burst (`f61540fa`).

### Atlassian demo walkthrough (SHOWCASE-ATL — SHIPPED)
- **Status:** SHIPPED. Reference: [`docs/plans/atlassian-standard-mapping-goal.md`](./atlassian-standard-mapping-goal.md), user-facing guide [`docs/demos/atlassian.md`](../demos/atlassian.md).
- **Scope:** 9-step anonymous visitor walkthrough of Atlassian's 2002–2026 journey through every BlockID surface (landing → dashboard → SVI report → 12-phase map → 7 C-Level agents → data-room → valuation → 12-chapter guide → wrap-up); typed fixture (20 milestones / 12 phase snapshots / 13 SVI scores / 7 agent reports / 65 data-room rows / 16 valuation snapshots); walkthrough shell + provider; E2E smoke covering the 9-step visitor journey.
- **Commits (Round 1a/1b/1c/2):** fixture extraction `aa2f8808`; walkthrough shell navigation test `5fed65f8`; goal doc `72d461a4`; guide mirror page (step 8) `42bfad17`; summary mirror (step 9) + landing CTA `4bcd4c09`; E2E smoke `pending-commit`.
- **Links:** `/showcase/atlassian?step=1` (landing) · `?step=2` (dashboard) · `?step=3` (SVI report) · `?step=4` (growth-phases) · `?step=5` (agents/ceo) · `?step=6` (data-room) · `?step=7` (valuation) · `?step=8` (guide) · `?step=9` (summary).
- **Follow-ups (open in `atlassian-standard-mapping-goal.md`):** P3-P7 nudge engine wiring, data-room auto-populate from public disclosure feed, Xero/Canva/SafetyCulture parallel walkthroughs.

---

## 4. Skills → Areas Map

| Skill | Owns |
|---|---|
| `typescript-pro` | shared libs (`web/src/lib/**`), types, pure helpers (commission, cogs, hash, scope) |
| `react-expert` | workspace/dashboard/drawer components, `share-mgmt-drawer`, sidebar, radar/line charts |
| `nextjs-developer` | routes, RSC, `generateMetadata`, App-Router segments, redirects |
| `secure-code-guardian` | auth, hashing, RLS, feature-gate manifest, Stripe-portal gate, CI R-01..R-09 |
| `au-compliance` | APP 5 notice, ATO/GST wording, ESIC/AFSL/AsIC disclaimers, Div 83A |
| `test-master` | vitest suites, Playwright specs, wire-shape pin patterns |
| `database-optimizer` / `db-migrate` | Supabase migrations (0091-0097), indexes, NOTIFY pgrst reload |
| `fullstack-guardian` | end-to-end features (data-room UI ↔ API ↔ DB), console + drawer + API + DB together |
| `code-documenter` | JSDoc, SCN rename, framework-overlay citations |
| `deep-research` | showcase RESEARCH.md authoring (Airwallex, Culture Amp), competitor rows |
| `architecture-designer` | canonical taxonomy publication, 12↔8 bucket-map spec, ADRs |
| `blockchain-expert` | Anvil chainId 420, Otterscan, SVToken, equity-for-solution contracts |
| `media-studio` | pitch videos, ProductHunt assets, thumbnails |
| `investor-relations` | pitch deck slides, data-room one-pagers, GTM memos |
| `customer-success` | onboarding wizard, VI translations, welcome emails |
| `senior-pm` | portfolio health, WSJF prioritisation on this SOT |
| `qa-lead` | release readiness, go-live checklist |
| **Pairs** | `react-expert` + `au-compliance` for consent modals; `db-migrate` + `secure-code-guardian` for RLS-heavy migrations |

---

## 5. Human-blocked Queue

| Item | What's blocked | What's needed | Ping |
|---|---|---|---|
| InfoVision seed (P1.5) | Reseller module row-1 insert | Auschain's InfoVision ABN + GST status (H.20) | Founder → LegalVision AU or existing counsel |
| Stripe env vars (P8.5) | Share-Management add-on Playwright green | Mint `STRIPE_PRICE_ADDON_SHARE_MGMT_MONTHLY` + `STRIPE_PRICE_ADDON_SHARE_MGMT_ANNUAL` in Stripe dashboard | Founder (Stripe account owner) |
| Stripe dashboard owner email | U.1 gap — record `stripe.account_owner_email` | Confirm at `dashboard.stripe.com` (`info@` vs `admin@blockid.au`) + verify ABN + `statement_descriptor` + payout bank | Founder |
| Reseller agreement executed (D4-CLO-02) | P3 go-live formality | Sign InfoVision-Auschain deed (template `docs/legal/reseller-agreement-template.md`) | Founder + human counsel |
| Equity-for-solution (PRC-EQ) | Pricing plan Phase 3 lane | `legal_review_passed=true` flag flip on jurisdiction gate | Founder + CLO agent + human counsel |
| Audit remediation #10 (#3/#6/#7 shipped) | Real-world audit ship-list | Real founder quotes for the 12 guide-chapter callouts — never invented | Founder |
| Founder tier labels (audit #1) | Canonical 8-stage vocab publication | Founder to approve final EN+VI labels for 8 stages | Founder |
| Stripe price `STRIPE_PRICE_FUNDING_REPORT` (G11 T0242) | A$3 Money Finder guest checkout | Mint one-off A$3.00 inc-GST price in Stripe dashboard; add env | Founder (Stripe owner) |
| Starter public label (G11 Q1, T0247) | Founder Radar packaging copy on `/pricing` | Decide "Founder Radar" rename vs "Starter + Radar badge" | Founder |
| Free-tier in-app deadline alerts (G11 Q2, T0245) | Radar sweep fan-out rules | Yes/no for A$3 buyers (in-app only, no email) | Founder |
| GA4 `hero_variant` custom dimension (G11 T0250) | Hero one-liner A/B measurement | Create dimension in GA4 property | Founder (GA4 admin) |
| `ABR_GUID` env present? (G11 T0244) | ABN lookup for `project_grant_profiles` | Confirm ABR web-services GUID in `web/.env` | Founder |
| Legal entity split (resolved 2026-09-10) | Footers/marketing vs billing/legal | **Rule (deliberate, in code comments `logo-band.tsx`, `(marketing)/page.tsx`): marketing surfaces = PPL Food PTY LTD; billing, legal disclaimers, tax invoices, consent notices, JSON-LD = Auschain PTY LTD (ACN 659 615 111 · ABN 79 659 615 111).** Founder chose PPL Food for public copy and no ABN there; tax invoices legally need the ABN, so the billing entity stays Auschain until founder + CLO decide otherwise. | Founder + CLO (only if invoices should change) |
| Stripe prices for evaluator rungs (G12 T0268) | Scout/Firm/Program self-serve checkout | **DONE 2026-09-10 13:25 UTC (founder approved):** prices already existed in Stripe live (`price_1Ttznb…` Scout, `price_1Ttznc…` Firm, `price_1Ttznd…` Program + annual) → synced into `plans.stripe_price_id` via `seed-stripe.mjs`; new one-off prices minted: Trust BizReport A$3 `price_1UE7mF…` (env `STRIPE_PRICE_TRUST_REPORT_5AUD`, id kept per D3) and Money Finder A$3 `price_1UE7mG…` (env `STRIPE_PRICE_FUNDING_REPORT`); env written to `web/.env` + `web/.env.runtime` (backups `.bak-2026-09-10-s3`), live from the S3 deploy | ✅ |
| Doctoral-research sentence (G12 D4, T0275) | Positioning claim #6 on /solutions/* and comparison page | **APPROVED by founder 2026-09-10:** "grounded in the founder's doctoral research (DBA) on startup valuation"; never "PhD" | Founder ✅ |
| Provider data-handling sentence (G12 T0275) | "confidential" claim in the ChatGPT comparison | **APPROVED by founder 2026-09-10 (data principle):** "Your data belongs to your startup. We store it so every report builds on your own evidence and the AI reasons on your case. Founder-consented access tiers control who sees what." T0275 publishes it on /solutions/*, comparison page and privacy policy, and verifies each provider's policy in the free-model chain so the claim holds end-to-end. | Founder ✅ (CISO verification at T0275) |
| ~~Stripe webhook: enable `customer.subscription.created` (G14-S33)~~ DONE 2026-09-17 | `subscription_created` event + `/api/health/stripe` `missing_events` | Stripe dashboard → webhook endpoint → add event | Founder (Stripe owner) |
| ~~Stripe prices for pricing v4 (G14, Wave A)~~ DONE 2026-09-17 | Fund / Intake link / Index API self-serve checkout; `svi-api/checkout` 503 | Mint 6 prices (`STRIPE_PRICE_INVESTOR_FUND`, `_ANNUAL`; `STRIPE_PRICE_ACCEL_INTAKE`, `_ANNUAL`; `STRIPE_PRICE_INDEX_API`, `_ANNUAL`) via `node scripts/seed-stripe.mjs --test-mode --dry-run --skus=investor_fund,accelerator_intake,index_api` → test → live; env into `.env` + `.env.runtime`; confirm `STRIPE_PRICE_ACCEL_STARTER(_ANNUAL)` exist (G12 §9.6 unticked) | Founder (Stripe owner) |
| ~~Migration 0400 `sync_plan_rows_v4` (G14)~~ applied 2026-09-16 | DB `plans` rows for Fund / Intake / Index API + Cohort 25/100 public flags | `scripts/db/apply-migration.sh 0400` + `migration-status.mjs --write` after the pricing v4 merge | Founder / ops (hand-applied per house rule) |
| **Telegram bot token (G15, 2026-09-18)** | ALL ops alerts (cron failures, backups, uptime guardian, error digest) — the old token was revoked, no replacement installed; alerts now fall back to e-mail (ADMIN_EMAIL, ≤ 30/h) | Founder: BotFather `/newbot` or `/token` → put `TELEGRAM_BOT_TOKEN` in `web/.env` + `web/.env.runtime` → `bash web/scripts/cron-runner.sh agent-guardian` proves a post | Founder (bot owner) |
| Deck v3 S9 pilot / LOI names (G14 F-9) | "Live product, real depth, first pilots" slide | Written consent from each named program / LOI signatory; `[[LOI placeholder]]` until then | Founder |
| G14 founder decisions F-1…F-9 | Use of funds 50/28/22 · pre-money A$2.5–4.0M / SAFE cap A$3.5M · `/methodology` weights hidden · intake `auto_report` off · feedback letter k=3/≥2 orgs ratings-only · verification multiplier + legacy TPV downgrade · `api.access` Fund+Program only · publish backtest v0 even if ρ < 0.3 · S9 names only with consent | Confirm or override before the sprint that needs each (goal doc §3); defaults ship if silent | Founder |

---

- **H.G13-F1** Unify public name "Trust BizReport" → "Trusted Business Report" (SKU key unchanged) — default yes at S-R1.
- **H.G13-F2** `/investors` (invest-in-BlockID pitch) → `/about/invest` 301 — CMO sign-off before S-IA5.
- **H.G13-F3** Show dimension weights to authenticated evaluators on the Investor Dossier (never on public `/tbr`) — svi-scoring guardrail exception; confirm before S-D1.
- **H.G13-F4** CEO + CFO chapters on paid Sonnet-class model inside A$3 COGS (`MODEL_AGENT_CEO/CFO`) — default yes at S-R2.
- **H.G13-F5** "500+ AU comparables" copy → live count until the table reaches 500 — default yes at S-R1.
- **H.G13-F6** Final EN/VI sidebar label set (PM §D.2, 40 rows) — default ship as proposed at S-IA1.
- **H.G14-F1…F9** see the G14 row above and goal doc §3 — F-1/F-2 before deck v3 is sent, F-3/F-6 before S36, F-4 before S35, F-5 before S34, F-7 before S38, F-8 before S39, F-9 before S9 names.

## 6. Sync-back rules

When you (loop agent) ship a task, you MUST — in the same commit or the immediately-following one:

1. **Update this doc's §3 shipped-log** with the commit SHA + area.
2. **Update the specific plan file's status** (e.g. flip a `- [ ]` to `- [x]`, bump `tick:`, update `current_focus:`, close a requirement row in §2 with the ship commit).
3. **If the task maps to a public surface** (roadmap page `/roadmap`, team page `/team`, status page `/status`, stats page `/stats`, pricing page `/pricing`, showcase page `/showcase/*`) — leave a marker in §7 for the next `update-status`/`update-roadmap` agent tick to sync.
4. **If the task closes a phase (Pn → done)** — spawn the `senior-pm` skill or leave a `[MARKER: phase-close]` in §7 so the next tick reviews cross-surface impact.
5. **If the task changes pricing or SKU shape** — update `web/src/config/pricing/plans.csv` AND `docs/pricing-upgrade-plan-2026-07-16.md` AND this doc's §2 in the same commit.
6. **If the task changes a public copy string** — ship EN + VI parity in one commit (never English-only).
7. **If the task is a security fix** — add a `- [ ]` line under a new §8 that this doc auto-appends on next SOT-refresh tick.
8. **Never delete rows from §2 or §3.** Requirements move to `shipped`; shipped-log rows stay.

---

## 7. Cross-repo sync markers

Live markers for downstream update agents. Consume by grepping this section for `[MARKER: …]`.

- `[MARKER: update /roadmap when a P{n} phase closes]` — reseller P8.5, audit R1-R10 land, pricing Investor SKUs land.
- `[MARKER: update /team when a C-Level agent ships a milestone]` — CHRO Div 83A, CISO D3-CISO-06 already shipped in window.
- `[MARKER: update /status when human-blocked item unblocks]` — H.20 InfoVision ABN, STRIPE_PRICE_ADDON_* env.
- `[MARKER: update /stats when a shipped-log entry crosses a bucket boundary]` — reseller tick crosses 300, roadmap Q3 closes >80%.
- `[MARKER: update /pricing when plans.csv changes]` — Investor SKUs (Angel/Advisor/VC), Accelerator SKUs.
- `[MARKER: update /showcase when a new case ships]` — Airwallex + Culture Amp shipped S19-B (audit #6); next candidates WiseTech, SEEK.
- `[MARKER: update /guide when a chapter callout ships]` — audit #10, 12-chapter arc.
- `[MARKER: update-status agent should consume §3 shipped-log commit count 294 for this cycle]`.
- `[MARKER: update-roadmap agent should consume §1 Top-5 open Q3 items list].`
- `[MARKER: update /roadmap when T0238 or T0239 closes]` (G11 Wave 1) — Money Finder lanes; `feature-upgrade-roadmap-v2.md` Q4 checklist + `ROADMAP.md` §4 rows mirror SOT §2 G11-P0..P13.
- `[MARKER: update /pricing when T0268 (G12 Evaluator tab) or G11-P11 ships]` — Starter gains Founder Radar (`money_radar` flag), A$3 `FUNDING_REPORT_3AUD` SKU; also touch `docs/pricing-upgrade-plan-2026-07-16.md` (amendment note already present).
- `[MARKER: phase-close]` — G11-P0 (plan-only) closed 2026-09-10.
- `[MARKER: update /pricing when 0400 + the 6 G14 Stripe prices land]` — Fund A$999 / Intake link A$249 / Index API A$299 / Cohort 25 A$500 / Cohort 100 A$1,500 public; `/pricing?persona=investor` + `?segment=programs` tabs; mirror `/vi/pricing`.
- `[MARKER: security-fix]` — `fd4a1eb3` (Stripe portal wholesale gate).

---

## 8. Shipped (last 30 days)

<!-- shipped:begin -->
_Auto-regenerated by `scripts/docs/regenerate-team-page.mjs` +
`scripts/docs/regenerate-changelog.mjs`. Hand edits **above** the
`<!-- shipped:begin -->` sentinel survive re-runs; anything between
the sentinels is overwritten._

**Reseller / wholesale (Phase 2.7)** — P11.40 `895969c`, P11.41 `4459a6e`, P11.42 `8c32ed6`, P11.43 `e299f24`, P11.44 `4003c39`, P11.45 `1d4da77`. Back-link: [reseller-module-plan.md](./reseller-module-plan.md).

**Compliance (Phase 2.8)** — P1n-gst-form `c832dd0`, P1n-s708-form `334b716`, P10-s708counter `0110952`, P10-s708counter-adapter `6d758a3`, P1n-esic-route `aa11769`. Back-link: [real-world-workflow-parity-audit-2026-07-23.md](./real-world-workflow-parity-audit-2026-07-23.md).

**Atlassian-goal / exit-readiness (Phase 2.9)** — P10 s708 counter adapter `6d758a3`, P11-acquisition-pattern `875e6a0`, P12a AU exits `adf61d9`, P12b investor pack `5d4af69`, P12b-cfo valuation `37613d1`, P12b-tile `/dashboard/exit-readiness` `87a1441`. Back-link: [atlassian-standard-mapping-goal.md](./atlassian-standard-mapping-goal.md).

**Real-world workflow parity (Phase 2.10)** — items 1-5, 8, 9 shipped in-window; items 6, 7, 10 remain founder-review. Back-link: [real-world-workflow-parity-audit-2026-07-23.md](./real-world-workflow-parity-audit-2026-07-23.md).

**Security** — `fd4a1eb3` (Stripe portal wholesale gate — CISO D3-CISO-06).

**Deploy manifest** — `web/.deploy-manifest.json` @ v2.0.0-beta.10 (git_sha `6d034c4b`, deployed 2026-07-24T18:21:49Z).
<!-- shipped:end -->

---

## Appendix A. Change log for this file

| When | Who | What |
|---|---|---|
| 2026-09-20 | Claude (G20 close) | v3.17.0 live (12/12, live-qa 210/1 → 0): feature inventory + 19 hidden features with clean cards, signed-in page sweep (35 defects fixed incl. streamed-redirect CSP errors, compare page crash, empty cohort page), purchase-path lane, workspace copy; review fixes; OAuth 503 pages → redirects. Product is sale-ready on the sold ladder; remaining items are founder-only Stripe/Telegram/Drive actions. |
| 2026-09-20 | Claude (G18 ship + G20 open) | G18 v3.16.0 live (`18c1c151e`, 12/12): pricing parity (SVI Quick Report had booked A$25 since 08-01; reseller Growth A$99 → A$69; change-plan for v2 ids), GST rule, trial parity, self-serve cancel + Billing Portal config created in Stripe (account had none), v3.16.0 docs/version, one message + forbidden-phrase guard; review fixes deploying. G20 Ready-for-sale opened (peer owns G19 Report Quality). |
| 2026-09-19 | Claude (G18 open) | Truth sweep opened: Stripe read-only audit committed; three lanes (pricing truth / version + docs / messaging) launched; SOT header refreshed (state, entity split, pricing v4). |
| 2026-09-19 | Claude (G17 close) | G17 live in three deploys (P1 `fc155500c`, P2 `55c71d258`, review `97a2ad910`, footer `5a0c3dbb1`): unicorn homepage (evaluator-first, no prices), one template on every marketing page + /vi, link checker in gate 8 + daily cron (0 broken on 525 pages), homepage smoke in gate 12; live-qa 191/0; screenshots in docs/design. |
| 2026-09-19 | Claude (G16 close) | G16 live (peer deploy `8cbb6bdb8` + review fixes `58406efa2`, live-qa 184/0): funnel events + `/admin/funnel`, locked TBR preview → A$3 quote-then-pay (the checkout had been unreachable), pilot comp + `/pilot`; review P1s fixed; pilot start→end verified on production. |
| 2026-09-19 | Claude (G17 open) | G17 Unicorn homepage & site template opened (`unicorn-homepage-2026-09-19.md`, ui-ux-pro-max research); Phase 1 agent launched. G16 lanes merged + live via the peer deploy `8cbb6bdb8` (12/12); 0411 applied; crons installed; first funnel report written; throw-away pilot start→end verified on production. |
| 2026-09-19 | Claude (G16 open) | G16 First dollar opened (`first-dollar-2026-09-19.md`); three worktree lanes A funnel truth / B paywall → A$3 / C pilot enablement launched. |
| 2026-09-18 | Claude (G15 follow-ups) | Follow-ups live `bac472af4` (12/12, live-qa 178/0): guardian uptime, watchdog restart forensics → found the watchdog was the restart-burst cause (kills a healthy server under build load) and fixed it, build_sha manifest, cron-log rotation, status root fix. Repo + system cleaned (10 merged branches, 95 stale session dirs, docker cache 214 MB, gc). Full plan review: all G1–G15 closed or founder-blocked. |
| 2026-09-18 | Claude (G15 close) | G15 Reliability live (`3ce353da8` → review fixes `7e86fb700`, 12/12, elevated live-qa 177/0): manifest truth + live-SHA gate, lock etiquette, error digest + latency SLO + `/api/status` v2, weekly restore drill (first run ok), AI health snapshot, stray-process sweep; nginx timing log + 3 crons installed; crontab drift reconciled. Findings: Telegram token 401 (e-mail fallback shipped; founder must mint a token), 2026-09-17 08:02 4× watchdog restarts uncaptured. |
| 2026-09-18 | Claude (G15 open) | Founder: focus reliability → G15 opened (`reliability-2026-09-18.md`), three worktree lanes R1 ship safety / R2 observability / R3 data safety + AI resilience launched; dead gateway/billing env pointers commented out; stray 5.7-day crawler killed. |
| 2026-09-18 | Claude (plan review) | Post-G14 review: all engineering goals G1–G14 closed or founder-blocked; G4 ranked backlog verified shipped in code, G8 closed (`/docs/unlocks` live); ROADMAP §4 legacy rows reconciled (ESOP UI, feedback, nurture D1/D4/D9 shipped). Live: 100 % uptime 24 h, 12/12, elevated live-qa 177/0. Traction unchanged: 66 users · 0 MRR · 0 trials — next goal must be conversion/GTM enablement, not features. |
| 2026-09-17 | Claude (G14 review) | Review deploy `036ba5c2e`: callAI wall-clock budget (interactive max(60 s, timeoutMs) across provider + model ladders), funding narrative 55 s deadline — fixes the Cloudflare 524 on POST /api/funding/report; S38 live via the peer deploy; elevated live-qa 177/0 — G14 fully live and clean. |
| 2026-09-17 | Claude (G14 batch 2 + review) | G14-S37 + S40 deployed (`bb692271d`, 0408/0410 applied, external-signals cron installed); post-ship review fix `7900bcb5c`: intake inbox quota-limit upgrade link, live-qa evaluator seat elevated to investor_angel, audit hmac warn-once. S38 still unmerged (peer worktree). |
| 2026-09-17 | Claude (G13 backlog) | G13 deferred backlog cleared in four sequential batches (code → commit → deploy each), 0404 applied; releases pruned 4.0 G → 1.4 G. |
| 2026-09-16 | Claude (G13 close) | S-D3 + S-R5 live, W5 review fixes live (P1 pdf-parse bundling + 5 P2 + 5 P3); G13 closed — 15 sprints in one day, 0390–0403 applied, 3 new live-qa lanes; deferred backlog + founder-only items recorded. |
| 2026-09-16 | Claude (G13 W4 review) | W4 review: P0 report `no_account` (S-R3 select), member dashboard 500, unpriced Fund/Intake CTAs, CSP-blocked persona redirect, onboarding bounce, open redirect, assessment race, taxonomy sub-sector, free-tier gate — all fixed and live 17:04. |
| 2026-09-16 | Claude (G13 W4) | S-IA4 + S-D2 + S-R4 live with G14 pricing v4 (0400/0401 applied); peer gate-12 rollbacks resolved (pilot CTA expectation); W5 next. |
| 2026-09-16 | Claude (G13 W3 close) | W3 live + live-qa 155/0; review fixes live (2 P1 money paths, 0399 hardening); W4 launched. |
| 2026-09-16 | CEO + Claude (G14 plan session; deck / pricing / docs agents) | G14 Investor Feedback opened: goal doc `g14-investor-feedback-2026-09-16.md` + folder (`00-pitch-feedback.md` 19 quotes, `01-gtm-evaluators-90d.md`), research instrument `docs/research/evaluator-interviews-2026-09.md`, T2 pilot offer v2; decisions D1–D6, founder F-1…F-9 → §5; §2 rows G14-S33…S40; G12 pricing-v4 pointer; §5 +4 rows (6 Stripe mints, 0400, S9 names, F-decisions); ROADMAP §4 Phase 3.3, roadmap-v2 Q4 block, GOALS Phase 5 rows, unicorn-masterplan Pre-Seed row amended (3 buckets, pre-money A$2.5–4.0M); ledger T0276–T0280. rev.319. |
| 2026-09-16 | Claude (G13 W3) | S-IA3 founder landing live; S-T2 mandate v2 (0393) + S-R3 pipeline unification (0398) merged and deploying; mandate-fit-refresh cron installed; deploy held once for a peer session's uncommitted tree (shared checkout). |
| 2026-09-16 | Claude (G13 W2 close + review) | S-R2 + S-D1 live; W2 post-ship review found a P0 (W4 double budget → degraded paid reports) + 3 P1 (charged fully-degraded reports, adapter instead of W4 doc, label leak) + 10 P2/P3 — all fixed same day, 0397 backfill; W3 launched. |
| 2026-09-16 | Claude (G13 W2, worktree agents + review) | W1 post-ship review P1 (unscored snapshots / SVI-0 valuation) + 6 fixes deployed; S-IA2 hub tabs live (97 redirects, 60 page moves, orphan guard); S-R2 agent ownership + knowledge injection + W4 chapters merged/deploying (0396); S-D1 running. |
| 2026-09-16 | Claude (G13 W1, 3 worktree agents) | G13 Wave 1 live: nav v4 + persona + redirects (S-IA1), ReportV2 + dimension owners + 17 SVG visuals + TBR chapters + F1/F5 (S-R1), startup taxonomy + 0394 + backfill (S-T1); 0395 applied; bq-export cron fixed (tsx from checkout, skip when BigQuery unconfigured). Register rows G13-W1-* → shipped. |
| 2026-09-15 | Claude (QA/QC sweep, S32-F) | Founder-requested full QA/QC after the Google console fix: URI verified, 900-page crawl 0 bad, desktop+mobile click tests, nginx canonical 301s, `/api/valuation/vc` empty 200, `/analyze` 401 skip, S32-F interactive provider ordering (Money Finder timeout), live-qa per-call timeouts (funding FK noise explained), repo/worktree/release cleanup. |
| 2026-09-15 | CEO + Claude (plan session; BA / PM / Product agents) | G13 Investor Clarity opened (plan-only): goal doc `investor-clarity-2026-09-15.md` + folder (3 codebase audits, BA Investor Dossier/taxonomy spec, PM/UX IA + post-login plan, Product/AI TBR v2 plan); 15 sprints in 5 waves; decisions D1–D10; founder decisions F1–F6 → §5; §2 rows G13-W1…W5; ROADMAP.md §4 Phase 3.2, `feature-upgrade-roadmap-v2.md` Q4 block, GOALS.md Phase 4 rewritten, G7/G12 amendment lines. Same day: SVI upload page rework live (multi-format, valuation-first handoff), growth-insights cron + GA4 event ordering fixed. |
| 2026-09-12 | Claude (S20-A worktree) | Audit coverage: `apiRoute()` wrapper + codemod wrapped 298 mutating route files (allow-list 94 with reasons), rows hash-chained in `audit_events`, migration 0335 (serialised chain trigger + verify RPC — **apply by hand**), nightly `audit-chain-verify` cron + `audit_chain` on `/api/status`, `/workspace/audit-log` project/actor/action filters + owner CSV, static coverage guard. Not pushed/deployed from the worktree. |
| 2026-09-11 | Claude (S19-A worktree) | G7 closed: founder delegated Q1–Q4, recommendations adopted as decisions (`ux-ia-startup-flow-goal.md` open_questions now carry `decision` / `decided_by` / `status: shipped` / `files`; P8 `shipped`). Code: `journey-step-ladder.tsx` single responsive list, mobile collapse + "Show all 12 phases" toggle, `#phase-current` skip anchor, `aria-current="step"`; test pins for Q1 (`workspace-layout.test.tsx`), Q2 (`nav-v2.test.ts`), Q3/Q4 (`journey-step-ladder.test.tsx`). G7 "Next action" → none. |
| 2026-07-23 | loop agent (SOT consolidation task) | Initial consolidation. §1–§7 authored; back-links applied to reseller-plan + unicorn-masterplan; memory pointer added. |
| 2026-09-11 | Claude (S19-B, delegated by founder) | G2 #7 + #6 shipped: `reseller_customers` pipeline stage (migration 0333, `customer-stage.ts` vocabulary pinned to growth phases/canonical stages, nightly `reseller-stage-sync` cron + crontab line, owner/admin manual override route with audit log, Pipeline column on /reseller/customers, weekly-digest stage-moves line); Airwallex + Culture Amp public-record showcases (sourced figures only, sitemap + /showcase index + JSON-LD). #10 left founder-blocked. |
| 2026-09-10 | CEO + Claude (review + hardening) | Post-ship review of the G11/G12 money & access paths ([`reviews/money-paths-review-2026-09-10.md`](./reviews/money-paths-review-2026-09-10.md)): 1 P0 (evaluator report charged after the run, failed spend swallowed) + 8 P1 fixed — spend-before-run + refund + idempotent retries, atomic `spend_credits_atomic` RPC (0324), Money Finder/draft spend-before-insert, Package buyers now get Growth extras + Radar sweep, additive Radar re-purchase, `funding-report-retry` cron, atomic batch claims + lease sweep + per-item quota (0325), founder-keyed evaluator money signals; `cron-runner` watchdog now sized from `--timeout`. Also: ESLint error baseline 271 → 0 (no behaviour change), investor "Let matching founders see me" opt-in, flaky nightly test fixed. Full suite 1,222 files / 29,860 green. Deploy scheduled 2026-09-11 12:05 UTC. |
| 2026-09-10 | CEO + Claude (implementation session) | G11+G12 **S0–S4 shipped and live** in one day via parallel worktree agents: 4 releases (S0/S1, S2, S3, S4) all 12/12 gates; migrations 0309–0321 applied; crontab gained `refresh-funding-sources`, `money-radar-sweep`, `evaluator-progress-weekly`; Stripe evaluator + A$3 prices minted. Ledger T0237–T0250, T0268–T0271, T0273–T0275 closed; S5 (T0272, T0251) shipped in a 5th release `fea2aa62b` the same evening (migrations 0322–0323, crons `evaluation-batch-runner`, `analysis-refresh-quarterly`) — **all 23 G11/G12 ledger tasks done**. |
| 2026-09-10 | CEO + Claude (pre-implementation review) | Reviewed G11+G12 against HEAD `120a840d3` and live v3.10.0: nav consolidated by `1c359f000`, DB `plans` B2B rows stale (0074 seed), csv/gate flag mismatch, A$5.50 SKU wiring, `/solutions/advisor` redirect selling Growth A$69, two privacy policies, entity name split (PPL Food vs Auschain → **PPL Food PTY LTD** decided), orchestrator ID collision at 12:00 UTC. Unified sprint plan S0–S5 with P0–P3; ledger tasks tagged `sprint`/`priority`; T0237/T0268/T0269/T0270/T0274/T0275 re-scoped; goal docs §9 appended; ROADMAP header price drift fixed. |
| 2026-09-10 | Founder | Approved G12 wording: doctoral-research sentence (DBA, never "PhD") and conservative provider data-handling sentence; §5 rows flipped to approved. Stripe evaluator prices remain to mint. |
| 2026-09-10 | CEO + Claude (plan session, 3rd pass) | G12 Evaluator Traction opened (plan-only): goal doc `evaluator-traction-2026-09-10.md` (ladder Scout/Firm/Program, A$3 per startup report, 6 differentiators + ChatGPT comparison, 90-day traction, appendices: 54 competitors, JTBD, ChatGPT evidence, AU sizing); ledger T0268–T0275; §2 rows G12-P1..P8; §5 +3 human-blocked; roadmaps + pricing plan + G11 cross-link merged. |
| 2026-09-10 | CEO + Claude (plan session, 2nd pass) | G11 execution plan approved: source/loop review findings (goal loops removed, auto-improve frozen, nextTaskId collision, version drift) → 6-wave founder-driven session model; ledger tasks T0237–T0251 added to `project-state.json`, 16 overlapping pending tasks merged, 16 duplicate IDs re-numbered (T0252–T0267), version 3.9.0→3.10.0; goal doc §8 rewritten; §2 rows carry T-ids; §5 gains 5 human-blocked rows; stale goal-loop notes fixed in `web/AGENTS.md` + `docs/ops/crontab-setup.md`. |
| 2026-09-10 | CEO + Claude (plan session) | G11 Money Finder opened (P0 shipped, plan-only): goal doc `money-finder-2026-09-10.md`, §2 rows G11-P0..P14, §7 markers; amendments recorded in G7/G9 goal docs, `feature-upgrade-roadmap-v2.md` Q4, `ROADMAP.md` §4, `pricing-upgrade-plan-2026-07-16.md`, `GOALS.md` Goal 3, `role-based-2026-07-25/founder.md` gap #8; seed data `web/content/data/grants-au.seed.json` + `programs-au.seed.json`. |
| 2026-07-23 | qa/regression sweep | Iteration 5-9 sync: G1 tick 290 → 317; G2 audit #1/2/3/4/5/8/9 flipped to shipped with commit SHAs (#6/7/10 remain founder-review-blocked); G4 Top-5 #3 PDF branding flipped to shipped; requirements register rows added for AUD-R1/2/3/4/5/8/9 + Q3-PDF-BRAND; §3 shipped-log expanded with audit-remediation, product/branding, QA/infra subsections. |

