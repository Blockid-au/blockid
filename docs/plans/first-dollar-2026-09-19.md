# G16 — First dollar: funnel truth · paywall → A$3 · evaluator pilot enablement

**Opened:** 2026-09-19 (founder: "làm như bạn đề xuất" after G15 closed; commit all, deploy live, test full, fix).
**Owner:** Claude session loop (3 worktree lanes → merge → full vitest → deploy → elevated live-qa → read-only review → fix → close).
**Tracked in:** `docs/plans/SOURCE-OF-TRUTH.md` § G16.

## 1. Why this, why now

All planned engineering (G1–G15) is closed or founder-blocked; the platform is deep and (since G15) observable. The numbers have not moved: **115 real sign-ups in the last 14 days (≈ 15/day)** yet **0 `report_orders`, 0 evaluator trials, 0 MRR**. The last-14-day `analytics_events` mix is `showcase_report_downloaded` 509 · `esic_gate.warn_missing` 120 · `feature_gate_hit` 97 (cap_table.write 42, investor.dealflow 35, intake.manage 11, data_room.access 9) · `dossier_view` 63 · `funding_report_paid` 12 · `svi_score_computed` **1** · `sign_up` **0** · `checkout` **0** — i.e. the funnel steps that matter are not even emitted, `/admin/funnel` reads a `funnel_events` table that does not exist, and nobody has ever reached the A$3 checkout. Founders sign up, hit gates, leave.

## 2. Non-goals

No new report features, no pricing changes (A$3 TBR, Scout/Firm/Program, v4 tiers unchanged), no Stripe coupons (comps are admin credit/plan grants — house rule), no new tables with `app_users` FKs (erasure map), no third-party analytics. Transparent pricing rule stands: credit cost + A$ shown before any charge, never charge without confirmation.

## 3. Lanes (parallel worktrees; each lane owns its files; none edits SOT/ROADMAP/live crontab)

### A — Funnel truth (server-side events + daily funnel + real `/admin/funnel`)
Files: `web/src/lib/analytics/events.ts` (event names/params only — additive), the emit points `web/src/app/api/auth/register/route.ts`, `web/src/app/api/auth/google/*` (or wherever Google sign-in creates the user), `web/src/app/api/svi/route.ts` (+ `run-for-project` if that is the first-analysis path), `web/src/app/api/reports/checkout/route.ts`, the Stripe webhook handler for `trust_report` purchases, `web/src/app/api/analytics/*` (client → server event ingest, if it exists — otherwise create `POST /api/analytics/event` accepting only allow-listed names with rate limit), `web/scripts/funnel-report.mjs` (+ core + tests), `web/src/app/(app)/(admin)/admin/funnel/page.tsx` (+ any API it needs), `web/src/lib/traction/snapshot.ts` (add funnel block), `docs/ops/analytics.md` (extend).
1. **Emit, server-side, exactly once per step**: `sign_up` (method email|google, persona), `svi_analyze` (first vs repeat), `svi_score_computed` (already exists — make sure the real path fires it), `report_view` (TBR page render, free|paid tier, page estimate), `paywall_view` (the free-tier cut / unlock rail was rendered — B emits from the client through the ingest route; A defines the name + params), `checkout` (A$3 session created; `sku`, `amount_cents`), `trust_report_purchased` (webhook), plus `feature_gate_hit` (exists). Every event carries `user_id`, `project_id?`, `session_id?`, `qa: true` for `qa-live-*` accounts so reports exclude them. Idempotency: `sign_up`/first `svi_analyze` must not double-fire on retries (use `event_id` = deterministic hash of user+step where sensible).
2. **Daily funnel**: `scripts/funnel-report.mjs` (cron 02:50 UTC via plain node, own flock) → `content/reports/funnel-daily.jsonl` `{date, signups, first_analyses, report_views, paywall_views, checkouts, paid, gate_hits:{feature:n}, conv:{signup_to_analysis, analysis_to_report, report_to_paywall, paywall_to_checkout, checkout_to_paid}}` over `analytics_events` (QA excluded), 7-day and 28-day rollups; Telegram/e-mail summary weekly (Mon) via the ops alert helper.
3. **`/admin/funnel` rewritten** on real data: yesterday / 7 d / 28 d table, per-step conversion, the top gate features, and the last 20 sign-ups with their furthest step (no emails on screen — user id prefix + persona). Delete the dead `funnel_events` code path and the CRO hypothesis block only if it reads the missing table (keep `cro-experiments` if it is pure).
4. `traction-snapshot.json` gains `funnel_7d` from the same reducer (one source of truth) — keep existing keys.
5. Tests for the reducer with fixture rows; route tests for each emit point (assert the event name + `qa` flag); `/admin/funnel` render test with a fixture jsonl.

### B — Paywall → A$3 (the free founder's path to the first purchase)
Files: `web/src/lib/report-v2/free-tier.ts` (projection only if needed), `web/src/components/tbr/v2/*` (locked-chapter preview + unlock rail), `web/src/components/paywall/ReportPaywallGate.tsx` (+ `ReportOrderView`), `web/src/app/(app)/(founder)/workspace/reports/business/business-report-client.tsx`, the founder gate-hit UI (`FeatureLocked*` / whatever `recordGateHit` surfaces render — find it), `web/src/lib/email-drip.ts` (D1 + a new "report ready, unlock for A$3" touch), `web/src/lib/analytics.ts` (client helper) to emit `paywall_view` / `checkout` via A's ingest route (call `trackEvent("paywall_view", …)` — if the ingest route does not exist yet in your worktree, add a minimal `POST /api/analytics/event` guarded by an allow-list; the merge will reconcile), `web/tests/live-qa/21-reports.spec.ts` (extend).
1. **Locked preview at the free-tier cut**: each trimmed chapter renders its title, the first sentence of its narrative, its primary visual as a blurred/skeleton block, and a single unlock rail: "Unlock the full Trusted Business Report — A$3 (one-off) · what you get: N chapters, valuation range, action plan, PDF + share link" → one click → the existing quote-then-pay flow (`ReportPaywallGate` semantics: show credits/A$ before the Stripe redirect; plan-included users see "included in your plan"). Never charge without the confirm step.
2. **Gate-hit surfaces sell the right thing**: when a free founder hits `cap_table.write`, `data_room.access`, `investor.dealflow` (evaluator-only → show the evaluator ladder, not Founder), the lock card shows the plan name + A$ price + trial terms + one CTA to `/pricing?feature=<feature>#tier-<id>` and emits `feature_gate_hit` with `surface` (currently blank).
3. **Nudge**: after the first `svi_score_computed`, if no purchase within 24 h, one e-mail "Your report is ready — unlock for A$3" deep-linking to the report (respect `email_preferences`, dedupe, never to `qa-live-*`); reuse the drip queue; D1's existing copy stays but links to the report, not the dashboard.
4. **Copy truth**: every A$ figure on these surfaces comes from the pricing source of truth (`plans-v2.ts` / `report-credit-cost.ts`), never a literal.
5. Tests: component tests for the locked preview (free vs paid vs plan-included), gate card per feature, drip touch dedupe; extend live-qa `21-reports` to assert the unlock rail + quote (no spend: `LIVE_QA_SPEND_OK` stays 0).

### C — Evaluator pilot enablement (the 90-day GTM's "free cohort scoring for one intake")
Files: `web/src/app/api/admin/pilots/route.ts` (+ `[id]`), `web/src/lib/pilots/*` (new), `web/content/pilots.json` (ledger, gitignored? — NO: committed, admin-edited via API, small), `web/src/app/(app)/(admin)/admin/pilots/page.tsx`, `web/src/app/(marketing)/pilot/page.tsx` (+ `/api/pilot/apply`), cron `pilot-expiry` (`web/src/app/api/cron/pilot-expiry/route.ts` + `scripts/crontab.production` append under `# G16-C`), `docs/ops/pilots.md`, `docs/plans/g14-investor-feedback-2026-09-16/01-gtm-evaluators-90d.md` § 3 (link only).
1. **One-click comp** (admin-only, audited): `POST /api/admin/pilots { email, program_name, seats?, days=30, intake_slug? }` → sets the evaluator's plan to `investor_vc_small` (Program) **without Stripe**, records `{id, user_id, email, program_name, tier, started_at, expires_at, credits_granted, intake_id, status}` in `content/pilots.json`, grants the report quota/credits via the existing admin credit-grant path, creates the intake link (`program_intakes`, reuse `lib/intake/program-intakes.ts`), sends the pilot welcome e-mail (offer terms from the GTM doc § 3: 30 days, ≤ 60 applicants, what is/isn't included, the data sentence verbatim) and an ops alert. Cap 5 active pilots (6th → 409 with the reason). Idempotent on email.
2. **Expiry**: cron daily 04:20 UTC — 3 days before expiry e-mail the evaluator + ops; on expiry revert plan to `free` **unless a Stripe subscription exists** for that user (never downgrade a payer), mark `status: expired`, alert. `GET /api/admin/pilots` lists with days-left, submissions count (`intake_submissions`), reports run (`evaluation_reports`), assessments.
3. **`/pilot` page** (public, indexable): the offer v2 terms, the 7 success criteria, the data sentence, a short form (program, name, email, cohort size, intake month) → `POST /api/pilot/apply` (rate-limited, honeypot, no account needed) → ops alert + auto-reply e-mail; stored in `content/reports/pilot-applications.jsonl` (no PII beyond what the form collects; erasure runbook note).
4. **Admin page** `/admin/pilots`: table + "Start pilot" form + "End early" action; links to the intake inbox and the cohort table.
5. Tests: route tests (admin gate 401/403, cap 409, idempotent, plan set, Stripe-subscriber guard on expiry), ledger read/write, page render tests; live-qa lane `30-pilot.spec.ts`: anon `/pilot` renders + apply → 200 (evidence), admin routes 401 anon.

## 4. Acceptance
- Full vitest green; tsc clean; eslint 0 errors; deploy 12/12; elevated live-qa ≥ 178 pass, 0 new failures.
- `/api/status.traction` still `ok`; `funnel-daily.jsonl` written by the first manual run; `/admin/funnel` renders real numbers; a QA free founder sees the locked preview + A$3 quote on the TBR page; `/pilot` live; admin can start a pilot end-to-end on a throw-away evaluator (then end it) — done by the main session after deploy.
- Post-ship review (read-only agent) → fixes → SOT G16 close-out + change-log row + memory.

## 5. Founder decisions (defaults ship if silent)
- F-1 Pilot comp tier = Program (`investor_vc_small`), 30 days, cap 5 — per GTM doc § 3.
- F-2 A$3 nudge e-mail at +24 h after the first score (one touch, respects preferences).
- F-3 `/pilot` public + indexable.
