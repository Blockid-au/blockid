# Post-ship review — S29 (A listing readiness + clean-room / B hardening) — 2026-09-13

Range `490734cd8..master` (91 files, +6 148 / −178): the S29-B hardening merge `57cad86e9` (+ ledger `59eeafb37`) is on `origin/master`; the S29-A merge `2bd45e5cd` was on local `master` and not yet pushed at review time, so the review is based on local `master`. Reviewed by reading every file in scope, plus `npx tsc --noEmit` (clean before and after) and the scoped vitest run (87 files / 1 373 tests before → 87 files / 1 376 tests after fixes, all green).

## Verdict

**GO — no P0/P1. Two P2s and two cheap P3s fixed in place (two commits + one new migration, not applied); the rest are open items or content notes for the founder's lawyer.** Security posture is sound: every mutating route in the range is wrapped in `apiRoute()` (`listing/profile` PATCH, `listing/readiness/pdf` POST, `data-room/clean-room` PATCH — catalogue regenerated, coverage suite green); roles match the spec (listing facts PATCH editor+, PDF GET/POST editor+, clean-room PATCH editor+, every GET viewer+); every read is keyed on the project scope (`projectScopeOrDeny`) and re-checked on the row (`row.project_id !== projectId → empty`); computed clean-room tasks answer 409 `computed_task` before the scope lookup; `parseListingFactsPatch` refuses unknown keys, non-ISO dates (`isIsoDate` round-trips through `Date`), non-finite / negative / out-of-range numbers, non-`FYyyyy` labels, non-UUID holder ids, duplicate FYs, > 10 / 500 entries, and 16 KB bodies. Rate limits (`crm-import` 10/h, `sim-place` 60/min, `listing-readiness-pdf` 30/h) are keyed on `user.id` through the shared `enforceRateLimit` (429 + `Retry-After` + the common body). The credit path shows the price before any spend (GET 402 `payment_required` with `cost`/`listedCost`/`balance`/`creditNote`, POST preview → `confirm: true`), spends before the render, refunds on a render failure, and stamps the one-off charge only after a successful render.

## Findings

| # | Sev | File:line | Issue | Status |
|---|-----|-----------|-------|--------|
| 1 | **P2** | `web/src/lib/listing/readiness.ts:347` (audited accounts) and `:541` (Nasdaq operating history) | Company age was measured to the **rules' as-at constant** (`ASX_RULES_AS_AT` / `NASDAQ_RULES_AS_AT`, both `2026-09-13`), not today. A company incorporated 2025-01 would still read "1.7 yrs" of operating history next year, so the Nasdaq Equity Standard 2-year row could never flip to met without a code change, and the ASX "young company" note would never expire. | **Fixed** 2ee725d40 — `ListingFacts.asOf?` (server sets today, UTC); builders use `asOfDate(f)`; tests pin `asOf: "2026-09-13"` and cover a later `asOf` plus the default-today path. |
| 2 | **P2** | `web/src/app/api/cap-table/route.ts:284` (`issue_shares`) | `shares` was only checked `> 0`; a fractional (`1.5`) or oversized count reached `incrementSharesHeld`, which correctly refused it as `invalid_delta`, but the route answered **500 "Failed to issue shares"** for a caller error. Before S29-B a fractional count was silently written into the bigint column. | **Fixed** 7fadba000 — `Number.isInteger(shares) && shares ≤ 1e12` → 400 before any DB read; test. |
| 3 | P3 | `web/src/lib/listing/readiness.ts:212` `computeNasdaqPublic` | Insider exclusion used `pct >= 10`. Nasdaq Rule 5005(a)(35) excludes holders of **more than** 10 % — a holder at exactly 10 % is public. Off-by-a-share in the conservative direction. | **Fixed** 2ee725d40 — `pct > 10`; basis text now cites 5005(a)(35) "more than 10 %"; boundary test (exactly 10 % public, 10.0001 % excluded). |
| 4 | P3 | `web/src/lib/listing/readiness.ts:348` (audited accounts) | The label says "shorter where the entity is younger" but a company younger than the audited period with ≥ 1 audited year was scored **not_met**, contradicting the note in its own basis. | **Fixed** 2ee725d40 — younger-than-`needed` with ≥ 1 audited FY → `not_confirmed` with the "since incorporation" note; an older company with too few years stays `not_met`. |
| 5 | P3 | `web/supabase/migrations/0381_increment_shares_held.sql:39` | `SET search_path = public` (spec: `public, pg_temp`). Postgres searches `pg_temp` first unless it is listed; the body is schema-qualified and EXECUTE is service_role only, so not a live hole. No delta bound in SQL. | **Fixed** 7fadba000 — migration **0383** (not applied): `SET search_path = public, pg_temp`, `p_delta ≠ 0 AND |p_delta| ≤ 1e12` (`invalid_parameter_value`), same grants/revokes; `MAX_SHARES_DELTA` mirrored in `shares-held.ts` (DRIP path guarded too). |
| 6 | P3 | `web/src/app/api/listing/readiness/pdf/route.ts:126–166` | Two concurrent `confirm: true` POSTs both read `pdfCreditsCharged = 0` and both spend 1 credit (the stamp is written after the render). Bounded by the 30/h limiter and 1 credit; a normal double-click is serialised by the client's `busy` flag. | **Open** — stamp a `pdf_charging_at` claim before the spend (conditional upsert `WHERE pdf_credits_charged = 0`), or accept the 1-credit worst case. |
| 7 | P3 | `web/src/lib/dataroom/clean-room-server.ts:49–56` `findRoom` | With a project scope and **no** room for that project the loader falls back to the owner's newest legacy room (`project_id IS NULL`). `data-room/auto-fill` and `data-room/readiness` do not fall back once a project resolves. For a multi-project owner, project B's clean-room page could show project A's legacy room's NDA / watermark / link signals. Owner-only data, computed rows only. | **Open** — drop the legacy fallback when `scope.projectId` resolved, matching the sibling routes. |
| 8 | P3 | `web/src/lib/expenses/bank-csv.ts:143` `normaliseDescription` | The formula strip now runs **inside** the dedupe hash. Rows imported before S29-B whose narration began with `= + - @` were hashed on the raw text; a re-import of the same statement now hashes on the stripped text and inserts a duplicate. Rare (bank narrations seldom start with those characters) and bounded to the founder's own rows. | **Open** — one-off backfill (`UPDATE … SET description = stripped, hash = recomputed WHERE description ~ '^[=+\-@]'`) in a 0384 if any project shows duplicates. |
| 9 | P3 | `web/src/lib/listing/server.ts:103` `loadBankProfit` | `.limit(5000)` newest-first: a project with > 5 000 categorised lines inside its trailing 12 months under-counts profit and mis-measures coverage. Fine at today's 5 k/project import cap. | **Report only.** |
| 10 | P3 | `web/src/lib/listing/readiness.ts:189` `computeFreeFloat` | Comment says the ESOP pool is excluded from issued shares; the code keeps `role: "esop"` rows in the denominator (affiliated → not free, but still issued). Understates free float when the ESOP row is an unissued pool (conservative), overstates nothing. | **Report only** — decide whether an `esop` row is a pool or allotted shares before changing the maths. |

### Verified clean (no finding)

- **Listing PDF money path** — `FEATURE_COSTS.listing_readiness_pdf = 1`; `listingPdfIncluded` (equity add-on `esop.manage` → Growth+/Startup Package via `hasGrowthExtras`, lookup failures never grant); charged once per project (`listing_profiles.pdf_credits_charged`, stamped only after a successful render, re-downloads of either exchange free); `canAfford` → 402 `insufficient_credits` before the confirm; `spendCredits` failure → 402 `credit_spend_failed`; render failure after a spend → `grantCredits(…, "refund")` + 500 `render_failed` with `retryCost`; GET answers 402 `payment_required` with the price so the page shows it before "Export"; `X-BlockID-Credits-Charged` header. The client calls GET only when `pdf.cost === 0`, otherwise preview → `window.confirm` with cost, credit note and balance → confirm.
- **Tax-statement regenerate retry** — `pendingRegenerateRetry` only fires when the newest `issued_at` stamp carries a `version ≥ 2` row **and** some shareholder is still on an older current row; the retry is restricted to `pendingKeys`, re-uses the run stamp as `now` (rows join the same run), `alreadyCharged` → cost 0. A complete regenerate followed by a new shareholder is not a retry (no stale rows) and is charged as a new run; a total failure refunds and leaves no stamp, so the next run charges. Tests pin all three.
- **AI categorise pro-rata refund** — `categoriseRefund` = `min(chargedUnits, ceil(failedRows / 100))` units × charged unit price, capped at what was paid, `{0, 0}` when nothing failed / charged; failed-batch rows are `undecided` (`source: null`) and `runAiOnQueue` skips them (stay queued, never stamped, counted in `failedRows` not `categorised`); total failure still takes the earlier full-refund path, so no double refund on the same run.
- **Burn precedence** — `pickBurnRate` (pure): Xero opex (`totalExpensesAud ÷ windowMonths`, default 3) → bank CSV (`monthsWithData > 0 && monthlyOpex > 0`) → `startup_metrics.burn_rate_aud` → none, each with a `RevenueSource` label and `takenAt`; `resolveBurnRate` loads what the route has not (owner-keyed snapshots); `/api/revenue`, `/api/pnl`, `/api/valuation`, `/api/valuation/vc` and the P&L opex line all go through it (the 1.5 × COGS estimate stays outside the precedence). `burnRateWithBankFallback` removed.
- **Atomic `shares_held`** — 0381 `increment_shares_held`: one `UPDATE … WHERE shares_held + delta >= 0 RETURNING`, `no_data_found` / `check_violation` raised, REVOKE from PUBLIC/anon/authenticated, GRANT service_role; TS helper feature-detects 42883/PGRST202 once per process (`console.warn` once), never falls back on a purposeful refusal (23514 / P0002 → `below_zero` / `not_found`), fallback guards `newTotal < 0` and keeps the `account_id` / `share_class_id` extras. Both callers are service-role code behind `apiRoute()`.
- **CRM** — `isProjectMemberOrOwner`: owner always, else an `accepted` `project_members` row re-checked on the returned row; lookup error → not a member → 400 `owner_not_member`; unassigned (`null`) passes. Import limiter 10/h per user; sandbox `place` 60/min per user (other actions unlimited by design); certificate `?annex=ess` without an issued annex → 409 `annex_not_issued` before any render.
- **Sector multiples** — `?dry=1` is fetch-only (`fetched` + `textChars`, no model call, `supabase` never created), `?dry=1&extract=1` runs the model with no writes, `fetchOnly` implies `dryRun`; body carries both flags for the heartbeat; docs table matches the code.
- **Migrations 0379–0382** — `IF NOT EXISTS` / `OR REPLACE` / `DROP TRIGGER IF EXISTS` / DO-guarded policies; RLS on both new tables with service-role ALL + owner SELECT via `projects.user_id`; FK to `public.projects(id) ON DELETE CASCADE` only (no user FK); jsonb `object` CHECKs, non-negative credits CHECK, `updated_at` triggers; 0382 partial index `(project_id, occurred_on DESC) WHERE category_source IS NULL` matches `loadAiQueue` / `countAiQueue` exactly (`.eq(project_id).is(category_source, null).order(occurred_on desc)`); ledger JSON updated for 0379–0382.
- **Maths** — `computeSpread` rounds `shares × price` to the cent before the `>=` so 1 000 × A$2.00 and 3 × 666.6667 qualify and 999 × A$2.00 does not; no price → `qualifying 0` and the row is `not_confirmed` only when the raw non-affiliated count already clears 300; free float = non-affiliated ÷ issued (founder / co-founder / director / executive / officer / ESOP roles + `restricted_holder_ids`); Nasdaq round lot = 100 shares, US$2 500 minimum-value nuance inclusive at the cent, 50 % taken as 150 of the required 300 (Nasdaq's stated reading); `scoreReadiness.pct = met ÷ (met + not_met)`, `not_confirmed` and `confirm_current_rule` counted separately, `null` when nothing is decided; `bankProfitFromLines` returns `null` under 12 whole months of coverage, revenue positive-only, expense categories via `categoryKind === "expense"` (grants / transfers / drawings excluded), cents-rounded; hash stability: `transactionHash("=AWS") === transactionHash("AWS")` pinned.
- **UI** — both pages: loading skeleton, error card, no-project notice, no-data-room notice (clean-room), viewer read-only (`canEditFacts` / `canTick`, "View only — viewer on this project cannot tick tasks"), computed rows non-tickable with the room's evidence, cost in the export button label and in the confirm dialog, AU English (capitalisation, categorised, licence), `LISTING_READINESS_NOTE` + `CLEAN_ROOM_NOTE` on page and `LISTING_READINESS_NOTE` + `AdviceDisclaimer` on the PDF, founder's entity block (name, ACN/ABN or "not supplied", address) on the PDF; `pdfSafe` swaps ≥ ≤ → − for Helvetica; no `any` outside the documented `SupabaseClient<any, any, any>` alias and the `<input step="any">` attribute; anchors `#share`, `#trust`, `#engagement` (investor-share-panel) and `#data-room-checklist` (data-room page) all exist; nav leaves Growth+ / phase 5 with `requireTierForPage` on both pages; exit page guide cards link to both.

## Content-accuracy notes for the founder's lawyer (report, not rewritten)

Thresholds as encoded in `web/src/lib/listing/readiness.ts` (as-at 2026-09-13 in the code). Every row carries `sourceRef` + `asAt`; the two `confirm_current_rule` rows (`asx.currency`, `nasdaq.currency`) and the escrow row on the assets path state no number as fact.

| Row | Encoded | Reviewer's view |
|-----|---------|-----------------|
| ASX LR 1.1 cond 8 spread | 300 non-affiliated holders, each parcel ≥ A$2 000 | Matches the post-Dec-2019 rule. Parcel is tested at the proposed issue price (or current mid when none); the rule tests at the issue/sale price — the basis says so. |
| ASX LR 1.1 cond 7 free float | ≥ 20 % (LR 19.12 definition) | Matches. Escrowed / restricted securities must be marked restricted by the founder — the basis says so. |
| ASX LR 1.2 profit test | A$1 m aggregated over 3 full FYs (1.2.4) **and** A$500 k over the last 12 months (1.2.5) | Matches; the going-concern / same-activity / audited-accounts conditions (1.2.1–1.2.3) are named in the basis. LR 1.2.5 requires the 12 months to end no more than 2 months before the application date — the bank-line window ends at the latest imported line and does not test that gap. |
| ASX LR 1.3 assets test | NTA ≥ A$4 m after raising costs **or** market cap ≥ A$15 m (1.3.1); working capital ≥ A$1.5 m (1.3.3(a)) | Matches. LR 1.3.2 (cash / commitments) is described, not scored. Working capital is the founder's entered figure — 1.3.3(a) is the pro-forma statement in the prospectus; the next step says so. |
| ASX LR 1.2.3 / 1.3.5(a) audited accounts | 3 FYs (profit) / 2 FYs (assets), shorter since incorporation | Matches; the half-year review (1.3.5(b)) and pro-forma balance sheet are mentioned but not scored. |
| ASX LR 1.1 cond 12 | issue / sale price ≥ A$0.20 | Matches (main-class securities; option exercise-price limb not scored). |
| ASX LR 1.1 cond 1A / 13 / 20 | constitution consistent with LR 15.11; corporate governance statement against the ASX CGC Principles **4th edition**; good fame and character checks | Condition numbering matches the post-2019 rules — confirm against the edition in force at application. The CGC consulted on a 5th edition in 2024 and did not proceed in 2025, so the 4th edition remains current at the as-at date; confirm before relying. |
| ASX Chapter 9 escrow | assets-test entities: Appendix 9B classes / periods, not asserted | Correctly `confirm_current_rule` until acknowledged; no period is stated. |
| Nasdaq 5505(a)(1) bid price | US$4, or US$3 closing (equity / net income standards) / US$2 closing (MVLS standard) with extra conditions | Matches 5505(a)(1)(A)–(B). Between US$2 and US$4 the row is `not_confirmed`, never `met`. |
| Nasdaq 5505(a)(2)–(4) | 1 000 000 unrestricted publicly held shares; 300 round-lot holders (100+ shares), ≥ 50 % holding ≥ US$2 500; 3 market makers | Matches. "50 %" is applied to the required 300 (150), which is Nasdaq's stated reading of "such Round Lot Holders"; confirm with counsel if the register is far above 300. Insider line now "more than 10 %" per 5005(a)(35) (fix #3). |
| Nasdaq 5505(b)(1)–(3) | Equity: equity US$5 m, MVUPHS US$15 m, 2-yr history; MVLS: US$50 m, equity US$4 m, MVUPHS US$15 m; Net income: US$750 k (latest FY or 2 of 3), equity US$4 m, MVUPHS US$5 m | Matches. Net income from bank lines is labelled "an indicator, not an audited figure". The 2025 amendment (IPO MVUPHS from offering proceeds) and the proposed minimum public offering are described in `nasdaq.currency` without figures — correct treatment. |
| Nasdaq 5605(b)(1), 5605(c)(2), 5610 | majority independent board; audit committee ≥ 3 independent members (10A-3, written charter); code of conduct | Matches; phase-in (5615(b)) and controlled-company (5615(c)) relief named, not scored. |
| AUD→USD | founder-entered `aud_usd_rate` (0 < r ≤ 10) | No live rate is fetched; every US$ figure says "(entered)". |

Disclaimers: `LISTING_READINESS_NOTE` ("readiness indicators … not legal advice and not an opinion on admission … confirm every row with the company's lawyers, auditors and a licensed adviser") is on the page and the PDF; the PDF adds `AdviceDisclaimer` and prints the founder's entity, never BlockID's, as the company.

## Fixes (this review)

| SHA | Change |
|-----|--------|
| 2ee725d40 | Listing readiness: `facts.asOf` (today) drives company age / operating history; Nasdaq insider line `> 10 %`; young company with ≥ 1 audited FY → `not_confirmed`. Tests. |
| 7fadba000 | Cap-table `issue_shares` fractional / oversized → 400; `MAX_SHARES_DELTA` (1e12) in `incrementSharesHeld`; migration **0383** (search_path `public, pg_temp`, server-side delta bound) — **not applied**. Tests. |

## Open items

1. (#6) Listing PDF concurrent-confirm double charge — claim stamp before the spend, or accept the 1-credit worst case.
2. (#7) Clean-room `findRoom` legacy fallback under a resolved project scope — match `auto-fill` / `readiness`.
3. (#8) Pre-S29-B narrations beginning with `= + - @` will not dedupe against a re-import — backfill in 0384 if duplicates appear.
4. (#9) `loadBankProfit` 5 000-line cap — page or aggregate server-side when the import cap rises.
5. (#10) Decide whether an `esop` cap-table row is an unissued pool (exclude from issued) or allotted shares (keep).
6. Apply **0383** with `scripts/db/apply-migration.sh web/supabase/migrations/0383_increment_shares_held_hardening.sql` and commit the ledger JSON; push the S29-A merge (`2bd45e5cd`) — it is still local-only.
7. Content: lawyer to confirm LR 1.1 condition numbering (1A / 13 / 20), the CGC edition, and the Nasdaq "50 % of such Round Lot Holders" reading at the application date.

## Test results

- `NODE_OPTIONS=--max-old-space-size=8192 npx tsc --noEmit` — exit 0 (before and after).
- `npx vitest run src/lib/listing src/app/api/listing src/lib/dataroom src/app/api/data-room/clean-room src/lib/pdf/listing-readiness-pdf.test.tsx src/lib/revenue src/lib/cap-table src/app/api/cap-table src/lib/dividends src/lib/expenses src/app/api/expenses src/app/api/investors src/app/api/secondary src/app/api/valuation src/lib/valuation src/lib/audit/coverage.test.ts src/lib/projects.scope-guard.test.ts src/lib/nav scripts/docs` — 87 files, 1 373 tests green before; 87 files, 1 376 tests green after.
