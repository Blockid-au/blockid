> **Disposition (2026-09-11):** no P0/P1; all five P2 fixed in the commit that adds this file — plus the API-wide credential scan now also covers `api/internal`, `api/status`, `api/history/ingest`, `api/webhook/github`, `api/admin/ai-health/trigger` (moved to `cronSecret()` / `safeEqualStrings`, GitHub HMAC length-guarded).

# Post-ship review 2 — range `2e96296de..HEAD` (5 commits, 2026-09-11)

Commits: `a0a1164ff` P2 pre-deploy fixes · `0276438c6`/`d6ec7c595` S8-E cron auth · `712012a39` SOT · `2c9bf412f` Gate-7 founder-phase split + graph guard.
The S6-C `solutions-shared.tsx` `pilotCta` change is commit `c5d46b762`, which is **before** `2e96296de` (already in a prior review); re-read only — plain `<Link>` to `ACCELERATOR_PILOT_HREF`, text through `fillPrices`, nothing new.
Verified by running: `cron-auth.test.ts` + `cron-auth.routes.test.ts` + `client-safe-graph.test.ts` (1617 pass, 9.3 s total; graph guard alone 0.83 s), all 30 colocated `api/cron/**/route.test.ts` (509 pass), `founder-phase.test.ts` + `scripts/docs/unlock-matrix.test.ts` + `report-quota.test.ts` (83 pass).

## P0 — none. P1 — none.

## P2

1. **Same ad-hoc `Bearer ${process.env.CRON_SECRET}` compare survives outside `api/cron/`** — `web/src/app/api/internal/ai-complete/route.ts:14-16`.
   Scenario: non-constant-time compare on a route that spends AI credits; and when `CRON_SECRET` is unset the literal `Bearer undefined` opens it (the exact fail-open S8-E removed from 14 cron routes). Not live-exploitable today (secret set in prod) but the static guard in `cron-auth.routes.test.ts` only walks `src/app/api/cron/**`, so this one is invisible to it. `api/status/route.ts:67-72` has the same `===` shape but only gates telemetry detail (lower value).
   Fix: `if (!isCronAuthorised(request)) return 401` in `ai-complete`; widen the `BANNED` scan root to `src/app/api/**` (or add `internal/` explicitly).

2. **`countTrialReportsUsed` with `started_at = null` now fails open on cost** — `web/src/lib/evaluations/report-quota.ts:386` → `return 0`, consumed by `getReportQuota` (`:401-402`) and `trial-end-reminder/route.ts:60`.
   Scenario: a `subscription_trial_state` row with `status='trialing'`, a future `trial_end` and `trial_start=null` (column is nullable, `0075_…sql:11`; webhook writes `toIso(sub.trial_start)`) gets `used=0 → remaining=1` on **every** call, so the 1-report trial allowance is never consumed — unbounded free full reports. Stripe always sends `trial_start` with `trial_end`, so this needs a hand-edited/backfilled row; prior review's alternative (`trial_end − trial length`) was the safe direction.
   Fix: `if (!trial.started_at) { if (!trial.ends_at) return 0; return countQuotaUsed(userId, new Date(Date.parse(trial.ends_at) - TRIAL_DAYS_MS).toISOString(), null); }` — or bound by `app_users.trial_started_at`, which the webhook always stamps (`stripe/webhook/route.ts:504`).

3. **Client→server-only graph guard misses several runtime `import()` shapes** — `web/src/lib/client-safe-graph.test.ts:66` `dynRe` only matches `import(` preceded by `await `, `= `, `( `, `return ` or `: () => `.
   Verified misses in the current tree: `founder-phase.ts:72-73` (`Promise.all([\n import("next/headers"), import("@/lib/projects")` — `@/lib/projects` is `server-only`), `svi/dimensions/stream/route.ts:682` (`void import(…)`), `free-summary/route.ts:185-186`. Unmatched in general: `dynamic(() => import("…"))` (next/dynamic), `.then(() => import(…))`, `require("…")`, template-literal specifiers. Today the founder-phase chain is still caught via its later `await import("@/lib/supabase")`, so no false pass yet — but a client file that dynamically loads `@/lib/projects` inside a `Promise.all([` would slip through Gate 7 again.
   Fix: `const dynRe = /(?<!typeof\s*)\bimport\(\s*["'\`]([^"'\`$]+)["'\`]\s*\)(?!\s*\.[A-Za-z])/g;` (typeof-prefix and `.Type` suffix still exclude type positions) plus a `require\(\s*["']([^"']+)["']\)` pass. Cost stays negligible (walk is 0.3 s).

4. **`waitForLoadState("networkidle")` has no timeout inside a 20 s test** — `web/tests/e2e/smoke/post-deploy.spec.ts:270`. Default is the 30 s navigation timeout, so a page that keeps a beacon/poll alive never reaches idle, the `.catch` never fires, and the *test* timeout kills the check (a fail, not a skip). Passed 18/18 on this deploy. Fix: `waitForLoadState("networkidle", { timeout: 3_000 }).catch(() => {})`.

5. **Stale/orphaned doc comments after the split** — `web/src/lib/nav/founder-phase.ts:31-35` header still says the pure half lives here with no static server imports; `:111` is a dangling `/** analysis_json.totalSVI … */` JSDoc with no declaration and no trailing newline. Cosmetic; fix by pointing the header at `founder-phase-shared.ts` and deleting the orphan.

## Checked OK

- **`isCronAuthorised`** (`lib/security/cron-auth.ts`): unset/blank `CRON_SECRET` → `false` before any compare (`:89`); empty/whitespace bearer → `bearerToken` null; empty `x-cron-secret`/`?secret=` skipped (`if (header)`); non-strings impossible (`Headers.get` → string|null) and `safeEqualStrings` guards `typeof`; SHA-256 digests are fixed 32 bytes so `timingSafeEqual` never throws; utf8 hashing deterministic and the secret is ASCII, so lone-surrogate folding cannot produce a false match; all presented forms evaluated without early return.
- **Opt-in sets == pre-migration accepted forms**: 15 `x-cron-secret` routes + `server-cleanup` = 16 `xCronSecretHeader: true` (diffed against `git grep x-cron-secret 2e96296de` — identical, test files aside); `querySecret` only on `nightly-clevel-review` and `agent-research` (the two pre-existing `searchParams.get("secret")` readers). Minor widenings only: `Bearer` + extra/trailing whitespace now accepted; `agent-research` now accepts any correct form when another is wrong (it used to take the first present). Narrowings: raw-token (no `Bearer`) rejected on `agent-research`; `local-dev` literal gone.
- **`cronSecret()`** used only in `agent-auto-improve:169`, `agent-orchestrator:145`, `daily-admin-report:208` (outbound `Authorization` headers) and `nightly-clevel-review:28` (presence check for the 503 pre-gate). Never serialised into a response, log line or thrown error.
- **GET/POST surface unchanged** for all 80 routes (diffed export sets against `2e96296de`); every crontab endpoint (`crontab.production`, POST via `cron-runner.sh:100`) resolves to a route with `POST` or `export { GET as POST }`; the GET curlers (`ai-token-guardian.sh:67` → `ai-health`, `setup-cron-trial-warning.sh:19` → `trial-charge-warning`) hit routes that export `GET`; cloud routines POST `agent-deploy` with Bearer, whose accepted form is unchanged. `post-deploy-checks.sh:65-67` expectations (wrong/empty → 401, correct → not 401) match.
- **401 bodies**: colocated tests (30 files) all green; the shared table asserts only `error|reason == "unauthorized"` and the four rewritten "open when unset" tests now pin fail-closed + prefix rejection.
- **`getCurrentUser = cache(...)`** (`lib/auth.ts:377`): React 19.2.4 / Next 16.2.5. In route handlers and in the server-action execution phase there is no RSC render request, so the react-server dispatcher's `getCacheForType` hands back a fresh `Map` — i.e. uncached, per call, no cross-request leakage (route handlers never run inside React's synchronous `performWork` window). Inside one RSC render the memo spans `(app)` layout → `(founder)` layout → page, all reading the same cookie. No single handler calls it twice around a mutation: multi-hit files (`founder-crud.ts`, `journal`, `equity/plans`, …) are one call per exported method; no `"use server"` file that calls `getCurrentUser` writes `app_users`; `getCurrentUserTier` was already `cache`d. No `spyOn`/`.name` consumers.
- **founder-phase split**: `founder-phase-shared.ts` imports only `workflow-steps` and `phase-taxonomy`, neither of which imports back — no cycle; `export *` + named import of the same module is legal and TS-clean (`unlock-matrix.mts:32` still resolves through the re-export, `unlock-matrix.test.ts` green); the only client importer (`founder-nav-context.tsx`) now points at `-shared`; no other `"use client"` file imports `nav/founder-phase`.
- **Deploy warm-up** (`deploy-live.sh:1118-1126`): `if curl -sf …; then break; fi` inside `set -e` — a 404/500 just exhausts 3 tries × 2 s and moves on; the gate itself is Playwright's exit code.
- **Trial fixes**: `buildTrialQuota({...trial, used:0})` → `remaining 1` (pure `buildQuota`); `trial-end-reminder` uses the same `countTrialReportsUsed`, so email and banner agree; `timeZone: "Australia/Sydney"` matches `trial-report-banner.tsx:47`; `dns.lookup(..., { order: "verbatim" })` accepted on Node v22.23.2 (checked live), `outbound-url.test.ts` green.
- **Test cost**: `cron-auth.routes.test.ts` ≈ 8 s (80 route imports, 120 s hook budget); `client-safe-graph.test.ts` 0.83 s.
