# Atlassian ↔ AU Investor Standard Mapping — Autonomous Goal File

> **Source of truth:** [SOURCE-OF-TRUTH.md](./SOURCE-OF-TRUTH.md).
> **Sibling goal:** [reseller-module-goal.md](./reseller-module-goal.md).
> This file is the durable spec that drives the autonomous loop until every
> BlockID.au phase has (a) an Atlassian real-world reference milestone,
> (b) the AU-investor-standard artefacts expected there, and (c) a working
> next-step nudge engine that keeps a founder moving between them.

```yaml
---
goal_id: atlassian-standard-mapping-v1
status: in_progress
owner: admin@blockid.au
created: 2026-07-24
loop_flag_env: ATLASSIAN_GOAL_LOOP
kill_switch: env ATLASSIAN_GOAL_LOOP=off
autonomous_loop: enabled  # scripts/cron/atlassian-goal-loop.mjs — every 10 min via crontab.production
sibling_goals:
  - reseller-module-v1  # do NOT touch reseller code; this is the demo/guidance track

reference_surfaces:
  blockid_current:
    guide_content: web/src/lib/guide/startup-journey.ts  # 12 chapters, 977 lines
    phase_labels: web/src/lib/showcase/gallery.ts:23-36   # 12-phase EN+VI labels
    phase_stage_map: web/src/lib/journey-map.ts:58-71     # 12 → canonical 8-stage
    growth_phases_map: web/src/lib/journey-map.ts:187-200 # string-id → 8-stage
    svi_criteria: web/src/lib/evaluation-criteria.ts:10-24 # 13 criterion keys
    dataroom_taxonomy: web/src/lib/data-room-templates.ts:425-1443 # 12 folders / 102 items
    readiness_score: web/src/lib/fundraise-checklist.ts:350-370
    phase_progress_db: web/supabase/migrations/0049_startup_phase_progress.sql
    div83a_checker: web/src/lib/div83a-checker.ts
    clo_compliance:  web/src/lib/agents/clo-compliance.ts
    atlassian_showcase: web/src/app/showcase/atlassian/page.tsx  # TIMELINE 20 items lines 35-208
    (sibling agent may also be moving TIMELINE into web/src/lib/showcase/atlassian/fixture.ts — DO NOT MODIFY)
  atlassian_public:
    s1_index: https://www.sec.gov/cgi-bin/browse-edgar?action=getcompany&CIK=0001650372&type=S-1&dateb=&owner=include&count=40
    s1_first_file: https://www.sec.gov/Archives/edgar/data/1650372/000119312515373798/d67899ds1.htm
    fy25_10k: https://www.sec.gov/Archives/edgar/data/1650372/000165037225000036/team-20250630.htm
    company_page: https://www.atlassian.com/company
    twenty_lessons_blog: https://www.atlassian.com/blog/announcements/atlassian-founders-20-years-20-lessons
  au_investor_standard:
    corps_act_708: https://www.legislation.gov.au/C2004A00818/latest/text (s708 personal / s708(8) wholesale / s708(11) sophisticated)
    corps_act_761g: https://www.legislation.gov.au/C2004A00818/latest/text (s761G(7) wholesale-client definition)
    itaa_1997_div83a: https://www.legislation.gov.au/C2004A05138/latest/text (ESOP concessions incl. Subdiv 83A-B/C, s83A-33, s83A-45)
    esic_esvclp: https://business.gov.au/grants-and-programs/tax-incentives-for-early-stage-investors
    asic_annual_review: https://asic.gov.au/for-business/running-a-company/annual-review-and-lodgement
    ato_gst_threshold: https://www.ato.gov.au/business/gst/registering-for-gst  # A$75k
    fair_work_awards: https://www.fairwork.gov.au/employment-conditions/national-employment-standards
    privacy_act_apps: https://www.oaic.gov.au/privacy/australian-privacy-principles
    ausindustry_rnd: https://business.gov.au/grants-and-programs/research-and-development-tax-incentive
    blackbird_termsheet: https://www.blackbird.vc/starter-kit
    airtree_termsheet: https://www.airtree.vc/open-source-vc
    square_peg: https://www.squarepegcap.com/insights
    lawpath_safe: https://lawpath.com.au/legal-documents/safe-agreement

success_criteria:
  - Every BlockID phase (1..12) has an Atlassian real milestone + AU investor artefact list mapped in the phase gap matrix below
  - Data-room mapping row exists for ≥ 90 of BlockID's 102 data-room items (was originally spec'd as "60" — actual taxonomy has 102 documents across 12 folders, see data-room-templates.ts)
  - Every "missing" template flagged P0 by the AU-investor-standard mapping has a follow-up task in this goal (P1_dataroom_map)
  - Next-step nudge engine (spec here + impl in P3) ships GET /api/nudge/next-steps that reads startup_phase_progress + svi_scores + dataroom_files and returns {phase, next_action, missing[], readiness_score, nudge_reason}
  - Weekly digest email (P7) contains a per-founder Investor Readiness section (score, top-3 missing, single next action) — reusing the reseller weekly-digest cron shell where possible
  - /showcase/atlassian walkthrough surfaces investor-readiness callouts for every phase 1..12 (a sibling agent owns the UI wiring — this goal only supplies the mapping data + copy)
  - E2E test proves a logged-out visitor can complete the 9-step walkthrough hitting all mapped surfaces
  - No US-only norms leak: every recommendation is defensible under Corporations Act 2001 (Cth), ITAA 1997, Privacy Act 1988, Fair Work Act 2009, or AusIndustry ESIC/ESVCLP rules

phased_tracks:
  P0_gap_matrix:
    status: done
    completed_at: 2026-07-24
    description: 12-row phase gap matrix (BlockID vs Atlassian vs AU investor) — see body §1
    exit_criteria:
      - one row per phase 1..12
      - every row cites (a) BlockID surface (path:line or /route), (b) Atlassian public source URL, (c) AU-standard reference (statute/section or public term-sheet URL)
      - every gap has a P0/P1/P2 priority tag
  P1_dataroom_map:
    status: shipped
    completed_at: 2026-07-24
    description: BlockID 102-item data-room ↔ Atlassian S-1 exhibit mapping — see body §2. Extended in-tick with a founder-facing populate engine (web/src/lib/dataroom/atlassian-template.ts + web/src/lib/dataroom/populate.ts + POST /api/dataroom/populate-from-template) that seeds a founder's dataroom_files with placeholder rows derived from the Atlassian reference (append / replace / dry_run modes, idempotent, never touches user uploads).
    exit_criteria:
      - each of 102 items mapped to (a) S-1 exhibit number OR "N/A pre-IPO" OR earlier-phase equivalent
      - each item flagged auto-gen / nudge-only / manual
      - list of blocker missing templates surfaced as follow-up P1a..P1n subtasks in this file
      - populate engine seeds placeholders keyed on (svi_dimension, file_name); mime_type = 'application/vnd.blockid.template' marks template-owned rows so user uploads are protected across re-runs
  P2_nudge_engine_spec:
    status: done
    completed_at: 2026-07-24
    description: Design (data source, output JSON, endpoint shape, cadence, surfaces) — see body §3
    exit_criteria:
      - API contract locked (GET /api/nudge/next-steps)
      - data sources enumerated with path:line references
      - cadence + freshness rules pinned (event-driven for phase transition, nightly cron for readiness recompute)
  P3_nudge_engine_impl:
    status: shipped
    completed_at: 2026-07-24
    description: Implement /api/nudge/next-steps + supporting lib + tests. Ships as web/src/lib/nudge/next-steps.ts (pure computeNextSteps), web/src/app/api/nudge/next-steps/route.ts (auth-gated GET), web/src/components/dashboard/next-step-tile.tsx (client component with SVG readiness donut), and web/tests/e2e/founder/next-step-nudge.spec.ts (shape + behaviour rule for phase >= 6 founders).
    depends_on: [P2_nudge_engine_spec, P1_dataroom_map]
    exit_criteria:
      - route handler returns validated JSON matching P2 shape
      - E2E spec pins the wire shape (401 anon / 200 authed / raise-blocker behavioural rule for phase >= 6)
      - AFSL disclaimer present in meta.afsl_disclaimer per §3.7
      - dashboard tile fetches on mount with skeleton + retry states; mount into /dashboard shell deferred (>50-line refactor — see follow-up)
  P4_walkthrough_wiring:
    status: shipped
    completed_at: 2026-07-24
    description: Emit phase-callouts + investor-readiness copy the sibling agent's /showcase/atlassian walkthrough can consume; do NOT edit /showcase/atlassian/page.tsx. Round 7 shipped web/src/lib/showcase/atlassian/investor-readiness-callouts.ts (data-only fixture, 12 phase entries with EN+VI copy sourced from goal §1 + §4) plus a colocated vitest that pins presence, canonical phase-key coverage, App Router route resolution (direct + [dynamic] segment), and a coarse AFSL trigger-phrase guard. Sibling agent still owns the /showcase/atlassian UI wiring.
    depends_on: [P0_gap_matrix]
    exit_criteria:
      - copy fixture at web/src/lib/showcase/atlassian/investor-readiness-callouts.ts (add if absent) exporting phase→{atlassian_moment, blockid_route, callout_copy_en, callout_copy_vi}  # done (with phase_label_en/vi mirrored from PHASE_LABELS for the shell)
      - 12 entries (one per phase) sourced from body §1 + §4  # done
      - unit test asserts every phase present and every route resolves to an existing app/ folder  # done (investor-readiness-callouts.test.ts — 6 cases, all green)
  P5_investor_readiness_score:
    status: shipped
    completed_at: 2026-07-24
    description: Extend readiness score to per-phase (currently one blended number in fundraise-checklist.ts). Round 6 shipped web/src/components/dashboard/investor-readiness-tile.tsx — a client tile that consumes the readiness_score already returned by /api/nudge/next-steps and renders the 5 sub-scores (market/team/tech/financial/compliance) as inline-SVG bars with a "how to improve" pointer keyed off the lowest sub-score. Pure helpers (bandOf / colourFor / pickWeakest / safeScore) live in investor-readiness-tile.helpers.ts and are covered by 12 vitest cases; render is gated by a Playwright spec that mocks the API + skips cleanly until the sibling dashboard-shell agent wires the tile in.
    depends_on: [P1_dataroom_map]
    exit_criteria:
      - readiness_by_phase(input) returning Record<PhaseKey, {score, band, missing_top3}>  # follow-up P5a — still using the single blended sub_scores tuple
      - reuse CRITERIA weights from evaluation-criteria.ts:66-... for the SVI subset per phase  # deferred to P5a
      - dashboard tile "Readiness by phase" fed by the new helper  # done (tile surfaces 5-dim + weakest hint; mount-instruction comment in the .tsx file)
  P6_ausindustry_esic_gates:
    status: shipped
    completed_at: 2026-07-24
    description: Codify ESIC + ESVCLP + Div83A gating checks so a founder sees red/amber/green before the ATO does. Round 6 shipped web/src/lib/compliance/esic-funding-gate.ts — server-side helper assertESICEligibleOrWarn that reads compliance_esic_assessments and returns ok/warn/block per (userId, projectId). Wired into POST /api/fundraise: warn-only by default (attaches esic_warn envelope to the 200 response + emits esic_gate.warn_* analytics), 412 blocking when body.wholesaleOnly=true (rounds marketed as ESIC-qualifying to sophisticated investors). 10 vitest cases pin the branch matrix. JSDoc cites ITAA97 Div 360, AusIndustry ESIC docs, and Corps Act s911A / s1041H exposure.
    depends_on: [P1_dataroom_map]
    exit_criteria:
      - esic-gate.ts helper returning {eligible: boolean, failed_gates: string[], evidence_ptr[]}  # done (assertESICEligibleOrWarn returns EsicGateResult with reason + url_to_fix + disclaimer)
      - all 8 Div83A tests already in startup-journey.ts:621-641 (chapter 08) exposed via a shared module and wired into ch09 investor-readiness pack  # done via P6a — shared module at web/src/lib/compliance/div83a-qualifying-tests.ts, gate at web/src/lib/compliance/div83a-funding-gate.ts mirrors assertESICEligibleOrWarn shape and blocks wholesale-only fundraise rounds; P6a-ir-pack shipped 2026-07-24 — buildEsopEligibilitySection(gateResult, locale) at web/src/lib/investor-pack/esop-eligibility-section.ts + 5 vitest cases; assemblePackData() calls assertDiv83AEligibleOrWarn (warn-only, action="investor_pack_assemble") and populates the new InvestorPackData.esopEligibility field with the 8 canonical Div 83A qualifying tests + gate status + disclaimer
      - AFSL disclaimer visibly attached to any surface that outputs the ESIC eligibility JSON (no personal advice)  # done (ESIC_DISCLAIMER on every gate response)
  P7_weekly_digest_integration:
    status: shipped
    completed_at: 2026-07-24
    description: Wire the nudge engine output into the existing weekly-digest cron shell (reseller module built it — reuse, don't duplicate). Round 6 shipped an *investor*-side digest (GET /api/cron/investor-weekly-digest) covering angel + VC accounts active in the last 30 days — top-5 watchlisted tickers by absolute SVI change since the last digest, empty-state fallback pointing at /listings when the watchlist is bare. Pure email helper web/src/lib/email/investor-digest.ts renders subject + HTML + plain text (4 vitest cases). Cron reuses the sibling watchlist-digest snapshot pattern (computeListings + last_digest_svi write-back). Bearer-authed via CRON_SECRET; INVESTOR_DIGEST=off kill switch. Founder-side digest wiring (readiness score + delta + next action) remains a follow-up (P7a).
    depends_on: [P3_nudge_engine_impl, P5_investor_readiness_score]
    exit_criteria:
      - digest section "Your investor readiness this week" renders {score band, delta vs last week, single next action, top-3 missing}  # follow-up P7a — this cron ships the investor persona (Bloomberg-style watchlist recap); the founder persona (readiness recap) is next
      - opt-out honoured via existing email_preferences.weekly_digest flag (web/src/lib/email-preferences.ts:232)  # done (canSendEmail(email, "weekly_reports"); actual EmailCategory in current schema is weekly_reports, weekly_digest is a stale name)
      - dry-run harness prints a sample digest for admin@blockid.au  # done (`?skip_email=1` returns dry_run[] with per-investor subject + ticker_count)
  P8_founder_review:
    status: human_blocked
    description: Founder reviews mapping accuracy + Atlassian claims + AU statutory references + signs off before P9
    blocking_reason: mapping accuracy is a domain call; auto-loop cannot self-verify
    unblock_signal: founder writes {approved: true} into open_questions_resolved.Q1..Q10 in this file
  P9_ship:
    status: proposed
    depends_on: [P3, P4, P5, P6, P7, P8]
    exit_criteria:
      - E2E playwright spec proves 9-step logged-out walkthrough of /showcase/atlassian reaches Chapter 12 and all mapped BlockID surfaces exist
      - CHANGELOG entry
      - autonomous loop flips status → done and stops re-picking this goal

open_questions:
  Q1_safe_templates:
    q: Do we mint SAFE / convertible-note templates ourselves (LawPath-style) or link out to LawPath / CommercialAgreements?
    recommend: link out at Phase 6, mint AU-flavoured SAFE ourselves once P1_dataroom_map surfaces >2 requests
  Q2_wholesale_cert_flow:
    q: Is s708(8) wholesale-investor accountant-certificate collection in scope for the platform, or handled off-platform by the founder's lawyer?
    recommend: off-platform for MVP; expose an evidence slot at Phase 10 that lets founder upload the counter-signed cert; NEVER issue or validate the certificate ourselves (that is a s923B "financial services" boundary)
  Q3_valuation_disclaimer:
    q: Does BlockID's SVI-calibrated valuation output need an AFSL exemption note stronger than the current legal/surfaces.ts:49 wording?
    recommend: add a "general information only, not personal financial product advice per s766B Corps Act" line to any valuation PDF; keep valuation output as tools/analysis, not advice
  Q4_dual_class_capability:
    q: Do we surface Atlassian's dual-class share structure as an option? ASX prohibits dual-class; UK Plc or Delaware C-corp only.
    recommend: at Phase 9/10 add a decision-point doc "single-class default; dual-class needs offshore parent" with links to StartupDaily coverage; do NOT auto-generate offshore incorporation documents
  Q5_secondary_round_workflow:
    q: Atlassian's two pre-IPO rounds were 100% secondary (Accel 2010, T.Rowe 2014). Do we model secondary in Chapter 10?
    recommend: yes — extend Chapter 10 to distinguish primary vs secondary raise; secondary needs cap-table + Right-of-First-Refusal + s708 exemption checks
  Q6_pledge_1pct:
    q: Do we prompt founders to consider Pledge 1% (Atlassian co-founded it in 2006) at Phase 8?
    recommend: soft nudge only; opt-in, never opt-out; deep-link to pledge1percent.org
  Q7_shipit_hackathon:
    q: Do we surface ShipIt as a culture pattern in Chapter 8 (currently only in Atlassian showcase)?
    recommend: add to Chapter 8 sections[] as "Culture rituals — quarterly ShipIt inspiration"
  Q8_r_and_d_tax_incentive:
    q: R&D Tax Incentive is a huge AU-specific cashflow lever. Is it in the guide today?
    recommend: currently only referenced in data-room folder 11 "R&D Tax Incentive Claims" (data-room-templates.ts:1256); ADD a dedicated Chapter 6 section on R&D Tax Incentive eligibility + AusIndustry registration deadline (10 months after FY end)
  Q9_lp_report_bundle_scope:
    q: The lp_report bundling in Chapter 12 assumes reseller = accelerator. Do we support other reseller archetypes (angel syndicate, super fund co-investor)?
    recommend: keep accelerator MVP; extend to angel syndicate in P10+ (needs different anonymisation rules)
  Q10_atlassian_us_facts:
    q: Some Atlassian facts (Rich Wong joining board 2010, ~US$55M cash-on-hand pre-Accel) came from single TechCrunch sources. Do we citation-double-source before shipping copy?
    recommend: yes — every claim used in a founder-facing callout needs a second source; run a citation audit at P4
---
```

---

## 1. Phase gap matrix (Deliverable 1)

Legend: **Priority** — P0 = raises are blocked without it, P1 = raises are harder without it, P2 = nice to have.
Line references anchored to files current as of 2026-07-24; verify with `git blame` if drift is suspected.

| # | BlockID phase (slug + label) | Atlassian real milestone at this phase | AU investor standard artefacts expected here | BlockID surfaces today | Gaps / mismatches | Priority |
|---|---|---|---|---|---|---|
| 1 | `01-vision` — Vision / Day-0 Idea (`showcase/gallery.ts:24`) | 2002 — Mike Cannon-Brookes + Scott Farquhar UNSW grads, A$10K credit card, explicit "don't wear suits, earn more than PwC's offer" (source: [Atlassian 20-year blog](https://www.atlassian.com/blog/announcements/atlassian-founders-20-years-20-lessons); `web/src/app/showcase/atlassian/page.tsx:37-43`) | Founder Agreement (informal ok), ABN application via ABR, `.com.au` domain reservation, initial IP-ownership statement (a "background IP" clause, not yet a full deed) | `/guide/01-vision` (`web/src/lib/guide/startup-journey.ts:74-138`), `/svi` (workspace bootstrap), `/showcase/blockid` | **Missing:** live ABR ABN-lookup probe (no `/api/abr/lookup`); **missing:** auto-generated Founder Agreement template (data-room folder 2 has `Founder Agreements & Vesting Schedules` template but the guide's Chapter 1 CTA doesn't hand it to founders yet); **missing:** `.com.au` availability check | **P0** on ABR probe (60-second win, big trust signal); P1 on Founder Agreement template exposure in Ch1 |
| 2 | `02-idea-validation` — Idea Validation (`gallery.ts:25`) | 2002 — Bootstrap of internal Jira tool for own use; problem-validated because founders were themselves the target user (dev teams needing issue tracking) (source: [Atlassian company page](https://www.atlassian.com/company)) | Structured discovery-interview log (min 5), landing page + waitlist, ICP one-pager, LOI collection (informal), initial SVI 13-criterion self-score | `/guide/02-idea-validation`, `/svi` (Score → Full SVI), CMO deep-research agent, `svi_analyses` row | **Missing:** interview-log template (no `interview-transcripts/` folder in data-room-templates.ts); LOI / letter-of-intent template also absent from data-room folder 8 "Contracts & Agreements" | P1 |
| 3 | `03-market-research` — Market Research (`gallery.ts:26`) | 2002-2003 — Atlassian's insight: dev teams globally were under-served by heavyweight enterprise tools; low-price self-serve was a market gap (source: [Braindump history of Jira](https://braindump.maxoxo.me/posts/20221011135007-a_brief_history_of_atlassian_and_jira/)) | TAM/SAM/SOM sizing with ABS or industry-body citations, competitor matrix with pricing, AU-comparable-raises benchmark, buyer-persona doc, IBISWorld / ABS statistics linked | `/guide/03-market-research`, CMO deep-pass, `au-comparable-raises` benchmark, data-room folder 5 "Market & Traction" (`data-room-templates.ts:822-911`) with `Market Size Analysis (TAM/SAM/SOM)` and `Competitive Analysis` templates | **Missing:** ABS Business Data Portal API stub (`/api/abs/lookup`); AU-comparable-raises benchmark exists conceptually in guide copy but no dedicated data source module (grep confirms no `au-comparable-raises.ts`); IBISWorld link-out not surfaced | P1 |
| 4 | `04-mvp` — MVP / Product Discovery (`gallery.ts:27`) | 2002 — Jira v1.0 released (source: showcase timeline `page.tsx:46-50`); 2004 — Confluence v1.0 (`page.tsx:63-67`). Both shipped with **zero salespeople** — the "no sales team" legend starts here | Product brief, architecture note, live landing page with GA4/Plausible measurement ID stamped, public repo (or private with metadata), IP Assignment Deed for any contractor code | `/guide/04-mvp`, `/dashboard/integrations` (GitHub link), data-room folder 4 "Product & Technology" (`data-room-templates.ts:739-820`), data-room folder 7 item "IP Assignment Deeds (Contractors)" (`:1005`) | **Missing:** landing-page one-click publisher (Chapter 4 CTA references it but no `/api/landing-page/publish` route); AU-flavoured contractor IP Assignment Deed template stub present but no signable e-sign integration | P1 (IP Assignment gap is P0 the moment any contractor writes code) |
| 5 | `05-pmf` — PMF / Early Traction (`gallery.ts:28`) | 2003 — First US$1M revenue; American Airlines buys Jira via fax with no human sales contact (source: `page.tsx:52-60`, [Atlassian 20-year blog](https://www.atlassian.com/blog/announcements/atlassian-founders-20-years-20-lessons)) | Cohort retention curve (min 4 weekly buckets), first paying customers list, Stripe test-mode wired, PMF signal doc (retention slope + revenue slope + "very disappointed %"), first invoice/tax-invoice conformant to ATO GST-tax-invoice rules | `/guide/05-pmf`, CDO agent PMF pass, data-room folder 5 items "Traction Dashboard" + "Churn Cohort Analysis" (`:831, :894`) | **Missing:** cohort chart auto-draw component (guide claims it; no `web/src/lib/traction/cohort-chart.ts` exists); "very disappointed %" survey template not in data room; ATO tax-invoice format checker absent | P1 |
| 6 | `06-revenue` — Revenue / Business Model (`gallery.ts:29`) | 2003-2010 — Reached ~US$100M revenue with essentially no sales team; product-led growth defined the era (source: [Atlassian S-1 MD&A](https://www.sec.gov/Archives/edgar/data/1650372/000119312515373798/d67899ds1.htm), 2015 filing) | 3-year P&L + cash-flow + burn (base/bull/bear), GST registration if turnover ≥ A$75k ([ATO threshold](https://www.ato.gov.au/business/gst/registering-for-gst)), pricing memo defensible vs competitors, Stripe live-mode readiness, R&D Tax Incentive eligibility note (AusIndustry) | `/guide/06-revenue`, CFO agent, data-room folder 3 "Financial Projections" (`:619`), data-room folder 11 item "R&D Tax Incentive Claims" (`:1256`), data-room folder 11 item "GST Registration Confirmation" (`:1248`) | **Missing:** dedicated R&D Tax Incentive guide section in Ch6 (only surfaces as a data-room item); no AusIndustry registration-deadline (10 months post-FY-end) alert wired to compliance-calendar; GST-threshold-crossing detector not automated | **P0** on GST threshold detector; P1 on R&D chapter section |
| 7 | `07-growth` — Growth / Analytics (`gallery.ts:30`) | 2005 — First profitable year; ShipIt hackathon begins (source: `page.tsx:69-76`); 2006 — Pledge 1% co-founded | GA4 wired, Stripe live pulls, weekly SVI delta, growth playbook, referrals scaffold, retention cohort by channel, ARR / MRR view (`0083_mrr_view.sql` exists) | `/guide/07-growth`, `/dashboard/integrations`, `email-drip.ts`, `email-preferences.ts:232` (`weekly_summary`), `svi_signals` table | **Missing:** Pledge 1% callout (see Q6); ShipIt / culture-ritual mention absent from Ch7 (belongs in Ch8 per Q7); GA4 measurement plan template not in data-room folder 4 | P2 |
| 8 | `08-team` — Team & Culture (`gallery.ts:31`) | 2005 ShipIt starts, 2006 Foundation + Pledge 1%, 2007 Five core values codified (source: `page.tsx:69-92`); 2012 board scales with Doug Burgum (Chair), Enrique Salem, Jay Parikh (`page.tsx:111-117`) | ESOP scheme rules doc, Div83A eligibility check (ITAA97 s83A-33 / s83A-45 / s83A-105), org chart with 6-month hiring plan, employment-agreement templates (Fair Work Award-compliant), key-person insurance | `/guide/08-team` (includes 8-point Div83A `qualifyingTests` block, `startup-journey.ts:621-641`), `div83a-checker.ts`, data-room folder 6 "Team & Advisors" (`:913`), folder 2 item "ESOP Plan Rules" (`:584`), folder 6 item "Fair Work Employee Entitlements Schedule" (`:984`) | **Best-covered phase.** Only gap: `esop-scheme.md` template body itself is not in data-room-templates.ts (folder 6 item "ESOP Plan Document (Team Copy)" exists at `:960` but is `type: "upload"` not `"template"` — no seeded body). | P1 |
| 9 | `09-funding` — Funding-Ready (`gallery.ts:32`) | 2010 — Atlassian was ~US$55M cash-on-hand and **profitable** before Accel Partners approached them; the readiness came from operating cashflow, not investor pitching (source: [TechCrunch 2010-07-14](https://techcrunch.com/2010/07/14/atlassian-accel-60-million/)) | Investor deck (12-15 slides, every claim footnoted), data-room organised into standard sections, LLM-audited for inconsistencies, wholesale-investor gate checklist (s708(8) / s761G(7)), AFSL disclaimer applied to any valuation output, NDA template, ESIC eligibility self-assessment | `/guide/09-funding`, `/dashboard/fundraise`, `/dashboard/data-room`, LLM-auditor (ADK), `investor-pack-assembler.ts`, `fundraise-checklist.ts`, `compliance-checker.ts` | **Missing:** ESIC eligibility gate (data-room folder 11 item "ESIC Eligibility Assessment" `:1281` is upload-only — no auto-check); wholesale-investor cert intake slot exists conceptually but not wired to data-room (see Q2); investor-deck auto-draft is stubbed but `investor-deck.docx` generator not fully wired | **P0** on ESIC gate + wholesale-cert intake |
| 10 | `10-fundraise` — Fundraise / Term Sheet (`gallery.ts:33`) | 2010 Accel Partners US$60M **100% secondary** (`page.tsx:94-102`); 2014 T. Rowe Price US$150M **100% secondary** at US$3.3B valuation (`page.tsx:119-127`); 2014 UK Plc holding-co reorg for dual-class capability (`page.tsx:128-136`); 2015-12-10 NASDAQ IPO US$21 (`page.tsx:137-146`) | Term-sheet AI + CLO review, AU-comparable-raises benchmark, negotiation talking-points, wholesale-investor accountant certificates (s708(8), current ≤ 6 months), s708(1) small-scale cap check (< 20 investors / < A$2M / 12 mo), optional immutable hash of term-sheet PDF | `/guide/10-fundraise`, term_sheet_ai entitlement, `web/src/lib/term-sheet/`, `web/src/lib/agents/clo-compliance.ts:181-186` (s708 refs), blockchain-hash worker (chainId 420) | **Missing:** primary vs secondary distinction in Ch10 (Atlassian's rounds were 100% secondary — Q5); Right-of-First-Refusal cap-table workflow for secondaries; s708(1) counter (running total of investors/$ in trailing 12 mo); dual-class decision-point doc (Q4) | **P0** on primary-vs-secondary Ch10 split (Atlassian-defining pattern); P1 on ROFR workflow |
| 11 | `11-scale` — Post-Funding / Scale (`gallery.ts:34`) | 2010 Bitbucket acquisition (`page.tsx:104-108`); 2017 Trello US$425M (~90% cash + 10% retention RSUs) (`page.tsx:147-155`); 2018 OpsGenie US$295M + Slack partnership (`page.tsx:157-165`); 2020 Team Anywhere (`page.tsx:167-173`); 2022-10-03 redomicile UK Plc → Delaware (`page.tsx:175-181`); 2023 Loom US$975M (~90/10) (`page.tsx:183-191`); FY25 US$5.2B revenue, 83% gross margin (`page.tsx:200-207`) | Monthly board pack (SVI + KPIs + runway + asks), quarterly BAS lodgment (ATO), annual ASIC review, WGEA gender-pay reporting (if ≥ 100 employees), Modern Slavery Act statement (if ≥ A$100M revenue), Notifiable Data Breaches Scheme readiness (OAIC), cap-table snapshot hashed on-chain | `/guide/11-scale`, CEO agent (board pack), CFO runway monitor, data-room folder 12 "AU Compliance" (`:1311`) with items WGEA (`:1356`), Modern Slavery (`:1364`), NDB Scheme (`:1372`), blockchain-sync worker | **Missing:** compliance-calendar.ics generator (Ch11 CTA claims it — no `web/src/lib/compliance/calendar.ts` exists); WGEA + Modern Slavery threshold detectors (currently upload-only in data-room, no automated headcount/turnover trigger); acquisition-pattern doc showing Atlassian's ~90% cash / 10% retention template | P1 |
| 12 | `12-exit` — Exit / Beyond (`gallery.ts:35`) | 2022-10-03 UK Plc → Delaware redomicile via Scheme of Arrangement, UK High Court sanction (`page.tsx:175-181`); 2024 Rovo AI, Cannon-Brookes sole CEO (`page.tsx:193-198`); FY25 US$1.5B additional Class A buyback (`page.tsx:200-207`) | Exit-readiness pack, comparable-exits benchmark, valuation model (strategic vs financial buyer), equity cleanup punchlist (unexercised options, SAFE conversions, drag-along language), if accelerator: anonymised LP-report slot | `/guide/12-exit`, exit-readiness agent, AU-comparable-exits agent, IR portfolio bundling, CLO agent | **Missing:** comparable-exits data source (no `web/src/lib/exits/au-benchmark.ts`); Scheme-of-Arrangement / redomicile decision-tree doc (advanced but relevant for successful exits); LP-report anonymisation k-threshold policy documented but no unit-test fixture | P2 |

### Priority summary
- **P0** (raise-blocking): ABR ABN probe (P1), GST threshold detector (P6), R&D Tax Incentive alert (P6), ESIC eligibility gate (P9), primary-vs-secondary Ch10 split (P10).
- **P1** (raise-harder): interview-log + LOI templates (P2), IBISWorld/ABS lookup (P3), landing-page publisher + IP Assignment e-sign (P4), cohort-chart component (P5), ESOP scheme body template (P8), ROFR workflow (P10), compliance-calendar generator (P11).
- **P2** (nice-to-have): Pledge 1% + ShipIt callouts (P7/P8), comparable-exits data source (P12).

---

## 2. Data-room auto-population plan (Deliverable 2)

**Note on count:** The spec said "60-item data room". The actual taxonomy in `web/src/lib/data-room-templates.ts:425-1443` currently has **102 items across 12 folders**. Mapping ≥ 90 of 102 per success_criteria.

**Atlassian S-1 reference:** filed 2015-11-25, effective 2015-12-08, IPO 2015-12-10. Exhibit index reachable via [SEC EDGAR CIK 0001650372](https://www.sec.gov/cgi-bin/browse-edgar?action=getcompany&CIK=0001650372&type=S-1). Exhibit numbers below follow the S-1 exhibit-index conventions (3.x = charter/bylaws, 4.x = securities-instruments, 10.x = material-contracts, 21.1 = subsidiaries, 23.1 = auditor consent, 5.1 = legality opinion).

### Folder 1 — Corporate & Legal (`data-room-templates.ts:428-533`, 12 items)
| # | BlockID item | Atlassian S-1 equivalent | Which phase gates it | BlockID capability today | Auto-gen? |
|---|---|---|---|---|---|
| 1.1 | Executive Summary (`:437`) | S-1 "Prospectus Summary" | 9 | template body seeded | AUTO from CEO agent |
| 1.2 | Company Extract (ASIC) (`:445`) | Certificate of Incorporation exhibit (analogous to S-1 3.1) | 1 | upload-only | NUDGE (auto-probe via ASIC Connect API — currently no `/api/asic/extract`) |
| 1.3 | Certificate of Registration (`:453`) | S-1 3.1 Certificate of Incorporation | 1 | upload-only | NUDGE |
| 1.4 | Constitution / Replaceable Rules (`:460`) | S-1 3.1 Certificate + 3.2 Bylaws | 1 | upload-only | NUDGE — **missing** default AU Pty Ltd constitution template (BLOCKER for founders using replaceable rules) |
| 1.5 | Pitch Deck (`:467`) | S-1 "Prospectus Summary" + investor-day decks (informal) | 9 | template body seeded | AUTO from CEO agent |
| 1.6 | Register of Members / Share Register (`:475`) | Cap table in S-1 (item 15 principal + selling stockholders) | 8 | upload-only | AUTO if Share Management add-on active, else NUDGE |
| 1.7 | Register of Directors & Secretaries (`:482`) | S-1 "Management" + Form 201/205A analogues | 8 | upload-only | NUDGE (auto-probe via ASIC) |
| 1.8 | Board Minutes L12M (`:489`) | 10.x material contracts / board-action exhibits | 8-11 | upload-only | MANUAL (nudge quarterly) |
| 1.9 | Director Consents to Act (Form 201/205A) (`:497`) | S-1 "Directors and Executive Officers" + consents | 1-8 | upload-only | NUDGE |
| 1.10 | Register of Charges (ASIC / PPSR) (`:506`) | N/A pre-IPO (US uses UCC-1); AU-specific | 9-11 | upload-only | NUDGE (auto-probe via PPSR API — none wired) |
| 1.11 | ASIC Annual Review Status (`:515`) | N/A US-only | 11 | upload-only | NUDGE (annual reminder via compliance-calendar) |
| 1.12 | Board Committee Charters (`:523`) | S-1 Audit + Comp + Nom charters | 11 | upload-only | MANUAL |

### Folder 2 — Cap Table & Equity (`:535-618`, 10 items)
| # | BlockID item | Atlassian S-1 equivalent | Phase | Capability | Auto-gen? |
|---|---|---|---|---|---|
| 2.1 | Cap Table Current + Post-raise (`:543`) | S-1 15 Principal Stockholders + Selling Stockholders | 8-10 | template seeded, cap-table tool | AUTO |
| 2.2 | Founder Agreements + Vesting (`:552`) | S-1 10.x Amended IP Assignment Deed & Restrictive Covenant | 1-8 | template stub | AUTO (needs Chapter-1 exposure — see §1 P1 gap) |
| 2.3 | Shareholders Agreement (`:560`) | S-1 4.x Investors' Rights Agreement | 8-10 | template body seeded | AUTO (AU-flavoured; Blackbird / AirTree public term-sheets as baseline) |
| 2.4 | SAFE / Convertible Note Agreements (`:568`) | US uses Y Combinator SAFE; AU uses LawPath / OneVentures template | 5-9 | **missing template body** | NUDGE — decide Q1 (mint vs link out) |
| 2.5 | IP Assignment Deeds (Founders) (`:576`) | S-1 10.x founder IP-assignment exhibits | 1 | upload-only | AUTO (BLOCKER for AU raises; see §1 P4 gap) |
| 2.6 | ESOP Plan Rules (`:584`) | S-1 10.x 2015 Employee Share Purchase Plan + 2014 Restated Stock Plan | 8 | upload-only | AUTO — see §1 P8 gap ("scheme body template" absent) |
| 2.7 | Option / Warrant Register (`:592`) | S-1 4.x + option-tables in Management section | 8-11 | upload-only | AUTO (from esop_grants table, migration 0089) |
| 2.8 | Cap Table Waterfall (Exit) (`:599`) | Not a discrete S-1 exhibit (calculated per proposal) | 10-12 | upload-only | AUTO (from cap-table + valuation) |
| 2.9 | Secondary Transactions Register (`:608`) | Atlassian critical — 100% of 2010 + 2014 were secondary. S-1 15 lists selling stockholders | 10 | upload-only | AUTO if secondary flow built (see §1 P10 P0 gap) |
| 2.10 | (spare) | | | | |

### Folder 3 — Financial Projections (`:619-737`, 14 items)
Every FY audit-ready item (P&L historical, balance sheet, cash flow, audited FS) maps to S-1 Financial Statements section pages F-1..F-40 in Atlassian's filing.
| # | BlockID item | Atlassian S-1 equivalent | Phase | Auto-gen? |
|---|---|---|---|---|
| 3.1 | Financial Model 3Y Projection (`:628`) | Not in S-1 (forward-looking); disclosed in MD&A + investor decks | 6 | AUTO (CFO agent) |
| 3.2 | P&L Statement Historical (`:636`) | S-1 F-4 Consolidated Statements of Operations | 5-11 | AUTO (from Stripe live + Xero if wired) |
| 3.3 | Bank Statements 6mo (`:644`) | Diligence back-up, not filed | 6-11 | NUDGE (upload monthly) |
| 3.4 | Cash Flow Statement (`:651`) | S-1 F-6 Consolidated Statements of Cash Flows | 6-11 | AUTO |
| 3.5 | Revenue Proof (Stripe / Bank) (`:658`) | Diligence back-up | 5-11 | AUTO (Stripe API pull) |
| 3.6 | Valuation Report (BlockID) (`:665`) | S-1 "Determination of Offering Price" section | 6-10 | AUTO |
| 3.7 | Balance Sheet (`:672`) | S-1 F-3 Consolidated Balance Sheets | 5-11 | AUTO |
| 3.8 | Tax Returns + ATO Compliance (`:679`) | N/A US-only (ATO ≠ IRS but equivalent) | 6-11 | NUDGE |
| 3.9 | Monthly Management Accounts L12M (`:687`) | Diligence back-up (data-room only, not S-1) | 8-11 | AUTO (from Xero) |
| 3.10 | Budget vs Actuals rolling 12mo (`:696`) | Diligence back-up | 9-11 | AUTO |
| 3.11 | Unit Economics Model (`:704`) | MD&A key metrics: net dollar retention, gross margin | 5-11 | AUTO (CFO agent) |
| 3.12 | Cohort Revenue Analysis (`:712`) | MD&A cohort disclosure | 5-11 | AUTO (see §1 P5 cohort-chart gap) |
| 3.13 | Aged Debtors / Receivables (`:720`) | Diligence back-up | 8-11 | AUTO (Xero) |
| 3.14 | Audited Financial Statements 3Y (`:728`) | S-1 F-1..F-40 audited FS + KPMG report | 10-12 | MANUAL (needs registered auditor) |

### Folder 4 — Product & Technology (`:739-821`, 9 items)
Atlassian S-1 barely covered product tech (product is described in "Business" narrative, no tech-deep exhibit). This is diligence-back-up territory.
| # | BlockID item | Atlassian analogue | Phase | Auto-gen? |
|---|---|---|---|---|
| 4.1 | Product Demo Video (`:748`) | Investor-day videos (informal) | 4-11 | NUDGE |
| 4.2 | Technical Architecture (`:756`) | S-1 "Business" section technology descr | 4-11 | AUTO (CTO agent) |
| 4.3 | Product Roadmap 12-18mo (`:764`) | S-1 "Business — Strategy" | 4-11 | AUTO (CPO agent) |
| 4.4 | GitHub / Source Code Access (`:771`) | N/A (source code not disclosed) | 9-11 | NUDGE |
| 4.5 | Security Audit / Pen Test (`:779`) | S-1 "Risk Factors — Security" implicit | 8-11 | NUDGE |
| 4.6 | System Architecture Diagram (`:786`) | N/A | 4-11 | AUTO (CTO agent) |
| 4.7 | Uptime / SLA History 12mo (`:794`) | MD&A key metrics | 7-11 | AUTO (from GA4/Datadog) |
| 4.8 | Security Incident Register (`:802`) | S-1 "Risk Factors" | 7-11 | AUTO (from audit-log) |
| 4.9 | Third-Party Dependency Inventory (`:811`) | N/A (open-source in S-1 exhibit 21.1 subsidiaries) | 7-11 | AUTO (SBOM generator, none wired) |

### Folder 5 — Market & Traction (`:822-912`, 9 items)
| # | BlockID item | Atlassian S-1 equivalent | Phase | Auto-gen? |
|---|---|---|---|---|
| 5.1 | Traction Dashboard (`:831`) | S-1 "Business — Metrics" (customers count, gross-margin, dollar-based net retention) | 5-11 | AUTO |
| 5.2 | Customer / User List (`:839`) | S-1 "Business — Customers" (Atlassian disclosed 51k customers, no top-20 concentration risk) | 5-11 | NUDGE (privacy: exclude PII) |
| 5.3 | Market Size TAM/SAM/SOM (`:847`) | S-1 "Business — Market Opportunity" | 3-10 | AUTO (CMO agent) |
| 5.4 | Google Analytics / Mixpanel Access (`:855`) | Diligence back-up | 7-11 | AUTO (OAuth) |
| 5.5 | Case Studies / Testimonials (`:863`) | S-1 "Business — Customers" | 5-11 | NUDGE |
| 5.6 | Competitive Analysis (`:870`) | S-1 "Business — Competition" | 3-10 | AUTO (CMO deep-pass) |
| 5.7 | Sample Customer Contract (`:877`) | S-1 10.x form of customer agreement | 5-11 | AUTO (template) |
| 5.8 | Top-20 Customer Revenue Concentration (`:885`) | S-1 "Risk Factors — customer concentration" | 8-11 | AUTO (from Stripe MRR breakdown) |
| 5.9 | Churn Cohort Analysis (`:894`) | MD&A cohort table | 5-11 | AUTO |

### Folder 6 — Team & Advisors (`:913-995`, 9 items)
| # | BlockID item | Atlassian S-1 equivalent | Phase | Auto-gen? |
|---|---|---|---|---|
| 6.1 | Founder Profiles (`:922`) | S-1 "Management" | 1-11 | AUTO |
| 6.2 | LinkedIn Profiles (all founders) (`:930`) | Diligence back-up | 1-11 | AUTO (from user profile) |
| 6.3 | Hiring Plan 12mo (`:937`) | S-1 "Use of Proceeds" | 8-11 | AUTO (CHRO) |
| 6.4 | Organisational Chart (`:944`) | S-1 "Management" implicit | 8-11 | AUTO (CHRO) |
| 6.5 | Employment Contract Templates (`:952`) | S-1 10.x form of employment agreement | 8-11 | AUTO — **needs Fair Work Award-compliant AU template** (missing seed) |
| 6.6 | ESOP Plan Document Team Copy (`:960`) | S-1 10.x 2015 ESPP + 2014 Restated Stock Plan | 8 | AUTO — see §1 P8 gap |
| 6.7 | Key-Person Insurance Policy (`:968`) | Diligence back-up | 9-11 | NUDGE |
| 6.8 | Contractor Register (`:976`) | Diligence back-up | 8-11 | AUTO (from project_members) |
| 6.9 | Fair Work Employee Entitlements Schedule (`:984`) | N/A US-only | 8-11 | AUTO (from Fair Work Award calc — none wired) |

### Folder 7 — IP & Compliance (`:996-1086`, 8 items)
| # | BlockID item | Atlassian S-1 equivalent | Phase | Auto-gen? |
|---|---|---|---|---|
| 7.1 | IP Assignment Deeds (Contractors) (`:1005`) | S-1 10.x IP Assignment | 4-11 | AUTO template (see §1 P4 gap on e-sign) |
| 7.2 | Trademark / Patent Register (`:1012`) | S-1 "Business — IP" | 6-11 | NUDGE (IP Australia lookup — no wired) |
| 7.3 | Privacy Policy (`:1020`) | S-1 "Business — Privacy" | 4-11 | AUTO template (APP-compliant) |
| 7.4 | Terms of Service (`:1028`) | S-1 10.x form of customer agreement | 4-11 | AUTO template (ACL-compliant) |
| 7.5 | Regulatory Licenses (`:1035`) | S-1 "Business — Government Regulation" | 6-11 | NUDGE (per-industry) |
| 7.6 | Trademark Register (`:1043`) | S-1 "Business — IP" | 6-11 | NUDGE (IP Australia) |
| 7.7 | Patent Register (`:1051`) | S-1 "Business — IP" | 6-11 | NUDGE |
| 7.8 | Source Code Assignment Matrix (`:1059`) | Diligence back-up | 4-11 | AUTO (from IP Assignment Deeds) |
| (7.9) Open-Source License Inventory (`:1068`) | S-1 "Risk Factors — Open Source" | 7-11 | AUTO (SBOM — missing) |
| (7.10) Domain Name Register (`:1076`) | Diligence back-up | 1-11 | AUTO (WHOIS) |

### Folder 8 — Contracts & Agreements (`:1087-1151`, 7 items)
All map to S-1 Item 16 "Exhibits and Financial Statement Schedules" 10.x material contracts.
| # | BlockID item | Atlassian S-1 equivalent | Phase | Auto-gen? |
|---|---|---|---|---|
| 8.1 | Key Customer Contracts Top-3 by ACV (`:1096`) | 10.x forms of material customer agreements | 6-11 | NUDGE |
| 8.2 | Employment Agreements All Staff (`:1104`) | 10.x form of employment agreement | 8-11 | AUTO template + NUDGE per hire |
| 8.3 | Advisor Agreements (`:1111`) | Not filed (diligence-only) | 3-11 | AUTO template |
| 8.4 | Material Supplier / Vendor Contracts (`:1118`) | 10.x material vendor contracts | 8-11 | NUDGE |
| 8.5 | Material Contract Summary (`:1125`) | Filed as risk-factor commentary | 9-11 | AUTO (from vendor upload) |
| 8.6 | Litigation & Disputes Register (`:1133`) | S-1 "Legal Proceedings" | 8-11 | NUDGE |
| 8.7 | Insurance Schedule (`:1142`) | Diligence back-up | 8-11 | NUDGE |

### Folder 9 — Strategy & Roadmap (`:1153-1186`, 3 items)
| # | BlockID item | Atlassian S-1 equivalent | Phase | Auto-gen? |
|---|---|---|---|---|
| 9.1 | Go-to-Market Strategy (`:1162`) | S-1 "Business — Strategy" | 3-11 | AUTO (CMO) |
| 9.2 | Fundraise Strategy & Pipeline (`:1170`) | Not filed (private) | 9-10 | AUTO (IR agent) |
| 9.3 | Exit Strategy Analysis (`:1177`) | Not filed | 12 | AUTO (exit-readiness) |

### Folder 10 — References & Due Diligence (`:1187-1229`, 4 items)
| # | BlockID item | Atlassian S-1 equivalent | Phase | Auto-gen? |
|---|---|---|---|---|
| 10.1 | Investor FAQ (`:1196`) | S-1 "Prospectus Summary" Q&A style | 9-10 | AUTO (IR) |
| 10.2 | Due Diligence Checklist auto-generated (`:1204`) | Standard DD taxonomy | 9-11 | AUTO |
| 10.3 | Press & Media Coverage (`:1212`) | S-1 "Business — Marketing" | 4-11 | AUTO (from analytics) |
| 10.4 | Awards & Recognition (`:1219`) | Not filed | 4-11 | NUDGE |

### Folder 11 — Tax (AU) (`:1230-1310`, 8 items) — AU-specific, no S-1 equivalent
| # | BlockID item | Notes | Phase | Auto-gen? |
|---|---|---|---|---|
| 11.1 | ATO Tax Residency Letter (`:1239`) | Required for offshore investor confidence | 6-11 | NUDGE |
| 11.2 | GST Registration Confirmation (`:1248`) | ATO issues GST branch registration | 6-11 | NUDGE (auto-probe via ABR + threshold detector — see §1 P6 gap) |
| 11.3 | R&D Tax Incentive Claims (`:1256`) | AusIndustry registration + ATO claim; 10-month post-FY deadline | 6-11 | AUTO template + calendar alert (calendar alert missing) |
| 11.4 | PAYG Withholding Compliance (`:1265`) | ATO monthly/quarterly | 8-11 | NUDGE |
| 11.5 | Income Tax Returns Last 3Y (`:1273`) | ATO annual | 8-11 | NUDGE |
| 11.6 | ESIC Eligibility Assessment (`:1281`) | ITAA97 s360; unlocks 20% investor tax offset + 10Y CGT exemption | 3-9 | AUTO (see §1 P9 gap) |
| 11.7 | ESVCLP Fund Eligibility Note (`:1291`) | Only relevant if investor is an ESVCLP fund | 9-10 | NUDGE (rare) |
| 11.8 | BAS Lodgement History 8 Quarters (`:1300`) | ATO quarterly | 8-11 | NUDGE |

### Folder 12 — AU Compliance (`:1311-1443`, 6 items) — AU-specific
| # | BlockID item | Notes | Phase | Auto-gen? |
|---|---|---|---|---|
| 12.1 | AFSL Exemption Note or Licence (`:1320`) | Corps Act s911A licensing / s923B "advice" boundary | 9-11 | AUTO template with disclaimer |
| 12.2 | Privacy Act 1988 Compliance Statement (`:1330`) | 13 Australian Privacy Principles | 4-11 | AUTO template |
| 12.3 | APP / Data Breach Register (`:1338`) | OAIC NDB Scheme (Privacy Act Part IIIC) | 7-11 | AUTO (from audit-log) |
| 12.4 | Fair Work Employee Entitlements Audit (`:1347`) | Fair Work Act 2009 NES | 8-11 | AUTO (needs Fair Work API — none) |
| 12.5 | WGEA Compliance Report (`:1356`) | Workplace Gender Equality Act 2012, mandatory ≥ 100 employees | 11-12 | NUDGE with headcount trigger (missing) |
| 12.6 | Modern Slavery Act Statement (`:1364`) | Modern Slavery Act 2018 (Cth), mandatory ≥ A$100M consolidated revenue | 11-12 | NUDGE with revenue trigger (missing) |
| 12.7 | Notifiable Data Breaches Scheme Readiness (`:1372`) | OAIC NDB, 30-day notification | 7-11 | AUTO template |

### Blocker templates missing (raise-critical)
1. **Default AU Pty Ltd Constitution** (Folder 1 item 4) — most founders using replaceable rules cannot show one to investors; MUST mint.
2. **AU-flavoured SAFE / Convertible Note body** (Folder 2 item 4) — resolves Q1; decide by P1_dataroom_map ship.
3. **Fair Work Award-compliant Employment Contract** (Folder 6 item 5) — AU has 121 modern awards; template must clearly state which award applies.
4. **ESOP Scheme Rules body** (Folder 6 item 6) — Div83A-conformant scheme rules needed; the checker exists but the source-of-truth scheme doc is not seeded.
5. **ESIC Self-Assessment worksheet** (Folder 11 item 6) — must let founder walk the 3 gateway tests + 100-point principles-based OR 100-point points-based OR ATO ruling; currently upload-only.
6. **AU IP Assignment Deed (Founder + Contractor)** (Folder 2 item 5, Folder 7 item 1) — templates stubbed but not e-signable.

---

## 3. Next-step nudge engine specification (Deliverable 3)

### 3.1 Purpose
For a given `(user, project)` compute in ≤ 200ms:
- `phase` — the current 12-phase key (1..12) they are in
- `next_action` — the single most important thing to do next
- `missing` — top-5 unfilled data-room categories or missing SVI-criterion evidence
- `readiness_score` — Investor Readiness (0-100) for their current phase
- `nudge_reason` — human-readable "why we are surfacing this"

### 3.2 Data sources (all cited with path:line)
| Source | Table / Module | Fields used |
|---|---|---|
| Phase state | `startup_phase_progress` (`web/supabase/migrations/0049_startup_phase_progress.sql:6-25`) | phase_id, phase_order, status, completion_pct, steps_json, ai_recommendations |
| Account fast-lookup | `svi_accounts.growth_phase_current` + `growth_completion_pct` (`0049_startup_phase_progress.sql:50-52`) | quick default when no per-phase row yet |
| Project fast-lookup | `projects.growth_phase_current` + `growth_completion_pct` (`:54-57`) | multi-project support |
| Latest SVI score | `svi_analyses` (`web/supabase/migrations/0007_svi_analyses.sql`) | latest per project, dimension scores |
| SVI criterion evidence | `evidence_items` (`web/supabase/migrations/0043_missing_tables.sql:59`) | criterion_key, quality_level, file/link count |
| Data-room file coverage | `dataroom_files` (`web/supabase/migrations/0017_user_source_folders.sql:22`) | folder, doc name, present/absent |
| Founder-pack signals | `founder_packs` (see `web/src/lib/idea-phase/persist.ts:213`) | early-Phase-1 hints |
| Fundraise readiness | `fundraise-checklist.ts:319-347` + `computeReadinessScore` (`:350-370`) | reuse the 70/30 blended score, but per-phase (see P5) |
| Existing SVI next-actions | `svi-analysis.ts:250-1400` (`SVIAnalysis.nextActions`) | fallback next_action when phase-specific rule empty |

### 3.3 Output JSON shape (contract-frozen)
```json
{
  "project_id": "uuid",
  "phase": {
    "key": 5,
    "label_en": "PMF / Early Traction",
    "label_vi": "PMF / Traction ban đầu",
    "canonical_stage": "mvp_early_revenue"
  },
  "next_action": {
    "id": "wire-founder-stripe-testmode",
    "title": "Wire your own Stripe in test-mode",
    "detail": "Chapter 5 CTA — a $1 test charge unlocks the Chapter 7 growth wiring later",
    "surface_route": "/dashboard/integrations",
    "estimated_minutes": 20,
    "priority": "P0"
  },
  "missing": [
    { "id": "cohort-retention-pdf", "category": "traction", "phase_first_needed": 5, "raise_blocker": false, "surface_route": "/dashboard/svi" },
    { "id": "founder-agreement", "category": "corporate", "phase_first_needed": 1, "raise_blocker": true, "surface_route": "/dashboard/data-room" },
    { "id": "ip-assignment-founder", "category": "captable", "phase_first_needed": 1, "raise_blocker": true, "surface_route": "/dashboard/data-room" },
    { "id": "abn-registration", "category": "corporate", "phase_first_needed": 1, "raise_blocker": true, "surface_route": "/dashboard/onboarding" },
    { "id": "pmf-signal", "category": "traction", "phase_first_needed": 5, "raise_blocker": false, "surface_route": "/dashboard/svi" }
  ],
  "readiness_score": {
    "current_phase": 5,
    "score": 42,
    "band": "not-ready",
    "delta_vs_last_week": 6,
    "svi_subset_used": ["idea", "market", "customer_size", "revenue"]
  },
  "nudge_reason": "You've completed 3 of the 6 Chapter 5 milestones. Wiring Stripe test-mode now unlocks Chapter 7 in one click instead of a 2-week project.",
  "meta": {
    "computed_at": "2026-07-24T02:00:00Z",
    "fresh_for_seconds": 3600,
    "used_sources": ["startup_phase_progress", "svi_analyses", "dataroom_files", "evidence_items"],
    "afsl_disclaimer": "General information only, not personal financial product advice under s766B Corporations Act 2001 (Cth)."
  }
}
```

### 3.4 API endpoint
- **Route:** `GET /api/nudge/next-steps?project_id={uuid}` (default: caller's most-recent project).
- **Handler location:** `web/src/app/api/nudge/next-steps/route.ts` (new).
- **Auth:** existing `withAuth` wrapper; RLS scoped to caller's `svi_accounts.id`.
- **Cache:** ETag on `updated_at` of the most-recent `startup_phase_progress` row for the project; `Cache-Control: private, max-age=3600` (matches `fresh_for_seconds`).
- **Errors:** `404` when project unknown; `403` when caller lacks project access; `200 { "state": "not-started", ...zero-payload }` when the founder has no SVI row yet (encourages onboarding rather than erroring).

### 3.5 Surfaces (where the payload renders)
| Surface | What it shows | Refresh trigger |
|---|---|---|
| `/dashboard` tile "Your next step" | phase + next_action + readiness score band | on page load + on `startup_phase_progress` mutation |
| In-app banner (dismissible per-day) | next_action.title + priority chip | on every dashboard nav |
| Weekly digest email (see P7) | readiness score + delta + top-3 missing + single next action | cron Monday 07:00 AEST (or founder-configured, per `email_preferences.ts:232`) |
| SVI report PDF appendix | full JSON dump + rendered checklist | on report generation only |
| `/showcase/atlassian` "you are here" overlay (sibling agent's UI) | phase label + canonical-stage badge | on page load only |

### 3.6 Cadence + freshness rules
- **Event-driven recompute** on any of: `startup_phase_progress.status` transition, `svi_analyses` insert, `dataroom_files` insert/delete, `evidence_items` insert. Fire via Supabase `pg_notify('nudge_refresh', project_id::text)`; a lightweight listener enqueues.
- **Nightly recompute cron** at 02:00 AEST covers projects with no events (kept-warm; freshness for the weekly digest).
- **No email spam:** hard rule — never send more than 1 nudge email per project per 7 days regardless of trigger.
- **Kill switch:** env `NUDGE_ENGINE=off` disables the endpoint + cron.

### 3.7 Fair-use guardrails
- **AFSL boundary:** the `next_action.detail` field NEVER prescribes a specific investment, pricing, or legal decision. It only points to a workspace surface. All personal-advice-shaped output routes to the AFSL disclaimer in `meta.afsl_disclaimer`.
- **Wholesale-investor status inference:** the engine NEVER concludes a user is a wholesale/sophisticated investor. That determination is off-platform (Q2).
- **ESIC eligibility:** the engine surfaces a "run the ESIC self-assessment" nudge but does not itself certify eligibility (P6 gate does the check, still marked "information only").

---

## 4. Atlassian ↔ BlockID walkthrough surface mapping (Deliverable 4)

The sibling agent is authoring a 9-step logged-out walkthrough at `/showcase/atlassian`. This table lists — per step — the Atlassian real-world moment it demonstrates, the BlockID surface a live founder would use at the same journey point, and what the demo needs to make the connection obvious.

| Step | Atlassian real-world moment | BlockID surface for a live founder | Demo callout copy | You-are-here indicator |
|---|---|---|---|---|
| 1 · `/showcase/atlassian` intro | 2002 UNSW grads with A$10K credit card (`page.tsx:37-43`) | `/svi` (idea seed form) + `/guide/01-vision` | "Every founder starts here. Type your one-liner — BlockID's Chapter 1 mirrors what Cannon-Brookes + Farquhar did on a whiteboard, minus the whiteboard." | phase-1 badge |
| 2 · `/dashboard` demo tile | 2003 first US$1M revenue from AA-buys-Jira-via-fax (`page.tsx:52-60`) | `/dashboard` "Your next step" tile (see §3.5) fed by nudge engine | "The green tile is what your workspace looks like once you have any SVI row. Atlassian didn't have a dashboard — imagine if they had." | phase-5 badge |
| 3 · `/svi-report` demo | Pre-2010 profitability + product-led-growth defining ratios (S-1 MD&A) | `/svi` full report + `svi_analyses.report_pdf_url` | "This is a real BlockID SVI report. The 13-criterion breakdown is what turns a founder hunch into an investor-defensible score." | phase-2 badge |
| 4 · `/growth-phases` demo | Ten-year path 2002-2012 (bootstrap → Accel → board scale) | `/guide` chapter grid (12 chapters) | "Atlassian didn't skip phases — they compressed some (no primary VC). The 12-chapter guide shows every phase; you decide which to compress." | phase-4/5/8 sequence |
| 5 · `/agents/[slug]` demo | 2007 Five core values (`page.tsx:86-92`), 2005 ShipIt (`page.tsx:69-76`) | `/dashboard/team` + CHRO agent output | "Your CHRO agent drafts the culture doc BlockID uses internally. Atlassian took years to codify five values; you get a starting draft in minutes." | phase-8 badge |
| 6 · `/data-room` demo | Atlassian S-1 Item 16 exhibit index (102 exhibits mapped in §2 above) | `/dashboard/data-room` 12-folder / 102-item structure | "Every exhibit Atlassian filed with the SEC has a BlockID counterpart. Grey items are what BlockID auto-generates for you." | phase-9 badge |
| 7 · `/valuation` demo | 2014 T. Rowe secondary at US$3.3B (`page.tsx:119-127`) | `/dashboard/valuation` + `/dashboard/cfo` | "SVI-calibrated valuation is a starting anchor, not personal financial advice. The AFSL disclaimer stays visible on every valuation output." | phase-10 badge |
| 8 · `/guide` demo | 2010 Accel 100% secondary + 2015 IPO (`page.tsx:94-146`) | `/guide/10-fundraise` chapter | "Both of Atlassian's pre-IPO rounds were 100% secondary — Chapter 10 shows you how to distinguish primary vs secondary when you get there." | phase-10 badge |
| 9 · `/showcase/atlassian` recap | FY25 US$5.2B revenue, +20% YoY, 83% GM (`page.tsx:200-207`) | `/dashboard/portfolio` (multi-project view) + `/showcase/blockid` (dogfood proof) | "You've seen where a workspace can lead. Start yours at /svi — the reseller link ?via=INFOVISION20 gives you 20% off your first paid tier." | phase-11/12 badge |

**Copy fixture location for P4:** author these as EN + VI entries in `web/src/lib/showcase/atlassian/investor-readiness-callouts.ts` (new file — check for sibling-agent conflicts before creating). Do **not** modify `/showcase/atlassian/page.tsx` directly.

---

## 5. AU statutory reference index (for future auto-loop tasks)

Compact lookup so subsequent ticks don't re-fetch:

| Concept | Statute / Rule | Section | Relevant BlockID phase |
|---|---|---|---|
| Small-scale personal offer exemption | Corporations Act 2001 (Cth) | s708(1) — max 20 investors / A$2M / 12 mo | 9-10 |
| Sophisticated investor test | Corporations Act 2001 (Cth) | s708(8) — A$2.5M net assets or A$250k gross income (2 FYs), needs qualified accountant certificate | 9-10 |
| Professional investor test | Corporations Act 2001 (Cth) | s708(11) — AFS licensee or A$10M+ portfolio | 10 |
| Wholesale client (services) | Corporations Act 2001 (Cth) | s761G(7) | 10-12 |
| Personal advice boundary | Corporations Act 2001 (Cth) | s766B | every SVI/valuation output |
| ESOP tax concessions | ITAA 1997 | Subdiv 83A-B (upfront) / 83A-C (deferred) | 8 |
| ESOP start-up tests | ITAA 1997 | s83A-33 (7 eligibility tests) + s83A-45(4) 10% cap + s83A-45(5) forfeiture / 3-year hold | 8 |
| ESIC 20% offset + 10Y CGT exemption | ITAA 1997 Div 360 | 3 gateway tests + principles or points test | 3-9 |
| ESVCLP fund concessions | Venture Capital Act 2002 (Cth) | ESVCLP registration via AusIndustry | 9-10 |
| R&D Tax Incentive | Industry Research and Development Act 1986 | AusIndustry registration ≤ 10 mo post-FY | 6-11 |
| GST registration threshold | A New Tax System (GST) Act 1999 | A$75k turnover | 6+ |
| Privacy — APPs | Privacy Act 1988 (Cth) | 13 APPs (Schedule 1) | 4+ |
| Notifiable Data Breaches Scheme | Privacy Act 1988 (Cth) | Part IIIC, ≤ 30-day notification | 7+ |
| WGEA reporting | Workplace Gender Equality Act 2012 | ≥ 100 employees | 11-12 |
| Modern slavery statement | Modern Slavery Act 2018 (Cth) | ≥ A$100M consolidated revenue | 11-12 |
| Fair Work NES | Fair Work Act 2009 (Cth) | 11 minimum standards + 121 modern awards | 8+ |
| ASIC annual review | Corporations Act 2001 (Cth) | s345A + regs | 11 annually |

---

## 6. Follow-up P1a..P1n subtasks (spun off from P1_dataroom_map)

- **P1a** mint default AU Pty Ltd Constitution template (Folder 1 item 4) — **shipped 2026-07-24**. Constitution body already existed at `web/content/templates/legal/au-pty-ltd-constitution.md` (Round 5.6a — 28.6KB, 13 declared placeholders, casting-vote variant toggle). This tick adds a dedicated vitest block in `web/src/lib/templates/legal-templates.test.ts` mirroring the P1c test shape: 4 cases pinning (a) registry entry as phase-1 corporate template, (b) Corporations Act anchors present in body (s135 displaced replaceable rules, s136 adoption, s140 contract effect, s254T solvency test, drag-along + tag-along + pre-emptive clauses, "proprietary company limited by shares" scope, top-of-doc AFSL disclaimer), (c) every non-toggle body `{{token}}` is declared in `placeholders[]`, and (d) applySubstitutions renders `drag_along_threshold: "75"` into `**75%**` and strips the casting-vote-disabled variant when only `casting_vote_yes` is truthy. All 10 legal-template tests green (6 existing + 4 new). Closes §2 Folder 1 item 1.4 (Constitution / Replaceable Rules — NUDGE with default body). Founder still must confirm registered office + ACN + first-director details in Ch1 CTA; template hard-codes s135(2) displacement so replaceable rules stop applying on adoption.
- **P1b** decide + implement Q1 (SAFE mint vs LawPath link) — **shipped 2026-07-24**. Decision: **mint** the AU-flavoured SAFE ourselves (rather than link out to LawPath). Rationale: the surrounding P1x ticks already minted the Pty Ltd constitution (P1a) and Fair Work employment contract (P1c), so keeping fundraising templates in-platform preserves the "one workspace, one artefact set" story the founder walkthrough sells; a link-out at Phase 9 would force a context-switch at exactly the moment we want the founder in `/dashboard/data-room`. Template body already existed at `web/content/templates/legal/au-safe.md` (Round 5.6c — 267 lines, 14 declared placeholders, 3 cap/discount variant toggles, NSW governing law, s708(8)/(11) sophisticated-investor reps, s254T solvency guard on Dissolution Event payout, s127 execution block, ACICA mediation-first dispute resolution, GST + stamp-duty carve-outs, Electronic Transactions Act e-signing anchor). This tick adds a dedicated vitest block in `web/src/lib/templates/legal-templates.test.ts` mirroring the P1a/P1c test shape: 4 cases pinning (a) registry entry as phase-9 fundraising template, (b) AU-specific anchors present in body that differentiate from a US YC SAFE (New South Wales governing law, s708(8), s708(11), s254T, s127, Chapter 6D, GST, stamp duty, Electronic Transactions Act, ACICA, AFSL disclaimer), (c) every non-toggle body `{{token}}` is declared in `placeholders[]`, and (d) applySubstitutions renders `variant_cap_and_discount: "true"` while stripping the two unselected variant blocks and never leaking section-toggle tokens into the rendered doc. All 14 legal-template tests green (10 existing + 4 new). Closes §2 Folder 2 item 2.4 (SAFE / Convertible Note Agreements — was "missing template body"). Founder still must confirm sophisticated-investor status off-platform per Q2.
- **P1c** mint Fair Work Award-compliant Employment Contract (Folder 6 item 5) — **shipped 2026-07-24**. `web/content/templates/legal/au-employment-contract.md` (13 sections: position + Modern-Award slot, employment-type toggle incl. s66A–66M casual conversion, NES leave table, Div-8 IP assignment with s195AV moral-rights consent, NSW *Restraints of Trade Act 1976* severance clause, s117 notice + s119 redundancy, WHS + APP + s7B(3) employee-records-exemption disclosure, s86 casual-loading toggle, execution + Annexures A/B). Registered in `web/src/lib/templates/legal-templates.ts` (26 placeholders, 4 statutory sources — Fair Work Act 2009, FWO Modern Awards list, SG Admin Act 1992, NSW ROT Act 1976). Test at `web/src/lib/templates/legal-templates.test.ts` (6 vitest cases: registry-file-existence, unique slugs + known categories + disclaimer, AU-anchor grep for NES/Award/Fair Work Act/Super Guarantee/moral rights, declared-placeholder-superset-of-body-tokens, applySubstitutions section-toggle branch matrix). Auto-served via existing `GET /api/templates/legal/[slug]` route + rendered on the `/legal-templates` grid. Closes §2 Folder 6 item 5 (Employment Contract Templates — AUTO). Founder still must confirm the correct Award (of the 121 modern awards); the template hard-codes the FWO award-viewer URL for self-serve.
- **P1d** mint ESOP Scheme Rules body (Folder 6 item 6) — **shipped 2026-07-24**. Template body already existed at `web/content/templates/legal/au-esop-scheme-rules.md` (Round 5.6b — 24.3KB, 8 declared placeholders, no section-toggle variants). This tick adds a dedicated vitest block in `web/src/lib/templates/legal-templates.test.ts` mirroring the P1a/P1b/P1c test shape: 4 cases pinning (a) registry entry as phase-8 employment template, (b) Div 83A anchors present in body (Division 83A, Subdivision 83A-B / 83A-C, s83A-33, s83A-45, 10-year Options cap, 10% shareholding cap, A$50m aggregated turnover, real-risk-of-forfeiture language, Good/Bad Leaver, Reserve Pool, Chapter 6D, ASIC Instrument 2022/1021, top-of-doc AFSL disclaimer), (c) every body `{{token}}` is declared in `placeholders[]`, and (d) applySubstitutions renders scheme identity + reserve-pool percentage while leaving unknown tokens like `{{scheme_name}}` visible so the founder sees the gap. All 18 legal-template tests green (14 existing + 4 new). Closes §2 Folder 6 item 6 (ESOP Plan Document Team Copy — was "AUTO — see §1 P8 gap 'scheme body template' absent") and §2 blocker template #4 (ESOP Scheme Rules body). The Div 83A checker (`web/src/lib/div83a-checker.ts`) still owns the runtime eligibility validation; this template is the source-of-truth scheme doc the checker validates against.
- **P1e** build ESIC Self-Assessment worksheet UI (Folder 11 item 6) — **shipped 2026-07-24**. Server page at `web/src/app/workspace/esic-assessment/page.tsx` (auth-gated, WorkspaceLayout wrapper, hydrates the latest `compliance_esic_assessments` row for the caller × active project). Client component `esic-assessment-client.tsx` walks the two Div 360 limbs: limb 1 (age + turnover cap A$200k + expenses cap A$1M + is_listed + has_r_and_d_expenditure toggle that flips the age cap from 3→6y) and limb 2 (100-point test — patents +50/+25, PBR +25, accelerator alumni +50, third-party capital ≥A$50k +50, R&D 15-50% +50 / >50% +75, uni licence +25, trademarks 0-pt-tracked; principles-based — five sub-tests). Submit POSTs to the existing `POST /api/compliance/esic` (auth + insert into `compliance_esic_assessments`); banner renders `is_esic` band (green/amber/red via `pickResultBand()`) plus `gaps[]` + `recommendations[]` + `ESIC_DISCLAIMER`. Pure helpers `esic-form.helpers.ts` (form defaults, `toEsicInput` / `fromEsicInput` round-trip, `pickResultBand`) covered by 6 vitest cases in `esic-form.helpers.test.ts` (form→wire mapping incl. NaN fallback, state round-trip, three band rules green/amber/red anchored against real `assessESIC` output). Nav link added to Fundraise group in `nav-groups.ts` (minPlan starter). Closes §2 Folder 11 item 6 (ESIC Eligibility Assessment — was upload-only) and §2 blocker template #5. Downstream `esic-funding-gate.ts` now sees a persisted assessment on every founder that runs the worksheet so the P6 warn/block gate can fire on real data instead of missing-row fallbacks.
- **P1f** wire AU IP Assignment Deed e-signing (Folder 2 item 5, Folder 7 item 1) — **shipped 2026-07-24**. Two deed-form templates minted at `web/content/templates/legal/au-ip-assignment-deed-founder.md` (17 declared placeholders, sole-director + second-director toggles) and `web/content/templates/legal/au-ip-assignment-deed-contractor.md` (17 declared placeholders, contractor-as-entity vs contractor-as-individual toggles). Both registered in `web/src/lib/templates/legal-templates.ts` under a new `ip` category — Founder deed phase-1, Contractor deed phase-4. Vitest coverage in `web/src/lib/templates/legal-templates.test.ts` adds two 4-case describe blocks (registry entry + statute-anchor grep + placeholder-superset + substitution-branch matrix, plus the category-allow-list gains `ip`). Wire-in: `web/src/lib/data-room-templates.ts` items "IP Assignment Deeds (Founders)" (folder 2 item 5) and "IP Assignment Deeds (Contractors)" (folder 7 item 1) both now carry `template_slug`, so the /dashboard/data-room tile can render a direct "Use BlockID template" CTA. AU legal anchors covered: **executed as a deed** (enforceable without consideration — critical for founder→own-company assignment); Copyright Act 1968 (Cth) Part IX moral rights consent (ss195AW/195AWA); Patents Act 1990 (Cth), Designs Act 2003 (Cth), Trade Marks Act 1995 (Cth), Circuit Layouts Act 1989 (Cth), Plant Breeder's Rights Act 1994 (Cth); retrospective effect + hold-on-trust bridge to close pre-incorporation gaps; Corporations Act 2001 (Cth) s127 execution block; **Electronic Transactions Act 1999 (Cth)** express e-signing consent (satisfies the deed signed-sealed-delivered rule under State/Territory equivalents including in respect of a document taking effect as a deed); Personal Property Securities Act 2009 (Cth) PPSR registration consent; Restraints of Trade Act 1976 (NSW) severance clause (founder only); Foreground/Background IP split with perpetual royalty-free sublicensable Background licence (contractor only); sham-contracting rep anchored on CFMMEU v Personnel Contracting [2022] HCA 1 and ZG Operations v Jamsek [2022] HCA 2 (contractor only); Fair Work Act 2009 (Cth) Part 3-1 sham-contracting cross-ref (contractor only). Closes §2 Folder 2 item 2.5 (Founders) and §2 Folder 7 item 7.1 (Contractors), and §2 blocker template #6 (AU IP Assignment Deed — Founder + Contractor). Third-party e-sign platform integration (DocuSign / SignNow OAuth webhook to auto-file the counter-signed PDF into `dataroom_files`) remains a follow-up under P1f-esign — the templates are e-signable as executed today (parties opt in via the express Electronic Transactions Act consent clause), but the callback-into-BlockID plumbing to auto-lodge the returned PDF is a separate integration.
- **P1g** wire ABR ABN-lookup probe (`/api/abr/lookup`) — **shipped 2026-07-24**. `web/src/lib/compliance/abn.ts` (normalize + modulus-89 checksum + optional live ABR AbnDetails probe gated on `ABR_GUID`), `web/src/lib/compliance/abn.test.ts` (12 vitest cases: normaliser boundaries, real ABN 79 659 615 111 checksum passes, obvious-invalid rejections, no-guid path, successful ABR parse, JSONP unwrap, ABR Message → live_error, non-200 → HTTP error), `web/src/app/api/abr/lookup/route.ts` (public GET, 400 on malformed input, checksum-only path when live guid absent, 1 h edge cache). Chapter 1 CTA can consume this to give founders the 60-second trust signal §1 phase 1 flagged as P0 raise-blocking.
- **P1h** wire ASIC Connect extract probe (`/api/asic/extract`) — **shipped 2026-07-24**. `web/src/lib/compliance/acn.ts` (normalize + modulus-10 checksum + optional live ABR AcnDetails probe gated on `ABR_GUID`), `web/src/lib/compliance/acn.test.ts` (13 vitest cases: normaliser boundaries, real ACN 659 615 111 checksum passes, obvious-invalid rejections, zero-check-digit edge case, no-guid path, successful ABR parse, JSONP unwrap, ABR Message → live_error, non-200 → HTTP error), `web/src/app/api/asic/extract/route.ts` (public GET, 400 on malformed input, checksum-only path when live guid absent, 1 h edge cache). Closes the §2 Folder 1 item 1.2 gap so Chapter 1 can now show founders both ABN + ACN trust signals side-by-side. Paid ASIC Company Extract (charges, historical officers) remains out of scope for the free probe.
- **P1i** build GST-threshold-crossing detector (auto-fires when trailing-12mo Stripe MRR × 12 ≥ A$75k) — **shipped 2026-07-24**. Library body already existed at `web/src/lib/compliance/gst-threshold.ts` (`assessGST` + `GST_THRESHOLD_AUD` = A$75,000 + `GST_REGISTRATION_WINDOW_DAYS` = 21) plus the POST/GET route at `web/src/app/api/compliance/gst-threshold/route.ts` (auth-gated, persists to `compliance_gst_status` per migration `0108_compliance_checks.sql`) and is already consumed by `web/src/components/dashboard/compliance-panel.tsx`. This tick adds the two founder-pipeline bridges + the missing test file: (1) `deriveMonthlyTurnoverFromDaily(events, now)` — bucketises any Stripe / `revenue_events` daily-transaction stream into 12 trailing monthly totals (UTC-anchored, drops future / >12mo / NaN entries silently) so a founder's Stripe pull can flow into `assessGST` without pre-aggregation; (2) `assessGSTFromMrr(mrrAud, opts)` — one-liner that mirrors the ATO projected-turnover test (current MRR × 12 = A$75k), for callers who only know current MRR (`web/src/lib/metrics/mrr.ts` shape); (3) `web/src/lib/compliance/gst-threshold.test.ts` (15 vitest cases pinning the four-branch matrix — `already_registered` / `urgent_register` / `register_within_21_days` / `none` — plus tolerance for NaN/negative/short/long series, and the two new helpers against a real trailing-12mo Stripe stream that trips `urgent_register` at A$84k). All 15 tests green. Closes §1 phase 6 P0 gap (GST threshold detector missing). Founder-side auto-fire cron reuse of `getRealMrrAud`-style pattern remains a follow-up once per-founder Stripe Connect is wired end-to-end (P1i-cron).
- **P1j** author R&D Tax Incentive Chapter 6 section — **shipped 2026-07-24**. Chapter 6 (`06-revenue`) now carries a `sections[]` array via the previously-declared but unused `Chapter.sections?: ChapterSection[]` slot (web/src/lib/guide/startup-journey.ts:66). The single seeded section `rnd-tax-incentive` (heading + 9-bullet body EN + VI) walks the R&DTI end-to-end: what the programme is (refundable 43.5% offset under A$20M aggregated turnover, tiered non-refundable above), R&D-entity eligibility (Pty Ltd / Ltd only), core vs supporting activities and the s355-25 Industry Research and Development Act 1986 hypothesis-driven definition (this is where refunds die), the A$20,000 minimum notional-deduction threshold, the 10-month AusIndustry registration deadline (30-June year-end → 30-April), the mandatory two-step process (AusIndustry portal registration + ATO Schedule 743 in the company tax return), contemporaneous-record obligations audit-tested for 4 years, common excluded expenditure (capital, feedstock, foreign-parent work without an Advance/Overseas Finding, grant-funded portions), and how BlockID.au wires it (data-room folder 11 slot, compliance-calendar 60-day reminder, au-compliance agent activity-descriptor drafting, RTAA advisor escalation before Chapter 9). Both marketing (`/guide/06-revenue`) and workspace (`/workspace/guide/06-revenue`) surfaces now render `sections[]` (small addition to the `ChapterBody` in each page — one map block per surface, mirrors the existing `qualifyingTests` styling). Tests: `web/src/lib/guide/startup-journey.test.ts` gains a "R&D Tax Incentive section (chapter 06 P1j)" describe block — 3 vitest cases pinning (a) presence + EN/VI alignment ≥ 6 bullets, (b) statutory anchor grep (AusIndustry, ATO, A$20, 43.5, 10 month, Schedule 743, s355-25), (c) only chapter 06 carries `sections` for now (mirrors the qualifyingTests single-chapter guard). All 15 startup-journey tests green (12 existing + 3 new). Closes §1 phase 6 P1 gap "dedicated R&D Tax Incentive Chapter 6 section" and Q8 recommendation ("ADD a dedicated Chapter 6 section on R&D Tax Incentive eligibility + AusIndustry registration deadline"). AusIndustry-registration-deadline calendar reminder (10 months post-FY) remains a follow-up under P1k (compliance-calendar.ics generator).
- **P1k** build compliance-calendar.ics generator + BAS / annual-review / AusIndustry-R&D reminders — **shipped 2026-07-24**. Pure module `web/src/lib/compliance/calendar.ts` (`buildComplianceCalendar` + `renderIcs`) plus authed route `GET /api/compliance/calendar` (serves `text/calendar; charset=utf-8`, `?download=1` forces `compliance-calendar.ics` attachment). Emits three event kinds inside a 12-month horizon: (1) BAS quarterly lodgment (Q1 due 28-Oct, Q2 due 28-Feb, Q3 due 28-Apr, Q4 due 28-Jul, standard 1-Jul-to-30-Jun FY, self-lodger dates from the ATO BAS due-dates page) gated on `compliance_gst_status.registered_for_gst`, (2) ASIC annual company review (`s 345A Corporations Act 2001 (Cth)`) anchored on the incorporation-date anniversary (reads `equity_plans.incorporation_date` as the closest ACN-registration-date proxy currently persisted; gracefully no-ops when absent or unparseable), (3) AusIndustry R&D Tax Incentive registration deadline (10-month post-FY window, `IR&D Act 1986 s 27A`) fed by the existing `buildRDCalendar()` output so the two surfaces never drift; skips FYs already `registered` / `not_applicable` / overdue. Every VEVENT carries a `DTSTART;VALUE=DATE` all-day block, stable deterministic UID (`bas-fy{yyyy-yy}-q{n}@blockid.au`, `asic-annual-review-{yyyy}@blockid.au`, `rd-registration-fy{yyyy-yy}@blockid.au`), fixed 14-day `VALARM` (`TRIGGER:-P14D`, matches Chapter 11 copy at `web/src/lib/guide/startup-journey.ts:870`), and an inline `URL` back to the authoritative ATO / ASIC / business.gov.au source. RFC 5545 line-folding (75-octet lines, CRLF join, continuation-space prefix) and text-escape (`\,`, `\;`, `\\`, `\n`) implemented in-module. `CALENDAR_DISCLAIMER` fixed on the `X-WR-CALDESC` calendar-level property so subscribers see "not tax or legal advice — BAS due dates assume standard 1-July-to-30-June FY and self-lodgment" every time they refresh. Test coverage in `web/src/lib/compliance/calendar.test.ts` — 14 vitest cases across four describe blocks: (a) BAS quarterly gating (no events when `gst_registered=false`, Q1 due 2026-10-28 with correct summary + UID + reminder, Q2 rolls into next calendar year at 2027-02-28, horizon caps everything before 2027-07-24), (b) ASIC anniversary (no events without ACN date, 2022-08-04 registration → 2026-08-04 event with `s 345A` grep, unparseable date gracefully skipped), (c) R&D deadline (skips `registered`, skips `not_applicable`, skips overdue, emits FY 2025-26 event on 2027-04-30 with `IR&D Act 1986 s 27A` grep + stable UID), (d) `renderIcs` RFC 5545 shape (starts/ends with `BEGIN`/`END:VCALENDAR`, fixed PRODID, `X-WR-CALDESC` present + fragment guard, one CRLF-terminated `BEGIN:VALARM` per event with `TRIGGER:-P14D` + `ACTION:DISPLAY`, deterministic UIDs across two calls). All 64 tests in `src/lib/compliance/` green (50 existing + 14 new). Closes §1 phase 11 P1 gap (compliance-calendar.ics generator missing) and delivers the reminder half of §1 phase 6 P1 gap "AusIndustry registration-deadline (10 months post-FY-end) alert wired to compliance-calendar" flagged by P1j. Founder still needs to enter incorporation_date (equity_plans row) for the ASIC branch to fire; the P1n follow-up will fold in WGEA + Modern Slavery threshold events once those detectors ship.
- **P1l** author primary vs secondary Chapter 10 split (Q5) — **shipped 2026-07-24**. Chapter 10 (`10-fundraise`) now carries a `sections[]` entry via the previously-declared `Chapter.sections?: ChapterSection[]` slot (web/src/lib/guide/startup-journey.ts:66). The single seeded section `primary-vs-secondary` (heading + 8-bullet body EN + VI) walks the distinction end-to-end: the primary-issues-new-shares vs secondary-transfers-existing-shares rule, the Atlassian precedent (2010 Accel US$60M + 2014 T. Rowe US$150M @ US$3.3B post — both 100% secondary — sourced from `web/src/app/showcase/atlassian/page.tsx:94-127` + TechCrunch 2010-07-14), when secondary is the right call (company doesn't need cash + early shareholders want liquidity + investor wants a bigger slice), the s707 Corporations Act 2001 (Cth) on-sale rule + s708(1)/(8) + s761G(7) re-classification founders must run on every secondary transferee, ROFR + pre-emptive-rights workflow anchored on Blackbird / AirTree public shareholders'-agreement templates, the Subscription Agreement (primary, s254A + Form 484) vs Share Purchase Agreement (secondary, Form 484 change-of-member + stamp-duty check on unquoted transfers with NSW-abolished-2016 / VIC-landholder carve-outs) documentation split, tax treatment fork (primary = capital contribution, not assessable to company; secondary = CGT event A1 ITAA 1997 s104-10 for the seller with Div 152 SBC / Div 115 50%-discount lookups + ESIC Div 360 offset DOES NOT survive secondary — s911A / s1041H exposure guard), and the BlockID.au wiring (Workspace → Fundraise deal_type tag primary/secondary/mixed → term-sheet AI clause-weighting split → blockchain-hash worker identical treatment → reseller Timeline surfaces deal_type alongside deal_valuation). Both marketing (`/guide/10-fundraise`) and workspace (`/workspace/guide/10-fundraise`) surfaces render `sections[]` via the existing `c.sections?.map()` blocks (web/src/app/guide/[chapter]/page.tsx:121 + web/src/app/workspace/guide/[chapter]/page.tsx:92 — same shape P1j chapter 06 uses). Tests: `web/src/lib/guide/startup-journey.test.ts` gains a "primary vs secondary section (chapter 10 P1l)" describe block — 2 vitest cases pinning (a) presence + EN/VI alignment ≥ 6 bullets, (b) statutory + precedent anchor grep (Accel, T. Rowe, US$60M, US$150M, s707, s708, Right of First Refusal, CGT event A1, Subscription Agreement, Share Purchase Agreement, Div 360, Form 484). The pre-existing "no other chapter carries sections yet" guard now allow-lists chapters 06 AND 10 (a Set<ChapterSlug>, not a two-branch if). All 17 startup-journey tests green (15 existing + 2 new). Closes §1 phase 10 P0 gap "primary vs secondary distinction in Ch10 (Atlassian's rounds were 100% secondary — Q5)" and open_question Q5 (extend Chapter 10 to distinguish primary vs secondary raise; secondary needs cap-table + ROFR + s708 exemption checks). The ROFR-waiver cap-table workflow (§1 phase 10 P1 gap) and s708(1) trailing-12mo counter remain follow-ups (ROFR workflow lives in /dashboard/data-room UI, not the guide content module; deal_type tag on Workspace → Fundraise is a schema change on `svi_analyses.deal_metadata` — separate ticks).
- **P1m** author dual-class decision-point doc (Q4) — **shipped 2026-07-24**. Chapter 10 (`10-fundraise`) now carries a second `sections[]` entry `dual-class-decision` (heading + 8-bullet body EN + VI) via the previously-declared `Chapter.sections?: ChapterSection[]` slot (web/src/lib/guide/startup-journey.ts:66). The section walks the decision end-to-end: (a) AU default = single-class ordinary under Corporations Act 2001 (Cth) Chapter 2D replaceable rules + ASIC Form 201, (b) what dual-class actually is (Class B founder-controlled 10-votes-per-share, Class A public one-vote-per-share, typically pari-passu on economics, common auto-conversion + sunset triggers), (c) why ASX blocks it (Listing Rule 6.9 requires one-vote-per-share on a poll — no waiver track exists for straight founder-control), (d) the Atlassian precedent (2014 UK Plc reorg for dual-class capability at page.tsx:128-136 → 2015 NASDAQ dual-class IPO → 2022 UK Plc → Delaware Scheme of Arrangement at page.tsx:175-181), (e) the two live paths (UK Plc scheme: Companies Act 2006, UK High Court sanction, ITAA 1997 Subdiv 124-M elective rollover, ~A$300k+ / 4-9 mo; Delaware C-corp: DGCL s102(a)(4), PFIC + GILTI + CFC exposure, ~A$500k+ / 6-12 mo), (f) governance + investor-appetite reality (AICD 2024 Governance Principles amber flag, S&P/ASX + MSCI index weight adjustments, Blackbird / AirTree / Square Peg have funded Canva + Culture Amp dual-class-in-US so appetite exists but term sheets carry sunset + protective-provisions basket), (g) the BlockID.au boundary — Workspace → Fundraise → Structure surfaces the decision as an education artefact + warm-intro list to Corrs / HSF / DLA / Cooley but does NOT auto-generate the Scheme of Arrangement, Delaware certificate, or PFIC election (s911A + s923B Corps Act exposure guard), (h) the single-class-with-founder-protections alternative that gives ~70% of the control benefit at ~5% of the structural cost (double-trigger vesting, founder-veto on named board matters, ROFR-on-transfer-to-competitor, founder-nominee director seat surviving dilution — all inside standard Blackbird / AirTree templates). Both marketing (`/guide/10-fundraise`) and workspace (`/workspace/guide/10-fundraise`) surfaces render the new section via the existing `c.sections?.map()` blocks (no page edits needed — reuses the P1l wiring). Tests: `web/src/lib/guide/startup-journey.test.ts` gains a "dual-class decision section (chapter 10 P1m)" describe block — 2 vitest cases pinning (a) presence + EN/VI alignment ≥ 6 bullets, (b) statutory + precedent anchor grep (Listing Rule 6.9, Class A, Class B, UK Plc, Delaware, Scheme of Arrangement, Subdiv 124-M, s911A, sunset, single-class). All 19 startup-journey tests green (17 existing + 2 new). Closes open_question Q4 recommendation ("at Phase 9/10 add a decision-point doc 'single-class default; dual-class needs offshore parent' with links to StartupDaily coverage; do NOT auto-generate offshore incorporation documents") and § P1_dataroom_map's raise-critical decision surface for offshore-parent founders. Workspace → Fundraise → Structure UI (the 4-question quiz that returns the recommendation) is the natural next tick — this ships the content, not the interactive quiz.
- **P1n** WGEA + Modern Slavery threshold detectors (auto-fire on headcount / revenue crossings) — **shipped 2026-07-24**. Two pure detectors added under `web/src/lib/compliance/`, mirroring the P1i `gst-threshold.ts` shape so downstream cron / dashboard-panel wiring can reuse the same result-envelope contract.
  - `wgea-threshold.ts` — `assessWGEA(input, now)` + `assessWGEAFromHeadcount(headcount, opts)` shortcut for callers that only know a current Australian headcount. Anchors on **Workplace Gender Equality Act 2012 (Cth) s3** (100-employee "relevant employer" definition) and **s3B(2)** (2-reporting-period grace once you drop below 100). Five-branch matrix: `not_required` (< 80) / `approaching_threshold` (80–99 warning band) / `report_required` (peak-in-trailing-12mo ≥ 100 — uses peak, not current, so a mid-year hire spike still fires) / `grace_period` (previously reported but now below 100, remaining periods surfaced in the reasoning string) / `already_reporting` (fires when `last_report_lodged_at` set + still a relevant employer). Also computes `next_report_due_iso` — the WGEA public-report deadline is 31-May immediately after the 1-Apr → 31-Mar reporting period, rolls to next year once today is past 31-May. `WGEA_DISCLAIMER` fixed on every result so the surface can never render the classification without the "confirm with WGEA helpline / registered consultant" hedge (avoids s923B / s911A Corps Act exposure).
  - `modern-slavery-threshold.ts` — `assessModernSlavery(input, now)` + `assessModernSlaveryFromRevenue(revenueAud, opts)` shortcut. Anchors on **Modern Slavery Act 2018 (Cth) s5** (A$100M consolidated-revenue threshold + Australian-entity-or-carrying-on-business-in-Australia scope), **s13** (statement due within 6 months of FY end, governing-body approval + responsible-member signature required), **s16** (7 mandatory reporting criteria), and **s6** (voluntary opt-in for entities below the threshold — surfaced in the approaching_threshold reasoning string as an acquirer-signal opportunity). Six-branch matrix: `not_required` (< 80M or foreign-entity-not-carrying-on-business-in-AU) / `approaching_threshold` (A$80M–99M warning band) / `statement_required` (projected current-period ≥ A$100M — pre-FY-end, warning) / `statement_due_soon` (last-FY ≥ A$100M + FY has ended + within 6-month window, critical) / `statement_overdue` (window has passed, critical + days-overdue count) / `already_lodged` (fires when `last_statement_lodged_at` set + last FY was above threshold). Also computes `statement_due_iso` (FY end + 6 months, clamped to end-of-target-month so 31-Dec-2026 → 30-Jun-2027 not 1-Jul-2027) and `days_until_statement_due` (signed integer, negative when overdue). `MODERN_SLAVERY_DISCLAIMER` cites AASB 10 controlled-entity aggregation so callers know "consolidated revenue" is not just the parent-entity P&L.
  - Test coverage — `wgea-threshold.test.ts` (13 vitest cases across 4 describe blocks: branch matrix incl. peak-headcount uses history not just current, grace-period-exit after WGEA_GRACE_PERIODS elapse, NaN / negative / non-numeric-string tolerance, shortcut behaviour incl. lodged-report roundtrip, deadline-rolls-year-boundary at 31-May, constants pin) + `modern-slavery-threshold.test.ts` (16 vitest cases across 4 describe blocks: branch matrix incl. projected-vs-last-FY split, statement_due_soon vs statement_overdue day-count sign, foreign-entity-not-carrying-on-business exit, NaN / negative tolerance, shortcut behaviour incl. carrying-on-business flag propagation, 30-Jun and 31-Dec FY-end 6-month-window arithmetic, constants pin). All 93 `src/lib/compliance/` tests green (64 existing + 29 new).
  - Wire-in path — the two `assess*` helpers are pure (no I/O, no imports besides types) and take a `now` arg, so a future `/api/compliance/wgea-threshold` + `/api/compliance/modern-slavery-threshold` route can reuse the exact pattern `web/src/app/api/compliance/gst-threshold/route.ts` established (auth-gated POST that persists to a new `compliance_wgea_status` / `compliance_modern_slavery_status` table alongside `compliance_gst_status`), and the existing `web/src/components/dashboard/compliance-panel.tsx` tile can grow two more rows keyed off the same result-envelope shape. That schema migration + dashboard-panel wiring is a separate tick (P1n-persist) — this tick ships the detector primitives only, closing **§1 phase 11 "WGEA + Modern Slavery threshold detectors (currently upload-only in data-room, no automated headcount/turnover trigger)"** and **§2 folder 12 items 12.5 (WGEA Compliance Report — was NUDGE with headcount trigger missing) and 12.6 (Modern Slavery Act Statement — was NUDGE with revenue trigger missing)**.
- **P1n-persist** WGEA + Modern Slavery persistence + dashboard wiring — **shipped 2026-07-24**. Delivers the schema + route + UI half of P1n so the pure detectors ship with a real end-to-end surface.
  - Migration `web/supabase/migrations/0112_compliance_wgea_modern_slavery.sql` — two tables `compliance_wgea_status` + `compliance_modern_slavery_status` mirroring the `compliance_gst_status` shape from 0108 (`input_json` / `result_json` snapshots + boolean flags + action_required text + urgency text + computed_at). RLS enabled default-deny (service-role only; founders reach the data through the auth-gated routes). Index on `(user_id, project_id, computed_at DESC)` matches the "latest snapshot" query pattern. Apply via the `docker exec -i supabase-db psql -U supabase_admin -d postgres -v ON_ERROR_STOP=1 < 0112_compliance_wgea_modern_slavery.sql` + `NOTIFY pgrst, 'reload schema'` recipe pinned at the top of every compliance migration (reference_db_migrations memory: manual apply — never auto on deploy).
  - Route `web/src/app/api/compliance/wgea-threshold/route.ts` (POST + GET, `dynamic = "force-dynamic"`) — mirrors `gst-threshold/route.ts` exactly: `getCurrentUser()` gate → JSON parse → required-field validation (`current_headcount_au: number`, `has_previously_reported: boolean`) → `assessWGEA(body)` → persist via `getSupabaseAdmin()` scoped to `getActiveProject(user.id)`. GET returns the latest saved snapshot or `{ result: null }` when unpersisted. Every non-happy-path response carries `WGEA_DISCLAIMER`.
  - Route `web/src/app/api/compliance/modern-slavery-threshold/route.ts` — same shape (POST + GET), required field is just `current_period_revenue_aud: number` (the rest of `ModernSlaveryInput` is optional per the pure-helper contract). Every response carries `MODERN_SLAVERY_DISCLAIMER`.
  - Dashboard `web/src/components/dashboard/compliance-panel.tsx` — grows from 4 tiles to 6. Adds `WgeaTile` (5-branch matrix: `not_tracked` slate / `report_required` red / `already_reporting` emerald / `grace_period` amber / `approaching_threshold` amber / `not_required` emerald) and `ModernSlaveryTile` (7-branch matrix mirroring the `ModernSlaveryActionRequired` union: `not_tracked` / `statement_overdue` red / `statement_due_soon` red or amber depending on days_until / `statement_required` amber / `approaching_threshold` amber / `already_lodged` emerald / `not_required` emerald). Grid switches from `lg:grid-cols-4` to `lg:grid-cols-3` (2 rows × 3 cols on desktop; 2×3 grid keeps parity with the existing GST tile height). Deep-links `/compliance/wgea` + `/compliance/modern-slavery` route stubs a follow-up milestone will build out (mirrors the existing `/compliance/{esic|s708|gst|rd}` stubs that Round 5.5a already routed).
  - Typecheck: `npx tsc --noEmit -p tsconfig.json` green (no diagnostics). No new test file added this tick — the persistence layer is a pass-through around the pure detectors, which already carry 29 vitest cases from P1n; a Playwright spec exercising the two routes end-to-end is deferred to a future tick that also builds out the `/compliance/wgea` + `/compliance/modern-slavery` detail pages.
  - Closes the "persist + dashboard-panel wiring is a follow-up (P1n-persist)" note in the P1n entry above and completes the WGEA + Modern Slavery user-facing loop: founder POSTs headcount / revenue → detector classifies → snapshot persists → dashboard tile renders on next load with the correct action band. Founder-driven POST hookup (a form on the `/compliance/wgea` + `/compliance/modern-slavery` detail pages that submits the input JSON) is the natural next tick (P1n-form) — the routes are ready to consume it today.
- **P1n-form** WGEA + Modern Slavery detail-page forms — **shipped 2026-07-24**. Closes the final "founder-driven POST hookup" leg of the P1n → P1n-persist chain: the dashboard tiles have deep-linked to `/compliance/wgea` + `/compliance/modern-slavery` since P1n-persist Round 6.3, but until this tick those routes 404'd. Two Next.js App Router pages ship with server auth-gates + client forms + colocated pure helpers + vitest coverage, mirroring the P1e ESIC self-assessment shape but simpler (single-limb form each, no WorkspaceLayout wrap because the routes live outside `/workspace/*`).
  - `web/src/app/compliance/wgea/page.tsx` — server component with `getCurrentUser()` gate (redirect to `/auth/login?next=/compliance/wgea` when anon), loads the latest saved snapshot via `getSupabaseAdmin()` scoped to `getActiveProject(user.id)`, and renders `<WgeaFormClient>` with `initialInput / initialResult / disclaimer=WGEA_DISCLAIMER`. `dynamic = "force-dynamic"` + `robots: noindex,nofollow` on the metadata so the calculator does not leak into search.
  - `web/src/app/compliance/wgea/wgea-form-client.tsx` — client component with two sections (`Current headcount` + `Prior reporting history`), a Toggle for `has_previously_reported`, TextField for `current_headcount_au` (required), a CSV TextField for `monthly_headcount_last_12_months` (optional, comma or space separated — parsed by the helper), a date-typed TextField for `last_report_lodged_at`, and a numeric TextField for `reporting_periods_since_last_over_threshold`. Submits `toWgeaInput(state)` to `POST /api/compliance/wgea-threshold`, renders the returned `WGEAResult` in a coloured banner keyed by `pickWgeaBand(result)` (red = report_required, amber = grace_period / approaching_threshold, green = already_reporting / not_required) with the reasoning string + peak-headcount + `next_report_due_iso`. `data-testid="wgea-result-banner"` on the result section so a future Playwright spec can assert the render.
  - `web/src/app/compliance/wgea/wgea-form.helpers.ts` — pure form helpers matching the P1e ESIC pattern: `WgeaFormState` (all-string field shape), `makeEmptyWgeaFormState()`, `toWgeaInput(state) → WGEAInput` (CSV parser drops non-numeric + negative entries silently, empty date/periods coerce to `undefined` so the POST body matches the wire contract), `fromWgeaInput(wire) → WgeaFormState` (round-trips the CSV history back to a comma-joined string), and `pickWgeaBand(result) → "green"|"amber"|"red"` that mirrors the dashboard compliance-panel WGEA tile colours so the pre-click + post-click states never disagree.
  - `web/src/app/compliance/wgea/wgea-form.helpers.test.ts` — 12 vitest cases across 4 describe blocks: `makeEmptyWgeaFormState` defaults; `toWgeaInput` (numeric parsing + CSV split with whitespace-or-comma separator + non-numeric/negative-entry drop + grace-period metadata preservation + garbage-input zero-fallback); `fromWgeaInput ↔ toWgeaInput` round-trip (fully-populated + minimal); `pickWgeaBand` covering all 5 branches (report_required → red, approaching_threshold → amber, grace_period → amber, already_reporting → green, not_required → green) — each anchored on an `assessWGEA()` call against the real detector so a helper-side band drift would show up as a test failure.
  - `web/src/app/compliance/modern-slavery/page.tsx` — identical server-page shape (auth gate → load latest snapshot → render `<ModernSlaveryFormClient>` with `disclaimer=MODERN_SLAVERY_DISCLAIMER`).
  - `web/src/app/compliance/modern-slavery/modern-slavery-form-client.tsx` — client component with two sections (`Revenue` + `Reporting history & scope`), four AUD TextFields (`current_period_revenue_aud` required + `projected_full_period_revenue_aud` optional + `last_full_period_revenue_aud` optional + `current_fy_end_iso` optional date), a Toggle for `is_australian_or_carrying_on_business_in_au` (default on — leave off only after formal advice), a date TextField for `last_statement_lodged_at`. Submits `toModernSlaveryInput(state)` to `POST /api/compliance/modern-slavery-threshold`, renders the `ModernSlaveryResult` in a coloured banner keyed by `pickModernSlaveryBand(result)` — banner surfaces current + projected + last-FY revenue (AU-formatted with `toLocaleString("en-AU")`), `statement_due_iso`, `days_until_statement_due`, plus the reasoning string. `data-testid="modern-slavery-result-banner"` for downstream E2E hook-in.
  - `web/src/app/compliance/modern-slavery/modern-slavery-form.helpers.ts` — pure form helpers: `ModernSlaveryFormState` shape (all-string number fields + boolean scope + two ISO dates), `makeEmptyModernSlaveryFormState()`, `toModernSlaveryInput(state) → ModernSlaveryInput` (required current-period revenue with garbage-fallback to 0, optional fields coerced to `undefined` when blank or non-numeric so the wire contract matches `ModernSlaveryInput` optionality), `fromModernSlaveryInput(wire) → ModernSlaveryFormState`, and `pickModernSlaveryBand(result) → "green"|"amber"|"red"` — the amber/red split on `statement_due_soon` uses `days_until_statement_due ≤ 30 ⇒ red` so a founder inside the last month sees the critical band even though the action-required key is the same.
  - `web/src/app/compliance/modern-slavery/modern-slavery-form.helpers.test.ts` — 13 vitest cases across 4 describe blocks: `makeEmptyModernSlaveryFormState` defaults (AU-carrying-on-business = true by default); `toModernSlaveryInput` (required-only path drops all optionals + full-optional path preserves all fields + garbage-numeric zero-fallback with non-numeric-optional undefined-fallback + foreign-entity opt-out flag); `fromModernSlaveryInput ↔ toModernSlaveryInput` round-trip (fully-populated + minimal); `pickModernSlaveryBand` covering 5 branches (statement_overdue → red anchored on a real assessModernSlavery call with last-FY 150M + FY ended, approaching_threshold → amber at 85M, statement_required → amber with future FY end + projected 120M, already_lodged → green with last-statement-lodged-at set, not_required → green via foreign-entity-not-carrying-on-business scope opt-out).
  - Both suites green: 25 tests across the two colocated `.test.ts` files (12 wgea + 13 modern-slavery). Typecheck `npx tsc --noEmit -p tsconfig.json` green. Closes the final "P1n-form" follow-up flagged at the end of the P1n-persist entry above and completes the WGEA + Modern Slavery user loop end-to-end: dashboard tile → deep-link → detail page form → POST → detector → persistence → next-page-load surfaces the result. `/compliance/esic` / `/compliance/s708` / `/compliance/gst` / `/compliance/rd` remain stubbed deep-links today — the ESIC form already lives at `/workspace/esic-assessment` and the other three surfaces feed off existing dashboards, so those routes are lower-value follow-ups (P1n-esic-route + P1n-gst-route etc.) that only exist to remove the 404 on the dashboard tile links.

### Round 6 follow-ups (spun off from P5 / P6 / P7)

- **P5a** true readiness_by_phase(input) helper — **shipped 2026-07-24**. Pure module `web/src/lib/nudge/readiness-by-phase.ts` exports `computeReadinessByPhase({ sviScores, dataroomRows }) → Record<PhaseKey, { score, band: "not-ready"|"warming-up"|"investor-ready", missing_top3: NudgeMissingItem[], criteria_used: CriterionKey[] }>`. Each of the 12 phases picks its own `CriterionKey` subset via `PHASE_CRITERION_SUBSET` (derived from §1 of this goal — e.g. phase 3 = market + customer_size + gtm_strategy, phase 9 = documents + dataroom + revenue + team + gtm_strategy, phase 12 = revenue + documents + team_structure + dataroom) and scores it as a weighted average using `evaluation-criteria.ts` CRITERIA[i].weight so the readiness signal stays consistent with the composite enhanced SVI score. Missing artefacts are phase-scoped (only rows with `phaseSlug ≤ phase`), filter out any `dataroom_files` row whose status is not `missing`/`archived`, and sort raise-blockers-first — the top 3 land in `missing_top3`. Bands mirror the existing tile UX: ≥75 investor-ready, ≥50 warming-up, else not-ready. Wired into `computeNextSteps` via a new `readiness_by_phase: Record<string, PhaseReadinessEntry>` field on `NudgeResult` (stringified PhaseKey ordinals so JSON round-trips cleanly through fetch on the client); the API route now surfaces it under `body.result.readiness_by_phase` on the existing `/api/nudge/next-steps` endpoint (no separate route). Test coverage in `web/src/lib/nudge/readiness-by-phase.test.ts` — 8 vitest cases pinning (a) 12-phase completeness + not-ready floor, (b) every subset only cites known `CRITERION_KEYS`, (c) weighted-average arithmetic (phase 3 with market/customer_size/gtm_strategy = 90/60/30 lands on 64 → warming-up), (d) band thresholds (all-80 → investor-ready across every phase, all-55 → warming-up), (e) dimension-key fallback + 0..1 fractional score normalisation, (f) missing_top3 phase scope + raise-blockers-first ordering, (g) present-dataroom-row filtering, (h) NaN / clamp / negative tolerance. All 8 tests green. Sibling tests still pass (20 tests across nudge + tile helpers). Typecheck green. Tile-side per-phase view (switching InvestorReadinessTile to consume `readiness_by_phase[currentPhase]` instead of the blended sub_scores tuple) remains a follow-up under P5a-tile — this tick ships the engine + API surface; the visual switch is a self-contained tile refactor that carries its own Playwright spec update.
- **P5a-tile** switch `<InvestorReadinessTile />` to consume `readiness_by_phase[currentPhase]` — **shipped 2026-07-24**. `web/src/components/dashboard/investor-readiness-tile.tsx` now branches on `pickPhaseEntry(result.readiness_by_phase, current_phase.slug)`: when a per-phase entry is present the tile renders the *per-phase view* (score + band + phase label header + 12-phase mini-chart with the current phase outlined in indigo + top-3 missing artefacts with `Blocker` chips for raise-blockers), and when it's absent (older API responses / not-started projects) it falls back to the *blended view* (overall score + 5 sub-score bars + "how to improve" hint) so nothing regresses. New pure helpers in `web/src/components/dashboard/investor-readiness-tile.helpers.ts`: `ALL_PHASE_SLUGS`, `PHASE_BAND_CLASS` (Tailwind pack keyed on `ReadinessBand`), `pickPhaseEntry(byPhase, slug)` (null-safe), `buildPhaseSeries(byPhase, slug)` (fills gaps with score=0/band=not-ready so the chart never has holes, clamps out-of-range scores via `safeScore`, marks the current phase with `isCurrent: true`). Legacy exports (`bandOf`, `pickWeakest`, `colourFor`, `SUB_SCORE_ORDER`, `SUB_SCORE_LABELS`, `safeScore`) retained so the blended fallback view + downstream callers keep working. The `data-view="per-phase"|"blended"` attribute on the tile root lets tests + shell wiring branch without inspecting internals. Vitest coverage in `investor-readiness-tile.helpers.test.ts` grows from 10 to 22 cases (12 legacy + 10 new pinning `ALL_PHASE_SLUGS` completeness, `PHASE_BAND_CLASS` colour parity, `pickPhaseEntry` null/undefined/missing-slug/non-numeric-score branches, `buildPhaseSeries` 12-point completeness + current-flag exclusivity + gap-fill + null-map tolerance + `safeScore` clamp on overshoot). Playwright spec `investor-readiness-tile.spec.ts` FAKE_PAYLOAD grew a full 12-phase `readiness_by_phase` map (with 3 missing_top3 entries on phase 6 — two raise-blockers + one diligence artefact); assertions now check `data-view="per-phase"`, the 12-slug ordering of the phase series, exactly one `data-current="true"` bar (at index 5 = phase 6), and that the top-3 missing list renders in `[true, true, false]` raise-blocker order. Skip path preserved so the spec passes cleanly on shells that haven't mounted the tile yet (P5b). Typecheck `npx tsc --noEmit -p tsconfig.json` green; targeted vitest `src/lib/nudge src/components/dashboard` green (47 tests across 4 files). Closes the P5a follow-up "Tile-side per-phase view (switching InvestorReadinessTile to consume `readiness_by_phase[currentPhase]` instead of the blended sub_scores tuple) remains a follow-up under P5a-tile". P5b (mounting the tile into `/dashboard/svi`) remains open — the sibling shell-layout agent owns that wiring; the mount-instruction comment at the top of the .tsx file still points to the correct slot.
- **P5b** wire <InvestorReadinessTile /> into `web/src/app/dashboard/svi/page.tsx` (or the founder shell) once the sibling shell-layout agent unlocks — **shipped 2026-07-24**. Mounted the tile in `web/src/app/dashboard/svi/page.tsx` immediately after `<NextStepTile />` in the founder shell. Both tiles hit `GET /api/nudge/next-steps` on mount (client-side, cache: no-store) so the founder sees the phase pill + next action (NextStepTile) plus the per-phase score + 12-phase mini-series + top-3 missing (InvestorReadinessTile) in a single dashboard scroll. Removed the deferred-mount comment in `web/src/components/dashboard/investor-readiness-tile.tsx` and replaced with a "shipped 2026-07-24" marker. Typecheck (`npx tsc --noEmit -p tsconfig.json`) green. E2E spec `web/tests/e2e/founder/investor-readiness-tile.spec.ts` — previously self-skipping via `test.skip(!mounted, ...)` — will now activate on the next Playwright run (seeded qa-founder required per `QA_FOUNDER_NUDGE_EMAIL`). Closes the last Round-6 follow-up; the only remaining P-track item on this goal is P8 (human_blocked) → P9 (proposed, depends on P8).
- **P6a-ir-pack** Chapter 8 body imports the shared Div 83A qualifying-tests fixture — **shipped 2026-07-24**. `web/src/lib/guide/startup-journey.ts` now imports `div83aQualifyingTestsFor` from `web/src/lib/compliance/div83a-qualifying-tests.ts` and Chapter 8's `qualifyingTests` block is `{ en: [...div83aQualifyingTestsFor("en")], vi: [...div83aQualifyingTestsFor("vi")] }` — the 40-line inline duplicate is gone. Copy drift between the guide body, the funding gate, and the (future) ESS scheme-rules appendix is now a type error, not a silent divergence. Existing pins hold: (a) `web/src/lib/guide/startup-journey.test.ts` "chapter 08-team publishes the eight Div 83A tests EN + VI" still passes (8/8 EN + 8/8 VI, every line > 20 chars), (b) `web/src/lib/compliance/div83a-qualifying-tests.test.ts` "matches the Chapter 8 body copy verbatim (no copy drift)" still passes — it now checks that Chapter 8 rebroadcasts the shared list, which is trivially true after the swap. All 24 tests across the two suites green. Typecheck `npx tsc --noEmit -p tsconfig.json` green (spread-to-mutable-array satisfies `LocalisedList` without weakening `readonly` on the source fixture). Closes the P6a note "The ch09 investor-readiness pack import will land in a follow-up (P6a-ir-pack) that swaps the packing library to consume `DIV83A_QUALIFYING_TESTS` directly instead of re-declaring the list" for the guide-side consumer; the investor-pack-assembler (web/src/lib/investor-pack-assembler.ts) has no Div 83A section today, so wiring an ESS-scheme-rules appendix onto that PDF is a separate feature-add, not a duplication cleanup.
- **P6a** Div 83A ESOP scheme-rules gate — **shipped 2026-07-24**. Two-file split mirroring the ESIC gate: (1) `web/src/lib/compliance/div83a-qualifying-tests.ts` exports the 8-point ATO start-up concession checklist as a typed fixture (`DIV83A_QUALIFYING_TESTS: readonly Div83AQualifyingTest[]` — each row carries `{key, section, en, vi}` with EN + VI parity + a stable ITAA-1997 anchor per test) plus `div83aQualifyingTestsFor(locale)` accessor and `DIV83A_QUALIFYING_TESTS_DISCLAIMER` — the fixture is the source-of-truth copy previously duplicated inside Chapter 8 body (`web/src/lib/guide/startup-journey.ts:662-682`) so future ticks can import it instead of re-declaring. (2) `web/src/lib/compliance/div83a-funding-gate.ts` exports `assertDiv83AEligibleOrWarn(supabase, {userId, projectId, requireEligible, action})` mirroring the `assertESICEligibleOrWarn()` shape exactly — reads `esop_option_grants` (migration `0089_esop_grants.sql`) scoped to `(user_id, project_id, status='active')`, applies a worst-first precedence over the branch matrix (`grants_ineligible` → `grants_unsure` → `unchecked_grants` → `check_stale` → pass), and returns the same `Div83AGateOk | Div83AGateFail` envelope shape as the ESIC gate. Six reasons: `no_grants_on_file` (pass-through — a round without ESOP grants cannot mislead an employee about Div 83A), `unchecked_grants` (grant with `div83a_status = NULL`), `check_stale` (`div83a_last_checked_at > 180 days`; stale-after horizon deliberately longer than the ESIC 90-day window because grant terms rarely change between checks — the check only re-fails when the *company* crosses the s83A-33(1)(a) A$50m turnover cap or the s83A-33(1)(c) 10-year age cap), `grants_unsure`, `grants_ineligible`, and `db_unavailable`. Warn-only by default (attaches `div83a_warn` envelope to the fundraise 200 response + emits `div83a_gate.warn_*` analytics); returns 412 when body.wholesaleOnly=true so wholesale-only rounds that pitch the ESS start-up concession cannot ship while an active grant fails. JSDoc cites ITAA97 Subdiv 83A-B/83A-C, ATO ESS start-up concession guidance, and Corps Act s911A / s1041H exposure. Wired into `web/src/app/api/fundraise/route.ts` immediately after the ESIC gate — same `try` block, same `getActiveProject(user.id)` lookup, same 412-on-block / warn-envelope-on-pass semantics; happy-path 200 response now carries `esic_warn?` AND `div83a_warn?` when either fires. Test coverage: `div83a-qualifying-tests.test.ts` (5 vitest cases pinning 8-row EN/VI parity, unique keys, s83A statutory anchor grep, verbatim match against Chapter 8 body copy via `getChapter("08-team")`, and disclaimer presence) + `div83a-funding-gate.test.ts` (15 vitest cases across 2 describe blocks pinning both warn-only and blocking-mode branch matrices — empty-grants pass, all-eligible-fresh pass, unchecked warn/block, stale warn/block, unsure warn/block, ineligible warn/block with precedence over unchecked+stale, db-null and query-error both surface as `db_unavailable`). All 113 `src/lib/compliance/` tests green (93 existing + 20 new). Typecheck `npx tsc --noEmit -p tsconfig.json` green. Closes P6 exit-criterion #2 ("all 8 Div83A tests already in startup-journey.ts:621-641 exposed via a shared module and wired into ch09 investor-readiness pack"). The ch09 investor-readiness pack import will land in a follow-up (P6a-ir-pack) that swaps the packing library to consume `DIV83A_QUALIFYING_TESTS` directly instead of re-declaring the list — this tick ships the shared module + gate; the packer swap is a self-contained refactor.
- **P6b** wholesale-only fundraise UI — **shipped 2026-07-24**. `web/src/app/workspace/fundraise/fundraise-client.tsx` grows a `wholesaleOnly` state + amber-highlighted opt-in card in Step 0 (below the SAFE/Convertible fields, above the quick summary) that flips the two `/api/fundraise` compliance gates (ESIC + Div 83A) from warn-mode to blocking-mode (412). Copy explains the trade-off — "tick this if you're marketing the round only to sophisticated / professional investors and pitching the ESIC 20% offset or Div 83A ESS start-up concession as a material claim; BlockID will refuse to save the round if your ESIC self-assessment is missing / stale / negative, or if any active ESOP grant fails the Div 83A gate" — with the s708(8)/(11) + s761G(7) statutory anchor in the label. Client posts `wholesaleOnly` in the request body alongside the existing FundraiseRound fields (matches `FundraisePostBody` in `web/src/app/api/fundraise/route.ts:18-20` — additive slot, no schema change). On 412 `calculateDilution()` sets a new `gateBlock` state and renders a red `data-testid="fundraise-gate-block"` banner with the block reason, human message, ESIC/Div 83A `disclaimer`, and a "Fix now" button linking to `url_to_fix` (`/compliance/esic` or `/compliance/div83a`). On 200 the client harvests `esic_warn` / `div83a_warn` envelopes into a `gateWarns` list and renders them in an amber `data-testid="fundraise-gate-warn"` banner ("Round saved — compliance follow-ups outstanding") with per-item "Resolve" deep-links, so warn-mode founders still see the compliance pointer without breaking the flow. Two structural interfaces (`GateWarn`, `GateBlock`) inline in the client so the file stays server-boundary-free — no server-side imports leaked. Typecheck `npx tsc --noEmit -p tsconfig.json` green. Closes the P6b follow-up in this file. Founder-side vitest / Playwright coverage of the toggle + banner branch matrix is a natural follow-up (P6b-test) — the gate behaviour itself is already pinned by the 25 vitest cases across `esic-funding-gate.test.ts` + `div83a-funding-gate.test.ts`, so this tick ships the UI wiring first and defers the E2E to the sibling QA agent.
- **P7a** founder-side weekly digest — **shipped 2026-07-24**. `GET /api/cron/founder-weekly-digest` (Bearer `CRON_SECRET`, `FOUNDER_DIGEST=off` kill switch, `?skip_email=1` dry-run) queries `app_users` where `account_type='founder'` and `last_login_at >= now()-30d`, then per founder: (1) loads active project + phase progress + latest SVI dimensions + data-room rows + evidence + compliance status using the same loader shape as the `/api/nudge/next-steps` route (inlined to avoid API↔cron coupling), (2) calls `computeNextSteps()` to derive current phase + next action + missing top-5 + readiness score, (3) fetches the previous `svi_readiness_snapshots` row via `fetchLatestReadinessSnapshot({..., before: nowIso})` for the delta baseline, (4) projects the nudge into a `svi_readiness_snapshots` row via `toSnapshotRow(..., source: 'digest')` and computes `computeReadinessDelta(current, previous)`, (5) renders the email via a new pure `buildFounderDigest()` helper (`web/src/lib/email/founder-digest.ts` — subject varies with `band_direction` up/down/same, HTML surfaces score + band label + delta arrow + next-action card + top-3 gap list with red `Blocker` chips on raise-blockers + AFSL disclaimer, plain-text mirror), (6) honours `canSendEmail(email, "weekly_reports")` (fully opt-out-safe — silently skip when the founder unsubscribed), (7) re-renders once with the `getUnsubscribeUrl(token, "weekly_reports")` link once we have it, (8) sends via `sendEmail()` and only persists the fresh snapshot via `writeReadinessSnapshot()` on a successful send so a partial failure does not corrupt next week's baseline. Test coverage `web/src/lib/email/founder-digest.test.ts` — 8 vitest cases across 3 describe blocks pinning (a) subject line branches (band-up → "investor-ready" mention + score, band-down → "slipped" + "blocker" wording, same-band → neutral "Phase N · N/100" fallback), (b) HTML body render (score + band label + delta line + next-action title/reason/cta + all 3 missing items + exactly 2 `Blocker</` occurrences when only 2 of 3 are raise-blockers + AFSL disclaimer + unsubscribe URL round-trip), (c) `nextAction: null` renders "caught up for this phase" copy + omits the "Top 3 gaps" block, (d) score clamp for 999 → 100 and NaN → 0, (e) HTML/attr injection — script tags escape to `&lt;script&gt;`, quotes escape to `&quot;`, ampersands escape to `&amp;`, (f) plain-text mirror carries phase / readiness / next action / `[BLOCKER]` chip / disclaimer. All 32 tests across `src/lib/email` + `src/lib/nudge` green (24 existing + 8 new). Typecheck `npx tsc --noEmit -p tsconfig.json` green. Closes P7 exit criterion #1 ("digest section 'Your investor readiness this week' renders {score band, delta vs last week, single next action, top-3 missing}") — the investor-side digest (P7 main) covered the *investor* persona; this ships the *founder* persona that P7 flagged as follow-up. Cron install line (`curl -H "Authorization: Bearer $CRON_SECRET" https://blockid.au/api/cron/founder-weekly-digest`) goes into the operator's crontab per `docs/ops/crontab-setup.md`; suggested cadence is Monday 07:00 AEST to match the reseller / investor digest windows. Deep-links into `/dashboard` for now; a future tick can route the missing-item CTAs into per-phase workspace panels once the sibling shell-layout agent unblocks P5b.
- **P7b** persist last-week readiness snapshot per project — **shipped 2026-07-24**. Delivers the baseline table + pure snapshot helpers so P7a's founder-side weekly digest can compute a week-over-week readiness delta without recomputing every historic tick. Migration `web/supabase/migrations/0113_svi_readiness_snapshots.sql` — table `svi_readiness_snapshots(id, user_id, project_id, phase_slug, overall_score, band, readiness_by_phase, missing_top3, next_action_title, source, computed_at)` mirroring the `compliance_gst_status` shape from 0108: default-deny RLS, `(user_id, project_id, computed_at DESC)` composite index for the "latest snapshot per founder-project" read + a secondary `computed_at DESC` index for the cron-side "scan-recent-snapshots" pattern the P7a cron will use to page through founders. `overall_score` guarded by a `[0,100]` CHECK, `band` guarded by a 3-value CHECK (`not-ready`/`warming-up`/`investor-ready`), `source` guarded by a 4-value CHECK (`nudge_engine`/`manual`/`cron`/`digest`) so the read side can filter to just the digest-eligible rows. Applied via the pinned docker-exec-psql-then-NOTIFY-pgrst recipe (reference_db_migrations memory — manual apply, never auto on deploy). Pure module `web/src/lib/nudge/readiness-snapshots.ts` exports (1) `toSnapshotRow(nudgeResult, {userId, projectId, source?})` — projects the `NudgeResult` from `computeNextSteps()` into the wire shape, clamps `readiness_score.overall` to `[0,100]`, derives the `band` from the clamped score using the same ≥75/≥50/else thresholds as the tile UX contract, keeps `project_id` null when a founder has no active project, and defaults `source` to `nudge_engine` so the /api/nudge/next-steps route can persist a snapshot on every fetch without a source arg; (2) `computeReadinessDelta(current, previous)` — pure delta helper returning `{score_delta, summary, band_direction, previous_computed_at}` covering four branches — first-snapshot (previous=null → `"First readiness snapshot"` summary + delta 0), same band + non-zero delta (renders `+N points up` / `−N points down`), band-up crossing (`crossed a band boundary upward` summary), band-down crossing (`slipped into a lower readiness band`); tolerates NaN / non-finite scores on the previous row so a corrupted historic snapshot cannot crash the digest render; (3) thin I/O helpers `writeReadinessSnapshot(sb, row)` (single-row insert returning `{id, computed_at}` or null on error) and `fetchLatestReadinessSnapshot(sb, {userId, projectId, before?})` (most-recent-first read; the optional `before` cutoff is what P7a will use to fetch the pre-digest baseline while writing the fresh snapshot in the same run). Test coverage `web/src/lib/nudge/readiness-snapshots.test.ts` — 12 vitest cases across two describe blocks: `toSnapshotRow` (6 cases pinning wire-shape projection + missing_top3 truncation to 3 + project_id null handling + score-clamp-and-band-derive for both overshoot (999→100 band investor-ready) and NaN/negative (→0 band not-ready) + source override for cron/digest/manual + empty-next-action tolerance); `computeReadinessDelta` (6 cases pinning first-snapshot summary + positive delta same-band summary + band-up crossing summary + band-down crossing summary with negative delta + no-change summary + NaN-previous-score tolerance so `Number.isFinite(delta.score_delta)` holds). All 12 tests green. Typecheck `npx tsc --noEmit -p tsconfig.json` green. Closes P7b entirely — the founder-side weekly digest cron (P7a) can now call `computeNextSteps()` → `toSnapshotRow()` → `writeReadinessSnapshot()` to persist the week's snapshot, then `fetchLatestReadinessSnapshot(..., {before: nowIso})` to grab the previous week's baseline, then `computeReadinessDelta()` to render the "Your investor readiness this week" digest section without a schema change on `startup_phase_progress`.

- **P6b-test** wholesale-only fundraise UI test coverage — **shipped 2026-07-24**. Extracts the response-classification + body-assembly logic previously inlined in `fundraise-client.tsx#calculateDilution()` into a pure, no-I/O helper module at `web/src/app/workspace/fundraise/fundraise-gate.helpers.ts` (colocated with the client so the client keeps its server-boundary-free import graph). Exports: (1) structural `GateWarn` + `GateBlock` types (previously duplicated inline in the client) mirroring the `esic_warn` / `div83a_warn` envelope from `web/src/app/api/fundraise/route.ts` lines 121/147/244–245 + the 412-body shape at lines 109–119 (ESIC) and 134–145 (Div 83A); (2) `parseGateWarn(raw)` / `parseGateBlock(raw)` defensive coercers that never throw and drop malformed payloads to null; (3) `classifyFundraiseResponse(status, data) → FundraiseGateOutcome` discriminated union covering the three UI branches (`kind: "block"` for 412 + recognised gate error, `kind: "ok"` for 2xx + `ok:true` with deduped warns preserved in route order, `kind: "error"` for everything else); (4) `buildFundraisePostBody(input) → FundraisePostBodyWire` that guarantees `wholesaleOnly` travels with every request and only attaches `safeDiscount` / `safeCap` for SAFE + convertible-note rounds (matches the ternary the client had inline); (5) `GATE_BLOCK_HEADING` copy pack keyed on the `error` discriminant so the tile heading + tests share the same source-of-truth strings. Client refactor: `calculateDilution()` now `fetch()`es the wire body from `buildFundraisePostBody(...)` and switches on `classifyFundraiseResponse(res.status, data)` instead of inline `res.status === 412 && !data.ok && typeof data.error === "string"` gymnastics — same three branches (`block` → `setGateBlock`; `error` → `setError`; `ok` → `setGateWarns([...outcome.warns])` + advance). The gate-block banner heading now reads from `GATE_BLOCK_HEADING[gateBlock.error]` so tests + UI can't drift. Test coverage `web/src/app/workspace/fundraise/fundraise-gate.helpers.test.ts` — 24 vitest cases across 5 describe blocks: `parseGateWarn` (4 cases — happy path + non-object rejection + missing-reason rejection + missing-message default), `parseGateBlock` (5 cases — both known errors + unknown-error rejection + non-string-error rejection + missing-field defaults), `classifyFundraiseResponse` (11 cases — 412 + esic block, 412 + div83a block, 412 + unknown-error error fallback, 200 + no warns, 200 + esic_warn + div83a_warn in route order, warn dedupe by reason, malformed warn payload skipped, 400 error, 500 error, non-object body network-error fallback, ok:false with no error string → "Failed to calculate round" default), `buildFundraisePostBody` (4 cases — SAFE includes safeDiscount/safeCap, convertible_note same, priced omits them, wholesaleOnly=true propagates on every instrument type), `GATE_BLOCK_HEADING` (1 case — both known errors covered with distinct copy). All 24 tests green. Typecheck `npx tsc --noEmit -p tsconfig.json` green. Closes the P6b "Founder-side vitest / Playwright coverage of the toggle + banner branch matrix is a natural follow-up (P6b-test)" note — the *vitest* half now pins every deterministic branch; the *Playwright* half (visual banner render + wholesale-only toggle click → 412 flow) is still a fit for the sibling QA agent's E2E suite because it needs a seeded fundraise-project + toggle interaction that the pure helper cannot exercise.

### Round 7 follow-ups (spun off from P0 phase-gap matrix — §1 phase 2)

- **P2b** mint AU Customer Discovery Interview Log template — **shipped 2026-07-24**. Closes the second half of §1 phase 2 P1 gap "interview-log template (no interview-transcripts/ folder in data-room-templates.ts)" — sits alongside the P2a LOI so Phase 2 (Idea Validation) has BOTH the pre-LOI evidence artefact (discovery log) and the post-LOI evidence artefact (indicative commitment). New template body at `web/content/templates/legal/au-customer-discovery-interview-log.md` — 9-section markdown with (1) APP1/APP5 informed-consent block that reads verbatim the collection notice a founder must give under *Privacy Act 1988* (Cth) Schedule 1, records four discrete consents (written log / named reference in investor materials / verbatim quote / audio-or-video recording), and gates the audio-recording paragraph on a `{{#recording_consent_yes}}` / `{{#recording_consent_no}}` two-branch toggle so a naïve founder cannot silently record without express APP3 collection consent, (2) interviewee identity table with a **compensation_offered** row + explicit "if anything other than 'None', disclose that compensation alongside every quote used in investor materials" guardrail (the s18 ACL / s1041H Corps Act red-line if a paid endorsement is dressed up as organic discovery evidence), (3) Rob-Fitzpatrick Mom-Test-disciplined 7-part script (past-behaviour question, current-workflow, quantified-pain table with both interviewee's-own-words + interviewer-normalised columns for frequency/time/cost/severity, prior attempts, purchase authority, willingness-to-test with a `{{#willing_to_pilot}}` toggle that stamps "evidence of demand only, NOT a binding purchase order — for that, see the au-customer-loi template", willingness-to-pay), (4) verbatim quotes section with a "never trim or paraphrase in a way that changes meaning — misleading conduct under s18 ACL / s1041H Corps Act" red-line, (5) red-flag & signal audit table (6 rows — 4 positive signals: specific past event / prior spend / introduction to peer / budget line + 2 red flags: hypothetical-language-only / interviewer-led-the-conversation) so founders cannot self-deceive by hearing only what they want to hear, (6) Build-Measure-Learn conclusion block (Validated / Partially validated / Disconfirmed) with a `{{#disconfirmed_hypothesis}}` toggle that blocks inclusion in the current-hypothesis evidence pack, (7) APP11/APP12/APP13 storage + retention table (storage location + retention days + role-based access list + access-request contact + deletion trigger), (8) investor-pack disclosure guardrails (5-bullet list: mark discovery-stage + non-binding / never aggregate without disclosing sample size / attribute quotes exactly as consented / mark hypothetical prices as indicative / retain version-controlled copy for diligence — anchored on s1041H Corporations Act 2001 (Cth) civil + criminal liability regardless of good faith), (9) interviewer + reviewer sign-off. Registry entry in `web/src/lib/templates/legal-templates.ts` — slug `au-customer-discovery-interview-log`, `category: "commercial"` (same category as P2a since it is a raise-evidence commercial artefact, not a legal instrument), `phase_slug: "phase-2"`, 70 declared placeholders (interviewer + party identity + consent quartet + interviewee identity + Mom-Test 7-part script + red-flag audit 6-row + BML conclusion + storage/retention + revision), 6 source citations (Privacy Act 1988 (Cth) APPs 1/3/5/6/11/12/13 + OAIC APP Guidelines Chapter B + Chapter 3 + ACL s18 + Corps Act s1041H + Rob Fitzpatrick Mom Test + Steve Blank Four Steps to the Epiphany). Test coverage `web/src/lib/templates/legal-templates.test.ts` gains 4 vitest cases mirroring the P2a / P1c / P1a shape: (1) is registered as phase-2 commercial template, (2) declares the APP + misleading-conduct anchors an investor's lawyer looks for (Privacy Act 1988 + Australian Privacy Principles + APP 1/5/11/12/13 + s18 + Australian Consumer Law + s1041H + Mom Test + past-behaviour + NOT LEGAL ADVICE + Compensation offered all grepped), (3) declares every non-toggle `{{token}}` in the body is present in `placeholders[]`, (4) renders the recording-consent branch matrix — recording-yes variant keeps "verifiable consent to audio recording" language + strips no-recording block + keeps pilot commitment block; recording-no + disconfirmed_hypothesis variant flips both branches + strips pilot block + keeps disconfirmation note; section-toggle names never leak into rendered output either way. All 34 legal-template tests green (30 existing + 4 new). Closes §1 phase 2 P1 gap "interview-log template (no interview-transcripts/ folder in data-room-templates.ts)" and §2 folder 5 shadow-item "Customer Discovery Interview Log" — previously NO-TEMPLATE, now NUDGE with default body + APP-compliant informed-consent block + Mom-Test discipline. Founder still supplies interviewee-specific facts + the four APP-consent responses; template hard-codes the "log is discovery-stage + non-binding + must not be aggregated without disclosing sample size" split so the artefact cannot silently drift into misleading investor communication under s18 ACL / s1041H Corps Act.
- **P2a** mint AU Customer Letter of Intent (non-binding) template — **shipped 2026-07-24**. Closes §1 phase 2 P1 gap "LOI / letter-of-intent template absent from data-room folder 8 Contracts & Agreements" and unblocks the raise-evidence half of Phase 2 (informal LOI collection is the AU-standard artefact investors expect between discovery-interview logs and paid MVP customers). New template body at `web/content/templates/legal/au-customer-loi.md` — 12-section markdown covering (1) purpose + authority-to-sign rep, (2) intended use case + stakeholders, (3) indicative-commercial-terms table (ACV / seats / start-date / payment / GST-exclusive with s29-70 GST-tax-invoice anchor), (4) conditions to Definitive Agreement (technical PoV against Annexure A + APP privacy + budget approval + statutory approvals), (5) BINDING confidentiality clause with explicit "paragraphs 5.2 / 8 / 9 / 10 / 11 are binding, everything else is non-binding present-intention only" split, (6) non-binding-nature clause naming the s18 Australian Consumer Law + s1041H Corporations Act 2001 (Cth) misleading-conduct anchors so the founder cannot accidentally overstate the LOI as committed revenue in a data room (also explicitly permits the Supplier to disclose the LOI to prospective investors + advisers + lenders provided the non-binding nature is stated), (7) Definitive Agreement target date, (8) non-binding confirmation, (9) publicity (customer name may be disclosed to investors under NDA in aggregate form but not on public marketing without consent), (10) costs (each party bears own), (11) governing-law + ADC-mediation-first dispute path, (12) Electronic Transactions Act 1999 (Cth) e-signing anchor. Signatures block executes under s127 Corporations Act 2001 (Cth) with both a `{{#second_signatory}}` co-signatory toggle AND a `{{#sole_director}}` s204A(1) sole-director-and-sole-company-secretary toggle so single-founder Pty Ltds can sign without inventing a second officer. Two Annexures (A acceptance criteria + B statutory approvals) keep the raise-specific detail out of the main body. New category `commercial` on the `LegalTemplateCategory` union (previously `corporate | employment | fundraising | ip` — LOI is neither legal-instrument nor fundraise-security, it is a commercial letter, so the union grows by one). Registry entry in `web/src/lib/templates/legal-templates.ts` — slug `au-customer-loi`, `category: "commercial"`, `phase_slug: "phase-2"`, 24 declared placeholders (customer identity + supplier identity + product + commercial figures + acceptance criteria + statutory approvals + revision date + both signatory-toggle variants), 6 source citations (ACL s18 + Corps Act s1041H/s769C/s127 + APPs + GST Act s29-70 + Electronic Transactions Act + ADC Commercial Mediation Guidelines). Test coverage `web/src/lib/templates/legal-templates.test.ts` gains 4 vitest cases mirroring the P1c / P1a shape: (1) is registered as phase-2 commercial template, (2) declares the AU misleading-conduct anchors an investor's lawyer looks for (s18 ACL + Australian Consumer Law + s1041H + non-binding + Confidentiality + s127 + s29-70 + Electronic Transactions Act 1999 + NOT LEGAL ADVICE + legally-binding-language split all grepped), (3) declares every non-toggle `{{token}}` in the body is present in `placeholders[]`, (4) substitutes party + commercial fields and honours signatory toggles — co-signatory variant keeps Bob Founder + strips sole-director block; sole-director variant keeps sole-director-and-sole-company-secretary line + strips second_signatory_name; section-toggle tokens never leak into rendered output. Registry-level whitelist test in `LEGAL_TEMPLATES registry` describe block updated from `["corporate", "employment", "fundraising", "ip"]` to include `"commercial"`. All 30 legal-template tests green (26 existing + 4 new). Founder still must confirm registered office + ACN + first-director details in the Signatures block; template hard-codes the "non-binding except confidentiality + publicity + governing-law" split so a mis-configured LOI cannot silently drift into a binding purchase order. Closes §2 folder 8 item "Letter of Intent (customer)" — previously NO-TEMPLATE, now NUDGE with default body + investor-safe disclosure clause.

### Round 8 follow-ups (spun off from P0 phase-gap matrix — §1 phase 12)

- **P12b-tile** ship the `/dashboard/exit-readiness` tile — **shipped 2026-07-24**. Closes the outstanding "`/dashboard/exit-readiness` tile" leg of the P12b / P12b-cfo follow-ups so the AU comparable-exits fixture (P12a) + investor-pack section helper (P12b) + CFO auExitCheck note (P12b-cfo) now have a founder-facing surface. New server page at `web/src/app/dashboard/exit-readiness/page.tsx` — auth-gated (`redirect("/auth/login?next=/dashboard/exit-readiness")`), `dynamic = "force-dynamic"`, `robots: { index:false, follow:false }`, resolves the caller's active project via `getActiveProject(user.id)` and passes `project.industry` into `buildExitBenchmarkSection({ sector })` at request time so the sector-scoped path + `usedFallback` widening are computed server-side (no client fetch needed — the pack helper is a pure fixture projection). Mounted inside `<WorkspaceLayout>` with Chapter 12 header copy pointing back at "the CFO valuation report already runs this same fixture as a VC-Method cross-check". New RSC `web/src/components/dashboard/exit-readiness-tile.tsx` renders `{topExits, summary, disclaimer, usedFallback, sector}` as (a) a sector-filter chip that shows "widened to all sectors" when the sector matched zero comps, (b) a 4-tile summary grid (deals in sample / median valuation / median revenue× / latest deal year), (c) a buyer-type chip row in stable order (strategic / IPO / PE / financial / secondary — zero-count buckets dropped), (d) a topExits table with company + buyer + type + AUD valuation + revenue× + year + source-link (rel="noopener noreferrer", title=sourceNote for the exact reported terms), and (e) the fixed `AU_EXIT_DISCLAIMER` footer. Pure helpers `exit-readiness-tile.helpers.ts` — `BUYER_TYPE_LABEL` (label pack keyed on `AuExitBuyerType`), `BUYER_TYPE_ORDER` (stable presentation order that leads with strategic + IPO, puts secondary last since it's a liquidity event not a company exit), `formatAudCompact(aud)` (billions with 1dp, millions as whole A$M, sub-A$M as A$k / raw integers; NaN / null / infinity → em-dash so the tile never renders "NaN"), `formatMultiple(m)` (large multiples whole numbers, small multiples 1dp; null / non-finite / non-positive → em-dash so the tile never claims an anchor multiple it cannot defend), `orderedBuyerTypeEntries(byBuyerType)` (drops zero-count buckets, returns entries in stable order). Vitest coverage `exit-readiness-tile.helpers.test.ts` — 12 cases across four describe blocks pinning (a) `formatAudCompact` em-dash on null / undefined / NaN / infinity + A$39.0B / A$3.3B / A$400M / A$50k / A$0 all correct, (b) `formatMultiple` em-dash on null / non-finite / non-positive + 42x / 10x whole + 1.4x / 9.9x one-decimal, (c) `BUYER_TYPE_ORDER` covers every `AuExitBuyerType` exactly once + leads with strategic + IPO + ends with secondary + every label truthy, (d) `orderedBuyerTypeEntries` drops zeros + returns entries in stable order + returns [] when every bucket is zero. All 12 tests green (33 tests green across the impacted `investor-pack` + `dashboard` + `workspace` suites). Nav wiring: `web/src/components/workspace/nav-groups.ts` "Grow & Scale" group grows one entry `{ href: "/dashboard/exit-readiness", label: "Exit Benchmark", icon: BarChart3, minPlan: "starter" }` between the existing `/workspace/exit` (Exit Modeling) entry and the group close so founders can discover the tile from the sidebar without touching the CFO Valuation page. Typecheck `npx tsc --noEmit -p tsconfig.json` green (exit 0). Closes P12b-tile follow-up. `data-testid="exit-readiness-tile"` + `data-used-fallback` attributes on the section root let a future Playwright spec attach without helper edits.
- **P12b-cfo** wire AU comparable-exits into CFO valuation as exit-realisation cross-check — **shipped 2026-07-24**. Closes the CFO Chapter 12 valuation-anchor half of the P12b follow-up ("CFO Chapter 12 valuation-anchor call (bolting this same fixture into `cfo-valuation.ts` as an exit-realisation cross-check on the VC-Method target return) + `/dashboard/exit-readiness` tile remain follow-ups under P12b-cfo + P12b-tile respectively"). New pure helper `auExitRealisationCheck(input, projection): AuExitRealisationCheck` in `web/src/lib/agents/cfo-valuation.ts` — sector-scoped (via VC-benchmark sector normalisation → falls back to full fixture when the sector filter matches zero comps, `usedFallback` flag surfaces the widening) with `minYear=2010` to drop pre-GFC comps that misprice today's multiples, prefers realisation events (`strategic | pe | ipo`) but retains secondaries as a fallback if the sector cannot muster ≥3 realisation comps so a niche sector still gets a signal. Returns `{sector, usedFallback, sampleSize, medianRevenueMultiple, medianValuationAud, latestYear, impliedExitArrAud, vcMethodExitValueAud, auPrecedentExitValueAud, deltaPct, verdict, anchorExits[≤3], note, disclaimer}` — `impliedExitArrAud` reuses the VC-Method exit-year horizon (`STAGE_EXIT_YEARS × 12` clamped to projection length) so the cross-check anchors on the same exit month the VC method already used, `auPrecedentExitValueAud = impliedExitArrAud × medianRevenueMultiple`, `deltaPct` compares AU precedent vs VC-Method exit value (>+25% → `au_above_vc_method`, <-25% → `vc_method_above_au`, else `aligned`, pre-revenue → `no_signal`). New `VcValuationReport.auExitCheck` field on the top-level report plus `auExitCheck.note` appended to `report.notes[]` so CFO Chapter 12 PDF renderers + IR portfolio bundling pack + `/dashboard/valuation` overlay all inherit the check without a separate wiring pass. `report.sources[]` now credits `au-benchmark.ts` so a diligence auditor can trace the check back to publicly-reported deals. Test coverage `web/src/lib/agents/cfo-valuation.test.ts` grows from 11 → 16 vitest cases with a new `auExitRealisationCheck (P12b-cfo)` describe block pinning (a) sector-scoped SaaS sample surfaces known Atlassian/OpsGenie/Trello/Loom/Nitro/MYOB/Nearmap/Elmo/WiseTech/Xero anchor deals + AU_EXIT_DISCLAIMER round-trip, (b) delta + verdict compute non-null for the revenue-positive default fixture input (SaaS has revenue-multiple comps so both sides fill), (c) pre-revenue skip path — `impliedExitArrAud/vcMethodExitValueAud/auPrecedentExitValueAud/deltaPct` all null + verdict `no_signal` + note flags "unchecked" while still surfacing AU comps so the pack can explain the miss, (d) niche-sector `cyber` normalises via VC-benchmark → `default` fallback path still returns a sample, (e) `buildVcValuationReport` threads the check into `report.auExitCheck` + `report.notes[]` (grep "AU exit precedent") + `report.sources[]` (grep `au-benchmark`). All 16 tests green. Typecheck `npx tsc --noEmit -p tsconfig.json` green. Closes the P12b-cfo half of the P12b follow-up. `/dashboard/exit-readiness` tile (P12b-tile) remains blocked on the sibling phase-12 UI agent — the data + note is now available to that tile via `buildVcValuationReport().auExitCheck`.
- **P12b** wire AU comparable-exits into investor-pack ch09+ (IR portfolio bundling) — **shipped 2026-07-24**. Closes the IR portfolio-bundling half of the P12a follow-up ("Downstream wiring (CFO Chapter 12 valuation-anchor + IR portfolio bundling + `/dashboard/exit-readiness` tile) deferred to P12b"). New pure module `web/src/lib/investor-pack/exit-benchmark-section.ts` mirroring the P6a-ir-pack `esop-eligibility-section.ts` shape — `buildExitBenchmarkSection({sector?, minYear?, limit?}) → ExitBenchmarkSection` returning `{sector, usedFallback, topExits[], summary, disclaimer}`. Sector-scoped when the founder has a `projects.industry` on file; falls back to the full fixture with `usedFallback: true` when the sector filter matches zero rows (rare / niche sectors) so the pack never renders an empty exit panel. Defaults: `minYear=2010` (drops pre-GFC comps that misprice today's multiples), `limit=6` (matches the AU-comparable-raises panel row count). Every row projects to `{company, buyer, buyerType, valuationAud, revenueMultiple, year, sourceUrl, sourceNote}` — no `ttmRevenueAud` or `structure` because those are diligence-only; the pack shows valuation + multiple as the anchor. Summary reuses `summariseAuExits()` for `{count, medianValuationAud, medianRevenueMultiple, byBuyerType, latestYear}` so the pack can render "12 AU tech exits since 2010, median A$X, X-Xx revenue multiple". Fixed `AU_EXIT_DISCLAIMER` (s766B Corps Act guard) attached to every section. Wired into `web/src/lib/investor-pack-assembler.ts`: new `exitBenchmark: ExitBenchmarkSection` field on `InvestorPackData`, `assemblePackData()` now calls `buildExitBenchmarkSection({ sector: sector ?? svi?.sector ?? null })` immediately after the ESOP eligibility section — same sector-inference chain the AU comparable-raises panel uses so both benchmarks agree on the segment. Test coverage `web/src/lib/investor-pack/exit-benchmark-section.test.ts` — 8 vitest cases across a single describe block pinning (a) defaults returning 6 rows sorted newest-first + fixed disclaimer + https source URLs + positive AUD valuations, (b) limit override honoured + never returns < 1 row (clamp), (c) sector filter case-insensitive via the underlying fixture helper, (d) fallback to full fixture when sector matches zero rows + `usedFallback: true` flag, (e) null / whitespace sector normalises to null (no filter, no fallback flag), (f) summary aggregates `count + medianValuationAud + latestYear` non-null + `byBuyerType` sum equals count, (g) minYear filter drops Xero (2007) at default `minYear=2010` + pulls it back in at explicit `minYear=2000`, (h) row shape preserves nullable `buyer` + nullable `revenueMultiple` correctly. All 8 tests green. Adjacent suites re-run — 24 tests across `src/lib/investor-pack` + `src/lib/exits` green (11 exits + 5 esop-eligibility + 8 new exit-benchmark). Typecheck `npx tsc --noEmit -p tsconfig.json` green. Closes the IR portfolio-bundling half of P12b. CFO Chapter 12 valuation-anchor call (bolting this same fixture into `cfo-valuation.ts` as an exit-realisation cross-check on the VC-Method target return) + `/dashboard/exit-readiness` tile (blocked on the sibling phase-12 UI agent) remain follow-ups under P12b-cfo + P12b-tile respectively.
- **P12a** mint AU comparable-exits data source (Folder 12 phase-12 P2 gap) — **shipped 2026-07-24**. Closes §1 phase 12 P2 gap "no `web/src/lib/exits/au-benchmark.ts`" — the CFO / IR / exit-readiness agents can now anchor Chapter 12 (Exit / Beyond) valuation ranges on a real fixture instead of prose citations. New pure module `web/src/lib/exits/au-benchmark.ts` — 22-row fixture of publicly-reported AU tech exits spanning five buyer types (`strategic` — Afterpay→Block, OpsGenie/Trello/Loom→Atlassian, Envato→Shutterstock, LiveHire→HireRoad; `pe` — Nitro→Potentia, MYOB→KKR, Nearmap→Thoma Bravo, Elmo→K1; `ipo` — Atlassian NASDAQ 2015, WiseTech ASX 2016, Xero NZX 2007, PEXA ASX 2021, Airtasker ASX 2021, Judo ASX 2021, Prospa ASX 2019; `secondary` — Canva 2021 tender, Atlassian Accel 2010, Atlassian T. Rowe 2014, SafetyCulture 2021; `financial` — Ignition 2022) with typed `AuExit` rows carrying company + sector + buyer + `buyerType` + `structure` (cash / cash_stock / stock / public_listing / secondary_sale) + `valuationAud` + optional `ttmRevenueAud` + optional `revenueMultiple` + `year` + `sourceUrl` + `sourceNote`. Every AUD figure rounded to nearest A$M with USD deals converted at announcement-date spot rate + `sourceNote` recording the exact reported terms so a downstream renderer can never over-precision the estimate. Two pure helpers: `getAuComparableExits({sector?, buyerType?, minYear?, limit?})` filters case-insensitively + sorts newest-first + secondary-sort by valuation-desc so the top-of-list is always the most recent + largest deal in the requested slice; `summariseAuExits(exits)` returns `{count, medianValuationAud, medianRevenueMultiple, byBuyerType, latestYear}` where the median helper is even/odd-branch aware (odd → middle element; even → averaged + rounded to 2dp for multiples) and null-safe on empty input. Fixed `AU_EXIT_DISCLAIMER` cites s766B Corporations Act 2001 (Cth) so any surface that renders these figures inherits the AFSL guard identical to the existing `au-comparable-raises.ts` pattern (mandatory across the platform per Q3). Test coverage `web/src/lib/exits/au-benchmark.test.ts` — 11 vitest cases across 4 describe blocks: (1) fixture invariants — ≥ 20 rows + HTTPS-only source URLs + positive valuation + finite year + positive revenue-multiple when present; (2) buyerType coverage — strategic/pe/ipo/secondary all present; (3) Atlassian reference deals — Atlassian + OpsGenie + Trello anchor rows exist so §1 phase 10/11 narrative claims (T. Rowe 2014 secondary, OpsGenie 2018, Trello 2017) are backed by fixture rows; (4) `getAuComparableExits` — sector filter case-insensitively (`"FinTech"` → 100% `fintech`); buyerType filter; minYear + limit + newest-first sort; unknown sector → `[]`; (5) `summariseAuExits` — empty-input zero-shape; full-fixture medians + buyerType-count + latestYear ≥ 2023; odd + even median branches both exercised. All 11 tests green. Typecheck `npx tsc --noEmit -p tsconfig.json` green (module ships as a leaf lib with no upstream imports beyond types, so a broken helper cannot cascade). Closes §1 phase 12 P2 gap "comparable-exits data source (no `web/src/lib/exits/au-benchmark.ts`)". Downstream wiring (CFO agent Chapter 12 valuation-anchor call + IR portfolio-bundling exit-readiness pack + `/dashboard/exit-readiness` tile once the sibling phase-12 UI agent unlocks) is a natural follow-up (P12b) — this tick ships the pure fixture + helpers only so any surface that needs an AU-flavoured exit benchmark can import it without waiting.

- **P5-cohort-wire** investor-pack traction cohort section (Folder 5 phase-5 P1 gap follow-up on P5-cohort) — **shipped 2026-07-24**. Wires the P5-cohort pure module (`web/src/lib/traction/cohort-chart.ts`) into the ch09+ investor pack alongside the ESOP-eligibility and exit-benchmark sections so the PMF signal has a defensible surface. New pure module `web/src/lib/investor-pack/traction-cohort-section.ts` — `buildTractionCohortSection({signups?, activities?, cohort_options?, svg_options?}) → {matrix, svg, summary, empty_state, investor_grade, disclaimer}`. Empty-state envelope returns automatically when both event streams are absent (the pack renders the cohort-chart empty-state SVG hint "log at least 4 weeks of signups" without a special-case branch upstream). Summary aggregates `average_week_retention[]` (null-skips young cohorts so W3 is not a false zero when no cohort has reached W3 yet), `best_cohort_iso` + `best_week1_retention` (falls back to highest W0 when no cohort has reached W1). `investor_grade` flips when both `meets_min_buckets` (>=4 consecutive cohorts) AND `best_week1_retention >= INVESTOR_GRADE_WEEK1_MIN` (0.4, anchored on Sean Ellis / Rahul Vohra weekly-active PMF conventions Blackbird / AirTree / Square Peg reference during diligence). Fixed `TRACTION_COHORT_DISCLAIMER` names Stripe / product-analytics cross-check so any pack surface inherits the "self-reported, not audited" hedge (avoids overclaiming under s18 ACL + s1041H Corps Act 2001 (Cth) in an investor communication). Wired into `web/src/lib/investor-pack-assembler.ts`: new `tractionCohort: TractionCohortSection` field on `InvestorPackData`, `assemblePackData()` calls `buildTractionCohortSection()` immediately after the exit-benchmark section (empty-state today because no signup / activity event source is wired end-to-end — live ingestion via Stripe / product-analytics OAuth is a follow-up under P5-cohort-ingest). Test coverage `web/src/lib/investor-pack/traction-cohort-section.test.ts` — 6 vitest cases pinning (a) empty-state envelope returns `empty_state=true` + zero cohorts + null summary + investor_grade=false + SVG carries the "No cohort data yet" hint, (b) disclaimer round-trips on both empty + populated builds and names "stripe" in the hedge copy, (c) `investor_grade=false` when < MIN_COHORT_BUCKETS cohorts + SVG surfaces "below investor threshold" hint, (d) `investor_grade=true` for 4×4 fixture with W1=0.75 across the older cohorts + best_cohort_iso resolves to a YYYY-MM-DD string, (e) `average_week_retention[]` null-skips young cohorts (old cohort W1=1 + new cohort W1=null → avg=1, both null at W3 → avg=null), (f) `svg_options.title` threads through to the renderer. All 6 tests green. Full `src/lib/investor-pack + src/lib/traction + src/lib/exits` suites re-run — 43 tests green. Typecheck `npx tsc --noEmit -p tsconfig.json` green. Closes the P5-cohort "Downstream wiring (Chapter 5 CTA renders the SVG into `/dashboard/svi` or the SVI report PDF's Traction section; nightly cron writes `cohort-retention.pdf` into `dataroom_files` under folder 5 traction/) remains a follow-up under P5-cohort-wire" note — the pack-section half now ships. Chapter 5 CTA + `/dashboard/svi` tile + nightly `cohort-retention.pdf` cron remain as P5-cohort-svi + P5-cohort-cron follow-ups because they need a sibling UI + storage agent to render the SVG into the founder-facing surfaces — this tick ships the pack-section slot so any surface that needs a cohort section can import it without waiting.

- **P5-cohort** cohort retention chart auto-draw component (Folder 5 phase-5 P1 gap) — **shipped 2026-07-24**. Closes §1 phase 5 P1 gap "cohort chart auto-draw component (guide claims it; no `web/src/lib/traction/cohort-chart.ts` exists)" — the Chapter 5 (PMF / Early Traction) body promises "Retention-cohort chart — auto-drawn from the weekly buckets into the DataRoom under traction/" at `web/src/lib/guide/startup-journey.ts:364` but no module existed to compute or render the matrix. New pure module `web/src/lib/traction/cohort-chart.ts` — `computeWeeklyCohortRetention(signups, activities, {reference_date?, cohort_count?, week_count?}) → CohortMatrix` and `renderCohortRetentionSvg(matrix, opts?) → string` mirroring the reseller/cohort-velocity.ts + startup-growth-phases.ts posture (dependency-free, string in / string out) so both the maths and the SVG can regression-test without touching Supabase, React, or a browser DOM. Cohort key = Monday 00:00 UTC of the signup week (ISO 8601 aligned; JS `getUTCDay()` 0-Sunday shifted to 7 before subtracting so Sunday signups land in the prior Monday's cohort not a phantom Sunday-anchored week). First-signup-wins per user_id so a re-registration cannot silently shift a user into a later cohort. Retention decimals in `[0,1]` — the SVG formats them as `Math.round(val * 100)%` so cell text stays legible at report scale. `null` (not `0`) surfaces for weeks that have not yet elapsed since the cohort start, so a young cohort renders as a truncated row rather than misleading false churn (Mixpanel + Amplitude follow the same rule; investors read that convention). Empty cohort weeks drop entirely so gaps in signup activity do not shift row alignment. Row order = oldest-first (top → bottom) so W0 lands top-left, matching the analyst-facing grid convention. `meets_min_buckets` boolean flips at `MIN_COHORT_BUCKETS = 4` — the "signal is investor-defensible" floor (Chapter 5 CTA says "at least two consecutive weeks" but 4 is the PMF-signal minimum investors expect, per Sean Ellis + Rahul Vohra's PMF-survey conventions). Defaults: `DEFAULT_COHORT_COUNT = 8` (trailing 8 weeks of cohorts) + `DEFAULT_WEEK_COUNT = 8` (8 retention columns) so a founder with 200 weeks of data cannot blow up a report PDF. `cohortCellColour(value)` maps decimals to a 5-band heatmap (`null → slate-200`, `0 → slate-100`, `<0.2 → red-200`, `<0.4 → orange-200`, `<0.6 → amber-200`, `<0.8 → green-200`, `≥0.8 → green-300`) that reads in both light + dark PDF backgrounds. SVG renderer surfaces an empty-state hint when no cohorts exist ("No cohort data yet — log at least 4 weeks of signups") and a below-min-signal amber-text warning when `meets_min_buckets = false`, so the founder sees the investor-readiness gap on the same visual instead of shipping a green-looking chart with 2 rows. Test coverage `web/src/lib/traction/cohort-chart.test.ts` — 13 vitest cases across 3 describe blocks: (a) `computeWeeklyCohortRetention` (8 cases pinning Monday-anchored cohort bucketing + oldest-first row order + empty-cohort drop, first-signup-wins-per-user_id, retention arithmetic across a 4-user × 4-week fixture (0.75/0.25/0.5/0.25 pattern), null-for-not-yet-elapsed weeks on this-week cohorts, trailing `cohort_count` window drops old signups, malformed input tolerance (invalid ISO + empty user_id both silently skipped), `meets_min_buckets` flip at 4 consecutive cohorts, stable-defaults pin), (b) `cohortCellColour` (1 case pinning all 8 colour-band branches incl. `null` + `NaN` + `0` edge cases + verbatim hex codes so a downstream renderer cannot silently drift), (c) `renderCohortRetentionSvg` (4 cases pinning `<svg xmlns=...>` prefix + one `<rect>` per cell + integer-% cell labels + cohort_week + `n={size}` chip render, empty-state hint text present + `MIN_COHORT_BUCKETS` count grep, below-min-signal hint fires when cohort_count < 4 and *doesn't* fire when count ≥ 4 (regression guard), and cell/padding options scale the SVG `width` attribute correctly). All 13 tests green. Typecheck `npx tsc --noEmit -p tsconfig.json` green (leaf lib with no upstream imports beyond `vitest` in the test file, so a broken helper cannot cascade). Downstream wiring (Chapter 5 CTA renders the SVG into `/dashboard/svi` or the SVI report PDF's Traction section; nightly cron writes `cohort-retention.pdf` into `dataroom_files` under folder 5 traction/) remains a follow-up under P5-cohort-wire — this tick ships the pure fixture + helpers only so any surface that needs a cohort retention heatmap can import it without waiting.

- **P3b** `/api/abs/lookup` route wrapper (Folder 5 phase-3 P1 gap follow-up on P3a) — **shipped 2026-07-24**. Public GET route at `web/src/app/api/abs/lookup/route.ts` mirroring the `/api/abr/lookup` + `/api/asic/extract` posture (no auth, `dynamic="force-dynamic"`, `runtime="nodejs"`, 1 h edge cache on 200s). Query surface: `?anzsic=<ANZSIC 2006 class code>` OR `?keyword=<industry term>` (`?sector=` accepted as an alias so the Chapter 3 CTA can use founder-vernacular field names without a client-side rename) plus optional `?addressablePct=0..1` (default 0.2) and `?targetSharePct=0..1` (default 0.01). Delegates to `estimateTamSamSom({anzsicCode, keyword, addressablePct, targetSharePct})` from `web/src/lib/market/au-market-lookup.ts` — no logic duplicated; pct parser clamps to `[0,1]` and falls back to defaults on non-finite input (matches the `parseFloat`-with-clamp posture the ABN route uses on the modulus check). 200 body surface: `{industry: {anzsic_code, division, label, keywords, cagr, business_count, sources, notes}, tam_aud, sam_aud, som_aud, addressable_pct, target_share_pct, suggestions, disclaimer}` — `suggestions[]` (up to 4 sibling ANZSIC classes for the keyword the founder actually typed) is populated on keyword lookups so a founder who typed "software" gets a pivot list back to IaaS / marketplace / other neighbouring classes; ANZSIC-pinned callers get an empty `suggestions[]` because they already picked the class. Fixed `AU_MARKET_LOOKUP_DISCLAIMER` (ABS-provenance + "refine with primary research before quoting to investors" hedge, cites s766B Corps Act 2001 (Cth) at source) travels on every 200 so downstream renderers cannot leak an anchor without the AFSL guard. Error posture: `400` `missing_lookup_input` when neither anzsic nor keyword supplied; `404` `industry_not_found` when the input matches no seeded row (with the raw input echoed back in `input.{anzsic,keyword}` so a keyword-typo caller can see what they sent). Test coverage `web/src/app/api/abs/lookup/route.test.ts` — 7 vitest cases pinning (a) 400 branch + machine-readable error code, (b) known ANZSIC returns TAM/SAM/SOM at default 20%/1% + disclaimer round-trip + 1h cache-control + empty suggestions[], (c) keyword lookup returns top pick + suggestions[] excludes the pick + all sibling entries have distinct anzsic_code, (d) `?sector=` alias resolves the same as `?keyword=`, (e) addressablePct/targetSharePct honour explicit values + clamp overshoot (5 → 1, -3 → 0) + fall back to defaults on garbage/empty input, (f) unknown ANZSIC → 404 + `industry_not_found` + input echo, (g) unmatched keyword → 404 + `industry_not_found` + keyword echo. All 7 tests green. Typecheck `npx tsc --noEmit -p tsconfig.json` green. Closes the P3a "Downstream wiring (Chapter 3 CTA renders TAM/SAM/SOM anchor into `/dashboard/svi` or the CMO deep-pass; `/api/abs/lookup` route wrapper if we later want a JSON endpoint mirroring the abn.ts + acn.ts route pattern) deferred to P3b" note — the route half now ships. Chapter 3 CTA + CMO deep-pass consumer wiring (which surface calls this route with the founder's project.industry) remains a follow-up under P3c because it needs the sibling market-research agent to pick which panel renders the response — this tick ships the endpoint contract so the surface can consume it without waiting.

- **P3a** AU industry-size lookup module (Folder 5 phase-3 P1 gap "IBISWorld / ABS lookup not surfaced; no `/api/abs/lookup` stub") — **shipped 2026-07-24**. New pure module `web/src/lib/market/au-market-lookup.ts` — offline seeded lookup keyed by ANZSIC 2006 class code (Australian and New Zealand Standard Industrial Classification, jointly published by ABS + Stats NZ). 12 industry snapshots covering the divisions AU startups actually operate in — SaaS (J5810), Data-processing / IaaS (J5921), Fintech (K6419), E-commerce (G4720), Healthtech (Q8511), Deeptech / R&D (M6910), Edtech (P8102), Agritech (A0169), Cleantech (D2611), Legaltech (M6913), HR-tech (M6931), Marketplaces (N7292) — every row anchored on ≥1 ABS release (8155.0 Australian Industry, 8501.0 Retail Trade, 8104.0 R&D-Businesses, 4660.0 Energy Account, 7503.0 Value-of-Ag-Commodities) plus a public IBISWorld / AusIndustry / RBA URL. Public API surface: `lookupByAnzsic(code)` (case + whitespace tolerant, null-safe), `searchByKeyword(query, limit=5)` (scored — exact-keyword-hit +100, label-prefix +40, label-substring +20, keyword-substring +10; ties broken by larger TAM), `listCoveredDivisions()` (division-key-sorted rollup for the tooling introspection layer), and `estimateTamSamSom({anzsicCode?, keyword?, addressablePct=0.2, targetSharePct=0.01})` returning `{industry, tamAud, samAud, somAud, addressablePct, targetSharePct, disclaimer}`. Fixed disclaimer `AU_MARKET_LOOKUP_DISCLAIMER` attached to every `estimateTamSamSom` result and exported for other consumers (e.g. `web/src/lib/agents/cfo-tam-sam-som.ts`) so no rendering surface can leak an anchor figure without the ABS-provenance hedge. Design rationale in the module JSDoc: offline seed keeps Chapter 3 available when ABS SDMX is under maintenance; live probes are already prioritised on the P0 raise-blocker path (ABR / ASIC / GST) so this stays anchor-only. 19 vitest cases in `web/src/lib/market/au-market-lookup.test.ts` across 5 describe blocks — (a) seed integrity (unique 5-char ANZSIC codes matching `^[A-Z]\d{4}$`, ≥1 ABS/AusIndustry/RBA source per row with https URL + publishedYear ≥ 2020, sane TAM/CAGR/businessCount ranges, every division key resolves to `ANZSIC_DIVISIONS`), (b) `lookupByAnzsic` (known-code match, case + whitespace tolerance, null for empty/unknown/null/undefined), (c) `searchByKeyword` (exact SaaS hit → J5810, fintech → K-division preference, blank / gibberish returns empty, limit respected), (d) `listCoveredDivisions` (counts sum to seed size, alphabetically sorted by division key), (e) `estimateTamSamSom` (SAM = TAM × addressablePct + SOM = SAM × sharePct arithmetic, default 20% + 1% percentages, out-of-range clamp to [0,1], keyword-fallback branch, null-on-no-match branch, `AU_MARKET_LOOKUP_DISCLAIMER` attached branch). All 19 tests green. Typecheck N/A (leaf lib with no upstream imports beyond `vitest` in the test file). Closes §1 phase 3 P1 gap "IBISWorld/ABS lookup". Downstream wiring (Chapter 3 CTA renders TAM/SAM/SOM anchor into `/dashboard/svi` or the CMO deep-pass; `/api/abs/lookup` route wrapper if we later want a JSON endpoint mirroring the abn.ts + acn.ts route pattern) deferred to P3b — this tick ships the pure fixture + helpers only so any surface that needs an AU industry anchor can import it without waiting.

### Round 9 follow-ups (spun off from P0 phase-gap matrix — §1 phase 7/8 P2 + open_questions Q6 + Q7)

- **P8-shipit** ShipIt culture-ritual callout on Chapter 8 (Q7) — **shipped 2026-07-24**. `web/src/lib/guide/startup-journey.ts` chapter `08-team` now carries a `sections[]` entry `culture-rituals-shipit` (heading + 8-bullet body EN + VI) walking the 24-hour hackathon cadence, why it works, how a five-person startup adapts it, the four-guardrail list that stops it becoming productivity theatre (self-organising teams only, demo-not-slides, IP under the existing IP Assignment Deed, no pre-demo CEO veto), when to start it (earliest at `team_v1` once ≥ 3 people), how to record it in the workspace (`shipit-{quarter}` project + CHRO pitch log + CEO retro landing in data-room folder 6 "Culture rituals"), and the AU Fair Work Act 2009 NES nuance (ShipIt hours = ordinary hours for permanent staff; contractors need a variation letter). Closes §1 phase 8 P2 gap "ShipIt / culture-ritual mention absent from Ch7 (belongs in Ch8 per Q7)" and open_question Q7. Test coverage in `web/src/lib/guide/startup-journey.test.ts` new "culture-rituals-shipit section (chapter 08 Q7)" describe block — 2 vitest cases pinning presence + EN/VI alignment ≥ 6 bullets AND anchor grep (`24-hour`, `2005`, `FedEx Day`, `quarterly`, `Fair Work Act 2009`, `IP Assignment Deed`).
- **P8-pledge1** Pledge 1% soft-nudge callout on Chapter 8 (Q6) — **shipped 2026-07-24**. Same chapter now also carries a `sections[]` entry `pledge-1-percent` (heading + 8-bullet body EN + VI) — walks the 2014 Atlassian + Salesforce + Rally co-founding history, the four ledgers (1% equity / product / profit / employee time), why the cheapest lever is 1% equity signed at seed valuation (a A$3M post-money seed costs A$30K of stock; the same 1% at a A$500M exit costs A$5M), the AU-specific tax treatment (ITAA 1997 s30-15 DGR-endorsement gate + Subdiv 124-M scrip-for-scrip exit implications), the Pledge-1%-Foundation-warrant cap-table pattern (kept off the register until liquidity so it does not add a voting party to every board resolution), the fastest-to-start pair (product-1% + time-1% via CHRO leave-policy variation), the "why not now" honesty check (revisit at Chapter 11 `scale_v1` anchored in the compliance-calendar), and the external `pledge1percent.org` link-out (BlockID.au does not intermediate the pledge). Guardrail language explicit: "soft nudge only", "never opt", "not a fundraise gate", "not a scoring input" — the SVI does not read this checkbox. Closes §1 phase 7 P2 gap "Pledge 1% callout (see Q6)" and open_question Q6. Test coverage `web/src/lib/guide/startup-journey.test.ts` two new describe blocks — 3 vitest cases pinning presence + EN/VI alignment ≥ 6 bullets + anchor grep (`pledge1percent.org`, `2014`, `Salesforce`, `Rally`, `1% of equity`, `1% of product`, `1% of profit`, `1% of employee time`, `DGR`, `ITAA 1997 s30-15`, `soft nudge only`) + explicit guardrail sentinel check ("never opt", "not a fundraise gate", "not a scoring input"). Pre-existing "no other chapter carries sections yet" guard now allow-lists chapters 06, 08 AND 10. All 24 startup-journey tests green (21 existing + 3 new). Typecheck `npx tsc --noEmit -p tsconfig.json` green.

### Round 10 follow-ups (spun off from P0 phase-gap matrix — §1 phase 10 P1)

- **P10-s708counter** s708(1) small-scale personal-offer counter (Folder 2/10 phase-10 P1 gap "s708(1) counter — running total of investors/$ in trailing 12 mo") — **shipped 2026-07-24**. New pure module `web/src/lib/compliance/s708-small-scale-counter.ts` exports `assessS708SmallScale({events, now?, preview?}) → S708CounterResult` mirroring the P1i `gst-threshold.ts` + P1n `wgea-threshold.ts` result-envelope shape so downstream cron / dashboard-panel wiring can reuse the same `{status, reason, next_action, disclaimer}` contract. Anchors on Corporations Act 2001 (Cth) s708(1)(a) (20-investor cap), s708(1)(b) (A$2,000,000 dollar cap), s708(3) (rolling 12-month window), and s707 on-sale rule for secondary transfers within 12 months of original issue (both `primary` and `secondary_on_sale` `offer_type` values feed the same counter — per s707(3) they consume the same headroom). 3-state classifier (`ok` / `warn` / `block`): warn amber-band when investor headroom ≤ 1 OR dollar headroom ≤ A$200,000, block red-band when either cap has already been reached OR when the optional `preview: {new_investors, amount_aud}` projection would breach either cap. Fixed `S708_SMALL_SCALE_DISCLAIMER` names the related-body-corporate / spouse / s707 on-sale caveats and cites Australian securities counsel — no rendering surface can leak the counter without the hedge (avoids s923B / s911A Corps Act exposure). Result envelope also carries `window_start_iso` / `window_end_iso` (trailing-365-day span computed from `now`), `investors_remaining` / `dollars_remaining_aud` (clamped ≥ 0), and `would_breach_investor_cap` / `would_breach_dollar_cap` (only set when preview supplied). Input tolerant of malformed events: bad ISO dates, blank/whitespace-only investor_ids, non-finite amounts, and negative amounts are silently dropped rather than thrown so callers can feed a `share_register` row set without pre-filtering. Investor de-duplication via `Set<investor_id>` (same email subscribing across tranches counts once, matching the ASIC "issue" counting convention). `next_action` copy is status-differentiated: green surfaces the `/dashboard/data-room` Folder 1 item 1.6 (Register of Members) logging nudge, amber pushes founders to run their next tranche through the `preview` mode before accepting, red enumerates the s708(8)/(10)/(11) wholesale + s738G CSF + full disclosure document escape hatches AND the s708(4) issue-to-existing-holders carve-out. Tests: 12 vitest cases in `web/src/lib/compliance/s708-small-scale-counter.test.ts` across 3 describe blocks — (1) window boundary (trailing-365d drop, de-dup within window, malformed-event silent skip), (2) status classification (ok/warn-investor/warn-dollar/block-investor/block-dollar branches at the exact statutory + amber-band edges), (3) preview mode (both cap-breach branches + a pass-through). All 12 tests green (`npx vitest run src/lib/compliance/s708-small-scale-counter.test.ts` — 1 file, 12 passed). Typecheck `npx tsc --noEmit -p tsconfig.json` green (exit 0). Closes §1 phase 10 P1 gap "s708(1) counter (running total of investors/$ in trailing 12 mo)"; downstream wiring (Chapter 10 CTA `/dashboard/fundraise` running-counter tile + fundraise-route pre-flight preview gate + `dataroom_files` register-of-members read adapter) deferred to P10-s708counter-wire because it needs a sibling UI + share-register-schema pass. Pure primitive shipped standalone so the sibling agent has a stable contract to build against.

### Round 12 follow-ups (spun off from P11-acquisition-pattern — §1 phase 11 P1 wizard)

- **P11-acquisition-wizard** pure deal-shape checker for the 90/10 template — **shipped 2026-07-24**. Chips off the pure-helper leg of the P11-acquisition-pattern tail-note ("Founder-facing UI helper (a 'run my deal through the 90/10 checklist' wizard on `/dashboard/exit-readiness`) remains a natural next tick under P11-acquisition-wizard"). New leaf lib `web/src/lib/exits/acquisition-pattern-check.ts` exports `assessAcquisitionPattern(input) → AcquisitionPatternCheck` — scores a founder-supplied deal shape (headline value, cash %, RSU %, escrow %, escrow months, RSU cliff + vest months, target industry keywords, buyer_is_foreign_govt_influenced, buyer_is_listed, change_of_control_consents_pct) against the reusable ~90% cash / ~10% retention-RSU template Atlassian ran on Trello 2017 + OpsGenie 2018 + Loom 2023 (as codified in the P11-acquisition-pattern §1 phase-11 Chapter 11 section). Result envelope mirrors the P1i / P1n / P10-s708counter shape: `{matches_atlassian_pattern, overall_signal: "green"|"amber"|"red", cash_pct, rsu_pct, earnout_pct, escrow_pct, splits_align, rsu_structure_ok, escrow_structure_ok, firb_pre_lodgment_required, firb_reason: "threshold_exceeded"|"sensitive_sector"|"foreign_govt_influence"|null, change_of_control_gap, change_of_control_consents_pct, warnings[], recommendations[], disclaimer}`. Exported constants — `CASH_PCT_ATLASSIAN_MIN=0.85`, `CASH_PCT_ATLASSIAN_MAX=0.95`, `RSU_PCT_ATLASSIAN_MIN=0.05`, `RSU_PCT_ATLASSIAN_MAX=0.15`, `ESCROW_PCT_MIN=0.10`, `ESCROW_PCT_MAX=0.15`, `ESCROW_MONTHS_MIN=12`, `ESCROW_MONTHS_MAX=18`, `RSU_CLIFF_MONTHS=12`, `RSU_VEST_MONTHS_MIN=24`, `RSU_VEST_MONTHS_MAX=36`, `CHANGE_OF_CONTROL_CONSENT_TARGET=0.95`, `CHANGE_OF_CONTROL_CONSENT_RED_FLOOR=0.7`, `FIRB_THRESHOLD_2026_AUD=339_000_000` — kept mutable-shape point-values so a downstream UI can render the bands to the founder without hard-coding. Semantics: (a) `normalisePct` accepts either 0-1 or 0-100 inputs (`> 1` divided by 100) and clamps overshoot to 1; garbage / negative / non-finite returns `null` so the classifier can't be fed a lie. (b) `splits_align` requires cash ∈ [0.85, 0.95] AND RSU ∈ [0.05, 0.15]; `rsu_structure_ok` requires 12-month cliff + 24-36-month vest; `escrow_structure_ok` requires 10-15% held 12-18 months — a deal missing all three ranges is `matches_atlassian_pattern=false`. (c) FIRB detection: buyer_is_foreign_govt_influenced=true triggers regardless of value / sector; any of the 19 sensitive-sector keywords (critical infrastructure, energy, telecommunications, port, airport, data centre, cyber, defence, quantum, artificial intelligence, biotech, genomics, space, satellite, critical minerals, rare earth, media) case-insensitively matched against `target_industry_keywords[]` triggers regardless of value; falling below both, headline ≥ A$339M FIRB Register 2026 threshold triggers. `firb_pre_lodgment_required` never downgrades the signal to red — it's a procedural runway, not a deal-killer (matches the Chapter 11 body copy that treats FIRB as a 30-60/60-90 day runway item). (d) Overall signal: `red` when consents < 70% (`CHANGE_OF_CONTROL_CONSENT_RED_FLOOR`) OR cash > 98% (retention handcuff missing) OR cash < 50% (sellers over-exposed to buyer stock risk); `green` when the template matches AND consents on track (or unspecified); else `amber`. (e) Warnings red-first ordered — CoC gap first (60-days-to-signing pointer with the "#1 cause of post-completion price adjustments" anchor), then cash concentration, then splits gap, then RSU structure, then escrow structure, then FIRB with reason-specific runway copy. (f) Recommendations always include CGT event A1 (ITAA 1997 s104-10) timing note + Subdiv 124-M scrip-for-scrip rollover eligibility note + disclosure-schedule mapping to data-room folders 1-8 + "engage specialist M&A lawyer before signing LOI (Corrs / HSF / DLA / Gilbert + Tobin)" pointer; buyer_is_listed=true additionally recommends the Subdiv 83A-C deferred ESS check on the RSU tranche for AU-resident recipients. Fixed `ACQUISITION_PATTERN_DISCLAIMER` cites the three Atlassian precedents + the CGT / Subdiv 124-M / Subdiv 83A-C / FIRB / change-of-control considerations + the "engage a specialist M&A lawyer before signing an LOI" line so no rendering surface can leak a classification without the hedge (avoids s923B / s911A / s1041H Corps Act exposure). Test coverage `web/src/lib/exits/acquisition-pattern-check.test.ts` — 14 vitest cases across 5 describe blocks: (1) template match (textbook 90/10 SaaS deal → green + matches + Trello 2017 headline as the anchor value; 0-100 percentage normalisation; garbage / negative / non-finite tolerance), (2) red branches (consents < 70% → red + red-first warning ordering, cash > 98% → red + retention-handcuff warning, cash < 50% → red + Subdiv 124-M rollover recommendation surfaces), (3) amber branches (80/20 splits → amber + not-matches + splits-diverge warning, 85% consents → amber + soft-gap warning without the "#1 cause" anchor, 18-month RSU vest → amber + rsu_structure_ok=false + 12-month-cliff warning), (4) FIRB detection (sensitive-sector cyber keyword under A$339M → required + sensitive_sector reason + green overall since template matches; foreign_govt_influenced under A$10M → required + foreign_govt_influence reason; under-threshold + non-sensitive + non-gov → not required + null reason + no FIRB warning), (5) constants + defaults (cash-band constants + FIRB threshold exposed, disclaimer + M&A-lawyer recommendation always present). All 14 tests green (`npx vitest run src/lib/exits/acquisition-pattern-check.test.ts` — 1 file, 14 passed). Adjacent suites re-run: 44 tests green across `src/lib/exits + src/lib/investor-pack`. Typecheck `npx tsc --noEmit -p tsconfig.json` green (exit 0). Follows the P5-cohort / P10-s708counter-adapter / P1i shape — a pure lib shipped standalone so the sibling exit-readiness-tile UI agent has a stable contract to build against without a schema coupling. The founder-facing form + client wizard that mounts this into `/dashboard/exit-readiness` (below the exit-benchmark tile) shipped in a follow-up tick as **P11-acquisition-wizard-ui** (see below).

- **P11-acquisition-wizard-ui** founder-facing wizard on `/dashboard/exit-readiness` — **shipped 2026-07-24**. Closes the P11-acquisition-wizard tail-note "The founder-facing form + client wizard that mounts this into `/dashboard/exit-readiness` (below the exit-benchmark tile) remains a natural next tick under P11-acquisition-wizard-ui once the sibling UI agent picks it up." Ships as a fully client-side wizard (no API route, no persistence, no schema churn) because `assessAcquisitionPattern` is pure — the whole check runs on every keystroke in `useMemo` so a founder sees the signal / warnings / FIRB gate flip live as they nudge fields off the default ~90/10 Atlassian template. Three new files, one edit:
  - `web/src/components/dashboard/acquisition-wizard.helpers.ts` — pure form helpers mirroring the P1n-form (`wgea-form.helpers.ts`) shape: `AcquisitionWizardFormState` (11 string-typed number/text fields + 2 booleans so `<input>` values round-trip cleanly), `makeEmptyAcquisitionWizardFormState()` (seeds cash=90 / rsu=10 / escrow=12 / escrow_months=15 / rsu_cliff=12 / rsu_vest=30 = the textbook Atlassian ~90/10 baseline; leaves `headline_value_aud` + `earnout_pct` + `change_of_control_consents_pct` blank so the founder must enter their own numbers before signals fire against them), `parseKeywords(csv)` (splits on `,` / `;` / newline with whitespace tolerance + drops empty tokens caused by trailing separators), `toAcquisitionInput(state)` (blank optional fields collapse to `null` — not `0` — so the checker's "unspecified" branches fire correctly; only `headline_value_aud` falls back to `0` because the checker requires a number), `pickAcquisitionBand(signal)` (mirrors `AcquisitionSignal` → `AcquisitionBand` 1:1 so the tile colour agrees with the checker's classification without a second threshold layer), `ACQUISITION_HEADLINE` (green/amber/red headline copy pack alongside the helpers so wizard + tests pull from the same source of truth).
  - `web/src/components/dashboard/acquisition-wizard.helpers.test.ts` — 11 vitest cases across 5 describe blocks: `makeEmptyAcquisitionWizardFormState` seeds textbook 90/10 defaults + leaves headline/earnout/consents blank; `parseKeywords` splits on `,` `;` `\n` + returns `[]` on blank + drops empty tokens; `toAcquisitionInput` coerces to numbers with blank→null on optionals + garbage→null on optionals + garbage→0 on required headline + FIRB flags + keywords propagate; `pickAcquisitionBand` mirrors the signal + stays consistent with a real `assessAcquisitionPattern` call on the default form (headline=A$425M + consents=97% → green + `matches_atlassian_pattern=true`) + flips to red when consents drop below the s708-red-floor even with a perfect template match; `ACQUISITION_HEADLINE` supplies a headline for every `AcquisitionSignal` branch. All 11 tests green (`npx vitest run src/components/dashboard/acquisition-wizard.helpers.test.ts` — 1 file, 11 passed).
  - `web/src/components/dashboard/acquisition-wizard-client.tsx` — `"use client"` component with three collapsible sections (Headline & splits — headline AUD + consents % + cash % + RSU % + earnout % ; RSU & escrow structure — cliff + vest + escrow % + escrow months; Buyer & FIRB context — target industry keywords textarea + `buyer_is_listed` toggle + `buyer_is_foreign_govt_influenced` toggle) and a live result banner (green/amber/red keyed on `pickAcquisitionBand(result.overall_signal)` with `data-testid="acquisition-wizard-result"` + `data-band="{band}"` for future E2E hook-in) that surfaces (a) the `ACQUISITION_HEADLINE[signal]` headline + `Icon` per band, (b) a 4-metric alignment strip (splits align / RSU structure / escrow structure / template match), (c) an amber FIRB chip when `firb_pre_lodgment_required=true` with a per-reason copy pack (`FIRB_REASON_LABEL` maps `threshold_exceeded` / `sensitive_sector` / `foreign_govt_influence` to founder-readable prose so the chip explains *why* FIRB triggered, not just that it did — anchored on the P11-acquisition-pattern Chapter 11 body copy), (d) the checker's `warnings[]` rendered red-first with the AlertTriangle icon, (e) the always-on `recommendations[]` (CGT event A1 + Subdiv 124-M + disclosure schedule + specialist M&A lawyer pointer + Subdiv 83A-C when `buyer_is_listed`) with the Info icon, and (f) the fixed `ACQUISITION_PATTERN_DISCLAIMER` footer so no render surface can leak a classification without the hedge. `data-testid="acquisition-wizard"` + `data-signal={overall_signal}` on the section root; `data-testid="acquisition-wizard-firb"` + `data-firb-reason={reason}` on the FIRB chip so a Playwright spec can attach without helper edits.
  - `web/src/app/dashboard/exit-readiness/page.tsx` — one-line import (`AcquisitionWizardClient`) + one-line mount immediately below `<ExitReadinessTile />` inside the existing `WorkspaceLayout` shell. Zero other changes to the page — the exit-readiness tile + header stay verbatim.
  - Typecheck `npx tsc --noEmit -p tsconfig.json` green (exit 0). Zero schema churn — no migration, no new API route, no persistence. Closes the last outstanding leg of the P11-acquisition-pattern / P11-acquisition-wizard / P11-acquisition-wizard-ui chain: Chapter 11 content section (P11-acquisition-pattern) → pure checker lib (P11-acquisition-wizard) → founder-facing wizard mounted on `/dashboard/exit-readiness` (P11-acquisition-wizard-ui). The wizard runs the same 14-case-covered checker the (future) Playwright spec at `web/tests/e2e/founder/acquisition-wizard.spec.ts` can hook onto via the `data-testid` + `data-signal` attributes above.

### Round 11 follow-ups (spun off from P1n-persist / P1n-form — compliance dashboard deep-link stubs)

- **P1n-esic-route** compliance dashboard deep-link stubs `/compliance/{esic,s708,gst,rd}` — **shipped 2026-07-24**. Closes the P1n-form tail-note "`/compliance/esic` / `/compliance/s708` / `/compliance/gst` / `/compliance/rd` remain stubbed deep-links today — the ESIC form already lives at `/workspace/esic-assessment` and the other three surfaces feed off existing dashboards, so those routes are lower-value follow-ups (P1n-esic-route + P1n-gst-route etc.) that only exist to remove the 404 on the dashboard tile links." Ships four Next.js App Router server pages mirroring the `/compliance/wgea` shell (auth-gated redirect to `/auth/login?next=<self>`, `dynamic = "force-dynamic"`, `robots: {index: false, follow: false}` metadata, `getActiveProject(user.id)`-scoped snapshot loader via `getSupabaseAdmin()`) — no client-side interactivity, no new schema, no new API routes. Each page renders the founder's latest saved snapshot plus a primary CTA into the existing workflow that owns the data, so the dashboard tile deep-link now resolves to a page that either shows current status or nudges the founder to the right form.
  - `web/src/app/compliance/esic/page.tsx` — reads `compliance_esic_assessments` (latest row per user × project), surfaces overall band (ESIC / not yet), 100-point score, early-stage-test pass/fail, principles-test pass/fail, `result.gaps[]`, `result.recommendations[]`. CTAs: primary "Run/Re-run self-assessment" → existing `/workspace/esic-assessment` form (P1e), secondary "Read Chapter 9 — Funding-ready" → `/guide/09-funding`. Empty-state copy explains what the Div 360 worksheet does. Footer inherits `ESIC_DISCLAIMER`.
  - `web/src/app/compliance/s708/page.tsx` — reads `compliance_s708_certs` (all rows per user × project ordered newest-first), classifies each via `classifyCert(cert_date, expiry_date, now)`, summarises via `summariseCerts()` for the 4-tile total/valid/expiring/expired header, renders the cert table (Investor / Basis — "A$2.5M net assets" vs "A$250k gross income" / Signed / Expires / Status). CTAs: primary "Go to fundraise workspace" → `/workspace/fundraise` (P6b), secondary "Read Chapter 10 — Fundraise" → `/guide/10-fundraise`. Footer inherits `S708_DISCLAIMER`.
  - `web/src/app/compliance/gst/page.tsx` — reads `compliance_gst_status` (latest row), surfaces trailing-12mo turnover, projected next-12mo, registered-for-GST flag, urgency band, and `result.reasoning` string. Empty-state copy explains the POST-your-own-input path via `/api/compliance/gst-threshold` (a founder-facing form is a future tick — deliberately deferred so this page ships without new plumbing). CTAs: primary "Read Chapter 6 — Revenue" → `/guide/06-revenue`, secondary external link to the ATO "Registering for GST" page (`rel="noopener noreferrer" target="_blank"`). Footer inherits `GST_DISCLAIMER`.
  - `web/src/app/compliance/rd/page.tsx` — loads `compliance_rd_registrations` rows and pipes them through `buildRDCalendar()` (same call the `/api/compliance/rd-calendar` GET route does) so the page renders the same shape without duplicating the derivation. Table columns: FY / Deadline / Days / Status (future / open / closing_soon / last_call / overdue) / Note. CTAs: primary "Read Chapter 6 — Revenue & R&D" → `/guide/06-revenue` (P1j), secondary external link to `business.gov.au — R&D Tax Incentive`. Footer inherits `RD_DISCLAIMER`.
  - Zero test churn — all four pages are auth-gated read-only render surfaces around pure helpers that already carry vitest coverage under `web/src/lib/compliance/`. Typecheck `npx tsc --noEmit -p tsconfig.json` green (exit 0). No new API routes, no new tables, no new client components — this tick is strictly the missing `page.tsx` layer that closes the 404. Founder-facing input forms for GST + s708 (the two compliance tiles that don't yet have a workspace form) remain lower-value follow-ups under P1n-gst-form + P1n-s708-form; the pages ship with a clear "how to add data" pointer so they're honest today.
- **P1n-gst-form** GST detail-page founder form — **shipped 2026-07-24**. Closes the P1n-esic-route tail-note "Founder-facing input forms for GST + s708 … remain lower-value follow-ups under P1n-gst-form + P1n-s708-form"; the WGEA + Modern Slavery pair got their forms in P1n-form and this tick brings GST to parity. Refactors `web/src/app/compliance/gst/page.tsx` from a read-only snapshot surface into an auth-gated shell that hydrates the latest `compliance_gst_status` row via `getSupabaseAdmin()` + `getActiveProject(user.id)` and hands `initialInput` / `initialResult` / `disclaimer=GST_DISCLAIMER` to a new `<GstFormClient>` — same shape as the WGEA + Modern Slavery detail pages so all three compliance tiles now have the same "dashboard tile → deep-link → detail page form → POST → detector → persistence → next-load surfaces the result" loop.
  - `web/src/app/compliance/gst/gst-form-client.tsx` — client component with two sections (`Monthly turnover history` — required actual CSV + optional projection CSV, both parsed by the helper so a raw Stripe paste is safe / `Registration status` — Toggle for `registered_for_gst`, date TextField for `registration_date`, text TextField for `abn` marked informational because the ABR probe already lives on Chapter 1). Submits `toGstInput(state)` to `POST /api/compliance/gst-threshold` (route already exists — this tick adds zero new routes), renders the returned `GSTResult` in a coloured banner keyed by `pickGstBand(result)` (red = urgent_register, amber = register_within_21_days, green = already_registered / none) with the reasoning string + trailing-12mo + projected-12mo + threshold + `will_cross_threshold_within_days` when populated. `data-testid="gst-result-banner"` on the result section for downstream Playwright hook-in — mirrors the `wgea-result-banner` + `modern-slavery-result-banner` naming.
  - `web/src/app/compliance/gst/gst-form.helpers.ts` — pure form helpers matching the WGEA + Modern Slavery shape: `GstFormState` (CSV strings + boolean + string date/abn), `makeEmptyGstFormState()`, `toGstInput(state) → GSTInput` (CSV parser drops non-numeric + negative entries silently so a raw Stripe export is safe; empty projection → `undefined` so the wire body matches the `GSTInput` optionality; blank abn/registration_date → `undefined`; trims whitespace on abn + registration_date so a paste with trailing spaces round-trips), `fromGstInput(wire) → GstFormState` (round-trips the CSVs back to a comma-joined string), and `pickGstBand(result) → "green"|"amber"|"red"` that mirrors the dashboard `CompliancePanel` GstTile colour matrix (red on `urgent_register`, amber on `register_within_21_days`, green on `already_registered` + `none`) so the pre-click + post-click states never disagree.
  - `web/src/app/compliance/gst/gst-form.helpers.test.ts` — 11 vitest cases across 4 describe blocks: `makeEmptyGstFormState` defaults; `toGstInput` (CSV parse + garbage/negative drop + projection preservation + blank-optional undefined-fallback + whitespace trim on abn/registration_date + registered_for_gst propagation); `fromGstInput ↔ toGstInput` round-trip (fully-populated + minimal); `pickGstBand` covering all 4 branches (urgent_register → red anchored on a real `assessGST()` call with `[10_000 × 12]` trailing turnover, register_within_21_days → amber with `[1_000 × 12]` actual + `[10_000 × 12]` projection, already_registered → green with `registered_for_gst=true`, none → green with `[500 × 12]` trailing turnover). All 11 tests green. Typecheck `npx tsc --noEmit -p tsconfig.json` green. Full `src/app/compliance/{gst,wgea,modern-slavery}` suite re-run: 36 tests green across 3 files.
  - Zero schema churn — no migration, no new API route, no new table. The `POST /api/compliance/gst-threshold` route + `compliance_gst_status` table (from migration 0108) already carry the same `input_json` + `result_json` snapshot shape the WGEA + Modern Slavery routes use, so `<GstFormClient>` reuses the existing plumbing end-to-end. Only s708 remains without a founder-facing form; P1n-s708-form (which needs the sibling share-register-schema agent to unlock the s708(1) counter's `events[]` source) is the next natural follow-up.
- **P1n-s708-form** s708(1) small-scale counter founder form — **shipped 2026-07-24**. Closes the P1n-gst-form tail-note "Only s708 remains without a founder-facing form; P1n-s708-form … is the next natural follow-up." Ships as a stateless preview tool (deliberately no persistence — the durable `events[]` source is still owned by the sibling share-register-schema tick) so a founder can already model tranches by pasting their personal-offer register into the form.
  - `web/src/app/compliance/s708/s708-counter-form.helpers.ts` — pure form helpers: `S708CounterFormState` (events_text CSV + preview toggle + preview_new_investors + preview_amount_aud, all strings so `<input>` / `<textarea>` round-trip cleanly), `makeEmptyS708CounterFormState()`, `parseS708EventsCsv(text) → S708OfferEvent[]` (comma-separated `offer_date, investor_id, amount_aud [, offer_type]` per line; strict `^\d{4}-\d{2}-\d{2}$` date guard, blank + malformed rows + `#` comments silently dropped; unknown `offer_type` values drop the tag but keep the row so a founder pasting a legacy register isn't punished), `toS708CounterInput(state) → S708CounterInput` (skips the preview when disabled OR when both preview numeric fields are zero/garbage/negative — matches the counter's "no preview supplied" branch so a founder who ticks the box but leaves the fields blank sees an unqualified assessment instead of a spurious `would_breach_*` flag), `pickS708Band(result) → "green"|"amber"|"red"` (ok→green / warn→amber / block→red — mirrors the amber-band trigger at `S708_WARN_INVESTOR_HEADROOM=1` + `S708_WARN_DOLLAR_HEADROOM_AUD=200_000` so the UI colour agrees with the counter's status literal).
  - `web/src/app/compliance/s708/s708-counter-form.helpers.test.ts` — 11 vitest cases across 4 describe blocks: `makeEmptyS708CounterFormState` defaults; `parseS708EventsCsv` (well-formed multi-row parse + comment-line drop + malformed-row silent skip incl. bad date / missing investor_id / non-numeric amount / negative amount / 2-column row / bogus offer_type + empty-input branch); `toS708CounterInput` (preview omitted when disabled + preview attached with floor/clamp on new_investors + preview omitted when both fields zero/garbage + negative-clamps-to-zero-then-omitted); `pickS708Band` (all 3 status branches anchored on real `assessS708SmallScale()` calls — empty→ok/green, 19 investors→warn/amber, 20 investors→block/red, dollar-cap preview breach→block/red). All 11 tests green.
  - `web/src/app/api/compliance/s708-small-scale-counter/route.ts` — POST-only, auth-gated via `getCurrentUser()` (401 anon), `dynamic="force-dynamic"`, `runtime="nodejs"`. Boundary validation: 400 `invalid_json` / `invalid_body` / `missing_events`; every event row runs through `isS708Event()` type guard so a client can't inject a duplicate-key attack or negative amount that would inflate the count in ways the pure counter's silent-skip would hide. Preview object accepted only when both `new_investors` + `amount_aud` are typed numbers. **No persistence** — this is a stateless preview, deliberately no `dataroom_files` / new-table churn; the sibling share-register-schema tick owns durable events. Every response carries `S708_SMALL_SCALE_DISCLAIMER`.
  - `web/src/app/compliance/s708/s708-counter-form-client.tsx` — client component mounted on the existing `/compliance/s708` page below the wholesale-certs table (page.tsx edit adds a two-line import + one-line mount, no other changes). UI: `<textarea rows={8}>` for the events CSV with monospace font + placeholder + inline column-format hint (`offer_date, investor_id, amount_aud [, offer_type]`); collapsible preview fieldset with checkbox + two `<input type="number">` fields; result banner keyed on `pickS708Band(result)` with distinct headline copy per branch (`ok` / `warn` / `block`), window range, `investor_count_12mo / investor_cap`, `dollars_raised_12mo_aud` + `dollar_cap_aud`, plus the counter's own `reason` + `next_action` strings so a founder gets the escape-hatch list (s708(8)/(10)/(11), CSF s738G, disclosure document) inline instead of having to re-read the guide. `data-testid="s708-counter-result-banner"` matches the WGEA / GST / Modern Slavery naming pattern so a future Playwright spec can attach without helper edits.
  - `web/src/app/compliance/s708/page.tsx` grows a one-line import for `S708_SMALL_SCALE_DISCLAIMER` + `S708CounterFormClient` and mounts the form beneath the wholesale-cert action row. The wholesale-cert table + summary tiles + `S708_DISCLAIMER` footer are untouched.
  - Typecheck `npx tsc --noEmit -p tsconfig.json` green (exit 0). Zero schema churn — no migration, no new table, no persistence. Closes the last founder-facing gap on the P1n → P1n-persist → P1n-form → P1n-esic-route → P1n-gst-form → P1n-s708-form chain: all four compliance detail pages (WGEA, Modern Slavery, GST, s708) now have a founder-facing form; ESIC's form lives at `/workspace/esic-assessment` (P1e). Durable events store for the s708(1) counter remains a follow-up under P1n-share-register (blocked on the sibling share-register-schema agent) — this tick unblocks the founder-facing surface today.

- **P10-s708counter-adapter** normalise share-register rows into the s708 counter's event shape — **shipped 2026-07-24**. Chips off the read-side of P10-s708counter-wire (still deferred as a whole because the durable share-register schema is owned by a sibling agent) by shipping a pure adapter callers can point at any share-register export today. New module `web/src/lib/compliance/s708-register-adapter.ts` exports `adaptShareRegisterRows(rows) → { events, skipped }` — a stateless normaliser that accepts a loose `ShareRegisterRow[]` (fields optional so callers can feed `equity_events` rows, an ASIC Form 484 CSV, or a founder-uploaded ROM verbatim) and returns a strict `S708OfferEvent[]` the existing `assessS708SmallScale` counter can consume unchanged plus a `skipped[]` audit trail of `(index, S708AdapterSkipReason)` pairs so a UI can surface "we couldn't parse N rows". Semantics: (a) `event_type` classifier maps `issue` / `primary` / `primary_issue` / `grant` / `allotment` → `primary` and `transfer` / `secondary` / `secondary_on_sale` / `on_sale` → `secondary_on_sale` (matches the s708 counter's `S708OfferType` union, which per s707(3) counts secondary transfers within 12 months of original issue against the same s708(1) headroom); blank `event_type` defaults to `primary`; anything else is skipped with `unknown_event_type`. (b) Exemption filter drops rows whose `exemption` tag names a non-s708(1) offer (`s708(8)` wholesale / `s708(10)` professional / `s708(11)` senior manager / `csf` / `s738G` / `prospectus` / `pds` / `ois` / `disclosure`), so a founder who has already classified their offers cannot double-count wholesale into s708(1) headroom by accident. Blank / null / `s708(1)` / `s708_1` / `s708.1` / `small_scale` / `personal_offer` all count — the counter itself is the guard-rail. (c) `investor_id` resolution walks `investor_id` → `investor_email` → normalised `investor_name` (trim + lowercase) so the same person still de-dupes across CSV columns / label conventions and the counter's `Set<investor_id>` continues to match the ASIC "issue" counting convention. (d) `amount_aud` resolution prefers explicit `amount_aud` → `consideration_aud` → `shares × price_per_share_aud`; missing all three skips with `missing_amount`, an explicit negative skips with `negative_amount` so a founder sees the bad row rather than having it silently swallowed. (e) Date normalisation accepts both `YYYY-MM-DD` and full ISO-8601 timestamps (extracting the date part), rejecting garbage with `invalid_date` distinct from `missing_date`. 11 vitest cases in `web/src/lib/compliance/s708-register-adapter.test.ts` across five describe blocks — event_type classification (primary vs secondary vs blank-default vs unknown-skip), exemption filter (non-s708 drops vs blank/null defaults to included), investor_id + amount fallbacks (id/email/name preference; missing-id vs missing-amount vs negative-amount branches; shares×price path), date normalisation (YYYY-MM-DD vs full ISO vs garbage vs missing), and an end-to-end run that feeds the adapter output straight into `assessS708SmallScale` with 19 personal-offer rows totalling A$1.8M plus a s708(8) wholesale row that must NOT eat into headroom — asserts the counter classifies `warn` on both investor-headroom and dollar-headroom guards. All 11 tests green (`npx vitest run src/lib/compliance/s708-register-adapter.test.ts` — 1 file, 11 passed). Typecheck `npx tsc --noEmit -p tsconfig.json` green (exit 0). Closes the read-side gap of P10-s708counter-wire so downstream cron / dashboard-panel loaders can pull rows from any source without pre-normalising. The remaining legs of P10-s708counter-wire (`/dashboard/fundraise` running-counter tile + fundraise-route pre-flight preview gate + durable share-register persistence) still depend on the sibling share-register-schema agent for the events store.

- **P11-acquisition-pattern** Chapter 11 acquisition-pattern section (§1 phase 11 P1 gap "acquisition-pattern doc showing Atlassian's ~90% cash / 10% retention template") — **shipped 2026-07-24**. Chapter 11 (`11-scale`) now carries its first `sections[]` entry `acquisition-pattern` (heading + 8-bullet body EN + VI) via the previously-declared `Chapter.sections?: ChapterSection[]` slot (web/src/lib/guide/startup-journey.ts:66). Walks the reusable ~90% cash / 10% retention-RSU template Atlassian has used on every headline post-funding acquisition end-to-end: (a) the three-precedent anchor (Trello 2017 US$425M, OpsGenie 2018 US$295M, Loom 2023 US$975M — sourced from `/showcase/atlassian` TIMELINE lines 147-155, 157-165, 183-191 + Atlassian FY18 + FY24 10-K schedules) so a founder can defend the same structure by pointing at three deals over six years, (b) why the 90/10 split works (cash gives target investors + founders a clean exit at closing; RSU keeps engineering + product leadership incentivised through the month-12→month-18 retention cliff), (c) how the RSU tranche is structured on Atlassian deals (1-year cliff + 24-36 month monthly vest, sized 2-4× annual base salary as headline value, forfeitable on separation, larger 'founder retention bonus' grant with a 4-year vest for target founders), (d) AU seller-side tax treatment where founders lose money if they don't plan the structure — CGT event A1 (ITAA 1997 s104-10) triggers on contract date not settlement date so the tax bill lands before the wire clears, Subdiv 124-M scrip-for-scrip rollover applies ONLY where consideration is shares (not cash + shares) so a 90/10 cash+RSU deal does NOT qualify for rollover on the cash tranche, RSU tranche may qualify for Subdiv 83A-C deferred ESS taxation for AU-resident employee recipients if the buyer is a listed company, (e) escrow + hold-back patterns (10-15% cash held escrow 12-18 months for indemnity claims + special indemnity escrow for known risks + FIRB approval condition-precedent for AU targets acquired by US listed buyers over the A$339M 2026 threshold or holding sensitive tech, with 30-60 days FIRB runway built into completion), (f) disclosure-schedule prep (the piece founders under-invest in — every material contract with a change-of-control clause needs to be listed with counter-party consent obtained pre-completion or completion made conditional on consent; missing consents are the #1 cause of post-completion price adjustments at 5-10% common; BlockID's data-room folders 1, 2, 4, 5, 6, 7, 8 at `data-room-templates.ts:428-1151` are structured so the schedule maps 1:1 to folder headings), (g) real-signal check — the four signals that distinguish being-acquired from pattern-matching-an-acquisition-thesis (unsolicited Corp Dev approach; competitive bake-off within 4-6 weeks; buyer's CEO/product-lead direct outreach in the first two calls; buyer asking for cohort data + eng-team org chart before financial diligence), (h) BlockID wiring — CFO exit-readiness assemble at `/dashboard/exit-readiness` (see P12b-tile) includes an 'acquisition-structure playbook' section citing the 90/10 template + AU tax + FIRB + escrow guardrails, au-compliance agent drafts the FIRB pre-lodgment questionnaire when target-industry keywords hit the sensitive-tech list, CLO agent runs a change-of-control clause scan across data-room folder 8 (Contracts & Agreements) 60 days out from any signed LOI, with an explicit "engage a specialist M&A lawyer before signing an LOI — the LOI is where 80% of the leverage is set" pointer to the AU sell-side practices at Corrs / HSF / DLA / Gilbert + Tobin. Both marketing (`/guide/11-scale`) and workspace (`/workspace/guide/11-scale`) surfaces render `sections[]` via the existing `c.sections?.map()` blocks — no page edits needed (reuses the P1j / P1l / P8-shipit wiring). Tests: `web/src/lib/guide/startup-journey.test.ts` gains an "acquisition-pattern section (chapter 11 P11-acquisition-pattern)" describe block — 2 vitest cases pinning (a) presence + EN/VI alignment ≥ 6 bullets, (b) precedent + statutory anchor grep (Trello, OpsGenie, Loom, US$425M, US$295M, US$975M, RSU, 90%, Subdiv 124-M, scrip-for-scrip, escrow, FIRB, CGT event A1, change-of-control). Pre-existing "no other chapter carries sections yet" guard now allow-lists chapters 06, 08, 10 AND 11 (a `Set<ChapterSlug>` gains one entry). All 26 startup-journey tests green (24 existing + 2 new). Typecheck `npx tsc --noEmit -p tsconfig.json` green (exit 0). Closes §1 phase 11 P1 gap "acquisition-pattern doc showing Atlassian's ~90% cash / 10% retention template" — the last remaining P1 content-gap surfaced on the phase 11 row of the §1 phase-gap matrix (WGEA + Modern Slavery threshold detectors closed by P1n, compliance-calendar closed by P1k). Founder-facing UI helper (a "run my deal through the 90/10 checklist" wizard on `/dashboard/exit-readiness`) remains a natural next tick under P11-acquisition-wizard — this tick ships the content, not the interactive tool.

- **P3d** CMO deep-pass agent-side wiring of the AU market lookup (§1 phase 3 P1 gap follow-up) — **shipped 2026-07-24**. New pure module `web/src/lib/report-pipeline/au-market-anchor.ts` exports `extractAnzsicCode(rawText)` (case-insensitive `/\b([A-Sa-s])\s*(\d{4})\b/` guard so "J5810", "j5810", and "J 5810" all canonicalise to `J5810`), `deriveIndustryKeyword(rawText)` (word-boundary scoring across every seeded `AU_MARKET_INDUSTRIES[*].keywords[]` plus a small label-token bonus; ties break on larger TAM to match `searchByKeyword`'s parent-market preference), and `buildAuMarketAnchorBlock({rawText?, anzsicCode?, keyword?, addressablePct?, targetSharePct?})` which returns a markdown block (or null) with ANZSIC code + label + TAM / SAM / SOM (formatted A$B / A$M / A$k) + CAGR% + business count + notes + full source citations + the fixed `AU_MARKET_LOOKUP_DISCLAIMER`. Precedence: explicit anzsicCode → explicit keyword → rawText-extracted ANZSIC → rawText-derived keyword. Wired into `web/src/lib/report-pipeline/agent-dispatcher.ts::buildUserPrompt` — the `market` criterion branch now calls `buildAuMarketAnchorBlock({rawText: context.rawText + criterionData.textInput})` and pushes the block into `parts[]` immediately before any existing `gr.competitiveResearch` blob, so every CMO market-research LLM call is grounded in ABS + IBISWorld anchors rather than hallucinated numbers. No new API route, no schema churn, no network call — pure text-in / text-out. Test coverage `au-market-anchor.test.ts` — 15 vitest cases across 3 describe blocks: `extractAnzsicCode` (null/empty tolerance, canonicalisation, whitespace between letter and digits, first-hit-only), `deriveIndustryKeyword` (empty-text null, fintech-pitch → fintech keyword, competing-signal preference for the industry with more hits, no-match null), `buildAuMarketAnchorBlock` (null when nothing matches, full-block render with all required headings + disclaimer, explicit ANZSIC wins over rawText keyword, rawText-extracted ANZSIC fallback, addressable/target-share overrides render in the block, unknown-keyword null, out-of-range pct overrides clamp to [0,1] via `estimateTamSamSom` defence-in-depth). All 15 tests green. Adjacent suites (`src/lib/report-pipeline` + `src/lib/market`) still green — 40 tests across 3 files. Typecheck `npx tsc --noEmit -p tsconfig.json` green (exit 0). Closes §1 phase 3 P1 gap follow-up "CMO deep-pass agent-side consumer wiring (auto-firing the [ABS] lookup during a market-research analysis) remains a natural next tick under P3d" — the CMO market-research wave now embeds the ABS + IBISWorld anchor at prompt-construction time so the LLM's TAM/SAM/SOM section is grounded in real numbers. Founder-facing Chapter 3 UI (P3c) and public route (P3b) still ship the same lookup direct to the founder; this tick makes the agent-side pipeline consume it too.

Each P1x / P2x / P3x / P5x / P6x / P7x / P8x / P10x / P11x / P12x / P1n-x subtask becomes a follow-up commit driven by the autonomous loop; each ships an evidence doc, template file, or code change and marks its checkbox here.

---

*Last updated: 2026-07-24 by autonomous loop (tick shipping P3d — CMO deep-pass agent-side consumer wiring for the AU market lookup, closing the P3c tail-note "CMO deep-pass agent-side consumer wiring (auto-firing the lookup during a market-research analysis) remains a natural next tick under P3d". New pure module `web/src/lib/report-pipeline/au-market-anchor.ts` — `extractAnzsicCode(rawText)` (case-insensitive `/\b([A-Sa-s])\s*(\d{4})\b/` so "J5810" / "j5810" / "J 5810" canonicalise to `J5810`, first-hit-only), `deriveIndustryKeyword(rawText)` (word-boundary scoring across every seeded `AU_MARKET_INDUSTRIES[*].keywords[]` with a small label-token bonus, ties break on larger TAM to match `searchByKeyword`'s parent-market preference), and `buildAuMarketAnchorBlock({rawText?, anzsicCode?, keyword?, addressablePct?, targetSharePct?})` returning a markdown block with ANZSIC code + label + TAM/SAM/SOM (formatted A$B / A$M / A$k) + CAGR% + business count + notes + source citations + `AU_MARKET_LOOKUP_DISCLAIMER`. Precedence: explicit anzsicCode → explicit keyword → rawText-extracted ANZSIC → rawText-derived keyword. One-line wire-in at `web/src/lib/report-pipeline/agent-dispatcher.ts::buildUserPrompt` — the `market` criterion branch pushes the anchor block into `parts[]` immediately before any existing `gr.competitiveResearch` blob, so every CMO market-research LLM call carries an ABS + IBISWorld anchor rather than a hallucinated TAM number. Pure text-in / text-out — no network, no DB, no LLM. Tests `au-market-anchor.test.ts` — 15 vitest cases across 3 describe blocks (extractAnzsicCode: null / canonicalisation / whitespace-tolerance / first-hit-only; deriveIndustryKeyword: empty-null / fintech-pitch / competing-signal / no-match-null; buildAuMarketAnchorBlock: null-when-no-signal / full-block-render / explicit-ANZSIC-over-keyword / rawText-extracted-ANZSIC / pct-overrides / unknown-keyword-null / out-of-range-pct clamp defence-in-depth). All 15 tests green. Adjacent suites still green — 40 tests across `src/lib/report-pipeline` + `src/lib/market`. Typecheck `npx tsc --noEmit -p tsconfig.json` green (exit 0). Founder-facing UI (P3c) and public route (P3b) already surface the same lookup direct to the founder; this tick makes the agent-side pipeline consume it too. The last remaining §1 phase-3 P1 gap ("Chapter 3 CTA + CMO deep-pass consumer wiring") is now fully closed.)*

*Previous tick (P3c — /guide/03-market-research + /workspace/guide/03-market-research now embed a founder-facing TAM/SAM/SOM lookup panel that calls the existing public GET /api/abs/lookup route (P3b) around the seeded au-market-lookup.ts fixture (P3a). No new API routes, no schema churn — a raw client-side `fetch()` on submit with the founder's keyword or ANZSIC 2006 class code plus optional addressable + target-share percentages. Three new files: (a) `web/src/components/guide/abs-lookup.helpers.ts` — pure helpers `AbsLookupFormState` (query + addressablePct + targetSharePct as strings so `<input type="number">` values round-trip cleanly) + `makeEmptyAbsLookupFormState(initialKeyword?)` + `isAnzsicCode(raw)` (uppercase-letter + 4-digit shape guard so "J5810" routes as `?anzsic=` and "saas" routes as `?keyword=`) + `parsePctInput(raw)` (converts 0..100 user input to 0..1 fractions; `>1` divided by 100 and passed-through `≤1` treated as already-fractional — matches the acquisition-wizard normalisePct convention; garbage/negative → null so the URL builder omits the param and lets the route apply its own 20% / 1% defaults; overshoot clamps to [0,1]) + `buildAbsLookupUrl(state)` (returns null for blank queries so the submit button stays disabled; otherwise URL-encodes keyword or upper-cases ANZSIC and attaches non-null pct params) + `formatAudCompact(n)` (billions with 1dp, millions as whole A$M, sub-A$M as A$k / raw AUD; null/NaN/infinity/negative → em-dash so the panel never misleads the reader) + `formatPct(fraction)` (whole percents cleanly, decimals with 1dp, sub-1% preserved with 2dp trimmed trailing zeros so 0.001 → "0.1%" survives). (b) `web/src/components/guide/abs-lookup.helpers.test.ts` — 21 vitest cases across 6 describe blocks: `makeEmptyAbsLookupFormState` defaults + initial-keyword trim; `isAnzsicCode` uppercase/lowercase/whitespace tolerance + rejection matrix (freeform / too-short / too-long / all-numeric / empty); `parsePctInput` blank/non-numeric-null + 0..100-to-fraction + already-fractional-passthrough + overshoot-clamp; `buildAbsLookupUrl` blank-null / ANZSIC-branch upper-cases / keyword-branch URL-encodes / fractional pct attachment / blank-pct-omission; `formatAudCompact` em-dash on 5 non-finite branches + billions/millions/sub-M/zero; `formatPct` em-dash on 3 non-finite branches + whole/decimal/sub-1% preservation. (c) `web/src/components/guide/abs-lookup-panel.tsx` — `"use client"` React component with EN + VI copy pack, marketing + workspace visual variants (colour + border adapts to the two guide surfaces), a text input for the query + two number inputs for the pct overrides, and a live result panel that renders TAM/SAM/SOM in a 3-tile grid + CAGR + business-count + notes + clickable ABS/IBISWorld/RBA/AusIndustry sources + a suggestions row of sibling-ANZSIC pills that re-run the lookup on click. Fixed `disclaimer` from `AU_MARKET_LOOKUP_DISCLAIMER` rendered on every success so no rendering surface can leak a TAM anchor without the ABS-provenance hedge (`s766B` Corps Act guard applied at source per P3a). `data-testid` attributes on panel root + query input + submit button + result block + TAM/SAM/SOM cells + label + suggestions + disclaimer + error banner so a future Playwright spec can attach without helper edits; `data-status={idle|loading|success|error}` on the section root reflects the fetch state. Two-file mount into `web/src/app/guide/[chapter]/page.tsx` + `web/src/app/workspace/guide/[chapter]/page.tsx` — one-line import each + a `c.slug === "03-market-research"` guard immediately after the `c.sections?.map()` block that renders `<AbsLookupPanel locale={locale} variant="marketing"|"workspace" />`. Zero other page edits — every other chapter surface stays verbatim. Typecheck `npx tsc --noEmit -p tsconfig.json` green for the new files (unrelated pre-existing errors in `src/lib/ga4/data-api-client.ts` + `src/lib/reseller/founder-attribution-linker.ts` remain — not touched by this tick). All 21 vitest cases green (`npx vitest run src/components/guide/abs-lookup.helpers.test.ts src/lib/market src/app/api/abs` — 3 files, 47 tests passed). Closes §1 phase 3 P1 gap follow-up "Chapter 3 CTA + CMO deep-pass consumer wiring (which surface calls this route with the founder's project.industry) remains a follow-up under P3c" — the Chapter 3 CTA half now ships; CMO deep-pass agent-side consumer wiring (auto-firing the lookup during a market-research analysis) remains a natural next tick under P3d because it needs the sibling market-research agent to decide when to call the endpoint during a report generation.)*

*Previous tick (P11-acquisition-wizard-ui — founder-facing client wizard mounted on /dashboard/exit-readiness beneath ExitReadinessTile. Runs the pure `assessAcquisitionPattern` helper (P11-acquisition-wizard) in-browser on every keystroke via `useMemo` — no API route, no persistence, no schema churn. Three new files: (a) `web/src/components/dashboard/acquisition-wizard.helpers.ts` — pure form helpers `AcquisitionWizardFormState` (11 string-typed number/text fields + 2 booleans) + `makeEmptyAcquisitionWizardFormState()` seeding textbook 90/10 defaults + `parseKeywords(csv)` + `toAcquisitionInput(state)` (blank optionals collapse to null so the checker's "unspecified" branches fire correctly; only headline_value_aud falls back to 0) + `pickAcquisitionBand(signal)` mirroring `AcquisitionSignal` 1:1 + `ACQUISITION_HEADLINE` copy pack; (b) `acquisition-wizard.helpers.test.ts` — 11 vitest cases across 5 describe blocks pinning defaults + keyword parsing + null-vs-0 coercion + FIRB flag propagation + band-mirrors-signal invariant with real `assessAcquisitionPattern` calls for green (headline=A$425M + consents=97%) and red (consents=50%) branches; (c) `acquisition-wizard-client.tsx` — `"use client"` component with three sections (Headline & splits / RSU & escrow structure / Buyer & FIRB context) and a live result banner keyed on `pickAcquisitionBand` surfacing headline + 4-metric alignment strip (splits/RSU/escrow/template match) + amber FIRB chip with per-reason copy pack (`FIRB_REASON_LABEL` maps threshold_exceeded / sensitive_sector / foreign_govt_influence to founder-readable prose) + warnings[] red-first + always-on recommendations[] with icons + fixed `ACQUISITION_PATTERN_DISCLAIMER` footer. `data-testid` + `data-signal` + `data-firb-reason` attributes on the section root + FIRB chip so a future Playwright spec can attach without helper edits. One-line edit to `web/src/app/dashboard/exit-readiness/page.tsx` mounts the wizard beneath the existing ExitReadinessTile. All 11 vitest cases green; `npx tsc --noEmit -p tsconfig.json` green (exit 0). Closes the last outstanding leg of the P11-acquisition-pattern / P11-acquisition-wizard / P11-acquisition-wizard-ui chain — Chapter 11 content section (P11-acquisition-pattern) → pure checker lib (P11-acquisition-wizard) → founder-facing wizard mounted on /dashboard/exit-readiness (P11-acquisition-wizard-ui).)*

*Previous tick (P11-acquisition-wizard — new leaf lib `web/src/lib/exits/acquisition-pattern-check.ts` exports `assessAcquisitionPattern(input) → AcquisitionPatternCheck` that scores a founder-supplied deal shape (headline value, cash %, RSU %, escrow %, escrow months, RSU cliff + vest months, target industry keywords, buyer_is_foreign_govt_influenced, buyer_is_listed, change_of_control_consents_pct) against the reusable ~90% cash / ~10% retention-RSU template Atlassian ran on Trello 2017 + OpsGenie 2018 + Loom 2023 (as codified in the P11-acquisition-pattern §1 phase-11 Chapter 11 section). Result envelope: {matches_atlassian_pattern, overall_signal: "green"|"amber"|"red", cash_pct, rsu_pct, earnout_pct, escrow_pct, splits_align, rsu_structure_ok, escrow_structure_ok, firb_pre_lodgment_required, firb_reason: "threshold_exceeded"|"sensitive_sector"|"foreign_govt_influence"|null, change_of_control_gap, change_of_control_consents_pct, warnings[], recommendations[], disclaimer}. Percentage inputs normalised — accepts both 0-1 and 0-100; garbage/negative/non-finite returns null so the classifier can't be fed a lie. Signal branches: red when consents < 70% OR cash > 98% (no retention handcuff) OR cash < 50% (sellers over-exposed to buyer stock); green when template matches AND consents on track (or unspecified); else amber. FIRB detection triggers on buyer_is_foreign_govt_influenced OR any of 19 sensitive-sector keywords (cyber/defence/quantum/AI/biotech/genomics/space/critical infrastructure/etc.) OR headline ≥ A$339M FIRB Register 2026 threshold — never downgrades the signal to red, treated as procedural runway (matches Chapter 11 body copy). Warnings red-first ordered — CoC gap first with the "#1 cause of post-completion price adjustments" anchor, then cash concentration, then splits gap, then RSU structure, then escrow structure, then FIRB with reason-specific runway copy (30-60 vs 60-90 days). Recommendations always include CGT event A1 (ITAA 1997 s104-10) timing + Subdiv 124-M scrip-for-scrip rollover eligibility + disclosure-schedule mapping to data-room folders 1-8 + specialist M&A lawyer pointer (Corrs/HSF/DLA/Gilbert + Tobin); buyer_is_listed=true additionally recommends the Subdiv 83A-C deferred ESS check. Fixed ACQUISITION_PATTERN_DISCLAIMER cites the three Atlassian precedents + the CGT / Subdiv 124-M / Subdiv 83A-C / FIRB / change-of-control considerations + "engage a specialist M&A lawyer before signing an LOI" line so no rendering surface can leak a classification without the hedge (avoids s923B / s911A / s1041H Corps Act exposure). Tests: 14 vitest cases across 5 describe blocks — template match (textbook 90/10 → green + Trello 2017 headline; 0-100 percentage normalisation; garbage tolerance), red branches (consents < 70%, cash > 98%, cash < 50%), amber branches (80/20 splits, 85% consents, 18-month RSU vest), FIRB detection (sensitive-sector cyber keyword under A$339M, foreign_govt_influenced under A$10M, under-threshold + non-sensitive + non-gov), constants + defaults. All 14 tests green; adjacent 44 tests green across src/lib/exits + src/lib/investor-pack. Typecheck `npx tsc --noEmit -p tsconfig.json` green (exit 0). Follows the P5-cohort / P10-s708counter-adapter / P1i shape — a pure lib shipped standalone so the sibling exit-readiness-tile UI agent has a stable contract to build against without a schema coupling. The founder-facing form + client wizard that mounts this into /dashboard/exit-readiness (below the exit-benchmark tile) remains a natural next tick under P11-acquisition-wizard-ui.)*

*Previous tick (P11-acquisition-pattern — Chapter 11 (`11-scale`) sections[] entry acquisition-pattern (heading + 8-bullet body EN + VI) closing §1 phase 11 P1 gap "acquisition-pattern doc showing Atlassian's ~90% cash / 10% retention template". Walks the reusable ~90% cash / ~10% retention-RSU template Atlassian used on Trello 2017 US$425M + OpsGenie 2018 US$295M + Loom 2023 US$975M end-to-end: three-precedent anchor sourced from /showcase/atlassian TIMELINE + Atlassian FY18/FY24 10-K schedules; why the split works (cash exit + RSU retention past the month-12→month-18 retention cliff); RSU structure on Atlassian deals (1-year cliff + 24-36 month monthly vest, 2-4× base salary headline, founder retention bonus 4-year vest); AU seller-side tax (CGT event A1 triggers on contract date not settlement — ITAA 1997 s104-10; Subdiv 124-M scrip-for-scrip does NOT survive a cash-heavy mix; Subdiv 83A-C deferred ESS may apply on the RSU tranche when buyer is listed); escrow + hold-back patterns + FIRB pre-lodgment for AU targets acquired by US listed buyers over A$339M 2026 threshold; disclosure-schedule prep with change-of-control consent as #1 post-completion price-adjustment cause; the four real-signal-vs-pattern-matching checks; and the BlockID.au wiring path through the CFO exit-readiness assemble + au-compliance FIRB questionnaire + CLO change-of-control scan + specialist M&A lawyer pointer (Corrs/HSF/DLA/Gilbert + Tobin). Tests: 2 new vitest cases in web/src/lib/guide/startup-journey.test.ts (presence + EN/VI ≥ 6 bullets + anchor grep for Trello/OpsGenie/Loom/US$425M/US$295M/US$975M/RSU/90%/Subdiv 124-M/scrip-for-scrip/escrow/FIRB/CGT event A1/change-of-control) + the pre-existing "no other chapter carries sections yet" guard now allow-lists chapters 06, 08, 10 AND 11 via a Set<ChapterSlug>. All 26 startup-journey tests green (24 existing + 2 new). Typecheck npx tsc --noEmit -p tsconfig.json green (exit 0). Both marketing (/guide/11-scale) + workspace (/workspace/guide/11-scale) surfaces render sections[] via the existing c.sections?.map() blocks — no page edits needed (reuses the P1j / P1l / P8-shipit wiring). Follows the P1j / P1l / P8-shipit / P8-pledge1 shape — a content section shipped standalone; the interactive "run my deal through the 90/10 checklist" wizard on /dashboard/exit-readiness is deferred to a P11-acquisition-wizard follow-up.)*

*Previous tick (P10-s708counter-adapter — pure adapter `web/src/lib/compliance/s708-register-adapter.ts` normalising loose share-register / equity-events rows into the strict `S708OfferEvent[]` shape `assessS708SmallScale` consumes, plus a `skipped[]` audit trail keyed on 7 distinct `S708AdapterSkipReason` values. Chips off the read-side of the deferred P10-s708counter-wire (still blocked on the sibling share-register-schema agent for durable persistence) so downstream cron / dashboard-panel loaders can pull rows from any source — `equity_events` (migration 0023), an ASIC Form 484 CSV, or a founder-uploaded dataroom register-of-members — without pre-normalising. Semantics filter out non-s708(1) exemptions (`s708(8)`, `s708(10)`, `s708(11)`, CSF `s738G`, `prospectus` / `pds` / `ois` / `disclosure`) so wholesale rounds can't accidentally shrink s708(1) headroom, classify `event_type` into the counter's `primary` / `secondary_on_sale` union (with the s707(3) on-sale rule matched by `transfer` / `secondary` / `on_sale` all mapping to `secondary_on_sale`), and walk sensible fallbacks for `investor_id` (id → email → normalised-name) and `amount_aud` (explicit → `consideration_aud` → `shares × price_per_share_aud`) before skipping. 11 vitest cases in `web/src/lib/compliance/s708-register-adapter.test.ts` across five describe blocks (event_type classification, exemption filter, investor_id + amount fallbacks, date normalisation, end-to-end with the counter) pin the branch matrix — all green (`npx vitest run src/lib/compliance/s708-register-adapter.test.ts` — 1 file, 11 passed). Typecheck `npx tsc --noEmit -p tsconfig.json` green (exit 0). Follows the P1i / P1n / P10-s708counter shape — a pure lib shipped standalone so the sibling UI + share-register-schema agents have a stable contract to build against without a schema coupling.*

*Previous tick (P12b-tile — /dashboard/exit-readiness founder tile): new server RSC at `web/src/app/dashboard/exit-readiness/page.tsx` (auth-gated redirect, `dynamic="force-dynamic"`, `robots:{index:false,follow:false}`) resolves the caller's active project via `getActiveProject(user.id)`, passes `project.industry` into `buildExitBenchmarkSection({ sector })` server-side (pure — no client fetch), and mounts `<ExitReadinessTile>` inside `<WorkspaceLayout>` with Chapter 12 header copy. New RSC `web/src/components/dashboard/exit-readiness-tile.tsx` renders sector-filter chip (with "widened to all sectors" hint when `usedFallback`), 4-tile summary grid (deals in sample / median valuation / median revenue× / latest deal year), buyer-type chip row in stable strategic → IPO → PE → financial → secondary order (zero-count buckets dropped), topExits table with company + buyer + type + AUD valuation + revenue× + year + source link (`rel="noopener noreferrer"`, `title=sourceNote`), and the fixed `AU_EXIT_DISCLAIMER` s766B footer. Pure helpers `exit-readiness-tile.helpers.ts` — `BUYER_TYPE_LABEL` / `BUYER_TYPE_ORDER` / `formatAudCompact` (billions 1dp, millions whole A$M, sub-A$M A$k or raw integers; null/NaN/infinity → em-dash) / `formatMultiple` (large whole numbers, small 1dp; null/non-finite/non-positive → em-dash) / `orderedBuyerTypeEntries` (drops zeros, returns stable order). Vitest coverage `exit-readiness-tile.helpers.test.ts` — 12 cases across four describe blocks pinning em-dash behaviour, unit formatting, buyer-type ordering completeness, and zero-count-drop invariants. All 12 tests green (33 tests green across the impacted `investor-pack` + `dashboard` + `workspace` suites). Nav wiring: `web/src/components/workspace/nav-groups.ts` "Grow & Scale" group grows one entry `{ href: "/dashboard/exit-readiness", label: "Exit Benchmark", icon: BarChart3, minPlan: "starter" }`. Typecheck `npx tsc --noEmit -p tsconfig.json` green (exit 0). Closes the last outstanding leg of the P12b / P12b-cfo chain — the AU exit fixture (P12a) now flows through the investor-pack section (P12b) + CFO auExitCheck (P12b-cfo) + a founder-facing tile at `/dashboard/exit-readiness` (P12b-tile), with `data-testid="exit-readiness-tile"` + `data-used-fallback` attributes on the section root ready for a future Playwright hook-in.*
