# Chapter 1 — Vision & Day-0 Idea

> Runtime source: `web/src/lib/guide/startup-journey.ts` (slug `01-vision`).
> This markdown is a mirror for offline reading and contributor PRs. The
> pages at `/guide/01-vision` and `/workspace/guide/01-vision` read the TS
> module, not this file — edit both to keep them in sync.

**Phase 1 · Vision / Day-0 Idea**

Turn a one-line idea into a workspace. Day 0 is about naming the problem,
the audience, and the change you want to make — before you write a line of
code or a dollar of financials.

## What the founder does

Create a new workspace (via `/svi` or a reseller invite link like
`?via=INFOVISION20`). Type the one-line idea, target customer, and the change
you want to see. That single sentence becomes the seed for every downstream
artefact.

## Agents invoked

- **CMO agent** — drafts a market-context memo from the one-liner (industry,
  buyer archetype, competitive space).
- **CEO agent** — writes an initial vision statement and 90-day priority list.
- **Auto-DataRoom** — provisions the Vision / folder with the idea summary
  and CMO memo.

## Expected outputs & how to interpret

- `idea-summary.md` — the one-liner, target customer, and change hypothesis.
- `market-context-memo.md` — CMO's read of the industry and closest three
  competitors.
- `svi_analyses` row (initial) — three-question SVI scaffold with placeholder
  scores you refine in Chapter 2.
- **How to read the CMO memo:** skim the "closest competitors" block first —
  if the list surprises you, your framing may be off. Adjust the one-liner
  before you spend credits on deeper research.

## Common pitfalls

- Writing the one-liner in feature language ("we build an app that…") instead
  of outcome language ("we help X do Y in half the time"). Outcome framing
  gives the CMO agent something to search.
- Skipping the target-customer field. Every downstream agent uses it as a
  filter; leaving it blank produces generic reports.
- Trying to price or valuate before Chapter 2. SVI cannot score what has not
  been validated — anchor on the idea first.

## On BlockID.au's showcase workspace

BlockID.au's own Chapter 1 doc is public at `/showcase/blockid`. The seed
one-liner reads _"BlockID helps Australian founders answer where their
startup is, what it's worth, and what to do next"_ — you can see the CMO
memo that generated from it and how the vision statement evolved when the
CEO agent iterated at Chapter 6.

## Next step

Ready to start? Open `/svi` and type your one-liner — the CMO agent begins
the moment you hit Analyze.
