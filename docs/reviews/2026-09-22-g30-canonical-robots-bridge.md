# Canonical robots bridge: prepared, not applied

Root observed both public and direct4107 returning the188-byte tracked public/robots.txt instead of the new app/robots.ts rules. The compiled canonical metadata body exists at `/data/releases/OYwvAM82CjG5RiQunpcNc/.next/server/app/robots.txt.body`:642 bytes, SHA256 `60eac6a58ff88948161579d2d0244f85a23bf36a1821af5809e0345a8f1c9466`, compiled source `23cc80693a9476e63d011d7ca0e6e88da76471e2`. It matches the app's exact public-demo allow list, localized private-route disallows and canonical sitemap. Installed Next metadata robots documentation was read before removing the conflicting source file.

Source change deletes only legacy web/public/robots.txt; future complete builds should serve the existing app metadata route. It does not modify the sealed current release or require an immediate replacement origin.

`scripts/ops/blockid-robots-bridge.py` defaults to an offline review plan. The prepared plan is `/tmp/g30-canonical-robots-review-23cc`, including the body, complete before/after nginx text and hashes. The proposed location is exact `/robots.txt` inside only the blockid.au/www.blockid.au server, aliasing a NEW owned read-only copy at `/data/blockid-discovery/<compiled-sha>/robots.txt`. The proposal preserves origin proxies, redirects, SVI aliases and staging. It adds text/plain, UTF-8, no-cache, no directory listing, disabled symlinks and GET/HEAD only.

After root review, apply requires a new direct/tmp output directory, `--apply` and `--expected-nginx-sha` equal to the reviewed original config digest. It acquires the shared deploy lock, verifies the compiled body/manifest and active4107 release/PID/listener/health, then creates new owned0444 body/0555 version directory without touching the sealed artifact. It tests the complete real nginx include tree, atomically installs the candidate site, runs normal nginx-t and gracefully reloads. Exact body-hash checks cover direct nginx and both public hostnames, with bounded fresh probes and existing canonical redirects. Active health is checked again. Failed verification restores the immediately preceding config only if no external edit occurred; unrelated edits fail closed for reconciliation. No app is restarted or stopped. A failed asset copy/install leaves its new directory unserved for inspection; it is never silently reused or removed.

The temporary alias MUST remain across current origin rollback so robots policy does not regress. On the next full release containing the source deletion, first prove direct-origin /robots.txt serves the correct current canonical metadata, then remove ONLY this temporary location under the same lock/test/atomic-reload/public verification procedure. Do not remove the alias merely because a new source commit exists. Retain its immutable file for rollback evidence until the operational retention policy permits cleanup. Robots directives are discovery guidance, not an access-control mechanism; no private page is fetched by these probes.

Three bounded read-only/mock checks passed: pinned compiled body and invalid policy rejection; actual config transform preserving other servers/upstream; failed-delivery restore and external-edit refusal. The review preparation acquired/released the shared lock and wrote only a new private/tmp plan. No /data copy, nginx installation, reload or public change has yet occurred at this commit. Source diff check passed.
