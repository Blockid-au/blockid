# G33 S0 + S1 live receipt — 24 September 2026

**Release:** SHA `464e16b14a0dfa60974d649e75f3849f0750d950` (commits `c254eef90` S0 and `464e16b14` S1). Release `3RV0pIPff_wqc6X4-Mk0c`, active origin 4132, warm 4131.

**Gates:** 8/10. Extended hydrated suite and lint were deferred and are not counted as passes. Live SHA matched, previous chunks still served (3/3), startup errors 0. Marked verified-good under the deploy lock with the founder-authorised accelerated soak (`--review-deferred`), after the canary below.

**Resource permit:** the sixth origin needed a release-owner resource permit. The founder approved issuing one in this session. The permit lasted 1 hour and was pinned to SHA `464e16b14` and the retained-origin digest. Admission checks passed: 22.9 GB available, load5 3.8, memory PSI 0.

## What shipped

| Item | Change | Evidence |
|---|---|---|
| T01 | `tbr_quality.status = down` when at least 2 runs produced no report and they are at least half the window, or when the two latest runs both produced none. Adds `noReportRuns`. The error digest alerts on `down` after 1 h (6 h debounce) | Before the canary, live `/api/status` read `down` with `noReportRuns 4/5` (previously `watch`) |
| T02 | Live-QA `43-free-reports` fails on a failed run, or on a run with more than 1 degraded chapter, unless `LIVE_QA_ALLOW_DEGRADED=1` | Canary passed under the stricter assertion |
| T03 | `cron-health` normalises legacy rows instead of crashing; `ga4-daily-pull` writes the standard shape | Unit tests; nightly run pending |
| T04 | The 8 writer tests left red by the revision rollout were updated to the approved revision behaviour | vitest 42 371 / 42 371 |
| T05 | DeepInfra text calls stream (SSE) with first-token, idle and total timeouts. Model order follows measured speed (EWMA; 24/09 priors V3.2 13 tok/s, Qwen 19, V4-Flash 32) | Transport tests against a local SSE server; live canary |
| T06 | Report budget reservations are bounded by request bytes instead of the context window. A settled attempt may be re-dispatched under a new identity; an in-flight attempt never | New `report-attempt-budget.test.ts` (24/09 repro); no "reservation unavailable" on origin 4132 |

## Canary (T07, real runs, approved spend)

| Run | Degraded chapters | Words | Cost | Grounded share | Duration |
|---|---|---|---|---|---|
| 02:13, before the fix | 8/8 | 0 | US$0.0007 | 0 | 122 s |
| 02:13, before the fix | 8/8 | 0 | US$0.0008 | 0 | 123 s |
| **04:00, after the fix** | **0** | **6 579** | US$0.0255 | 0.62 | 420 s |
| **04:00, after the fix** | **0** | **4 064** | US$0.0342 | 0.60 | 420 s |

## Still open

- **S1 acceptance** requires 5 consecutive real runs; 2 of 5 are done.
- **T16 (new, P0 follow-up):** both runs used the full 420 s background deadline. On origin 4132 the CEO summary hit `Worker timeout (total 22–34 s)`, the run's remaining wall clock. SYNTH needs a reserved window, as W4 already has, so the executive summary is not a placeholder.
- **Grounding** of 0.60–0.62 is below the 0.85 KPI. That is covered by E03/Q02, not S1.
- **`report_revisions` is still empty.** The free path does not write revisions yet; that is S3 writer coverage.
- The S2 integrity items (audit chain, `svi-snapshot`, migration source) are unchanged.

DeepInfra-only routing and the US$0.50/report cap are unchanged.
