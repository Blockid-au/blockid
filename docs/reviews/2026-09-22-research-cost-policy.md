# Free-first research provider admission — 2026-09-22

The latest founder preference supersedes the earlier DeepInfra-primary model ordering: use suitable free models first, then paid only under configured provider spending policy. Quality/capability requirements remain mandatory. This module selects only fresh, verified entitlement/quota and healthy model candidates with task-specific verified capability and quality fit. Free tier must have both request and maximum-token allowance. It never lowers the task quality threshold to use an unsuitable free model. Candidate IDs are explicit, not an invented list of currently free providers.

Paid models default to zero budget. A trusted owner spending grant and bound server quote must cover the task, model, maximum tokens, expiry and USD cost. No new per-call user prompt is required when that durable owner policy already exists. The supplied grant budget must mean authoritative available funds net of commitments/reservations. No actual model registry or runtime switch is connected here.

Brave query defaults remain 3 per question, 6 per business research batch, 30 per UTC day and 900 per UTC month, across free and paid usage. The authoritative snapshot must bind the stable question/batch IDs and current UTC periods; authenticated clients cannot mint fresh IDs to reset counters. Counters include requests already reserved by concurrent workers and other known consumers, not just successful local results.

Brave free allowance is consumed first when authoritatively known. The latest owner instruction permits automatic paid fallback under a configured maximum **$5 USD paid exposure per month**, separate from any advertised free credit. Missing or unknown free allowance is represented by `null`: reserve full list price against that $5 cap. Never assume unused free credit exists; other clients may consume it. Confirmed owner policy, current per-query price quote and authoritative committed/reserved spend are required for paid remainder. At exhaustion, admission stops. A zero cap disables paid fallback. This module sets no live configuration and has not verified the provider account's pricing/free credit.

Provider-owner permission is separate from customer report-credit consent: both model and Brave results explicitly return `customerCreditConsent: false`. This policy cannot authorize charging a customer's wallet. The independent report quote/consent and wallet reservation flow must still apply.

**Admission only, not execution or reservation.** Every success returns `executionAllowed: false` with `atomic_quota_and_budget_reservation` required next. The later executor must atomically reread and reserve quota/spend, avoid concurrent overrun, apply provider request/output token limits, and settle actual costs including failed calls. Freshness is capped at five minutes for preparation, never a license to charge against stale counters. Stable server scope and durable owner policy IDs are required from trusted readers; these objects must never come directly from client JSON.

No credentials were accessed; no network/provider API call, Brave output cache, database change, production configuration, model switch or deployment occurred. Focused synthetic tests cover quality/verification/quota denial, paid grant binding, every Brave cap, free-first paid remainder, reservations, unknown free allowance and monthly exhaustion. Targeted strict TypeScript passes.
