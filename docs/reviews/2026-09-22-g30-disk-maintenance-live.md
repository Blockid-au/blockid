# G30 disk and repository maintenance — live22 September2026

User authorized cleanup and an80% automatic threshold. No business data, release artifact, running application or uncommitted work was removed.22 clean integrated worktrees were removed with normal git worktree remove, never --force; branch history retained. Five finished G30 trees were checked for ancestor/equivalent committed file content;17 historical merged trees had only dependency symlinks or regenerable TypeScript build info among ignored files. Owner process cwd/exe/root/fd/mapping inspections found no references before removal. Dirty, unfinished or ignored-data-bearing trees remain. Primary runtime telemetry changes remain intact. Evidence path lists: /tmp/g30-clean-worktrees-removed.json and /tmp/g30-old-worktrees-removed.json. Release retention dry-run:9 protected,0 removable.

Disk observed before /34%,/data21%,/tmp66%; after /32%,/data21%,/tmp60%. Values are rounded df readings; no exact reclaimed-byte claim. Available RAM approximately22GB,8 CPUs and1-minute load approximately1.5 at acceptance. Both public homepages returned200. This is a current operational sample, not a performance guarantee.

## Automatic policy

Source scripts/cron/g30-disk-pressure-guard.py is installed root-owned0444 at /usr/local/lib/blockid-maintenance/disk-guard.py. Source units live in scripts/ops/systemd and installed at /etc/systemd/system. Timer enabled and active, every5minutes; oneshot actual Result=success,ExecMainStatus=0. Root service sees full /proc, private tmp is disabled to share the real deployment lock. CPUQuota20%,MemoryMax256M,IOSchedulingClassidle, low CPU priority and45second service timeout bound resource use.

At>=80% on /,/data,/tmp, guard considers only explicitly allowlisted old /tmp build/deploy/weekly cleanup logs on pressured filesystems. No wildcard directory, release, package, docker, database, worktree or source deletion. Age uses both mtime and ctime>=14days; exact owner UID1001, regular single-link file and no open fd/mmap references required. Deployment lock must already exist and be acquired exclusively without blocking. Budget20seconds,50logs and256MiB per invocation. Missing process visibility/lock or identity changes fail closed. Threshold does not guarantee enough disposable data exists; remaining pressure is recorded with action_needed and nonzero service result. There are no external notifications or process kills.

Only one atomic latest snapshot is stored at /var/lib/blockid-maintenance/latest.json, root-only. Actual controlled maintenance found0 eligible old logs; no logs were deleted in this phase. The disk reductions came from reviewed worktree removal. Weekly existing cron now invokes a compatibility wrapper using this guard. Two overlapping broad legacy schedules were removed from user crontab with private backup in ~/.local/state/blockid-maintenance/crontab-before-disk-guard-20260922.txt. All other cron entries preserved. Legacy wrappers continue recognizing --disk-only/--procs for compatibility but perform no process sweep. The uptime guardian preserves health/rollback behavior and no longer truncates shared JSONL or deletes broad /tmp matches. scripts/cleanup-merged-worktrees.sh is now read-only inventory: merged status alone does not prove idle ownership.

## Validation and operations

12 focused tests passed, including threshold boundary, symlink/hardlink/owner/age protection, open-file retention, size budget, denied process visibility, missing lock and busy lock. Shell syntax checked for each changed script; systemd-analyze verify passed. A real privileged dry-run exposed Linux protected_regular rejecting O_CREAT on the user-owned deployment lock; commit67eb52778 removes creation and actual rerun succeeded. Actual root maintenance, scheduled service invocation and wrapper dry-run succeeded. No application rebuild or live-origin restart was needed; BlockID compiled23cc80693 and SVI compiled2b541c6 remain unchanged.

Inspect: sudo systemctl status blockid-disk-guard.timer; sudo cat /var/lib/blockid-maintenance/latest.json. Update installation using reviewed source and sudo install -m0444; installed copy is intentionally independent from app rollback. Do not restore the old destructive scripts/crontab as a rollback. If a guard issue occurs, disable only its timer pending repair; preserve normal uptime/recovery timers. Backups, active/warm releases, incomplete billing work and live runtime records remain protected. Persistent pressure requires owner-scoped retention/capacity work, not expanding deletion to unknown data.
