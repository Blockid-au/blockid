# G30 — Separate visual observations from numeric inputs

Founder continuation23/09/2026. DeepInfra-only, US$0.50/report shared text/vision budget unchanged. Additional test suites, lint, browser acceptance and paid inference probes deferred by founder instruction; production compilation and minimal operational admission remain required.

## BlockID source

`dd966d47d` separates native file/user-entered text from OCR/vision narrative in new file intakes. Deterministic signals and maturity no longer read model observations or business claims embedded in an upload filename. An explicit empty scoring source is preserved through bounded storage, job hydration, deterministic valuation and report-v2 submitted-financial quotation extraction. This prevents the valuation fallback from reparsing visual revenue/SAFE caps out of the narrative. Native/user text remains self-reported, not independent financial verification.

Full visual observations remain available for narrative explanation with their limitations. Blank native pages retain their provenance positions but no longer generate slide-classification calls. This does not certify every LLM narrative claim or remove an indicative SVI-based valuation when verified financials are absent. Historical rows without the new field retain legacy behavior; no historical reports were rewritten. Colocated contract expectations updated, not executed.

Origin4118 was explicitly stopped and quarantined using the retirement helper under the shared deploy lock. It was outside active4121/warm4120 with zero tracked activities and unresolved registered jobs. The scoped unknown-work acknowledgement is not proof of complete quiescence; artifacts are preserved. No cap increase or permit bypass.

## SVI source

`2399e6682c2a666fff9012ab66a6ee555e57e830` includes persisted visual extraction coverage in HTML and plain-text report emails, in EN/VI. Limitations precede conclusions. File/page/image labels and statuses are escaped in HTML; no new images or source text are retained. Existing reports without coverage metadata remain compatible. No email is sent by the deployment operator.

## Live receipt

Completed23/09/2026 at11:43UTC.

- BlockID compiled `dd966d47d7c42bbb360de8598ec5ff1b30c151e4`, BUILD_ID `rcd6hLuGC96oDAJbj8sJ-`, active4122/warm4121. Production build/type compilation succeeded; public SHA matched, local/public/auth/static HTTP checks passed. GET `/api/intake` returned405 as expected for POST-only. Source tracing included all13 sharp ESM files with byte-for-byte matches, no manual packaging repair. Marked operationally good after the founder-authorized60-second minimum soak with extended review explicitly deferred. Current warm4121 already contains the prior sharp repair.
- SVI compiled `2399e6682c2a666fff9012ab66a6ee555e57e830`, BUILD_ID `bWFZVgiwvZfm-zaTlgpia`, active4209/warm4208. Isolated production build succeeded in1m52s, peak2GiB; public build identity verified by promotion helper, service boot enabled. Zero prior report changes, no operator-started provider calls or emails. Protected store/auth/research runtime remained unchanged.

Operational logs: `/tmp/g30-scoring-source-deploy.log`, `/tmp/g30-scoring-source-mark-good.log`, `/tmp/g30-svi-email-{build,launch,static,promote}.log`. These minimal deployment checks do not certify semantic report accuracy or replace deferred suites/browser acceptance.

## Remaining qualification

End-to-end visual accuracy, chart/table factual validation, per-claim citation verification and model cost-per-accepted-report holdouts remain open. Office embedded images are not complete rendered slides. Current document visual processing remains bounded to three units/55 seconds. Broader G30 jobs/research/billing gates are not closed by this slice.
