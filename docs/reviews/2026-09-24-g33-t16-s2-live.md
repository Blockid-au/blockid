# G33 T16 + S2 live receipt — 24 September 2026

**Live:** `30da5e778` (origin 4138, T16f), preceded by `964efd3ad` (origin 4137). Deploys in this round, in order:

| SHA | Content |
|---|---|
| `98f87d687` | T16, T09, T08 code |
| `7223e4545` | T16b |
| `6e37c4ea9` | T10, T11, T16c |
| `f58be9a1e` | T16d |
| `964efd3ad` | T16e |

Each deploy used the quick profile (extended review, unit-in-gate and candidate suites deferred and not counted as passes). Before every commit the full suite ran locally with typecheck 0 errors and vitest 42 3xx/42 3xx green. Each deploy got a resource permit pinned to its SHA and one retired origin: 4127, 4128, 4129, 4130 and 4131. The founder approved these retirements (4127 explicitly, the rest under a standing rule for this G33 session). The rule applies only to the oldest origin that is neither active nor previous, with 0 tracked activities and 0 unresolved jobs, SIGTERM only, and its release kept. `--acknowledge-untracked-work` was recorded each time.

## Report pipeline (S1 follow-ups)

| Item | Change | Why (canary evidence) |
|---|---|---|
| T16 | W4 stops at `deadline − SYNTH reserve` (90 s, ≤ ¼ of the deadline). Chapter calls may not start inside it, and the summary races the hard deadline | The summary had only 22–34 s left before the 420 s deadline |
| T16b | Model "fits" only when estimate ×1.5 ≤ remaining time; no attempt starts with < 15 s left; background W4 150 s (W1–W3 180 s / W4 150 s / SYNTH 90 s); unset env vars fall back to defaults | V3.2 was picked on an optimistic average; 6 calls started with 5 s left; 2 chapters overran a 120 s W4. The new test caught `Number("")` = 0, which would have set the W4 reserve to 0 |
| T16c | A stream that times out after producing tokens is also a speed sample | The speed average learned only from successes |
| T16d | When no model fits, the fastest known model goes first | "Nothing fits" kept the quality order, so the summary went to the slowest rung |
| T16e | A DeepInfra `engine_overloaded` / "Model busy" 429 strikes the model, not the provider. Account-level 429s still strike the provider | 18 Qwen 429s struck the whole provider out of the run, and one report had 0 words |

The 420 s background deadline is unchanged because the paid order drain is capped at 480 s.

## Integrity (S2)

- **T08, audit chain.** Root cause: the trigger took `id` before the chain lock and used the highest id as the tip, which left five forks between 18 and 21/09. Every row's own hash still recomputes.
  - SQL `0460` was applied live in one transaction. The previous trigger is kept as `pending-authority/0460_rollback_trigger.sql`. The SQL assigns ids under the lock and adds `audit_events_verify_graph`: every hash recomputes, every parent exists, there is one genesis, and forks are allowed only at the five pinned parents.
  - Scratch-DB tests on a copy: allowlist ok; no allowlist → `fork` at 6702; tampered row → `curr_hash_mismatch`; deleted row → `dangling_prev_hash`; concurrent inserts → linear, ids in chain order.
  - **Live: 13 971 rows verify.** The SQL is kept in `web/supabase/pending-authority/`, not `migrations/`, so release `schemaDigest` and warm rollback are unchanged.
- **T09, `svi-snapshot` cron.** It selected `svi_accounts.user_id`, a column that never existed. The owner now comes from `projects.user_id`, and the test fake fails like the real schema.
- **T10, `report_revisions`.** The source was restored as `pending-authority/0461` after a column-, constraint-, index- and policy-level match with the live table.
- **T11, mail to erased accounts.** `sendEmail` refuses `@erased.blockid.au` recipients before any transport.

## Canary runs (real, approved spend)

| Time (UTC) | Release | Degraded | Words | Grounded | Duration | Note |
|---|---|---|---|---|---|---|
| 04:00 ×2 | 464e16b14 | 0 / 0 | 6 579 / 4 064 | 0.62 / 0.60 | 420 s | summary starved |
| 06:15 ×2 | 98f87d687 | 2 / 0 | 4 502 / 3 955 | 0.53 / 0.60 | 420 s | summary on V3.2 timed out |
| 06:45 ×2 | 7223e4545 | 0 / 0 | 4 108 / 4 276 | **0.82** / 0.73 | 353 / 420 s | one summary on V3.2 timed out |
| 07:18 ×2 | 6e37c4ea9 | 0 / 1 | 4 505 / 3 808 | 0.47 / 0.67 | 420 s | both summaries on V3.2 timed out |
| 07:48–08:37 | f58be9a1e | 5 / 7 / 1 | 3 828 / **0** / 3 919 | — | — | provider struck by Qwen 429s; the session was interrupted and its QA account was erased by hand afterwards |
| **09:10 / 09:11** | **964efd3ad** | **0 / 0** | 4 296 / 402 | 0.67 / 0.89 | **292 s** / 337 s | no provider strike; run 2's summary refused "reservation unavailable" with a healthy US$0.03 ledger → T16f |
| **09:43 ×2** | **30da5e778** (T16f) | **0 / 0** | 4 022 / 3 906 | 0.60 / 0.67 | 416 s | no summary failure, no provider strike, no refusal |

Cost per run is US$0.025–0.034.

**T16f** (`30da5e778`, origin 4138, verified-good): the refusal now names the coordinator's reason, and the ledger lock waits 3.5 s instead of 1.5 s for about 10 parallel calls on one scope. **4 consecutive runs with 0 degraded chapters** (09:10 → 09:43). **S1 acceptance** (5 consecutive runs) is not claimed yet. Most runs pass, but provider capacity varies run to run. Grounded share (0.47–0.82) is still below the 0.85 KPI, which is E03/Q02 work.

Still open: T12–T15, S3 writer coverage (Lane A), and moving `pending-authority` SQL into migrations together with the schema-authority transition.
