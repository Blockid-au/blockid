# Financial controller composition on the authority0447 baseline

Source preparation only; no production record, SQL, runtime flag or traffic mutation. Restores the reviewed ddb883c60 exact0443–0445 expansion core/controller/catalog attestation on current source, with a narrow serving-state adaptation.

When a financial transition record exists (including a symlink, malformed or partial record), its decision takes precedence over both same-manifest equality and the independent0447 edge. A failed or partial financial transition cannot silently fall through to ordinary rollback. When no financial record exists, the current0447 path remains unchanged. The authority reader now rejects missing/unsealed records before trying any database probe.

The existing process verifier still requires exact process/socket/artifact identity and refuses drained origins. Its only explicit staging exception is pending:3, needed by the exact receipt candidate before all three approved migrations are applied; arbitrary pending schema values are rejected. Default registration still requires schema status ok. No deploy-live migration gate was relaxed.

Controller operations require both existing deployment and Stripe reconciliation locks. The restored reference contract requires actual paused capabilities on every live retained process, actual immutable runtime manifests, exact ledger and schema fingerprints, unchanged SQL/fixtures, and three post-expansion endpoint checks before sealing. It never executes SQL. Schema SQL and fixture dependencies are deliberately not brought into canonical source in this phase.

Validation: seven core expansion tests, seven controller/composition tests, fifteen authority transition tests and fourteen current serving-state tests passed, with no production database or network probes. This is not a production migration rehearsal.

Remaining release mechanics are explicit: compatible current/warm pair; retirement of incompatible writers; independent pending:3 candidate build/launch/smoke and exact resource admission; approved SQL application/observation/sealing; controlled prepared-candidate promotion; successor enrollment integrated with deployment. The normal deploy script still refuses financial SQL changes. Unpaused erased-account capability must be supported by the actual financial schema/runtime contract before it can pass sealed financial admission; the staging runtime must not advertise an atomic legacy grant guarantee merely from a preflight lookup. No financial activation is authorized by this source commit.
