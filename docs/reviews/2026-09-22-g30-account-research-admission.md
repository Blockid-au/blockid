# G30 — account/session admission and market discovery preparation

Founder selected Brave Search on 22 September 2026, supplied a key and explicitly authorized automatic paid search after free allowance without per-search confirmation. Initial local paid cap is $5/month; unknown free balance reserves full list price against a conservative $5 gross cap. DeepInfra remains the main planned analysis provider, with suitable verified free models preferred where quality and quota suffice. Four bounded Brave credential-validation requests occurred separately from deployment; no customer credit transaction or model call occurred.

## Session exchange

Release A adds strict legacy handoff validation, durable nonce consumption, dual cookie readers and transitional legacy mint. Before the fixed sunset it mints compatible legacy cookies; after sunset it automatically mints v2, so warm rollback still supports new sign-in. Release B always mints purpose/audience/domain-separated v2 cookies. Existing BlockID producer already sends a signed uid/exp/n handoff; session cookies cannot satisfy this exact shape. Nonces are consumed atomically and durably in a shared private directory before minting, including across processes and restarts. Key rotation is not performed, preserving creator-record signatures.

Legacy cookie reading ends2026-09-30T00:00:00Z and never extends the original expiry. V2 cookies use an exact seven-day issued-at/expiry relationship. Exchange redirects stay on the SVI origin, with private/no-store and no-referrer. Account-wide revocation checks are still separate work; this is not a complete account lifecycle implementation.

Both releases must be built before cutover. Promote compatibility A then v2 B; designated warm rollback must be A or newer. Older origins remain retained but are not eligible for authenticated rollback after v2 activation. Validate B→A→B and preserve the public legacy-analysis guard. No user/customer token is used in deployment probes; actual cookie exchange/concurrency is tested with isolated synthetic keys and stores.

Six focused exchange test groups pass for each version, covering cross-process races, replay after reload, cookie substitution, fixed legacy sunset, v2 compatibility and sign-in after sunset. Root adds bounded nonce maintenance: at most2,000 scanned/500 removed/5seconds per run, only strict private regular receipts whose expiry AND modification time are older than24hours; unknown/live/unsafe files stay. Two isolated cleanup tests pass. No broad disk deletion or live report cleanup.

## Wallet scope

Sourcece96e500a adds an injected authoritative wallet admission adapter. It requires active user, explicit report/account association, a site-specific wallet grant and a bound quote, rejecting missing/revoked/expired/mismatched/stale snapshots. Reads must form a consistent authoritative snapshot no older than30seconds. The adapter reuses the existing request contract and still returns execution disabled.17 focused tests and targeted strict typecheck passed. This unused source module is not a deployed spending service: actual DB-backed grants, lifecycle, atomic reservation/revalidation and quote issuance remain open.

## Brave onboarding and retention

Official setup: create account at https://api-dashboard.search.brave.com/register, activate Search, then generate a key under API Keys. Search lists$5 per1,000 requests and$5 recurring monthly credit. The official FAQ permits initial prepay$0 with a card verification hold; the earlier zero-paid suggestion is superseded by founder authorization for automatic paid fallback within the local $5/month cap. Key belongs only in private server configuration, never chat/client/Git. Sources: https://brave.com/search/api/ and https://api-dashboard.search.brave.com/documentation/resources/help-feedback and https://api-dashboard.search.brave.com/documentation/guides/authentication.

The FAQ prohibits retention of API-returned data without a separate arrangement. Discovery output must remain ephemeral; a returned URL/title/rank list is not automatically permitted persistent report evidence. Resolve provider retention rights and the independently acquired evidence policy before production research activation. No support email is sent by this work.

The bounded discovery adapter is source preparation only: explicit public query and spend admission, fixed Brave endpoint, limited queries/results/bytes/time, cancellation and no scraping fallback. Search snippets are not evidence. The key is now private server configuration, outside Git, with mode 0600. HTTP 200 confirms acceptance; four validation requests produced zero web results, reserving at most $0.02 nominal cost. Empty results do not establish competitor absence. Atomic shared production budget wiring, authoritative allowance reconciliation and licensed evidence retention remain prerequisites. This does not yet give existing reports new market research or close G30.

## Source preparation and live evidence — 22 September 2026, 16:07 UTC

BlockID source commits ce96e500a (wallet admission), 75d44b93d (free-first cost policy), bec5b97a7/83e252784/c81dbf0f5 (bounded ephemeral discovery and valid empty Brave response handling) are merged but unused by live customer research. Focused suites: 17 wallet, 10 cost-policy, 24 discovery checks; targeted strict typechecks passed. Model free entitlement/quality/health/quota must be current; paid model selection requires trusted policy and budget. Brave limits: 3 searches/question, 6/batch, 30/day, 900/month, with atomic reservation required before production execution. Local private policy and validation ledger do not substitute for integrated production enforcement.

SVI corrected B2 d99703da19a8f44762d684384294eb24aa3bb951 / BUILD i8DQsX1ka-9U1xDZVwt6d serves 4215. Warm A2 58c7d5c8ceccda72e380fc47b86bf2ca0a6f7dd6 serves 4214. Both full builds passed. Initial unserved candidates 4212/4213 failed a real-origin no-referrer header probe; exact Next header ordering was corrected before public activation. The paired rollout executed A2 → B2 → A2 rollback → B2 forward under the shared deployment lock. Twelve static samples, 81 unchanged raw-report hashes, invalid-token rejection, private/no-store/no-referrer, the legacy analysis guard, public /live and /vi/live 200, and unauthenticated intent 401 passed. Non-SVI nginx and BlockID robots hashes are unchanged. Boot units and monitor timer are active. No valid customer token exchange was performed in production.

Promotion now requires svi-session-v2-reader-durable-nonce-v1; older retained origins cannot be selected as equivalent authenticated rollback targets. Cleanup timer svi-handoff-nonce-cleanup.timer is enabled and active; bounded cleanup service completed successfully. Cleanup implementation is sealed under /data/startupvalueindex-ops-auth/70e379a2463062dca0d189cc62258a1758419353; no customer reports are removed. Consumer tests now use v2 sessions (test-only source commit 731ab12; 11 checks passed in the isolated agent workspace).

G30 remains IN PROGRESS. Next: real account lifecycle/revocation and explicit wallet/report grants; atomic production budget and durable jobs; source discovery with evidence rights and semantic acceptance; quote/consent/reserve/publish/capture; qualified score/valuation updates. No customer research activation, automatic rescore or whole-plan completion is claimed. BlockID production remains v3.33.0 at 4108.
