# SVI scoped AI and report UI — live release

Live application source: `8b18140105264767e87a993f0405a36fcda70258`, BUILD_ID `uHo0DAP9dF6F7iaIhSNl2`. Candidate now serves port 4202, PID 1526563, from `/data/startupvalueindex-releases/8b18140105264767e87a993f0405a36fcda70258/.next/standalone`. Unit `svi-release-8b18140105264767e87a993f0405a36fcda70258.service`: active, zero restarts, 4 GiB memory / two CPU cores. Operations-only probe fix is source `84a76cc`; it does not change compiled application identity.

The report AI client now selects the authenticated BlockID scoped gateway using the release controller's active origin. Actual runtime `SVI_SCOPED_REPORT_POLICY=1`, shared internal token matches BlockID, shared analysis cache remains unchanged and `SVI_RECOVER_RUNS=false`. No local paid spillover is used on this path. No free model is approved yet. Updated EN/VI report controls remove unsupported model/time claims, retain previous answers on bulk failures, and provide progressive disclosure and return links. This does not activate new market-research jobs, wallet holds or paid re-analysis capture.

## Build and rollout evidence

- Fresh detached worktree and private installed dependency copy; no dependency installation or build in the legacy live `.next` directory.
- Aggregate-capped build completed successfully, including TypeScript and static generation. Observed peak memory 2 GiB, runtime approximately 1 minute 53 seconds; build unit and descendants exited before candidate launch.
- Candidate/legacy dependencies and private runtime configuration fingerprinted separately from code/public artifacts. Compiled module 7666 is byte-identical in both candidate and legacy run API/report page, SHA256 `b5703ad7b17241ef8c3e7abb25a140bbfe86706e7b1fdefa92074381ed948f1d`. Reader capability is bound to this compiled evidence, not an invented legacy source attestation. Both Server Action maps are empty. Unversioned public assets are identical.
- Independent candidate homepage, listings, about and health passed. Both public hostnames returned the expected new BUILD_ID after the scoped nginx switch. Current BlockID traffic remained at 4106; staging and the old SVI process were preserved.
- Immutable union contains 180 static files, digest `9f93ec94e92bc8dd74cb7087499bf5148a930d48e95da48e72197562a3b462cd`. SVI-only nginx alias preserves this union through forward/rollback switches. Six old/new public asset samples matched bytes; JavaScript MIME type and immutable cache header verified.
- A tab opened on the old build navigated via the existing About link to a fresh document after the switch: old marker cleared, page loaded, white background/dark text. This is a bounded cross-build navigation check, not whole-site visual acceptance.

## Actual warm rollback drill

An initial rollback probe returned an old/new identity too early after graceful nginx reload. Recovery restored the exact previous configuration; stable metadata was reconciled only after process/artifact/config and both public hosts were reverified. No process was stopped. Controller probe now uses unique query strings and at most three fresh attempts per host; expected identity is still mandatory. Nine focused controller tests passed, including previous-worker-then-expected response and persistent mismatch refusal.

The repeated rollback succeeded at 11:46 UTC: both hosts returned legacy BUILD_ID `2Ja8S2dWg8KyzknxFlivJ`, all six old/new asset samples passed, and the browser loaded listings. Forward promotion then restored `uHo0DAP9dF6F7iaIhSNl2`; monitor passed. Legacy PID 2895933 at 4002 remains warm, with original jobs untouched. Public health, source parity and the drill do not certify all nullable valuation rendering, concurrent-file writers, business accuracy, paid research or host-loss recovery. Those remain separate plan gates.

Logs: `/tmp/svi-g30-build-resume.log`, `/tmp/svi-g30-launch-candidate.log`, `/tmp/svi-g30-static-and-admission.log`, `/tmp/svi-g30-warm-rollback-retry.log`, `/tmp/svi-g30-forward-after-drill.log`. Private state is under the application owner's `.local/state/startupvalueindex-runtime`; no secret values are included here. No paid benchmark, SQL migration or outbound notification was performed.
