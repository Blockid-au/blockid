# G30 cross-site ↻ Re-analyze source review — plan only

Inspected BlockID plan worktree at `7fcbb8d5d` and sibling repository `/home/dovanlong/startupvalueindex.com` HEAD `f172362d6f31bc40c6c27ba367a3e09e9f170953` on22 September2026. No feature edits, external requests, customer data reads, model calls, charges or deployments. These are source observations, not a live browser/runtime/auth acceptance result. Sibling source was read in place; no files there were modified.

## Exact button and handler inventory

| Surface | Source | Confirmed behavior |
|---|---|---|
| SVI existing narrative ↻ Re-analyze | `startupvalueindex.com/src/components/investor/AnalyzeButton.tsx:70` | Immediately POSTs `{slug, field, locale}` to `/api/agent/analyze`; no visible quote, credit confirmation, operation key or cancel. Disables current button while busy, shows elapsed time; updates local narrative/score/weight/rationale on success. Retry makes another POST. Placeholder UI hardcodes `claude-opus-5` and typically15–45s; this is display text, not measured actual routing/SLA. |
| SVI field API | `startupvalueindex.com/src/app/api/agent/analyze/route.ts:19` | Per-IP60/hour check, field allow-list, `readBySlug` completed-state lookup, `analyzeField`, then `applyFieldNarrative`. No authenticated principal, document authorization, wallet check, reservation, idempotency key or persistent job is present in this handler. `src/middleware.ts:5` excludes API paths. Infrastructure protections outside these inspected files are unverified. |
| SVI field analysis | `startupvalueindex.com/src/lib/enrichment/per-field-analyze.ts:86` | Context includes company, financial/valuation fields, first25 claims and actual8,000-character raw excerpt (comment says20k). One `generateJson` call returns narrative, free-string `key_evidence`, numeric score/weight and rationale. Fields cover16 canonical questions plus team/competition/market/valuation/risk enrichment. No search/fetch/source-read occurs in this function. Existing competitor context is not fresh market verification. |
| SVI persistence | `startupvalueindex.com/src/lib/decision/patch.ts:18`; `src/lib/decision/store.ts:81` | Shallow copied state gets selected narrative/evidence fields; returned score/weight/rationale are not stored in this patch path and composites are not recomputed here. `writeRun` writes run and slug JSON separately with `fs.writeFile`, not atomic rename/CAS despite patch comment claiming atomic replacement. Concurrent read-modify-write can lose updates; no observed production lost update is asserted. No new immutable revision/diff or updated-analysis date is assigned in this path. |
| BlockID full stream rerun | `web/src/components/svi/svi-stream-analysis.tsx:1820` | Done button invokes `reset(); startAnalysis()` immediately. Request at1505 includes project/dims/deckText/mode but no tier/quote/key. Exact effect on durable prior snapshot depends on runner/save path; UI reset is confirmed, deletion of durable prior report is not. |
| BlockID dimension retry / stop | Same file1732–1738 | Retry invokes `startAnalysis([key])`. Stop aborts browser fetch and marks not running; this does not prove server job cancellation or provider call termination. |
| BlockID stream API | `web/src/app/api/svi/dimensions/stream/route.ts:44,72,93,145` | Auth/project membership checks; viewer can generate but persist requires write role. Tier defaults free. Explicit new deck plus dims is rejected as full_analysis_required. Paid tiers canAfford before run, spend after successful non-cache delivery unless request aborted; spend failure only warns. There is no keyed reserve/capture/refund transaction here. Claiming duplicate charges already occurred would exceed evidence. |
| BlockID partial orchestration | `web/src/lib/report-pipeline/orchestrator.ts:487,586,618,707–738` | Dimension filter/seeded prior criteria and bypassed chapter cache; partial rerun disables first-pass research and some full-report critic/executive passes. It is not the requested fresh-market criterion/question research transaction. |
| Other BlockID labels | `web/src/components/svi/svi-dashboard.tsx:503`; `web/src/components/workspace/living-report.tsx:81`; `web/src/app/(app)/(founder)/workspace/reports/business/business-report-client.tsx:976` | Respectively link home, redirect `/?reanalyze=<slug>`, and link `/workspace/raise/deck`. They are navigation entry points, not proof the same scoped reanalysis workflow runs. In the bounded homepage search no reanalyze-query consumer was located; wider alternate routing remains unverified. |

## Cross-site coupling, not a shared-wallet guarantee

BlockID `web/src/app/api/auth/svi-handoff/route.ts` and `web/src/lib/security/svi-handoff.ts` define a short-lived signed UID handoff to an SVI cookie on a separate origin. Shared identity does not establish which billing account pays, project write authority or permission to edit a public report.

SVI `src/lib/ai/client.ts` can use BlockID’s internal `/api/investor-portal/ai-generate` proxy before other providers. The inspected BlockID proxy imports the general AI client, not proof that the dedicated G30 DeepInfra report-role policy controls this path. Provider cost and customer credit are separate concerns. Proxy boundary/configuration and live serving origin must be verified before paid workflow activation. No tokens or environment values were read for this audit.

## Classification

**Confirmed source gaps:** no scoped quote/idempotent credit transaction in these button paths; SVI handler lacks in-handler auth/ownership and has API excluded from inspected middleware; fresh research is absent from SVI field helper and disabled on BlockID partial path; SVI returned per-field score/weight is not persisted by selected patch; write implementation contradicts atomic-replacement comment; free-form evidence strings do not authenticate citations; hardcoded UI model/SLA labels do not attest routing.

**Risks inferred from code, not observed incidents:** concurrent SVI writes may overwrite each other; repeated POSTs can trigger repeated AI work; preflight/post-delivery billing can race; browser abort may leave provider work running; mutable field patch may diverge from totals/exports.

**User-observed billing remains unreconciled:** lack of billing in the inspected SVI handler does not refute a reported credit deduction. Trace the actual request, wallet owner, receipt, ledger events and upstream jobs across both sites before concluding where/why a deduction occurred. Do not assert the user was not charged from this source review. Proposed behavior requires an approved quote and a successfully saved scoped result before capture.

**Unknown:** actual live per-domain build/route/reverse-proxy configuration; account quota/credits associated with SVI runs; infrastructure authorization; checkout settlement effects; real concurrent mutation/charge incidents; every SVI public/private consumer and export. Implementation starts with this inventory, not assumptions that the two sites are identical or share a wallet.

Merged implementation requirements are authoritative in `docs/plans/SOURCE-OF-TRUTH.md` §6.7. This note is evidence, not a competing execution plan.
